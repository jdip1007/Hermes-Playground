# Wiki Index

> 内容目录。每个 wiki 页面按类型列出，附一行摘要。
> 查询前先读此文件找到相关页面。
> Last updated: 2026-06-21 | Total pages: 46 concepts + 2 entities + 50 changelogs | Tracking: **v0.17.0** (hermes-agent/main HEAD `87ab373`)

## Entities

- [[aiagent-class]] — 核心对话循环类，管理 LLM 交互和工具调用
- [[memorystore-class]] — 记忆系统核心类，管理 MEMORY.md 和 USER.md

## Concepts

### 核心架构
- [[tool-registry-architecture]] — 中央工具注册系统，声明式注册+集中调度，循环导入安全
- [[auxiliary-client-architecture]] — 辅助 LLM 客户端路由器，多 provider 解析链+适配器模式+自动降级
- [[browser-tool-architecture]] — 多后端浏览器自动化，accessibility tree 文本表示+三层安全防护+并发隔离
- [[web-tools-architecture]] — Web 搜索/提取/爬取 7 个 plugin 后端，按 capability 独立选 provider
- [[skills-system-architecture]] — 渐进式披露 + Curator 后台维护（archive/prune/list-archived）
- [[memory-system-architecture]] — 冻结快照模式、原子写入、安全扫描
- [[agent-loop-and-prompt-assembly]] — Agent 循环、系统提示构建、平台提示、执行指导
- [[skills-and-memory-interaction]] — Skills 与 Memory 的互补关系和决策树
- [[toolsets-system]] — 工具分组系统、递归解析、24 个 hermes-* 工具集
- [[session-search-and-sessiondb]] — FTS5 搜索 + LLM 摘要的跨会话回忆
- [[provider-transport-architecture]] — Provider Transport ABC（Anthropic / Chat / Responses / Bedrock）
- [[provider-plugin-system]] — **新** ProviderProfile ABC + 30 个 plugin（v0.13.0+）

### 工具与能力
- [[code-execution-sandbox]] — execute_code 沙箱，7 工具限制+UDS/File RPC 两种通信模式
- [[context-references]] — @file/@folder/@diff/@url/@git 引用系统，安全沙箱+注入量限制
- [[voice-mode-architecture]] — Push-to-talk 语音交互，STT（3 Provider）+ TTS（5 Provider）
- [[lsp-integration]] — **新** LSP 语义诊断集成（v0.14），agent/lsp/ 11 modules + file-mutation footer 三层后写校验
- [[checkpoints-architecture]] — Checkpoint Manager v2，透明文件系统快照（v0.13.0）
- [[i18n-and-locales]] — i18n 国际化（v0.13.0），薄切片本地化（仅 hermes 自身静态消息）

### 性能与优化
- [[parallel-tool-execution]] — 智能并发安全检测，三层分类 + 路径冲突检测
- [[prompt-caching-optimization]] — 冻结快照保护 prefix cache，75% 成本节省
- [[fuzzy-matching-engine]] — 8 策略链模糊匹配
- [[smart-model-routing]] — 智能模型路由，10级上下文长度解析链
- [[large-tool-result-handling]] — 三层溢出防护

### 安全与可靠性
- [[security-defense-system]] — 5 层防御体系，20 个 P0 闭合（v0.13.0+v0.14.0），100+ 威胁模式 + Wave 4 Dashboard OAuth + security-guidance
- [[dashboard-auth-oauth-gate]] — **新** Dashboard OAuth 鉴权闸门（v0.14, 2026-05-27）：可插拔 ABC + Nous Portal Provider + WS 单次性 ticket
- [[interrupt-and-fault-tolerance]] — 中断传播、凭证池轮换、Fallback 模型链
- [[credential-pool-and-isolation]] — 多密钥自动轮换、Profile 隔离
- [[tool-loop-guardrails]] — 工具调用循环护栏（v0.12.0），exact failure / same-tool failure / idempotent no-progress 三维检测

### 多 Agent
- [[multi-agent-architecture]] — 5 种 Agent 协作机制（delegate/MoA/Background Review/send_message/**Kanban**）
- [[kanban-multi-agent-board]] — 可持久化任务板，SQLite + 心跳 + 熔断 + 幻觉拦截（v0.13+）
- [[goal-and-ralph-loop]] — `/goal` + `/subgoal` 持续推进系统（v0.13/v0.14）

### 平台与扩展
- [[cli-architecture]] — 经典 CLI + Ink TUI 双前端、斜杠命令补全、Skin 引擎
- [[configuration-and-profiles]] — 分层配置、Profile 隔离、自动迁移
- [[hook-system-architecture]] — Hook 系统（Gateway Hooks + Plugin System），事件驱动+工具注册+上下文注入
- [[mcp-and-plugins]] — MCP 集成、插件钩子系统、OAuth 支持
- [[terminal-backends]] — 7 种终端后端（含 Vercel Sandbox）、环境抽象、持久化 Shell
- [[cron-scheduling]] — 内置调度器、自然语言调度、多平台投递、no_agent watchdog
- [[trajectory-and-data-generation]] — 轨迹保存、批量运行器、RL 训练环境
- [[prompt-builder-architecture]] — 系统提示模块化组装，注入防护+技能缓存+模型特定指导
- [[context-compressor-architecture]] — 自动上下文压缩，结构化摘要+迭代更新+工具对完整性保障
- [[model-tools-dispatch]] — 工具编排与调度，异步桥接+动态 schema 调整+参数类型强制
- [[gateway-session-management]] — 网关会话管理，多平台会话隔离+PII 脱敏+重置策略
- [[messaging-gateway-architecture]] — 22 平台统一网关（含 Teams/LINE/SimpleX/Google Chat 插件平台）
- [[hermes-proxy]] — OAuth 订阅 → OpenAI 兼容本地代理（v0.14）
- [[skin-engine]] — YAML 驱动的皮肤/主题系统
- [[worktree-isolation]] — Git Worktree 并行隔离模式

### 更新日志（按时间倒序）
- [[2026-06-21-update]] — **179 commits 单日同步**（hermes-agent `5a53e0f0 → 87ab373`，v0.17.0 维护未发新版；约 70 fix / 16 feat / 13 test / 6 chore / 16 docs + 23 merge；~26h 单日窗口；266 文件 +17145/−1079）：**⭐ Compression cluster 9 commit**（`14ef631` `agent/context_compressor.py:1935-1950 _effective_protect_first_n()` 衰减（`compression_count >= 1 or self._previous_summary` 后返 0）+ `1975` `_protect_head_size` 经新方法（修 #11996 早期轮不再化石化）+ `7ace96b` `:613-640` rotation 失败回滚 `agent.session_id = old_session_id` + `agent._session_db.reopen_session(old_session_id)` 替之前游离 phantom id + `hermes_cli/goals.py:282 migrate_goal_to_session()` rotation 后 move-not-copy 让 `/goal` 跟随 + `:694-701` `on_session_start(platform=)` kwarg 转发让 context-engine plugin 不再 source=unknown（修 #33618/#33906/#33907/#27633）+ `2f3177a` `agent/auxiliary_client.py:121 _aux_interrupt_protection = threading.local()` + `:128-141 aux_interrupt_protection()` ctx mgr + `agent/context_compressor.py:1588 with aux_interrupt_protection(): response = call_llm(...)` Codex aux 流轮询中断 flag 时尊重（修 #23975 摘要不再静默退化为 "summary unavailable"）+ `1f874df` `:1281-1285` `active_task` 单点渲染 + `:1311-1315 HISTORICAL_IN_PROGRESS_HEADING` "historical context only"（修 #49307 fallback 摘要不再把最新用户问题在三个 heading 重复，避免模型重答 + 埋掉新消息）+ `b6a4638` `:1604-1609 raise RuntimeError("Context compression LLM returned empty content ...")` + `:1632` 窄化 RuntimeError 仅 "no llm provider configured" 字面进 600s cooldown（修 #11978/#11914 OpenAI 兼容代理 200 返空 content 不再静默替换为空摘要）+ `3509be7` `:697 _MIN_CTX_TRIGGER_RATIO = 0.85` + `:699-724 _compute_threshold_tokens()` 静态方法 + 两站点 `__init__:659` / `update_model:769`（64K-ctx 模型在 ~54.4K 触发而非永不触发，修 #14690）+ `d0de460` `tui_gateway/server.py:9822-9865` slash `compress` 分支调 `summarize_manual_compression(...)` 替返空串（修 #46686 TUI `/compress` 显示前后摘要）+ `04730f3` 新 `hermes_cli/context_switch_guard.py` + `_merge_warning(result, text)` + `ModelSwitchResult.warning_message` 拼接 + 路由进 `cli.py`/`gateway/slash_commands.py`/`tui_gateway/server.py`（会话中模型切换若触发 preflight 压缩则预警）+ `41e0c10 agent/conversation_compression.py:728-735 notify_compression_complete` 改 `agent._compression_warning = _cc_msg; agent._emit_status(_cc_msg)` 替 `_vprint`（修 #36908 TUI/gateway 经 `status_callback` 收）+ `1ca2972` log instead of swallow preflight warning + 一致 TUI warning field）+ **⭐ Kanban cluster 8 commit + 1 feat**（`d164ed0` `hermes_cli/kanban_db.py:3499-3504 release_stale_claims` + `:5786-5791 enforce_max_runtime` + `:6086-6091 detect_crashed_workers` + `:3570-3576 reclaim_task` RESET SQL 全加 `AND claim_lock IS ?` 让 stale reclaim no-op 不再 desync task vs task_run（修 #50366）+ `1f4c5ae` `gateway/kanban_watchers.py:26-55 _resolve_auto_decompose_settings()` 每 tick 重读 + `:1029-1031` per-tick closure + `:1131-1133` 调度器 loop 调（`kanban.auto_decompose` 切换不需重启 #50358）+ `84ba83b` `:1191-1192 _INIT_LOCK_TIMEOUT_SECONDS=10.0` + `:1228-1309 _cross_process_init_lock` 重试非阻塞 + 超时 WARNING + yield 无锁（幂等 init 作 backstop）+ `:1619-1634 connect()` 快速路径绕文件锁（修 #50353）+ `9630ec6` `:7376-7389 _default_spawn()` `env["TERMINAL_CWD"] = workspace` 钉到任务工作区（修 #50348 修 file_tools `_resolve_base_dir` + AGENTS.md 查找）+ `e217fd4` 三新 plugin hook 注册 `hermes_cli/plugins.py:192-194 VALID_HOOKS`：**`kanban_task_claimed`**（dispatcher 进程 commit 后 spawn 前 `:3277-3283`）+ **`kanban_task_completed`**（worker 进程，携 `summary` `:3951-3958`）+ **`kanban_task_blocked`**（worker 进程，携 `reason` `:4383-4390`）+ `_fire_kanban_lifecycle_hook(event, task_id, **fields)` `:107-129` write commit 后 best-effort 调用，吞所有异常（#50349）+ `e581740` `:1312-1394 _dispatch_tick_lock` board-scoped flock + `:5369 DispatchResult.skipped_locked: bool` + `:6573-6623 dispatch_once` 包 `_dispatch_once_locked`（同一 kanban.db 两 dispatcher 不再并发跑 reclaim/spawn/写 #50331）+ `15cfc28` `:5062-5103 _resolve_worktree_workspace` 无路径任务锚 `board.default_workdir` + 缺/非绝对/非 git 显式 `ValueError` 永不静默落 `Path.cwd()` + `d79f67f` `:5032-5059 _ensure_git_worktree` git worktree add 实物化 + 复用经 `_git_common_dir` 短路 + `:5006-5011 _is_linked_worktree_checkout` + `:5021-5029 _repo_root_for_worktree_target` + `:5062-5130 _resolve_worktree_workspace` 4 case + 分支默 `wt/<task.id>` + `:6886-6903 _dispatch_once_locked` 取回 resolved 分支经 `set_workspace_path`+`set_branch_name` 持久（worktree 任务现真有 linked worktree 在 `<repo>/.worktrees/<task.id>` 上））+ **⭐ Security cluster 9 commit**（`ed96669` `tools/url_safety.py:285-287 is_always_blocked_url()` + `:359-365 is_safe_url()` `ip_str.split('%')[0]` 剥 IPv6 scope-ID（`%eth0`）再 `ipaddress.ip_address()` + 不可解析地址改 fail-closed `return False`（防 IPv6 scope-ID 旁路绕过 URL 安全）+ 测试 `87ab373` 覆 `tests/tools/test_url_safety.py` 4 新 test + `ed3d12a` `hermes_cli/web_server.py:10992 _ws_client_reason` + `:11019 _ws_client_is_allowed()` `ws.client is None` 或 `host == ""` 时返 fail-closed `False` + 调度器 `_ws_request_reason/_ws_request_is_allowed`（:11110/:11115）+ 4 endpoint + `5b45fb2` `plugins/kanban/dashboard/dist/index.js:337 MARKDOWN_ALLOWED_TAGS` + `:354 sanitizeMarkdownAttrs()` + `:368 sanitizeMarkdownHtml()` + `:387 MarkdownBlock` 包装 `dangerouslySetInnerHTML`（白名单 a/code/em/h1-h4/li/p/pre/strong/ul + `<a>` 要求 `https?://` 或 `mailto:` + `target=_blank rel="noopener noreferrer"`）+ `ae46699 + 87615f4` `hermes_cli/backup.py:907 restore_quick_snapshot()` snapshot_id 校验拒 `/`/`\`/`.`/`..`/空 + `:916` snap_dir traversal 校验 + `:936/:943` manifest src/dst 双校验经 `.resolve().relative_to()`（防 manifest 含 `../../outside.txt` 逃 HERMES_HOME）+ `3b56d3a` `tools/kanban_tools.py:36 import redact_sensitive_text` + `_handle_complete:488` + `_handle_block:621` + `_handle_comment:709` summary/result/metadata/block reason/comment body 全经 `redact_sensitive_text(..., force=True)` 后入 DB + 测试 `tests/tools/test_kanban_redaction.py +191`（防 API key/token 漏入后续 worker prompt）+ `a18bae6` `hermes_cli/config.py:6439 _SECRET_CONFIG_KEYS` frozenset + `:6459 redact_config_value()` 递归结构化 masker 经 `agent.redact.mask_secret()` 屏蔽（防 Cloudflare `cfut_` 等不透明 token 逃 vendor 正则；修 #50245/#50313 `config show/set` `api_key` 屏蔽）+ `9078b4b` `tools/file_tools.py:286 _is_blocked_device_path()` + `:305 _is_blocked_device()` `os.path.normpath(os.path.expanduser())` 折叠 `/dev/../dev/zero` 和 `/dev/./urandom` + 走最多 20 个 symlink 跳每跳检 + 最终 realpath 校验 + `def3f63` `_is_blocked_device(filepath, base_dir=None)` 签名扩展 + `:312-315` 相对路径 prefix `base_dir` + `:856-857 read_file_tool` 计算 `device_base = None if Path(path).expanduser().is_absolute() else _resolve_base_dir(task_id)`（相对工作区 symlink 不再对 Python cwd 解析）+ `71274f2 + 16899ae` `:663 _looks_like_read_file_line_numbered_content()` ≥2 编号行 + ≥60% 比例 + 连续递进 + `:698 _is_internal_file_tool_content()` + `:1263-1267 write_file_tool` 守 + 拒后返 "Refusing to write internal read_file display text..."）+ **⭐ Gateway/API server cluster 13 commit**（`b5b8a4c` `gateway/stream_consumer.py:1459-1464 _has_prefers_hook` 检测（`type(self.adapter)` + `__dict__` 双查防 MagicMock）+ finalize gate `:1466-1474` 经 hook 时仅 `_prefers_fresh` 决定，时间 fallback 仅无 hook 时（修 #47048 Telegram table+bullet 双渲染）+ `4c206b9` + `79f2978` `plugins/platforms/{discord,raft,slack,telegram,whatsapp}/adapter.py` 顶层 `sys.path.insert(0, ...)` `parents[2]` → `parents[3]`（修 cron 包命名空间冲突 #49410）+ `03563da` `gateway/run.py:9077 _hyg_hard_msg_limit = 5000` + `hermes_cli/config.py:1262` 默改 5000（修 #50194 hygiene 硬上限不再在大-ctx 模型的健康长会话上触发）+ `4c349e8` `gateway/run.py:9287-9321 if _hyg_rotated or _hyg_in_place:` 守 `rewrite_transcript`（修 #21301 hygiene throwaway agent 无 session_db 不能 rotate 时不再无条件用 head+summary 替换全 transcript 造成永久数据丢失）+ `8ac5e90` `gateway/run.py:1022 _JSON_MEDIA_TOOL_PATH_FIELDS = ("host_image", "image", "agent_visible_image")` + `:1119 _collect_history_media_paths()` + `:15712 _history_media_paths: set = _collect_history_media_paths(agent_history)`（修 #46627 `image_generate` JSON payload 路径入历史去重集，压缩后不再重发生成图）+ `c11c510` `gateway/run.py:4154 _queue_or_replace_pending_event` + `:4326` 中断模式 follow-up 新调用点（修 #43066 sub-bug 2 busy 模式连续文本不再 newline-merge）+ `d19aabb` `:4675 _finalize_shutdown_agents` + `:6700` 关机路径调 + `:4693 _flush = getattr(agent, "_flush_messages_to_session_db", None)`（修 #13121 drain-timeout 中断的轮经 idempotent identity-tracked flush 不再丢）+ `93ea9b0` `gateway/platforms/base.py:582-605 _max_inbound_media_bytes` + `:684 cache_image_from_bytes` + `:919 cache_video_from_bytes` + 可配 `gateway.max_inbound_media_bytes`（128 MiB 默 / 0 禁；防 Discord Nitro 500MB 上传 OOM 杀网关）+ `bb77a8b` `hermes_cli/main.py:8380 _pause_windows_gateways_for_update` + `:8438-8483` 未映射快照 loop + `:8520-8545` 重放 + 守 `gateway.status.looks_like_gateway_command_line`（修 #50090/#50373 未映射 Windows gateway 更新后 detached watcher 重生）+ `7a131f7` `gateway/platforms/base.py:760 supports_async_delivery: bool = False` + 新 contextvar `async_delivery_supported`/`HERMES_SESSION_ASYNC_DELIVERY` + `api_server.py:3669 _bind_api_server_session` + `tools/terminal_tool.py`/`delegate_tool.py` 加 `notify_unsupported`/sync fallback（修 #50319 stateless HTTP path 不再静默 no-op `terminal(notify_on_complete=True)` 和 `delegate_task(background=True)`）+ `e499d69` `gateway/platforms/api_server.py:797-836 _resolve_max_concurrent_runs` + `:3652` gate + 统一 cap 跨 `/v1/chat/completions`/`/v1/responses`/`/v1/runs` + 上限到达返 OpenAI 风格 429 + `Retry-After`（可配 `gateway.api_server.max_concurrent_runs` 默 10 / 0 禁；修 #50007 DoS 上限）+ **active_agents/busy/drainable 可观测性 4 commit**：`51a338a gateway/run.py:3668 _persist_active_agents` + 轮边界 publish（`:5267` claim/`:8445` 启动恢复/`:13585` 释放）+ `0ee7546 gateway/status.py:645 derive_gateway_busy` + `:670 derive_gateway_drainable` + `web_server.py:1867-1923 /api/status` + `api_server.py:1150-1174 /health/detailed` surface + `b577f25 gateway/status.py:624-636 parse_active_agents` + `web_server.py:154-157 _resolve_restart_drain_timeout` 共享 + `4d7bb38 write_runtime_status` 写边钳制 + `hermes_cli/gateway.py:4638-4640` draining 第三 coercion 站统一）+ **⭐ Telegram cluster 9 commit**（`6183e8c hermes_cli/config.py:2161 "rich_messages": False` 默 + `plugins/platforms/telegram/adapter.py:500 _rich_messages_enabled = self._coerce_bool_extra("rich_messages", False)`（Bot API 10.1 rich 改默认 off 经 `platforms.telegram.extra.rich_messages: true` opt-in）+ `ea056b0` CJK 文本绕过 rich 回 legacy MarkdownV2 + `a966932` 表格 exempt 自 rich newline 硬换行 + `31e59fe` slash-command 富文本输出保留换行（#46070）+ `796f618` chunk 标记移出 code fence 不被代码栅栏吞 + `c1f11f8` 流式 rich final 经 editMessageText 也索引 message_id + `29e5e12` 自 native rich echo 恢复 reply 文本 + `09a96ba gateway/run.py` 在流 finalize 前暂停 Telegram typing indicator + `565b7c8` 最终回复后 typing indicator 残留修复经 stop-typing RPC）+ **⭐ Cron cluster 6 commit**（`07424da` ticker `except BaseException` 不死 + heartbeat 感知 status + `f1f36b3` 迁移期时区偏移修复防双触发 + `65d7c7f` `action='run'` 立即执行替之前等下次 ticker + `4cc28aa` Telegram DM-topic cron 经 `DeliveryRouter` 让 topic 信息保留（修 #22773）+ `d548908` 直播适配器 send 后显式 confirmation 步骤替静默失败（修 #38922/#47056/#43014）+ `73b9226` `model.default` 经显式解析链 + 缺 model fail-fast 替静默 GPT-4 fallback）+ **Plugins/Session lifecycle 7 commit**（`9d883ac hermes_cli/plugins.py` 加 `ctx.profile_name` wrap `hermes_cli.profiles.get_active_profile_name()` 经 HERMES_HOME 在 gateway/kanban worker 也工作（修 #50346）+ `9f67ba1 agent/turn_finalizer.py:142-168` + `:385-386` `_save_trajectory`/`_cleanup_task_resources`/`_persist_session` 三段各自 try/except + `result["cleanup_errors"]`（修 #50009 finalize_turn 永不丢 final_response）+ `b17180d agent/agent_init.py:1108 _end_session_on_close = True` 默 + `run_agent.py:3261-3265 AIAgent.close()` step 7 调 `session_db.end_session(session_id, "agent_close")` + `gateway/run.py:9258` hygiene helper 关 + `agent/background_review.py:584` review fork 关（subagent/cron close 路径不再泄露 `ended_at IS NULL` 行）+ `9e4fe32` background review fork opt out + `56255f8 hermes_state.py:69-95 _collect_delegate_child_ids()` 用父 id seed `found` 集合 + 过滤返回（循环 `_delegate_from` 链不再删父）+ `99233fa cli.py:11702 _persist_active_session_before_close` + `:14432` 调 + `c7e8854 tui_gateway/server.py:383-453 _finalize_session()` 经 `agent._persist_session(snapshot, ...)` 优先 `_session_messages` + `entry.py:140-142 _log_signal()` SIGHUP/SIGTERM 显式 `_shutdown_sessions()` 在 `sys.exit(0)` 前）+ **Misc cluster**（`c0409a8 gateway/platforms/base.py SEND_ERROR_KINDS` 7 类（too_long/bad_format/forbidden/not_found/rate_limited/transient/unknown）+ `classify_send_error()` + `SendResult.error_kind` 可选字段（22 新 test + 210 adapter regression 绿；纯加无 behavioral 变化）+ `2609bcc` i18n Spanish 完整（README.es.md 220 / CONTRIBUTING.es.md 602 / SECURITY.es.md 322 + locales/es.yaml Matrix section + README.md Spanish badge；47 i18n test 含 catalog key + placeholder parity）+ `65a477f` Desktop About 加 Update now 按钮（#50186）+ `8666fd7 + 461fcc0` 模型可见性对话框保留其他 provider hide-all（跨 provider）+ `5e3e89c` Hindsight embedded daemon 健康 grace timeout 可配（#50341）+ `7bc6f18` Hindsight root 用户跳 local_embedded daemon（Docker 容器场景）+ `472c068 + 7b9a0b3` MCP "unknown method" ping keepalive fallback（#50028）+ `9578e52` Photon 探测意外 sidecar 死亡触发 reconnect + `2a45423` Envoy overflow 归类可重试 + 加 typing cooldown + `2f4f23f agent/codex_runtime.py:28 _codex_note_to_tool_progress()` + `:263-282 _on_codex_event` closure 接 `on_event=` 入 `CodexAppServerSession`（Codex app-server `item/started` → Telegram tool-progress 桥接 #38835）+ `e0498bd agent/usage_pricing.py:451-490` `_OFFICIAL_DOCS_PRICING` 四 Bedrock Claude 行加 `cache_read_cost_per_million`/`cache_write_cost_per_million` + `:595 _normalize_bedrock_model_name()` 剥 `us./global./eu./ap./jp.` 跨区域前缀（修 #50307）+ `a4b1554 gateway/whatsapp_identity.py:77 to_whatsapp_jid()` + `plugins/platforms/whatsapp/adapter.py:730/791/826/938/956/1244` 全出站 bridge call wrap `chat_id`（裸号 `+50766715226` → `50766715226@s.whatsapp.net`，Baileys `jidDecode` 不再 crash）+ `491579f gateway/platforms/whatsapp_common.py:374 resolve_whatsapp_bridge_dir()` HERMES_HOME mirror `shutil.copytree scripts/whatsapp-bridge`（防 Docker `/opt/hermes` 只读）+ `4467c22 agent/transports/chat_completions.py:175/:205 ChatCompletionsTransport.convert_messages` `msg.pop("timestamp", None)` 严格 OpenAI 兼容 provider 不再 400 + `ac83365 + e74033b scripts/install.ps1:125-132` `ConvertTo-LongPath` 一次性 re-`Set-Item` `Env:TEMP`/`Env:TMP`（Windows 8.3 短 `FIRST~1.LAS` 路径不再噎住 Node/Electron）+ `6f5f58e + 41ba90f tools/process_registry.py:1038-1046 poll()` 移除 `_completion_consumed.add(session_id)` + `:187 _poll_observed: set` + `_drain_should_skip()` 跳 session ∈ `_completion_consumed ∪ _poll_observed`（修 #10156 CLI drain dedup 在 poll 改只读后仍工作）+ `3e354b6 agent/agent_init.py:812 + :867` 路由/fallback 客户端 header 捕获插入 `default_headers` + `run_agent.py:3576 _create_httpx_client` `api.githubcopilot.com` base URL 短路裸 `httpx.Client()` 避免破 Copilot 路由的 socket-keepalive + `6984026 tools/browser_tool.py:621-643 _is_local_backend()` 加查 `TERMINAL_ENV`（terminal backend 为 docker/modal/daytona/ssh/singularity 时启 SSRF 守）+ `8a506ed agent/credential_pool.py:2065-2087 _prune_stale_seeded_entries()` 新 `prune_env_sources` kwarg 默 True + `:2196-2200 load_pool()` 传 `prune_env_sources=False`（缺 `MINIMAX_API_KEY` 的进程不再为其他进程删除持久 `env:MINIMAX_API_KEY` 行）+ `587b5b9 agent/memory_provider.py:299 backup_paths()` 新 hook + `hermes_cli/backup.py:132/:135/:357-369/:586-587` `_EXTERNAL_PREFIX="_external/"` + `_collect_memory_provider_external_paths()` + 三 plugin override 声明 `~/.honcho`/`~/.hindsight`/`~/.openviking`（修 #50325 backup 捕获 HERMES_HOME 外 memory provider 状态）+ `99f3072 + 1e0b3a2 + 1965d56` 模型切换 3 修（失败 swap no-op + token calibration 重置 + tool 输出预算缩放 #50375/#23767）+ `824c9d3` `model.api_base` → `model.base_url` 别名（#50385）+ `f726908` Desktop/Windows 防 in-app 更新级联进 backend 重启循环（#50381）+ `a9c8025` approval 阻塞 gateway 等候中 honor 中断（#8697）+ `f79e0a7 + b7f6cb9` Email 配置硬化（缺 config non-retryable + IMAP/SMTP host 验证 #40715）+ 16+ docs commit 含 SKILL.md template 加 Prerequisites/How to Run + agents background delegation durability + hooks subagent_start hook + Docker 网关 tool-loop 硬停 + i18n + AUTHOR_MAP 10+ salvage）— 验证基线 `/tmp/hermes-agent` clone @ `87ab373` + 4 个并行 research agent 实证（A: Security 9 commit / B: Kanban 8 + lifecycle hooks / C: Compression 9 + Gateway 13 / D: Telegram 9 + Cron 6 + Misc 30+）
- [[2026-06-20-update]] — **69 commits 单日同步**（hermes-agent `01f581d8d → 5a53e0f0`，v0.17.0 维护未发新版；约 34 fix / 8 feat / 6 chore / 5 test / 3 refactor / 3 docs / 1 style + 8 merge；~22h 单日窗口；250 文件 +12225/−3725）：**⭐ Compression in-place compaction 6 commit**（`compression.in_place:bool=False` opt-in flag `hermes_cli/config.py:1290` + 保留单 session_id 替代 rotation 模型 + 新 `archive_and_compact` 软归档（`hermes_state.py:2700+` + `:2734-2737 UPDATE messages SET active=0, compacted=1 WHERE session_id=? AND active=1`）+ 新 `compacted` 列 3 态语义 `hermes_state.py:570 compacted INTEGER NOT NULL DEFAULT 0` + `:3489-3494` 文档化 active=1/compacted=0=live, active=0/compacted=1=compaction-archive in search, active=0/compacted=0=rewind out search + `search_messages` WHERE 放宽 `(m.active=1 OR m.compacted=1)` `:3528-3532` 主 FTS5 + `:3611-3614` trigram CJK + 边界 hook 双模触发 `agent/conversation_compression.py:625 _is_boundary = bool(_old_sid) or in_place` + `:635-641 on_session_start(boundary_reason="compression")` + `:651-658 on_session_switch(reason="compression")` + gateway auto-compress `gateway/run.py:15818 _compacted_in_place` + `:15872-15874 history_offset = 0 if (_session_was_split or _compacted_in_place) else len(agent_history)` + `/compress` slash gate 扩 `gateway/slash_commands.py:2636-2659 if rotated or _in_place:` + `agent._last_compaction_in_place: bool` 信号 + 6 commit 全 rebase 至 `committer date 2026-06-20T10:57:07 -0700` + 解 PR body 列 14 个 rotation 相关 issue 集群 `#33618/#14238/#33907/#36777/#45117/#42228/#48989`）+ **⭐ Background delegation fan-out PR #49734**（单 commit `ea8a8b4a` 5 文件 +721/-333 `delegate_tool.py` dispatch 净重写 644 行；新 `tools/async_delegation.py:337 dispatch_async_delegation_batch(...)` 批量当**一个**异步单元提交占单槽位（不论 N）；runner `tools/delegate_tool.py:2247 _execute_and_aggregate()` join 所有子；完成事件 `is_batch=True` `tools/async_delegation.py:389` + 单 consolidated 渲染 `tools/process_registry.py:1549-1628 _format_async_delegation` header `:1586` + TASK i/N `:1614`；模型级 `background` 入参作废由 `run_agent.py:5210-5222 _dispatch_delegate_task` 顶层强制 `background = (not _is_subagent)`（depth 0 顶模型调 → 异步 / depth ≥ 1 orchestrator subagent → 仍同步）；3 并发限 `delegation.max_concurrent_children` + `max_async_children=3 默` + `max_spawn_depth`；满槽**拒绝退化 inline sync** 非排队 `tools/delegate_tool.py:2561-2569`；`a6f08ff0` 1 行 docs `tools/delegate_tool.py:2901` 澄清 subagent 模型 config 级非 per-call）+ **⭐ Blank Slate setup mode PR #36733**（单 commit `11c6f4c7` 3 文件 +376/0 `hermes_cli/setup.py:235` 新增 + 新测 +131 + quickstart 文档 +10；第三 setup 模式（**不**是新 subcommand）`hermes_cli/setup.py:2853-2861`；`_run_blank_slate_setup` `:3056-3134` wizard；`_blank_slate_minimal_toolsets` `:2989` 写 `platform_toolsets.cli=["file","terminal"]` `:3006` + `agent.disabled_toolsets` 全减集（所有已知 toolset 减 `{file, terminal}` 算自 `CONFIGURABLE_TOOLSETS + plugin keys + plain TOOLSETS` 三源 `:3026`）；`_blank_slate_minimize_config` `:3031` 关 `compression.enabled=False`/`memory.memory_enabled=False`/`memory.user_profile_enabled=False`/`checkpoints.enabled=False`/`smart_model_routing.enabled=False`/`session_reset.mode="none"`/`display.tool_progress="all"` + `agent.max_turns=90`；bundled-skill opt-out `:3117-3118 set_bundled_skills_opt_out(True)`；单一 gating invariant `hermes_cli/tools_config.py:1528-1536 _get_platform_tools` 末尾减 `agent.disabled_toolsets` 覆盖非配置 platform-toolset recovery；`_blank_slate_walkthrough` `:3137-3214` 逐节 opt-in skills/tools/plugins/MCP/gateway；migration 回 Full 装可经 `hermes tools/hermes skills opt-in --sync/hermes mcp add/hermes plugins/hermes setup agent`）+ **⭐ Gateway adapters → bundled plugins 9 个迁移**（单 commit `56001054`（salvage PR #41284）124 文件 +3647/-2583；slack/dingtalk/whatsapp/matrix/feishu/telegram/wecom/email/sms 全从 `gateway/platforms/<name>.py` 移到 `plugins/platforms/<name>/{adapter,plugin.yaml,__init__}.py` + 旧路径全删无 stub；卫星 `feishu_comment.py/feishu_comment_rules.py/feishu_meeting_invite.py` + `telegram_network.py` + `callback_adapter.py/wecom_crypto.py` 同址迁移；signal 不迁仍在 `gateway/platforms/signal.py 1701 行` + `signal_format.py` + `signal_rate_limit.py`；注册 `register(ctx)` PluginContext `hermes_cli/plugins.py:290` → `ctx.register_platform()` `:770` → `PlatformEntry` `gateway/platform_registry.py:39` → `platform_registry.register(entry)` `:816`；发现 `_scan_directory` walk `plugins/platforms/` 一级 `hermes_cli/plugins.py:1186-1190` + 每 `plugin.yaml` manifest 含 `kind: platform`；slack `plugins/platforms/slack/adapter.py:4077 register(adapter_factory=_build_adapter, setup_fn=interactive_setup, apply_yaml_config_fn=_apply_yaml_config, cron_deliver_env_var="SLACK_HOME_CHANNEL", standalone_sender_fn=_standalone_send, max_message_length=39000)`；Matrix YAML 补 4 键 `allowed_users`/`ignore_user_patterns`/`process_notices`/`session_scope` `plugins/platforms/matrix/adapter.py:4288 _apply_yaml_config` `:4297/:4312/:4317/:4319`；~60 个 `tests/gateway/test_*.py` 文件原地 retarget；单一新 `tests/gateway/test_slack_plugin_setup.py +57`；`c3292794` 跟进 2 文件 +2/-2 补 56001054 blanket rewrite 漏的 2 字符串字面量 `tests/gateway/test_telegram_webhook_secret.py:34` + `tests/tools/test_windows_native_support.py:769`）+ **Desktop 浮动 composer popout PR #49488**（4 commit；**全 renderer-only 不开新 `BrowserWindow` 无 IPC channel**；`apps/desktop/src/store/composer-popout.ts` 两 nanostore `$composerPoppedOut`/`$composerPopoutPosition` + setter `setComposerPoppedOut(line 48)`/`setComposerPopoutPosition(line 64)` + 持久化 localStorage `hermes.desktop.composerPopout.{enabled,position}` lines 5-6 + `POPOUT_WIDTH_REM=19.5` line 19 + 默 `{bottom:24,right:24}` line 23 + clamping line 57-60；component `apps/desktop/src/app/chat/composer/index.tsx:196 popoutAllowed=!isSecondaryWindow()` 二级窗（Cmd+Shift+N 小窗/subagent watch 窗）强制 docked；拖拽-停靠 hook `apps/desktop/src/app/chat/composer/hooks/use-popout-drag.ts:93 useComposerPopoutGestures` + 4 dock zone 常量 `DOCK_ZONE_BOTTOM_PX=72`/`CENTER_TOLERANCE_PX=150`/`VERTICAL_FALLOFF_PX=260`/`HORIZONTAL_FALLOFF_PX=220`（lines 19-24）+ 接近度 `dockProximityOf` line 72；切换 `handleComposerToggle` `index.tsx:233-235` 双击 grab 区触发 + 拖拽 `handleComposerPopOut` `:221-224` / `handleComposerDock` `:226-229`；**无键盘快捷** 纯手势；`eed78d6e` polish peel-off 中心对齐光标 + `composerPanelCard` 统一 + chip Backspace 原子 `rich-editor.ts`；`f697c97e` 1 行 border-radius）+ **Provider 失效转移 5 commit**（`5a53e0f0 fix(compression): abort on auth failure instead of rotating into a degraded session` aux 摘要 401/403 不再进降级会话 `agent/context_compressor.py:771 _last_summary_auth_failure: bool = False` + `:1593-1602 _is_auth_error` 状态码/字符串识别 + `:2335 if not summary and (self.abort_on_summary_failure or self._last_summary_auth_failure):` 不管 config flag 都 abort；`f22dd8a7 fix(agent): fail over to fallback provider on persistent auth failure (401/403)` agent loop is_auth 真失败转 fallback `agent/conversation_loop.py:2842-2858` + 新 retry-state flag `agent/turn_retry_state.py:65 auth_failover_attempted: bool = False`（一次性 guard）+ `agent/error_classifier.py:89 return self.reason in {FailoverReason.auth, FailoverReason.auth_permanent}`；`c884ff64 fix(agent): keep system-prompt model identity in sync across provider failover` `rewrite_prompt_model_identity(agent, model, provider)` `agent/chat_completion_helpers.py:1045-1071` 只改最后一次 `Model:`/`Provider:` 出现 + `_sync_failover_system_message(agent, api_messages, active_system_prompt)` `agent/conversation_loop.py:469-492` patch in-flight `api_messages[0]["content"]` + 8+ failover 站点 wire；`c253b073 fix(model): clear stale endpoint credentials across switches` 新 `clear_model_endpoint_credentials(model_cfg, *, clear_api_key=True, clear_api_mode=True)` 中央 helper `hermes_cli/config.py:3934` + 12 picker flow 接入 `hermes_cli/model_setup_flows.py` lines 27/128/347/1256/1678/1804/1894/2008/2092/2286/2580/2735 + gateway `/model` 条件 `gateway/slash_commands.py:1243/1435 if str(result.target_provider or "").strip().lower() != "custom":` + web UI 4 站点；`95a3affc fix(model): keep Nous picker from restoring stale custom keys` Nous picker 重 `load_config()` `hermes_cli/model_setup_flows.py:331-333` 防 in-memory stale 覆盖盘上清洁 config；`37a4dd49 fix(auth): heal poisoned Nous inference URL on refresh instead of retaining it` 新常量 `hermes_cli/auth.py:71 DEFAULT_NOUS_INFERENCE_URL = "https://inference-api.nousresearch.com/v1"` + 双站点修 `:5440-5441/:5719-5720 inference_base_url = refreshed_url or DEFAULT_NOUS_INFERENCE_URL` 防 stale `stg-` URL 永驻）+ **Signal 加固 14 commit**（**Signal adapter 不在 §4 大迁移**仍在 `gateway/platforms/signal.py 1701 行` + `signal_format.py` + `signal_rate_limit.py`；`5649b864 (#49260)` `_validate_send_result(self, result) -> (success, error_message)` `gateway/platforms/signal.py:1020` 解 signal-cli `results[]` 找 `type != "SUCCESS"` 或 `success == False` + `_rpc`（`:983-989`）抛 `SignalRateLimitError(retry_after=...)` 替静默返；`3a6c171e` `gateway/platforms/signal.py:1075 [Signal] Sending response` info 日志 + `cron/scheduler.py::_deliver_result` `target_errors` 列表（line 813）收 live-adapter 失败 + extend `delivery_errors`；`905820b5` 新文件 `gateway/platforms/signal_format.py 140 行 markdown_to_signal(text)->tuple[str, list[str]]` UTF-16 偏移 BOLD/ITALIC/STRIKETHROUGH/MONOSPACE + `_normalize_bullet_markers` `- `/`* `/`+ ` 转 Unicode `• ` 围栏外 + `gateway/platforms/signal.py:1007` 包装器；`da34fca2` ADTS AAC 识别 `gateway/platforms/signal.py:83 _guess_extension` lines 105-109 `if (data[1] & 0xF6) == 0xF0: return ".aac"`（ADTS ID=0 layer=00 vs MP3 ID=1 layer∈{01,10,11}）+ `_remux_aac_to_m4a(aac_data)` `:141` ffmpeg `-c:a copy -movflags +faststart` 10s timeout + 调用点 `:906`；`06ca1e99` `utils.py:326 env_float(key, default=0.0)` 镜像 `env_int`；`abafba07` STT-fallback 注释纠正 + 包装器 type `tuple[str, list[str]]` + AAC test 用 ffmpeg 运行时生成（`anullsrc -t 0.5 -c:a aac -f adts`）；`a7dd98c8` 11 文件 22 处 raw `int(os.getenv(...))`/`float(os.getenv(...))` 替为 `env_int`/`env_float`；`96b10327` 群组消息自身 mention 剥（require_mention 分支内）+ `32a97a20` hoist 出分支应用所有群消息 `gateway/platforms/signal.py:640-641 if is_group and text:` + `account_norm = self._account_normalized` 剥 `@<account>` + `@<uuid>` 后 `text.replace("  ", " ").strip()` 折叠双空格；`40b6ac9a` stop-typing RPC `gateway/platforms/signal.py:1517-1538` 取消 typing-indicator 时显式 `sendTyping params["stop"] = True`（rpc_id `typing-stop`, `log_failures=False`）；`96db7c68` quoted reply context 保留 + 新实例属性 `_sent_message_timestamps: set[str]` cap 500 + 新 helper `_extract_quote_author`（`:773`）/`_quote_references_own_message`（`:790`）/`_remember_sent_message_timestamp`（`:809`）+ `MessageEvent` 加 `reply_to_author_id/reply_to_author_name/reply_to_is_own_message` 3 字段；`332f88f6` echo ring 加固 `OrderedDict[int, float]` 替 `set` + cap 50→512 `_max_recent_timestamps`（`:315`）+ `_recent_sent_ttl_seconds=300.0` 5min TTL（`:316`）+ `_track_sent_timestamp` MRU+TTL+evict + 新 `_consume_sent_timestamp(ts) -> bool`（`:1110`）替 `set.discard`；`2f862832` test 同步用 `_consume_sent_timestamp`；`26d9a3c7` quote-cache FIFO `_sent_message_timestamps OrderedDict[str, None]`（`:324`）+ `_remember_sent_message_timestamp` MRU + `popitem(last=False)` FIFO-evict（`:809-820`）防 `set.pop()` 删任意元素丢失新值；6 个 merge commit PR #49530/#49558/#49563/#49583/#49678/#49680）+ **Teams 原生附件 PR #49308**（`5f55f0ff` 2 文件 +144/-10；`plugins/platforms/teams/adapter.py` 不在 §4 迁移列表本就是 bundled plugin；新 `_send_media_attachment(self, chat_id, source, default_mime, caption, media_label)` shared helper `:1192-1240` + `send_video` `:1273-1288`（默 `video/mp4`）+ `send_voice` `:1290-1305`（默 `audio/mpeg`）+ `send_document` `:1307-1323`（默 `application/octet-stream`）；`microsoft_teams.api.Attachment(content_type, content_url)` + `MessageActivityInput().add_attachments(...)` SDK；URL ref / data URI 双路径 lines 1216-1224；无显式 size cap）+ **Language-aware title generation PR #45296**（`cf58f1a5` 4 文件 +74/-1；`agent/title_generator.py:22-27 _TITLE_PROMPT` auto-match + `:29-34 _TITLE_PROMPT_PINNED_LANGUAGE` 带 `{language}` 占位 + 选择 `:73-74 generate_title` `prompt = _TITLE_PROMPT_PINNED_LANGUAGE.format(language=language) if language else _TITLE_PROMPT`；新 config key `auxiliary.title_generation.language` 默 `""` schema 默 `hermes_cli/config.py:1457` + reader `agent/title_generator.py:37-48`；detection-by-instruction 无 code-level locale 检测）+ **Creative-ideation skill v2.1.0 PR #42402**（`d799284b` 28 文件 +2269/-190；skill 目录 `optional-skills/creative/creative-ideation/`；version bump `SKILL.md:5 version: 2.1.0`；22 新方法 in `references/methods/`：affinity-diagrams/analogy-and-blending/biomimicry/chance-and-remix/compression-progress/creative-discipline/defamiliarization/derive-and-mapping/first-principles/jobs-to-be-done/lateral-provocations/leverage-points/oblique-strategies/oulipo/pataphysics/pattern-languages/polya/premortem-and-inversion/scamper/story-skeletons/triz-principles/volume-generation；配套 `method-catalog.md +88`/`heuristics.md +85`/`anti-slop.md +106`/`exercises.md +71`/`full-prompt-library.md` 重构 +206）+ **Dashboard 隐藏 sidecar 会话 PR #49269**（`a7983d5a` 8 文件 +129/-11；新 `HERMES_SESSION_SOURCE` ContextVar/env var 多站点 `gateway/session_context.py:52,73,105,128,160` + `_session_source_for_agent` helper `run_agent.py:92-101` + `AIAgent._ensure_session_db_row` `run_agent.py:528` + session-switch start_context `:594` + `tui_gateway/server.py:1151-1156 _session_source(session)` 读 `session["source"]` 默 `"tui"` + 3 个 `db.create_session` 调用点 `:1258/:5774` + Web `ChatSidebar.tsx:202 source: "tool"`；dashboard 生 sidecar gateway session 在 DB 行创建时标 `"tool"` 而非 `"tui"`，history filter 据此隐藏）+ **内联键盘 model picker 默 persist PR #49176 salvage**（4 commit；`2fe78d1a` 镜像文本 `/model` persist 块到 inline-keyboard callback `gateway/slash_commands.py:1155 is_global=persist_global` + `:1219-1247` 写 `config.yaml` via `save_config` + 扁平 `model:` 字符串 coerce 嵌套 dict + 双分支消息 `:1282-1285`；2 test + 1 merge）+ **终止 busy session internal-event 中断 PR #49738**（`680732c1` `gateway/run.py:4172-4184 if getattr(event, "internal", False): return False` early guard —— `delegate_task(background=true)` + 终端 `notify_on_complete` 不再以 "Interrupting current task" ack 中止活跃轮）+ **gateway resume restart loop 修**（`75ed07ac` 新 `_strip_dangling_tool_call_tail` `gateway/run.py:940-980` 剥尾部 `assistant(tool_calls)` 无 `tool` 答 + 重写 resume-pending system note `:15755-15776` 禁止再执行 restart 命令 —— 杀 gateway 的工具调用如 `docker restart` 不再无限循环）+ **工具 model whitelist 不剥 prompt/source images**（`d45addc2` `tools/image_generation_tool.py:615 if k in supports or k == "prompt"` + `:669-672 _required = {"prompt", "image_urls"}` 防 FAL 配丢必需键静默生空 request）+ **slash exec dispatch payload TUI/Desktop 双修**（`857d0244 (#49337)` TUI `ui-tui/src/app/createSlashHandler.ts:77-117 handleDispatch` shared 抽取 + 在 `slash.exec` 响应上**先**试 `asCommandDispatch(r)` 让 skill/send/alias/prefill 不渲染裸文本；`1b7b4d13 (#49358)` Desktop 镜像 `apps/desktop/src/app/session/hooks/use-prompt-actions.ts:918-967 handleDispatch` + `parseCommandDispatch(result)` 先试）+ **Raft check_fn 静默**（`8cf7df86` `plugins/platforms/raft/adapter.py:100-116` 删两 `logger.warning` 无 raft CLI 用户不每 `load_gateway_config` 警告）+ **Desktop GPU 加速被禁横幅 + 隐藏 link-title 窗口静音 + 右侧栏 tooltip + memory shutdown hook warning + CLI agent ref publish + 5 条 AUTHOR_MAP salvage + 3 stale comment 清** — 验证基线 `/tmp/hermes-agent` clone @ `5a53e0f0` + 4 个并行 research agent 实证（A: Compression in-place + auth failover 11 commit / B: Blank Slate + delegation + composer popout + Teams + lang titles + Creative-ideation 8 commit / C: gateway adapter migration + Signal 16 commit / D: 23 个 misc commit + AUTHOR_MAP + 7 merges）
- [[2026-06-19-update]] — **181 commits 跨日同步 + ⭐ v0.16.0 → v0.17.0 release**（hermes-agent `03d9a95 → 01f581d8d`，**v0.17.0 已发**经 `2bd1977d8 chore: release v0.17.0 (2026.6.19)` + Desktop fallback 同步 `866f1d65c (#49236)`；约 76 fix / 39 feat / 14 test / 14 chore / 10 refactor / 4 docs / 24 merge；~24h 主活跃窗口；282 文件 +20287/−1661）：**⭐ Gateway Multiplex Phase 0-4 主轴 9 commit**（`gateway/config.py:553 multiplex_profiles:bool=False` + `gateway/session.py:616-651 _session_key_namespace/build_session_key(profile=)` + `:813-831 SessionStore._resolve_profile_for_key(source)` + `gateway/platforms/webhook.py:60-65 _PROFILE_REJECTED` 哨兵 + `:199-202` `/p/{profile}/webhooks/{route_name}` 路由 + `:413-441 _resolve_request_profile()` + 新文件 `agent/secret_scope.py 205 行` `_MULTIPLEX_ACTIVE` 全局 + `class UnscopedSecretError` + `_SECRET_SCOPE: ContextVar` + `get_secret(name) -> Optional[str]` fail-closed + `_GLOBAL_ENV_EXACT/PREFIXES` allowlist + `set_multiplex_active/build_profile_secret_scope/load_env_file` + `gateway/run.py:46-52 @contextmanager _profile_runtime_scope` + `:1269 class MultiplexConfigError` + `:1275-1283 _PORT_BINDING_PLATFORM_VALUES` frozenset + `:2401 _profile_adapters: Dict[str, Dict[Platform, BasePlatformAdapter]]` + `:5367-5370 set_multiplex_active()` + `:6726-6794 _start_secondary_profile_adapters()` + `:6796-6854 _start_one_profile_adapters()` + `:6857-6864 _make_profile_message_handler()` stamp source.profile + `:6866-6879 _adapter_credential_fingerprint()` salted hash + `hermes_cli/gateway.py:3854-3933 _guard_named_profile_under_multiplexer(force=False)` lifecycle guard + `gateway/status.py:578-603 write_runtime_status(served_profiles=)` observability + 新文档 `website/docs/user-guide/multi-profile-gateways.md 133 行` + 4 套测试 phase0/phase1/credential-isolation/adapter-registry/lifecycle）+ **⭐ Cron Chronos NAS-mediated scale-to-zero provider 11 commit**（新 `cron/scheduler_provider.py:27-178` `class CronScheduler(ABC)` + `is_available()` 不做网络 IO invariant + `start/stop/on_jobs_changed/fire_due/reconcile` 五 hook + `class InProcessCronScheduler` "builtin" 60s ticker + `:114-151 resolve_cron_scheduler() -> CronScheduler` 读 `cron.provider` + 新 `plugins/cron/__init__.py 344 行` discovery + `register_cron_scheduler` 装饰器 + `cron/jobs.py:1076-1090 _machine_id()` + `:1093-1127 claim_job_for_fire(job_id, *, claim_ttl_seconds=300)` 多机 at-most-once CAS + `:979-981 mark_job_run()` 清 fire_claim + 新插件 `plugins/cron/chronos/__init__.py 241 行` `class ChronosCronScheduler` + `_armed` cache + `start()` 不阻塞循环（NAS webhook 推）+ `on_jobs_changed/fire_due/reconcile` + 新 `_nas_client.py 123 行` `class NasCronClient` HTTP wrapper + 新 `verify.py 103 行` `_FIRE_PURPOSE="cron_fire"` + `verify_nas_fire_token(*, token, expected_audience, jwks_or_key, issuer, leeway_seconds) -> Optional[Dict]` 仅 RS/ES + `get_fire_verifier() -> Callable` pluggable + `gateway/platforms/api_server.py:3345-3402 _handle_cron_fire(request)` POST `/api/cron/fire` 401/400/202 + dashboard surface 同走 + `cron/scheduler.py:961-1041 _run_job_script()` + `:1034 env=_sanitize_subprocess_env(os.environ)` 防 provider 凭据泄漏给用户脚本 + docs `docs/chronos-managed-cron-contract.md`）+ **⭐ MCP late-connecting tools snapshot 8 commit**（`tools/mcp_tool.py:4361-4480 refresh_agent_mcp_tools(agent, *, quiet_mode=False)` + `agent._tool_snapshot_generation: int` 原子 publish + `published_gen_raw = getattr(agent, "_tool_snapshot_generation", -1)` + `if snapshot_generation < published_gen: return` 拒陈旧 + `agent._tool_snapshot_generation = max(published_gen, snapshot_generation)` + `agent/turn_context.py:126-129 build_turn_context()` `has_registered_mcp_tools()` 廉价预检 + `refresh_agent_mcp_tools(quiet_mode=True)` + adversarial 3 轮 review（round 1 cache parity + gates + races / round 2 stale-publish race + parity holes / round 3 generation capture adjacency + gateway contract note / revert ACP rebuild）+ `tools/mcp_tool.py:22/291 _DEFAULT_KEEPALIVE_INTERVAL=180` + `:292 _MIN_KEEPALIVE_INTERVAL=5` + `:1694-1696` `keepalive_interval = max(min, float(config.get("keepalive_interval", default)))` 让短 TTL HTTP/SSE MCP 不每轮 reconnect + 新 `tools/mcp_tool.py:1235-1389 class ElicitationHandler` MCP 1.1+ 服务端主动 elicitation 协议 + `:1342 captured = getattr(self.owner, "_pending_call_context", None)` + `:1354 captured.copy().run(request_elicitation_consent, ...)` gateway-aware approval routing）+ **⭐ Managed scope 系统级配置层 10 commit**（新 `hermes_cli/managed_scope.py 215 行` `_DEFAULT_MANAGED_DIR=Path("/etc/hermes")` + `get_managed_dir()` 三级回退 `$HERMES_MANAGED_DIR` env → `/etc/hermes` 非 pytest → None + `load_managed_config/load_managed_env` LRU cached + fail-open + `apply_managed_overlay(config) -> dict` 深合并 + env expansion + `managed_config_keys() -> set` + `is_key_managed(dotted_key)` + `is_env_managed(name)` + `hermes_cli/config.py:5673-5726` user config 加载后调 overlay + `:6119-6211` 写 set 经 `is_key_managed/is_env_managed` 拒掉 managed 键 + `:5840-5842` doctor surface 显 managed_keys + managed `.env` 后置覆盖 + cli.py + standalone loaders + config→env 桥都走 overlay + 新文档 `website/docs/admin/managed-scope.md` admin guide）+ **⭐ Unified provider catalog 8 commit**（新 `hermes_cli/provider_catalog.py:56-68 @dataclass(frozen=True) class ProviderDescriptor` 字段 `slug/label/description/auth_type/tab("keys"|"accounts")/api_key_env_vars/base_url_env_var/signup_url/order` + `:45-52 _ACCOUNTS_AUTH_TYPES` frozenset + `:71-73 tab_for_auth_type(auth_type)` + `:83-165 provider_catalog() -> list[ProviderDescriptor]` 单源 + `:168-170 provider_catalog_by_slug()` + `hermes_cli/web_server.py:3971-4056 _catalog_provider_env_metadata()` 经 catalog 派生 `/api/env` Keys tab 含 Bedrock + `:4059-4100 get_env_vars(profile)` 挂 `provider/provider_label` + `:5836-5881 _build_oauth_catalog()` Accounts tab 经 catalog `flow="external"` 含 catalog-only + `:5884-5924 list_oauth_providers(profile)` + Desktop `apps/desktop/src/app/settings/providers-settings.tsx:121 OAuthPicker()` + Keys tab 按 backend grouping + API-keys 搜索 + priority-sorted + parity test 锁 GUI ⊇ `hermes model`）+ **Raft 新平台插件 3 commit**（新 `plugins/platforms/raft/plugin.yaml` `name:raft-platform / label:Raft / kind:platform / version:1.0.0 / author:botiverse / requires_env:RAFT_PROFILE` + `adapter.py 774 行` `:174 _make_activity_event` schema `raft-activity.v1` + `:260 class ActivityQueue` bounded telemetry + `:438 class RaftAdapter` HTTP 服务器接 wake hints + `:768-774` 注册 7 个 activity hooks + bridge subprocess `stdin=DEVNULL` + AUTHOR_MAP skyzh@mail.build→xxchan）+ **simplify-code skill `db744e7d1`**（`skills/software-development/simplify-code/SKILL.md 212 行` 灵感来自 Claude Code `/simplify` + risk-tiered SAFE/CAREFUL/RISKY + Chesterton's Fence `git blame` 检 fence 缘由 + AI slop 检测（redundant comments / `as any` cast / null-check）+ silent failure 检测（空 catch）+ 3 并行 subagent reviewer reuse/quality/efficiency + 触发关键词 `simplify` / `review my code` / `clean up`）+ **html-artifact skill 加 `9362ce257 (#48899)` 后立即 revert `92451151c`**（HEAD 上 ABSENT）+ **Relay WS-only Phase 3 + hosted gateway 实际 connect 修 + self-provision 改 relay-config+NAS-token + terminal 块禁内部 gateway lifecycle**（`d2c53ff55 (#48294)` `gateway/relay/adapter.py:85,89 set_inbound_handler/set_interrupt_inbound_handler` 全绑 WebSocketRelayTransport 删 signed-HTTP fallback + `a64fc490f (#48828)` hosted gateway 真 connect 修 + `2c6e266e8 (#48724)` `gateway/relay/__init__.py:207 self_provision_relay()` `:219` 触发改"relay_url 配 + NAS token + 无 secret 钉"非 is_managed() + `245b95b09` `tools/terminal_tool.py:2068-2082` gateway 进程内拒 `hermes gateway start/stop/restart` 防 agent 自杀）+ **image_generate 加 image-to-image / editing `c02192ff6 (#48705)`**（`agent/image_gen_provider.py +79` ImageGenProvider ABC `generate()` 加 keyword-only `image_url + reference_image_urls` + 新方法 `capabilities()` 声明 modalities + max_reference_images（默 0 text-only 向后兼容）+ `success_response.modality` + `normalize_reference_images()` + `tools/image_generation_tool.py +426` 顶层 schema 暴露 + 动态 schema 反映当前模型 actual edit capability + 6 plugin 更新 openai/xai/krea/fal/openai-codex + 349 行新 test + text-only 友好错误消息）+ **Desktop UX 18 commit**（Cmd+K Restart gateway `apps/desktop/src/app/command-palette/index.tsx:368` + `apps/desktop/src/store/system-actions.ts:15 $gatewayRestarting` + 状态栏 spinner + Refresh Models cache bust `apps/desktop/src/app/shell/model-menu-panel.tsx:119 refresh:true` + 通知 click `apps/desktop/src/lib/session-ids.ts:15` resume stored session + selectable slash/shell logs `preview-console.tsx:88 selectedLogIds` + reasoning-effort picker `web/src/components/ReasoningPicker.tsx` + session switcher panel `apps/desktop/src/app/session-switcher.tsx:16` + sessions sort by 最近活动 + sessions 实时刷新 + `b936f92b2` prefill 指令通知（/goal /undo）+ 重命名 Restart messaging→Restart gateway + 可选 logs + composer 加 image-to-image 提示）+ **Gateway 鲁棒性 12 commit**（cached agent `max_iterations/max_turns` 每轮从 config 刷新 `gateway/run.py:1206-1214 _current_max_iterations()` + `:14802-14804` refresh + Windows restart silent outage 修 `gateway/status.py:166-203 looks_like_gateway_command_line()` 严格 matcher + `hermes_cli/gateway_windows.py:1304-1325 _wait_for_gateway_absent()` + `:1327-1353 restart()` hardened + Discord 非对话 metadata 跟踪 `gateway/run.py:429-439 _non_conversational_metadata()` + `plugins/platforms/discord/adapter.py:622-625 _DISCORD_NONCONVERSATIONAL_METADATA_KEYS` + `:668-720 _DiscordNonConversationalMessageTracker` 类 + Telegram `/title` topic 命名 `gateway/slash_commands.py:2805-2821` propagate `_schedule_telegram_topic_title_rename()` + Telegram opt-in 在线/离线指示 `gateway/platforms/telegram.py:477-494 _status_indicator_enabled/_status_online_text/_status_offline_text` + `:2245-2298 _set_status_indicator()` async + Discord reply 上下文 hydrate `plugins/platforms/discord/adapter.py:28-40 _Snowflake` + `:4255-4257 _fetch_channel_context(reply_target=)` + `:4315-4382` reply-anchored window scan + clarify dict-choice unwrap `tools/clarify_tool.py:23-53 _flatten_choice()` + Discord buttons soft-truncate）+ **Kanban 5 commit**（机器全局 singleton 锁 `gateway/kanban_watchers.py:26-74 _acquire_singleton_lock/_release_singleton_lock` 防 embedded dispatcher 多副本 + `hermes_cli/kanban_db.py:128-132 RECLAIM_DEFER_GRACE_SECONDS=120` + `:5154-5210 _worker_survived_termination/_defer_reclaim_for_live_worker` + `_terminate_reclaimed_worker` ProcessLookupError → terminated=True + cross-platform UTF-8 lock）+ **OpenViking + Hindsight 6 commit**（OpenViking structured sync attribution + tool parts + `plugins/memory/openviking/__init__.py:124-125 _SYNC_TRACE_ENV` + `_OPENVIKING_RECALL_TOOL_NAMES` + `:2461-2464` 空 tool_id guard + `_TOOL_STATUS_*` 常量 + alias sets + `agent/message_content.py _message_text()/_message_matches_text()` static helpers + Hindsight `plugins/memory/hindsight/__init__.py:53 _MIN_CLIENT_VERSION="0.6.1"` + `:101-111 _ensure_cloud_client_dependency()` lazy install + `plugin.yaml:5 hindsight-client>=0.6.1`）+ **Agent 核心 8 commit**（Anthropic aux 经 stream `agent/anthropic_adapter.py:2538-2589 _is_stream_unavailable_error()/create_anthropic_message()` + `run_agent.py:4079-4085 _anthropic_messages_create()` + FTS5 trigram fallback `hermes_state.py:775-792 _is_fts5_unavailable_error/_is_tokenizer_unavailable_error` + `:858-862 _warn_fts5_unavailable()` 有条件触发 + 非可重试 API 错误摘要 `_summarize_api_error()` 防 HTML 漏 + `hermes_cli/debug_bundle.py` 含 gui.log + session store 不可用警告 + worktree 互斥锁 `e48554a3e`）+ **CLI / TUI 10 commit**（`/model` 切换默认跨会话持久 `hermes_cli/model_switch.py:362-391 resolve_persist_behavior()` + `:304-365 parse_model_flags()` returns is_session + `cli.py:6974` + `hermes_cli/config.py:1590 cli_refresh_interval=1.0` 状态栏 keep-alive + viewport 宽度变化清屏 + worktree 互斥锁 + tmux 内 fast-echo 关 `tui_gateway/server.py` 防游标漂移 + TUI 托管 dashboard chat 不死 + idle exit 重启 + `/update` 守 + pending-input 经 `command.dispatch()`）+ **Dashboard 8 commit**（chat TUI argv 出 event loop + `/api/cron/fire` 路由 + DS Button prefix/size API + upload temp file disconnect 清理 + python-multipart 锁 + Slack allowed users + `"*"` 通配符 + sessions sort by 最近活动）+ **Sessions / Backup / Misc 10 commit**（`hermes_state.py` 递归 CTE 检 compression ancestor 让 child session 继承祖先 title + `hermes_cli/backup.py EXCLUDED_BACKUP_DIRS` frozenset 排除 `.venv/node_modules/__pycache__` + Stripe Projects skill docs + `b266ad748` undici audit bump + `3ca0ef7e3 (#48883)` hashless npm via importNpmLock + `36851fa57 (#48541)` Docker WebUI 只读源支持 + Windows managed-node `7a7b56d49/fcc169057/d4e7dd609 (#49254)` + 多条 AUTHOR_MAP salvage（lgalabru / salesondemandio / OpenViking / carlos.dddo / Cdddo / JoaoMarcos44 / channkim / Sahil-SS9 / tt-a1i 等））— 验证基线 `/tmp/hermes-agent` clone @ `01f581d8d` + 4 个并行 research agent 实证

- [[2026-06-18-update]] — **164 commits 跨日同步**（hermes-agent `d1ecebc → 03d9a95`，仍 v0.16.0 **未发新版**；约 70 fix / 25 feat / 16 test / 12 chore / 10 refactor / 7 docs / 3 perf / 21 merge；~48h 双日窗口；285 文件 +25193/−1708）：**⭐ Relay/A2 Trust-Boundary 传输 Phase 0-3 主轴（23 commit）**（新文件 `gateway/relay/descriptor.py:77 CapabilityDescriptor` frozen dataclass + `to_json/from_json` + `:76-120 from_platform_entry` + `CONTRACT_VERSION=1` additive-only + `gateway/relay/adapter.py:44-130 RelayAdapter(BasePlatformAdapter)` 从 descriptor 读 `MAX_MESSAGE_LENGTH/message_len_fn/supports_draft_streaming/supports_code_blocks` + `:32-41 _utf16_len`/`_LEN_FNS` + `:79-110 connect()` 注 `_on_inbound` handler + `:139-149 on_interrupt` + `:190-221 send_follow_up` + `gateway/config.py:167 Platform.RELAY` + 新 `gateway/relay/transport.py:1-75 RelayTransport(Protocol)` lifecycle/inbound/outbound 全声明 + 新 `gateway/relay/ws_transport.py:102-299 WebSocketRelayTransport` newline-delimited JSON 帧协议 + `hello/descriptor/inbound/outbound/interrupt/outbound_result/interrupt_inbound` 7 帧 + uuid `requestId` 相关 future + 30s handshake/outbound timeout + `:57-100 _event_from_wire` SessionSource wire→dataclass + `:158-174 _upgrade_headers` Bearer 鉴权 + 新 `gateway/relay/auth.py:50-135 make_upgrade_token/verify_token` HMAC_SHA256 多 secret 轮换窗 + 新 `gateway/relay/inbound_receiver.py:204 InboundDeliveryReceiver` aiohttp 双 POST routes `/message + /interrupt` 验 `x-relay-signature/x-relay-timestamp` over 原始 raw 字节（re-serialize 会破 HMAC）+ 300s replay 窗 + 新 `hermes_cli/gateway_enroll.py:250 hermes gateway enroll` 子命令解 Nous Portal token + POST `/relay/enroll` + 持久化 `GATEWAY_RELAY_ID/SECRET/DELIVERY_KEY` 到 `~/.hermes/.env` + `is_managed()` 拒 + Phase 3 `gateway/relay/__init__.py:116-340 self_provision_if_managed()` MANAGED + relay 已配 + 无 secret 钉时进程内自供给 + 凭据仅驻内存（永不写盘）+ `relay_endpoint/relay_route_keys/_provision_url/_post_provision` + 6 套测试（descriptor / roundtrip / interrupt / registration / ws_transport / auth / inbound_receiver / no_stub_leak / sheds_crypto / contract_conformance）+ 新 `docs/relay-connector-contract.md 143 行` v1 契约 §6 A2 trust-boundary 模型重写 + `tests/gateway/relay/stub_connector.py:66 StubConnector` 测试专用 + Telegram round-trip 泛化证明 + `237fa7d29` 删 `HERMES_GATEWAY_RELAY` dark-launch flag 改配置驱动激活）+ **⭐ /billing 终端账单 #45449**（新 `agent/billing_view.py 295 行` surface-agnostic `BillingState` + `parse_money/format_money` Decimal + `CardInfo/MonthlyCap/AutoReload` dataclass + fail-open builder + 新 `hermes_cli/nous_billing.py 406 行` 类型化 HTTP client 接 4 个 `/api/billing/*` endpoint + `BillingScopeRequired/BillingRateLimited/BillingAuthError` 异常 + Idempotency-Key + `cli.py +657 行` 5 个 screen overview/buy→confirm→poll/auto-reload/monthly-limit + `_show_billing` handler + 2s/5min 可取消 poll + `tui_gateway/server.py +216 行` `billing.state/charge/charge_status/auto_reload/step_up` JSON-RPC + 结构化 success envelope + 新 `ui-tui/src/app/slash/commands/billing.ts 332 行` 状态机 + `ConfirmReq` overlay 复用 + insufficient_scope→lazy step-up / no_payment_method→portal 分支 + 新 `ui-tui/src/components/billingOverlay.tsx 684 行` + `hermes_cli/auth.py +97 行 NOUS_BILLING_MANAGE_SCOPE/nous_token_has_billing_scope()/step_up_nous_billing_scope()` lazy 重 auth 不弹模型 picker + 6 套测试 51+6+7+16+12 共 1266 行 + CommandDef 加入 `_SLACK_VIA_HERMES_ONLY`）+ **⭐ OpenViking session-boundary race 12 commit 收口**（4 并发原语：state lock + `plugins/memory/openviking/__init__.py:1664 _inflight_writers: Dict[str, Set[Thread]]` per-session writer registry + `:2080-2102 _spawn_writer` 自注册/自反注册 + `:2129-2156 _drain_writers(sid, timeout)` loop + re-join + `_prefetch_generation` 单调 int worker capture/拒过期写 + `:2232-2260 _finalize_session_async` daemon offload drain+commit；`813a4e383 (#28296)` `:2373-2439 on_session_switch` hook + bounded join 10s + commit if turn_count>0 + drain stale prefetch；`a30b40c73` sync_turn `:2309-2320` capture sid before worker + on_session_end `:2360-2370` reset turn_count；`eddbf291a` re-check is_alive after join 跳 commit + on_memory_write 同 sid 快照 + prefetch_generation 拒 stale；`91e9459e1` per-session writer dict 替单槽 `_sync_thread`；`00c045b43` 综合硬化；`f3b813c02` 测试 preserve content/write writes；`3ac6551ba` `:2399 rewound=kwargs.get("rewound")` `:2411-2430` rewound 时跳旋转只 invalidate prefetch；`c83544890` 命令线程不卡 drain+commit；`2a5d51c16 (#48206)` adapt current api；`4de4a4e2d (#47973)` type-correct sentinels；`5494c1e9b (#48262)` `:858-863 _write_ovcli_config` reuse `atomic_json_write(mode=0o600)`；`99a20f8d9` plugin 期望更新）+ **⭐ Memory batch ops 单轮多操作 #48507**（`tools/memory_tool.py:450-563 MemoryStore.apply_batch(target, operations)` 全 op 按最终预算校验 + 原子写 + 任一失败回滚整批 + `:792-826 _apply_batch_write_gate` 跨 op auth + `:839-922 memory_tool(operations=[{action,content?,old_text?}])` dispatcher + 桥接外部 provider `agent/agent_runtime_helpers.py:1842-1875` + `agent/tool_executor.py:1012-1050` 遍历 batch op 对 add/replace 调 `on_memory_write` + `agent/background_review.py:300-408` batch op 在 summary 显示；live 验证：consolidate-and-add 从 7 调用降到 1）+ **⭐ Skills 用户修改 bundled-skill 发现+diff CLI**（`tools/skills_sync.py:825 list_user_modified_bundled_skills()` 用 manifest origin-hash 基线 + `:813 _is_tracked_user_modification` 共用谓词防 sync loop/discovery 漂移 + `:881 diff_bundled_skill(name)` 文件级 `difflib.unified_diff` 检 modified/added/removed/binary + `:836 _read_for_diff` 单读返 (bytes,text) + `hermes_cli/skills_hub.py:1152 hermes skills list-modified [--json]` + `:1185 hermes skills diff <name>` + `:1870+` `/skills list-modified` `/skills diff` slash 镜像 + `subcommands/skills.py:164/:194` argparse + `hermes update` 提示在 `main.py:9063 + 6073` 两路径都点向新命令 + 不变式测 `tests/hermes_cli/test_update_modified_notice.py:50` 5 行窗内必现 hint；`f6fac60e6` refactor `_skill_file_list()` helper 去重 + diff 契约删 `user_present`；`25c590ccd` rmtree guard 拒 SKILLS_DIR root（不只外部）+ `f1254c8ea (#48200)` `:695 _rmtree_writable()` 严格 child 守 + `hermes_cli/config.py:2502-2510 pre_update_backup` 默 True；6 测 132 行 + 5 case scope guard `tests/tools/test_skills_sync.py:237`）+ **⭐ Langfuse trace state turn/request 隔离**（`plugins/observability/langfuse/__init__.py:247 _trace_key(task_id, session_id, turn_id="", api_request_id="")` 三 tier turn-scoped/request-scoped/legacy；turn_id 优先；`:231 _scope_prefix` 抽取统一 task/session/thread fallback ladder；`:65 _MAX_TRACE_STATE=256` + `:713 _evict_stale_locked()` LRU 驱逐非-finalizing turn 防无界增长 + 驱逐前结 root span；hook 签名升级 `:785/:843/:883/:1050/:1056` 全传 turn_id+api_request_id；端到端隔离回归 `tests/plugins/test_langfuse_plugin.py:254 TestTurnTraceIsolation` 2 turn 2 trace + `:381 test_non_finalizing_turns_do_not_grow_state_unboundedly` 50 turn vs cap=8 + `:402` key 字符串钉死）+ **⭐ Anthropic OAuth wire mcp__ 双下划线前缀 #47723**（关键 wire-format 变更：单下划线 `mcp_` 被 Anthropic 分类器算第三方 → HTTP 400 + extra-usage 计费 lane；`agent/anthropic_adapter.py:375 _MCP_TOOL_PREFIX="mcp__"` + `:2349+ tool prefix 跳重` + `agent/transports/anthropic.py:91 _MCP_PREFIX="mcp__"` + `:2370-2391 _to_oauth_wire_name()` 归一化所有 `mcp_` 到 `mcp__`（裸 native + MCP server 工具如 `mcp_linear_get_issue`）应用于 tool 定义和消息历史 tool_use 块 + `:132-151 normalize_response` 反向解经 registry 查找：registered name → single-underscore + bare → bare name → 兜底保留 `mcp__` wire；测试重写：零单下划线到达 wire；mcp__ 圆 trip 解为 registered name；实证：mcp_ 翻请求到 extra-usage / mcp__ 在 plan）+ **⭐ Curator skill consolidation 改 opt-in #47840**（`agent/curator.py:64 DEFAULT_CONSOLIDATE=False` + `:190-203 get_consolidate()` 读 `curator.consolidate` 默 False + `:1428 run_curator_review(consolidate=None)` None 读 config / True/False 覆盖 + `:1546-1570` `not consolidate` 跳 fork aux-model review 写 prune-only；`fc1119ca6` rollback safety snapshot 自保护：protect set 经 `snapshot_skills()` 穿到 `_prune_old()`；`992b92238` restore 收紧到 archive 14 位时间戳后缀防同前缀无关 skill）+ **Dashboard 流式 multipart 上传 #47663**（`hermes_cli/web_server.py:1486-1560 upload_managed_file_stream` `UploadFile+Form` 1 MiB 块直读到 temp + 100 MB cap streaming 中 enforce 413 + 原子 os.replace + 加 python-multipart 依赖；前端 `api.uploadFile` 改 multipart/form-data；5 endpoint 测）+ **supervised gateway --replace #47555** + **Docker self-modification 硬化 #47490** + **runtime-pid 本机 only / await-safe config scope / Chat tab session-end 恢复 #47674 / 完整 MCP catalog 明细 #48520** + **Memory provider 设置 UX 10 commit**（手动 setup path + 密钥类型前置 + 预创建 0600 TOCTOU 关 + chmod 失败日志 + autostart 不健康端点跳）+ **Hindsight memory provider 入 Desktop 配置 #37546**（新 `apps/desktop/src/app/settings/provider-config-panel.tsx 1-182` + schema-driven `hermes_cli/memory_providers.py 1-149 ProviderField/ProviderFieldOption` + GET/PUT `/api/memory/providers/{name}/config`）+ **xAI grok-composer-2.5-fast picker + 200k ctx + native web_search swap-only + incomplete-call 守**（`agent/model_metadata.py:7` grok-composer 200000 / `:3+ grok-composer-2.5-fast→262144` + `agent/transports/codex.py:60+/43+` swap-only web_search + `agent/codex_responses_adapter.py:69+` 不算 in_progress 为 incomplete + `hermes_cli/models.py:21+` curated 加入）+ **Codex 设备码 429 重试 #47860**（`hermes_cli/auth.py:78+` capped 指数退避 4 次 + Retry-After 尊重 + raise `AuthError(code=CODEX_RATE_LIMITED_CODE)` 含 wait hint）+ **Codex session_id/x-client-request-id headers 复原 #47335**（`agent/transports/codex.py:26+`）+ **Model picker max_models=0 ≠ unlimited 语义锁 + 移除 50 cap #48279**（`hermes_cli/model_switch.py:10+` 改 `is not None` check / `build_models_payload/list_authenticated_providers/list_picker_providers` 默改 None）+ **Prompt 跨平台 platform_hints override**（`agent/agent_init.py:1242` 读 `agent._platform_hint_overrides` + `agent/system_prompt.py:61-113 _resolve_platform_hint()` append/replace/shorthand + `hermes_cli/config.py:2173-2189 DEFAULT_CONFIG`）+ **Context-file cap 按模型 ctx 自适配 #47846**（`agent/prompt_builder.py:961-984 _dynamic_context_file_max_chars()` `ctx_tokens*4*0.06` floor 20K ceiling 500K + `:1540-1549 _truncate_content(context_length, read_path)` 标记点 truncated 文件路径 + 引导 read_file）+ **parallel-tool-call steer 去重**（`agent/prompt_builder.py` 删 Google-only 重复段 + 修正注释）+ **Resume 跟随 compression-tip #48633**（`hermes_state.py:2820-2841 resolve_resume_session_id()` 经 `get_compression_tip()` forward 跟随 + 只跟 `end_reason='compression'` parent，不混 delegation/branch；`tui_gateway/server.py:4419-4442 session.resume` 走 resolver）+ **/compress 跳旋转保护**（`gateway/slash_commands.py:2588-2613` 仅当 `_compress_context` 产新 sid 才调 `rewrite_transcript`；否则保原 transcript log 警告）+ **旋转前 flush 待持久化消息 #47202**（`agent/conversation_compression.py:512-523` + `cli.py:5975-5989`）+ **Kanban 自动订阅 calling session on create**（worker 在持久通道里调 `kanban_create` 自动订阅；门 `kanban.auto_subscribe_on_create` 默 True）+ **Backup hermes import 跳易失 gateway 状态 #48243**（`_IMPORT_SKIP_NAMES={gateway_state.json, gateway.pid, cron.pid, gateway.lock, processes.json}` basename 跳）+ **macOS launchd in-tree 推迟 #47842**（`hermes_cli/gateway.py:51` 检 `_is_pid_ancestor_of_current_process()` 委派 bootout→bootstrap 给 detached `start_new_session=True` helper）+ **Update install-method stamp scope 改 code-tree #48188**（不再放 `$HERMES_HOME/.install_method` 防 Docker bind-mount 污染主机 stamp）+ **Photon iMessage mixed attachment text 保留 #46818**（`plugins/platforms/photon/adapter.py:119+` 处理 `'group'` content + `_normalize_binary_payload` helper + sidecar `index.mjs/patch-spectrum-mixed-attachments.mjs` 155 行 postinstall+startup 应用 + Windows pathToFileURL + CRLF↔LF）+ **cua-driver MCP 操作员环境 scrub #37878**（`tools/computer_use/cua_backend.py:3+ _sanitize_subprocess_env()` 防 credential exfil 到第三方 binary）+ **MCP catalog Unreal Engine 5.8**（`optional-mcps/unreal-engine/manifest.yaml 54 行` HTTP `127.0.0.1:8000/mcp` 无 auth）+ **Agent empty-name 工具调用循环抑制 #47967/#48109**（`agent/conversation_loop.py:26+` empty-name → 短 anti-priming error 无 catalog dump / 非空 unknown 仍获 catalog）+ **多模态用户内容持久化保留**（`run_agent.py:10+` 避免 text-only persist_user_message override 覆盖 ACP image block；Closes #44242）+ **agent-callable send_message tool 撤回 #47856**（`toolsets.py:14+` drop model-tool 注册 + 空 'messaging' toolset；send engine 保留给 cron/gateway notifier/`hermes send` CLI；Yuanbao 指 `yb_send_dm`）+ **search_files headroom densification #47866**（`tools/file_operations.py:250-278 _densify_matches()` path-grouped lossless 57.8% token 降；≥5 matches 触发；ripgrep path-ordered 不变式）+ **Approval glob allowlist #43051**（`tools/approval.py` glob 命令 allowlist 守 + shell chaining 防）+ **Container 检测 cgroup-v2/containerd/CRI/K8s #47131**（`hermes_constants.py:486+` 加 `KUBERNETES_SERVICE_HOST` env + `kubepods/containerd/crio` marker + `/proc/self/mountinfo` fallback）+ **Windows 跨进程日志轮替**（`hermes_logging.py:36+ ConcurrentRotatingFileHandler as RotatingFileHandler` 仅 win32 + portalocker pywin32 锁 + concurrent-log-handler==0.9.29 marker）+ **install.ps1 PowerShell host 解析 + EAP 松绑后 uv venv fail-fast + native stderr 松绑**（`scripts/install.ps1:320 Get-PowerShellHostExe` 用 `Get-Process -Id $PID` 取本机绝对路径 / `:186 Invoke-NativeWithRelaxedErrorAction` EAP=Continue 包 git/uv / `:1460-1471` relax 后立刻捕 `$LASTEXITCODE` 非 0 throw）+ **Desktop Hindsight 入面板 + stranded session 窗恢复 + electronDist 动态 + Electron 40.10.2 钉 + PTY before-quit + secondary 窗 streaming + perf 硬化 23 commit**（`apps/desktop/scripts/run-electron-builder.cjs 57 行 require.resolve("electron/package.json")` + `electron-builder.cjs --build-only` retry once `apps/bootstrap-installer/src-tauri/src/update.rs:296-328 rebuild_needs_retry()` + `react-simple-icons@=13.11.1` + nix npmDepsHash 刷新 + `markdown-text.tsx:461 MAX_MARKDOWN_CHARS=200_000` + `:463-500 HugeTextFallback` content-visibility:auto + `:541/546/549 RTL dir="auto"` + `markdown-preprocess.ts:154-160 extend()` 替 spread 防 V8 overflow + `electron/main.cjs:6554-6558 before-quit dispose terminalSessions` + `electron/session-windows.cjs:24-34 chatWindowWebPreferences { backgroundThrottling:false }` + `desktop-controller.tsx:753-785 dismissError` 双清 + `tui_gateway/server.py:5004 _ensure_session_db_row` `/title` 前置创行 + `lib/model-status-label.ts:25-34 currentPickerSelection()` pre-session 黏 + `store/updates.ts:99-145 SKEW_TOAST_COOLDOWN_MS=24h` 24h snooze + `use-message-stream.ts:1084-1109 review.summary` 持久 system 消息）+ **Telegram reply 媒体附件**（之前 sync 已覆盖：`gateway/platforms/telegram.py:5563-5606 _cache_replied_media()` 下载 + cache + 文本附 `[Replied-to <kind> '<name>' saved at:…]`）+ **TUI Enter slash 完成 + MCP late refresh + MCP 计数 connected only #48425/#48403/#48402**（`ui-tui/src/domain/slash.ts applyCompletion` trailing-space-only 算 noop fall-through 提交 + `tui_gateway/server.py:3413 _schedule_mcp_late_refresh` 30s 等 discovery + rebuild snapshot 仅 pre-first-turn 安全 + 重 emit session.info + `ui-tui/src/components/branding.tsx:223+ mcpConnected=filter(s.connected)` 替全部配置计数）+ **CLI keystroke 延迟 + URL 不当 path 补全 + 弃用 login 优雅降级 #47701/#47706**（`cli.py:12714 ThreadedCompleter(_completer)` 把补全移出 event loop / `hermes_cli/commands.py:1291+ if "://" in word: return None` 拒 URL token / `hermes_cli/subcommands/login.py:10` 删 `choices=` + `description=` 让任何 provider 都到 handler 打 redirect）+ **dump 显 commit 日期 #48104**（`hermes_cli/dump.py:59 _get_git_commit_date()` 跑 git log 返 YYYY-MM-DD；Docker/wheel 无 .git 时返空）+ **6 条 AUTHOR_MAP salvage**（iamlukethedev / srojk34 / infinitycrew39 / Adolanium / #47904 / Rivuza 全进 `scripts/release.py` Python dict）— 验证基线 `/tmp/hermes-agent` clone @ `03d9a95` + 5 个并行 research agent 实证

- [[2026-06-16-update]] — **65 commits 单日同步**（hermes-agent `cffd6e3c8 → d1ecebc`，仍 v0.16.0 **未发新版**；约 25 fix / 18 feat / 6 chore / 5 test / 4 docs / 1 refactor / 6 merge；~28h 单日窗口；本期 65 commit 全部 committer date ∈ (2026-06-15 20:35 UTC, 2026-06-16 20:40 UTC] 的真正新增；`cffd6e3c8..HEAD` 总 141 commit 中其余 76 个为前期 sync 已覆盖的旧 PR salvage，本期不重复记录）：**⭐ Desktop composer model picker 全面替代 statusbar（PR #46959 4 commit）**（新文件 `apps/desktop/src/app/chat/composer/model-pill.tsx 72 行` 订阅 `$currentModel/$currentProvider/$currentFastMode/$currentReasoningEffort` + `apps/desktop/src/store/model-presets.ts 86 行` per-model effort/fast preset 本地化 `STORAGE_KEY="hermes.desktop.model-presets"` 不写穿网关 `agent.reasoning_effort` + `apps/desktop/src/store/session.ts:18-23` 4 个 composer localStorage key + `apps/desktop/src/app/shell/model-menu-panel.tsx:45 ModelMenuCloseContext` Context 关闭 + `989d5d0` `model-status-label.ts:71 /-\d{8}$/` 折叠日期 pinned snapshot + `7d938cc` `agent/conversation_loop.py:303 _stored_prompt_matches_runtime` + `tui_gateway/server.py:1684 _persist_live_session_system_prompt` + `:1705 _append_model_switch_marker` 注 `[System: The active model for this chat has changed to {model} via provider {provider}…]`）+ **⭐ Compact session window 家族 PR #46951**（`apps/desktop/electron/main.cjs:5087 spawnSecondaryWindow({sessionId,watch,newSession})` + `:5148 createSessionWindow` + `:5155 createNewSessionWindow` + `apps/desktop/src/store/windows.ts:9 NEW_SESSION_WINDOW_FLAG="1"` + `:33 isNewSessionWindow()` + `:110-114 openNewSessionInNewWindow()` + 新 keybind `session.newWindow` 默认 `mod+shift+n` + 新文件 `apps/desktop/src/store/session-sync.ts 25 行` BroadcastChannel `hermes:sessions` 跨窗口 sidebar 刷新 + `0f75e99` secondary 窗口隐 titlebar 工具组/statusbar/intro 经 `isSecondaryWindow()`）+ **⭐ Watch window subagent 流 PR #47060**（`tools/delegate_tool.py:870-876` 转发 `subagent.text` + `:1638-1647 _relay_child_text(delta)` 经 `child.run_conversation(stream_callback=_relay_child_text)` + `tui_gateway/server.py:907-913 _start_agent_build` lazy watch 跳 + `:2878-2884` 父 emit 对 `subagent.text` 跳 + `:2945-2951 _mirror_subagent_to_child` 把 `subagent.text→message.delta` 推 child sid + `:4248-4258` lazy `session.resume` 在 `_child_run_active` 时早返空 history WSL race 修 + `chat/index.tsx:124-127` `ChatHeader` 在 `isSecondaryWindow()` 早返 + `thread-list.tsx:265-270` 不透明拖条）+ **远端 gateway artifact 鉴权下载 PR #46895 + 全局 remote profile REST 路由 PR #47011**（`hermes_cli/web_server.py:253 _QUERY_TOKEN_API_PATHS=frozenset({"/api/files/download"})` + `:1425 @app.get("/api/files/download")` Content-Disposition attachment 鉴权门 + `apps/desktop/src/lib/media.ts:62 mediaExternalUrl(path)` 远端感知 + `apps/desktop/electron/connection-config.cjs:175 pathWithGlobalRemoteProfile()` global-remote 模式自动加 `?profile=`）+ **Electron pack 失败镜像 redownload PR #47266**（`hermes_cli/main.py:5113 _electron_dist_binary` + `:5144-5194 _redownload_electron_dist(... mirror=)` + `:5489-5493` 镜像 `https://npmmirror.com/mirrors/electron/` 重试）+ **`/browser` 限本地 gateway PR #47245**（`apps/desktop/src/lib/desktop-slash-commands.ts:33` 加 `'browser'` 进 `DesktopActionId` + `use-prompt-actions.ts:1148-1219` `$connection.mode==='remote'` 时报"local gateway only"）+ **外部 provider 终端断开**（`apps/desktop/src/app/right-sidebar/store.ts:16 $terminalInjection` + `runInTerminal(command)` + `terminal/use-terminal-session.ts:680-700` 自动写 `${command}\r`；`disconnect_command` 进 provider schema）+ **⭐ `display.memory_notifications` 三态 off/on/verbose 3 feat**（`hermes_cli/config.py:1441 "memory_notifications":"on"` + `agent/agent_init.py:303 agent.memory_notifications` + `agent/background_review.py:626 summarize_background_review_actions(notification_mode=)` + Memory `➕/✏️/➖` 预览 120/60 char + `gateway/run.py:14805-14812` 网关解析 + 5 个新单测；`4cf9d80` skill_notifications verbose 预览 `tools/skill_manager_tool.py:616/667/764` `_change:{description/old,new}` 进 background_review + `📝 Skill '{name}' patched: "old" → "new"` 渲染；`c2c55c4` 全 provider 共用 `_strip_skill_scaffolding` `agent/skill_commands.py:58 extract_user_instruction_from_skill_message()` + `_SKILL_INVOCATION_PREFIX/_SINGLE_SKILL_MARKER/_BUNDLE_*/_RUNTIME_NOTE` 标记常量 + `agent/memory_manager.py:435 _strip_skill_scaffolding` 静态 + `prefetch_all/queue_prefetch_all/sync_all` 三调；`e3adbb5` openviking 先在插件内 `_derive_openviking_user_text()` 后 c2c55c4 改 delegate 共享 helper）+ **⭐ Hooks `session:compress` 事件 `e76e7b5`**（`agent/agent_init.py:198 event_callback: Optional[Callable[[str, dict], None]]` 接口 + `:430` 赋值 + `agent/conversation_compression.py:611-618` 触发 payload `{platform, session_id, old_session_id, compression_count}` + `gateway/run.py:14374 _event_callback_sync` 桥 `_hooks_ref.emit(event_type, context)` 经 `asyncio.run_coroutine_threadsafe` + `:14747 agent.event_callback=_event_callback_sync` + `run_agent.py:388` 透传 + 3 个新测）+ **`prompt.context_file_max_chars` 可配 + 截断警告 contextvar 隔离**（`hermes_cli/config.py:1110 "context_file_max_chars":20_000` + `agent/prompt_builder.py:962 _get_context_file_max_chars()` + `:1516 _truncate_content(max_chars=)` + `:1522` 警告 "increase context_file_max_chars or trim the file!"；`6ebc449` `agent/prompt_builder.py:11 import contextvars` + `:984 _truncation_warnings = contextvars.ContextVar("context_file_truncation_warnings", default=None)` + `_record_truncation_warning` + `drain_truncation_warnings()` 隔离）+ **`tool_progress_style` → `tool_progress_grouping` 改名（同日 fc956b9 引入→6373aba 改名）**（`gateway/display_config.py:35 _GLOBAL_DEFAULTS` + `:242 _normalise` 保 `accumulate|separate` + `gateway/run.py:13688` 解析 + `:14027 can_edit = progress_grouping != "separate"` + `hermes_cli/config.py:1498 "tool_progress_grouping":"accumulate"` + `tests/gateway/test_display_config.py:454-510 TestToolProgressGrouping`）+ **Codex pool-only rate-limit 不弹 relogin `9a59ad7`**（`hermes_cli/auth.py:723 CODEX_RATE_LIMITED_CODE="codex_rate_limited"` + `:3875 _codex_pool_rate_limit_status()` 扫 `auth_store["credential_pool"]["openai-codex"]` `last_status="exhausted"` + 429/quota/rate_limit + `:3809-3826 raise AuthError(code=CODEX_RATE_LIMITED_CODE, relogin_required=False)` + `:6003-6014 get_codex_auth_status()` 返 `{rate_limited:True, reset_at}`）+ **xAI 默认 `grok-build-0.1` `bbc842d+f4ef70f`**（`hermes_cli/models.py:113 _XAI_STATIC_FALLBACK[0]="grok-build-0.1"` + `:121 _XAI_TOP_MODEL="grok-build-0.1"` + `plugins/web/xai/provider.py:49 DEFAULT_MODEL="grok-build-0.1"` + `model_setup_flows.py:520 _prompt_model_selection` fallback；docs `web-search.md/xai-grok-oauth.md` 同步；grok-4.3 仍在 `:114`）+ **Models live+curated 合并 + curated-first + validate 兜底 PR #46857 3 commit**（`630b438` `hermes_cli/models.py:2263-2280` Anthropic 分支 `curated = list(_PROVIDER_MODELS.get("anthropic", []))` → `merged = list(curated)` → 大小写 insensitive 追 live-only ID；`ee7b8a4` `:3585 validate_requested_model` + `:3947-3962` curated catalog 兜底返 `accepted=True, recognized=True`；`658ac1d` `:1762 _model_in_provider_catalog(name_lower, providers)` + `:3952` 验证用纯目录 helper + `:1861, 1896` 其他 catalog 成员检查走该 helper）+ **`model.base_url` 透传 fetch_models `1b962f0`**（`providers/base.py:162-217 fetch_models(*, api_key, base_url, timeout)` 解析顺序 `models_url → base_url → self.base_url + "/models"` + `hermes_cli/models.py:2374 live = _p.fetch_models(api_key=api_key, base_url=base_url or None)` + custom/openrouter 子类 override；新测 `tests/providers/test_fetch_models_base_url.py 140 行`）+ **无 api_key 仍探针 /v1/models `1039e90`**（`hermes_cli/model_switch.py:1741-1758` 注释 "With an api_key: always probe… Without an api_key AND no explicit models: probe anyway" + `should_probe = bool(api_url) and discover and (bool(api_key) or not has_explicit_models)`）+ **自定义 provider dedup 保留 `b7fa62c`**（`hermes_cli/inventory.py:175-195` 先扫 `is_user_defined` 收 `user_models` 再 `if row.get("is_user_defined"): continue` 才调 `_is_aggregator(slug)`）+ **GLM-5.2 入 OpenRouter+Nous `b2da39a` PR #47391**（`hermes_cli/models.py:64 ("z-ai/glm-5.2","")` + `:187 "z-ai/glm-5.2"` Nous（在 glm-5.1 前）+ `:263 "glm-5.2"` zai picker + `website/static/api/model-catalog.json`）+ **auxiliary auto 任务沿主 fallback chain PR #47235**（`agent/auxiliary_client.py:3106 _fallback_entry_api_key` + `:3117 _resolve_fallback_entry(... ←resolve_provider_client)` + `:3135 _try_main_fallback_chain` 读 `get_fallback_chain(load_config())` 迭代跳 failed/main/unhealthy + `_resolve_auto` 接 `task` 参在 Step 2 插 + `resolve_provider_client(... task=None)`）+ **Telegram 富文本默认开 + 流式 final 仍 rich PR #46890 `a6364bf`**（`gateway/platforms/telegram.py:428 _rich_messages_enabled` 默认翻 True + `:984 _needs_rich_rendering()` 表/任务列表/`<details>`/block-math + `:1006 _rich_eligible()` 共享核心去 expect_edits 门 + `:1257 _try_edit_rich(...)` editMessageText + rich_message 路 + `:2680-2681 if finalize and self._rich_eligible(content): rich_result = await self._try_edit_rich(...)`）+ **Rich-reply 跨重启 send-time 索引恢复 `01ae9b8+8ed16a7`**（新文件 `gateway/rich_sent_store.py` `_MAX_ENTRIES=1000` + `record(...)/lookup(...)` + JSON store 在 `~/.hermes/state/rich_sent_index.json`；`telegram.py:1248-1249` 发送后 record `(chat_id, message_id)→content` + `:6718-6720` reply 时 text+caption 都空 → `reply_to_text=rich_sent_store.lookup(...)`；107 行新测试 4 个）+ **Slack `/debug` via `/hermes` PR #47248 `4d470b3`**（`hermes_cli/commands.py:1057 _SLACK_VIA_HERMES_ONLY=frozenset({"credits","debug"})` 加 debug；与 Telegram 对齐）+ **Mattermost notify-only flat-fallback `16fc717+5a0e0d3`**（`gateway/platforms/base.py:80 _mark_notify_metadata(metadata)` 浅克隆置 `notify=True` 应用 `:3899/:4005/:4055/:4282`；`plugins/platforms/mattermost/adapter.py:155 _thread_root_for_send` + `:170 _last_post_failure_is_broken_thread_root` 400/404+body 标识 + `:181 _post_preserving_thread` 仅 `metadata.get("notify")` 且 broken-root 时 flat-fallback；接 `:352/:534/:575/:663` text/image/file/document 发送）+ **WhatsApp/email `send_image(metadata=)` `e65d74b`**（`gateway/platforms/whatsapp.py:843-862` + `gateway/platforms/email.py:675-683` 接 metadata 参 + 新契约测试 `tests/gateway/test_media_metadata_contract.py 80 行` + `_accepts_metadata()` helper 扫所有 adapter）+ **Photon `spectrumProjectId` 收敛 `5b3fa26`**（`plugins/platforms/photon/auth.py:15-17` post-unify `project.id==spectrumProjectId` 全统一 + `:168 load_dashboard_project_id()` 优先 `spectrum_project_id` 而非 legacy `dashboard_project_id` + `:185` entry-key fallback 链 + 删 `get_project()` 与 `spectrum: true` 切换始终 provisioned）+ **⭐ Honcho 网关身份树 gateway-gated 替换部署形状 PR #44431 merge `d2b34e8`**（`bb5cb32 _migrate_pin_peer_name()` pop `pinPeerName` 写 `pinUserPeer` 经 `plugins/memory/honcho/cli.py:380-391`；`d7dfeed _gateway_platforms()` 在 `cli.py:394-408` 懒导 `gateway.config.load_gateway_config` + `:681-707` 菜单 `[1] just me / [2] me + others / [3] only others / [s] skip / [e] edit raw keys` + `:724-735` orphan-guard + `:741/:754-758/:766-769` 写 `pinUserPeer`/`userPeerAliases`/`runtimePeerPrefix`；docs `99feb03` + `23a7458` + `c7513df` non-agent + pin-overrides-aliases；隐私 `2708c33`+`1544813` alice + UID `7654321`；后续 `6dde7d4` `memory-providers.md:98/:211` + `c7513df` clarify）+ **Skills 跳支持目录 `9137b86`**（`agent/skill_utils.py:50 SKILL_SUPPORT_DIRS=frozenset(("references","templates","assets","scripts"))` + `:73 is_skill_support_path(path)` + `:68 should_exclude_skill_path` OR；`tools/skills_tool.py:84/1027/1032/1043/1050/1077` 接 `_is_skill_support_path` 守 `skill_view`/发现）+ **shop-app → CLI 化 shop skill v1.0.1**（`5094325` `optional-skills/productivity/shop/SKILL.md:2-7 name:shop version:1.0.1 prerequisites.commands:[curl,node]` 引 `catalog-mcp.md/direct-api.md/safety.md/legal.md`，旧 `productivity/shop-app/SKILL.md` 删；`d7668aa` description ≤60 char + 作者 Joe Rinaldi Johnson；`e236bb8` 文档 regen `productivity-shop.md 238 行`）+ **TUI session-create no-race 测试隔离 PR #47230 `2483200`**（`tests/test_tui_gateway_server.py:4901-4942` 快照 `server._sessions` → `_saved_sessions` + `clear()` + `finally` 还原 + `agent_ready.wait(timeout=2.0→10.0)` + 断 `built` 返回值；指 `server.py:1011 'replaced'` check）+ **5 条 AUTHOR_MAP salvage**（x1erra/chimpera/cyb0rgk1tty/Joe Rinaldi Johnson/zimigit2020/MrDiamondBallz 全进 `scripts/release.py` 而非 `tools/release/AUTHOR_MAP.json` Python dict）— 验证基线 `/tmp/hermes-agent` clone @ `d1ecebc` + 4 个并行 research agent 实证

- [[2026-06-15-update]] — **77 commits 单日同步**（hermes-agent `2a14e8957 → cffd6e3c8`，仍 v0.16.0 **未发新版**；约 44 fix / 7 test / 4 feat / 4 chore / 3 docs / 2 🐛 fix / 2 refactor / 1 revert / 1 redact / 1 nix / 1 ci / 1 change / 1 unscoped / 5 merge；~31.5h 跨夜单日窗口；164 文件 +18569/−17867）：**⭐ feat — 异步后台子代理 `delegate_task(background=true)` `c66ecf0bc (#40946)`**（新文件 `tools/async_delegation.py 386 行` `_records` 注册表 + lock 内原子容量检查 + ThreadPoolExecutor daemon + `process_registry.completion_queue` 推 `type="async_delegation"` event + `tools/delegate_tool.py:2261-2270` **从 `parent_agent._active_children` 解绑**让父中断/缓存 evict/`/new` 不杀后台 + `tools/process_registry.py:1549-1616 _format_async_delegation()` 渲 `[ASYNC DELEGATION COMPLETE — <id>]` 自包含块 + `gateway/run.py:5393/:12337-12382` watcher 2s 轮询非 async event 立即 requeue + `_inject_watch_notification` 注入合成消息 + CLI `/stop`/`/agents` + `tui_gateway` 按 `delegation_id` dedup + `hermes_cli/config.py:1778 delegation.max_async_children=3` + 473 行 15 测试）+ **feat — Hindsight observation_scopes 可配置 `a376ca008`**（`HINDSIGHT_RETAIN_OBSERVATION_SCOPES` env + `_OBSERVATION_SCOPE_KEYWORDS={"per_tag","combined","all_combinations"}` + tool retain 路径切 `aretain → aretain_batch` 因前者不接此参）+ **feat — `/status` 显 model+context `ead38107a`**（`gateway/slash_commands.py:395-509 _handle_status_command()` + 17 locale 同步）+ **feat — Display 显 `delegate_task` goals `5035fa902`**（`agent/display.py:180 _delegate_task_goal_parts` + tool preview / cute 行）+ **Auth — xAI OAuth 跨 profile 解析 + write-through global root + access skew 120→3600s（PR #43589 salvage 5 commit）**（`hermes_cli/auth.py:3899 _xai_oauth_state_from_store()` 扫 `credential_pool["xai-oauth"]` + `:3946-3956 _read_xai_oauth_tokens()` profile 缺 → global root + `:4007 _write_through_xai_oauth_to_global_root()` 含 pytest 安全带 `:4030` + `:111 XAI_ACCESS_TOKEN_REFRESH_SKEW_SECONDS=3600`）+ **MCP — fail-fast noninteractive OAuth `dcc321695`**（`tools/mcp_oauth.py:756-763 build_oauth_auth()` raise `OAuthNonInteractiveError` 含 `hermes mcp login` hint）+ **Disk-cleanup protected walks 显式 frozenset + 后序 DFS `c1a70a543/40699c329/a688d2a1b`**（`_EMPTY_DIR_PROTECTED_TOP_LEVEL` 14 项 + `_EMPTY_DIR_SWEEP_PRUNE_DIRS` 6 项 + `sweep_stack: List[Tuple[Path, bool]]` 显式后序 DFS 替 `rglob("*")` 全遍历防 gateway event loop 卡分钟级）+ **API debug dump 全字段 redact `ad58dd51a + aab2e99ba`**（`agent/agent_runtime_helpers.py:1228-1231 dump_api_request_debug()` 经 `redact_sensitive_text(force=True)` + `agent/redact.py:107 _PREFIX_PATTERNS` 加 `r"ntn_[A-Za-z0-9]{10,}"` Notion + dump 无条件触发）+ **Gateway scoped lock 第三 stale 分支 `ec05d2bc3`**（`gateway/status.py:646-660` PID + start_time 都非 None 且相等但 `not _looks_like_gateway_process(existing_pid)` 时调 `_read_process_cmdline` 检 cmdline 可读 → `stale=True`）+ **Bedrock `boto3>=1.34.59` 软守 `2cddc9c89`**（`agent/bedrock_adapter.py:61 _MIN_BOTO3_VERSION=(1,34,59)` + `:64-88 _require_boto3()` 解析 tuple；parse 失败不阻塞）+ **s6 / Supervisor 5 修硬化**（`40d7c264f #46266` `register_profile_gateway(start_now=False)` + `(tmp_dir/"down").touch()` marker；`61ee2dbfd #46291` logs gateways 父目录 chown；`b77096726 #46292` `_write_gateway_desired_state()` + `container_boot.py:258 _read_desired_state` 优先 `desired_state` 字段；`c2b7669ad #46289` `rm -f "$log_dir/lock"` 在 s6-log exec 前；`95715dcb0 #46483` `_apply_profile_override` 守 `HERMES_S6_SUPERVISED_CHILD` 让 supervised `gateway-default` 跳过 sticky `active_profile`）+ **Container / Dashboard 4 修**（`4eb0ff639 #46290` `service_manager.py:118` gate 改为裸 `_s6_running()` 修 Fly Firecracker；`aca11c227 #46293` `_is_dashboard_container()` autodetect 跳 reconcile；`80f8ffc74 #46487` machine-dashboard reroute 钉 `get_default_hermes_root()`；`f79551378 #45120` Windows `taskkill /F /T /IM hermes.exe` + 800ms sleep 释 `_bcrypt.pyd` 锁）+ **文件工具 session cwd 第二步（PR #46552 salvage 5 commit）**（`tools/file_tools.py` resolver 在 live-cwd 与 `$TERMINAL_CWD` 间插 "registered task/session cwd override" 步 + `tools/terminal_tool.py:1037-1052 resolve_task_overrides(task_id)` 单 helper + `file_tools.py:99-113 _sentinel_free_abs_cwd(raw)` 抽 sentinel + expanduser + isabs validation）+ **Cron `jobs.json` 跨进程 fcntl/msvcrt 锁 `e5b4cf7be + 733472952`**（`cron/jobs.py:58-59` RLock + `:70 _jobs_lock()` contextmanager Unix `fcntl.flock(LOCK_EX)` / Windows `msvcrt.locking(LK_LOCK,1)` + 全 mutator 包 + `agent/curator_backup.py` 也走 `_jobs_lock`）+ **Process — completion 五态 + termination_source `be7c919bf #46659`**（`tools/process_registry.py:103-104 completion_reason` 枚举 `exited|killed|lost|failed_start|already_exited` + `termination_source` 枚举 `process.kill|kill_all|backend_lost|failed_start` + `format_process_notification` 按 reason 选 status 文本）+ **WhatsApp group JID 路由 target `ea49a7963 + 0d82060c7`**（`tools/send_message_tool.py:47-49 _WHATSAPP_JID_RE` 匹配 `@g.us|s.whatsapp.net|lid|broadcast|newsletter` + `gateway/channel_directory.py:26 CHANNEL_ALIASES_PATH` overlay 标 group `type="group"`）+ **Desktop cluster 10 commit**（`@assistant-ui/store` 0.2.13 pin + vite build CI + macOS Electron pack 重命名前补缺脚本 + 远端 profile 切 `$connection` resync 让远端 image 走 bytes attach + dashboard chat sidebar profile-scoped + 显式 model 切换绕 broken provider + HERMES_HOME 从 Windows registry 读 + tsconfig ES2023 + hover-reveal scrollbar inset）+ **Install / Deps**（npm bundled-Node global prefix 重路写 `$HERMES_HOME/node/etc/npmrc` + websockets 入核 + Teams SDK installable extra + dev setuptools 81→torch 兼容）+ **CI**（Nix CI 临时拆 + Desktop renderer build on PRs + esbuild/vite 锁回升 + `hermes_cli/main.py` autostash）+ **Discord — slash 100 cap PR #46078**（`_DISCORD_MAX_APP_COMMANDS=100` + `slot_cap-1` 保 `/skill` group + 200 commands 回归测）+ **`revert(web)` Parallel keyless search 撤回 `f3fe99863 #46350`**（净 -345 行 + 删两测试文件）+ **Misc**：`a829e04d6 #46345` profile clone 配置迁移 + `b2a476646/2a08b8c86` `hermes debug` 显终端 backend override + `2f2e3616b` browser inactivity timeout 入 config + `5b2604df9` state v11 跳 v10 trigram 回填 + `9df1a1a8d` doctor 加 nvidia vendor-slug-accepting + `60cc42e38` inventory user/aggregator 模型去重 + `ba3883cd1/49e743985` MiniMax M3 reasoning extra_body 入 `MiniMaxProfile.extra_body_for()` + `975b9f0a5/f7c1cbe66` docs 标准 installer + 桌面下载站点根 + 4 条 AUTHOR_MAP — 验证基线 `/tmp/hermes-agent` clone @ `cffd6e3c8` + 4 个并行 research agent 实证

- [[2026-06-14-update]] — **83 commits 单日同步**（hermes-agent `0a865e5 → 2a14e8957`，仍 v0.16.0 **未发新版**；约 54 fix / 6 perf / 6 feat / 5 test / 3 chore / 1 refactor / 1 docs / 1 style / 1 unscoped / 5 merge；~23.5h 单日窗口；166 文件 +8907/−692）：**SSL CA bundle fail-fast guard `a218a0f15` 主轴 + 5 修硬化**（新建 `agent/ssl_guard.py 94 行` + `agent/errors.py SSLConfigurationError` + escape hatch `HERMES_SKIP_SSL_GUARD` + `hermes_cli/doctor.py:316-318` "SSL / CA Certificates" 节 + 调用点上移到 `agent/agent_init.py:903-905 _create_openai_client` setup 内）+ **MCP stdio config exfil-shape 拦截 `972a9885e (#46083) + a27d7e68c (#46112)`**（新建 `hermes_cli/mcp_security.py 96 行` 双门匹配 `_SHELL_INTERPRETERS` frozenset + `_EGRESS_PATTERN` `curl|wget|nc|ncat|socat|/dev/tcp/|Invoke-WebRequest|...` + `_EXFIL_HINT_PATTERN` `.env|--data-binary|-X POST|<file`；`validate_mcp_server_entry()` 与 `_filter_suspicious_mcp_servers()` 在 `register_mcp_servers` 任何 stdio spawn **前**调用）+ **MCP loop preservation `4936a49a0`**（`_stop_mcp_loop(only_if_idle=True)` 让 dashboard probe 不撕碎 live agent 共享 event loop）+ **Approval 敏感文件三连 `da28d5d11 + abd69b811 + 2b67e96ae`**（`tools/approval.py:455` 新 DANGEROUS_PATTERNS 守 `cp/mv/install` 进 `~/.ssh / .netrc / .bashrc / .hermes/config.yaml` + `:577 _rewrite_resolved_user_home()` 折叠绝对 HOME → `~/` + `:208 _USER_SENSITIVE_WRITE_TARGET` 守 `sed -i / perl|ruby -i` in-place 编辑） + **file-safety user-write deny 松绑 `81e42335a (#45947)`**（`agent/file_safety.py:49-53` 删 `.bashrc/.zshrc/.profile` 等并删 `control_file_names` 块；HEAD 只守 `mcp-tokens/` 与 `pairing/`；与 approval-gate 互补） + **Read 工具 — 原生抽取 `.ipynb` / `.docx` / `.xlsx` `817f39231 (#37082)`**（新 `tools/read_extract.py 248 行` stdlib-only `json/zipfile/xml.etree`，**零外部依赖**；`EXTRACTABLE_EXTENSIONS = frozenset({".ipynb", ".docx", ".xlsx"})`；50MB / 5000 行 / 256 列上限；集成在 `tools/file_tools.py:763-808`） + **Desktop 原生 OS 通知 + 完成音效 + 通知设置面板**（`b4ba3f5e3` `apps/desktop/src/lib/completion-sound.ts 519 行` 14 Web Audio 合成预设（无音频文件）+ `630a4ef03` `apps/desktop/src/store/native-notifications.ts 203 行` 5 类型 `approval/input/turnDone/turnError/backgroundDone` + 1s 节流 + `document.hidden||!document.hasFocus()` 门 + `apps/desktop/electron/main.cjs:5611-5635 hermes:notify` IPC + Approve/Reject action 按钮 + `b0288ae9b` 完成音 picker 迁到 `notifications-settings.tsx`） + **Desktop polish 5 连 `526a1e24b PR#46029`**（`9cbb91abd #46014` clarify-tool UX + `715b69172` Compaction "Summarizing thread" 汇总指示 `agent/conversation_compression.py:43-49 COMPACTION_STATUS_MARKER` + Gateway `_status_update` 识别 → `kind="compacting"` + Desktop `compaction.ts` store + `1eb13744b` 滚屏保活 + `49dd91d68 #46030` CopyButton `context-menu-item` variant） + **Desktop misc 4 修**（`08d89e7ab #46197` 思考 shimmer 仅在 disclosure label + `6b76284c7 #45853` 离屏审批跳底按钮 surface "Approval needed" 经 `requestScrollToBottom()` + `0428945b5 #46073` `apps/desktop/electron/backend-env.cjs:70-78 normalizeHermesHomeRoot()` 剥 `/profiles/<name>` + `89bdb1e54` `hermes_cli/web_server.py:10659/10709 read_text(encoding="utf-8")`） + **Agent — 模型 refusal 不重试改直透 content_filter `c8ad2ca99 PR#46013` 4 连**（`bb46bf8ce` Anthropic + Chat Completions transport 把 refusal 映射 `finish_reason="content_filter"` + `12c84d6c7` 只在 refusal 是唯一 payload 时算终止 + `10bd01972` 共享 `_content_policy_blocked_result()` builder `agent/conversation_loop.py:411-433` + `_CONTENT_POLICY_RECOVERY_HINT` 常量 `:405-408` 提 `hermes fallback add` 提示） + **Codex reasoning thinking-only `069bfd654/1d584a301`**（`run_agent.py:905-936 _disable_codex_reasoning_replay()` 弹 `codex_reasoning_items` + `:3278-3343 _is_thinking_only_assistant()` 把 `item.type=="reasoning"` 列表项当 thinking-only） + **Gateway 中断 replay 清理 `5191c1c2c + 2c174bce2`**（`gateway/run.py:767-776 _is_interrupted_tool_result()` 识别 exit_code 130/-1 + `:779-822 _strip_interrupted_tool_tails()` 删被中断 assistant→tool 段 + `:825-837 _AUTO_CONTINUE_NOTE_PREFIX="[System note: Your previous turn"` + `_AUTO_CONTINUE_FALLBACK_PREFIX="[System note: A new message"` + `:840-854 _strip_auto_continue_noise()` 保新输入） + **Gateway 启动 auto-resume 串行 `10bad2faf #46074`**（`gateway/run.py:4642-4699` 新 `_queue_startup_restore_event()` / `_drain_startup_restore_queue()` / `_finish_startup_restore()` + `_schedule_resume_pending_sessions()` `:4701`） + **Gateway repaired-turn 持久 `2b4873f7f #46071`**（`run_agent.py:1548-1641 _flush_messages_to_session_db()` 改 `_flushed_db_message_ids` 身份追踪取代位置切片防 `repair_message_sequence` 缩短历史时丢新尾） + **Gateway intentional silence tokens `293c04fef + 0e22bf643 #46105`**（`gateway/response_filters.py:13 SILENT_REPLY_TOKEN="NO_REPLY"` + `:18-23 LIVE_GATEWAY_SILENT_MARKERS=frozenset({"[SILENT]","SILENT","NO_REPLY","NO REPLY"})` + `:30-44 is_intentional_silence_response()` 精确匹配 ≤64 字符 + 全响应等于 token 才静默 + 静默轮仍留 transcript） + **Gateway shell-supervisor guard `7433d5f0e + 143679305`**（`hermes_cli/gateway.py:3798-3818 _running_under_gateway_supervisor()` 检 `INVOCATION_ID/HERMES_S6_SUPERVISED_CHILD/XPC_SERVICE_NAME!="0"` + `:3821-3858 _guard_supervised_gateway_conflict()` + `:3861-3880 _guard_existing_gateway_process_conflict()` 改用 PID 文件 `get_running_pid()`） + **Gateway 回复消息媒体附件 `efbe1635d #46107`**（`gateway/platforms/telegram.py:5563-5606 _cache_replied_media()` 从 `msg.reply_to_message` 下载 + `cache_media_bytes` + 追加 `event.media_urls/media_types` + 文本附 `[Replied-to <kind> '<name>' saved at: <path>]`） + **Profile / HOME 隔离 `b00060ce5 + 723c2331b + d842155da + 9f33d673e`**（`hermes_constants.py:311-359 get_real_home() / _iter_real_home_candidates()` + `:362-395 get_subprocess_home()` 看 `TERMINAL_HOME_MODE` env `auto`/`real`/`profile` + `:398-405 apply_subprocess_home_env()` 设 `HERMES_REAL_HOME` 与 `HOME` + TUI `tui_gateway/server.py:1222-1236 _set_session_cwd()` 走 `_session_db(session)` profile-scoped helper） + **Skills cluster 5 commit `6c34088a1 PR#46237 + fc2b8b3d3 PR#46236`**（`7f245b003 #45966` `gateway/run.py:14376-14414` 跨进程缓存失效经 message_count 对比 + `3bc4a2ff7` `:12801-12850 _refresh_agent_cache_message_count()` 每轮 rebase + `7bbe7024c #46201` `agent/skill_utils.py:318 get_disabled_skill_names(platform)` 全局/平台 disabled **并集** + `ce19fdb7c` 解析点全跟进 `hermes_cli/skills_config.py:27-42` + `tools/skills_tool.py:573-594` + `4e6d05c6a #46149` `agent/skill_utils.py:275 _RAW_CONFIG_CACHE` mtime+size keyed）+ **Skills bundled-update 备份崩溃安全 `3581131e7 + 9a2b97632`**（`tools/skills_sync.py:509-525 / 590-595 / 608-625` 三段孤儿 `.bak` 复原 / 清陈旧 `.bak` 防 move 失败 / restore 前清部分 `dest`） + **Telegram rich messages 24h 内两次翻转**（`57c2a55be` 端点 404 退路硬化 → `12682d96b feat 配置 opt-out 还原`（默 True） → `9459057d7 #46102` math crash 守 → `a1f51feb7 #46206`（HEAD）默 **False** + `prefers_fresh_final_streaming()` 无条件返 False，HEAD 配置 `gateway/platforms/telegram.py:426 _coerce_bool_extra("rich_messages", False)` + `hermes_cli/config.py:1993 "rich_messages": False`） + **Email SMTP_SSL port 465 + IPv4 兜底 `04d4471d7 + cf7d5932f`**（`gateway/platforms/email.py:354-394 _connect_smtp()` 合并 4 处 + port 465 走 `smtp_ssl_cls(...)`，其余 `SMTP+starttls`；超时/连接错 retry `_connect(ipv4_only=True)`；`:110 _IPv4SMTP_SSL` 自定 `_get_socket` 用 `socket.create_connection(AF_INET, SOCK_STREAM)`） + **Models — ZAI GLM-5.2 1M 上下文 `bff78a34d` + Kimi K2.7 Code 原生 picker `2a14e8957 #46309`**（`agent/model_metadata.py:270 "glm-5.2": 1_048_576` 在 `"glm": 202752` 前；`hermes_cli/models.py:60/181/260/285` curated + preferred + provider 全链路；`hermes_cli/auth.py:619-620` coding-global/coding-cn endpoint probe；`provider_model_ids()` 对 `{"kimi-coding","kimi-coding-cn"}` 合并 curated 与 live `fetch_models()` 保留 K2.7-code 即便 live API 漏报） + **OpenRouter 元数据磁盘缓存 `13a1bd0f8 #46114`**（`agent/model_metadata.py:116-119 get_hermes_home()/cache/openrouter_model_metadata.json` + `:154-164 atomic_json_write(indent=0, separators=(",", ":"))` 紧凑 JSON + `:110 _MODEL_CACHE_TTL=3600` 1 小时 + 内存→磁盘→live `/api/v1/models` 三级回退） + **Perf wave 5 commit**（`afc861550 #46065` Webhook `_prune_delivery_info()` 用 `Deque` 懒 O(stale) + `_prune_seen_deliveries()` 节流 + 每路由 `_RATE_WINDOW_SECONDS=60.0` deque + `110687914 #45831` `tools/process_registry.py:132 _completion_event: threading.Event` + `:874 set()` + `:1098-1127 wait(timeout=min(1.0, remaining))` 替 `time.sleep(1)` + `8f278403d #45948` `tools/code_execution_tool.py:475 _rpc_server_loop(stop_event)` settimeout `5→0.05` + `5105c3651 #46079` `api_server.py _normalize_chat_content` 用 `total_len` 累加替 `sum(len(p) for p in parts)` O(n²)→O(n) + `4b5ba112a #45979` `agent/conversation_loop.py:74-100 _image_error_max_dimension(error)` 正则解析 provider 错误 clamp `[512, 8000]`） + **Misc**：`bf8effad0 #43852 utils.py:63 atomic_replace()` 跨设备 EXDEV/EBUSY → `shutil.copyfile+copystat+fsync+unlink` 兜底 + `85e6232a0` Anthropic proxy `/v1` 端点（`build_anthropic_client()` 剥尾 `/v1` + `_endpoint_speaks_anthropic_messages()` 识 `/anthropic/v1`）+ `8d5d36d79 #28479 model_tools.py:1115-1125 handle_function_call()` 透 `session_id` 到 `registry.dispatch` 两路径 + `e986e3fc6 + 1b16c4817` Provider OAuth 账号移除 `DELETE /api/providers/oauth/{provider_id}` 守 `_require_token` + `_oauth_provider_disconnect_hint` + `1fa761f8d #36142 tools/file_operations.py:291 _SEARCH_TIMEOUT_MARKER_RE` `exit_code==124` 时剥 marker 保部分结果 `limit_reason="search_timeout"` + `288f7026e` Weixin "Personal" 重标 + `d76a58bd1 hermes_cli/main.py:357-385 _resolve_sudo_user_profile_env()` sudo profile system install + 3 条 AUTHOR_MAP — 验证基线 `/tmp/hermes-agent` clone @ `2a14e8957` + 4 个并行 research agent 实证

- [[2026-06-13-update]] — **391 commits 跨日同步**（hermes-agent `6c752ca3a → 0a865e5`，仍 v0.16.0 **未发新版**；约 211 fix / 39 chore / 35 feat / 21 test / 18 refactor / 10 docs / 9 perf / 30 merge；~48h 双日窗口；含两批 historic salvage 2026-05-19~05-23 ≈167 commit；446 文件 +31366/−4724）：**Desktop Composer status stack + 实时 subagent 窗口 + 用户气泡流式中可编辑 `d62979a (#44630)`** + **Stick-to-bottom + 跳底按钮 `bbf020e (#45263)`**（`use-stick-to-bottom: ^1.1.6` + 删 `thread-virtualizer.tsx 481 行` 改 `thread-list.tsx 307 行`）+ **Perf 三连 `492c402 (#45343)`**（`ChatRuntimeBoundary` 隔 `$messages` + LRU `BLOCK_CACHE_MAX` + `REVEAL_MIN_COMMIT_MS = 33` + warm AudioContext 省 850ms）+ **Worktree-aware 侧栏 + WorkspaceHeader/ReorderableList 抽取 `e906726`** + **WhatsApp Business Cloud API 新平台 `984e6cb`**（`gateway/platforms/whatsapp_cloud.py 1956 行` + HMAC webhook + wamid dedup） + **Photon spectrum-ts 3.0→3.1 + 原生 markdown/reactions + telemetry CLI 切换 `573c4e6/9bfff6e/156f4fb/0a963d8`** + **Telegram 富文本永远 on `a59d5e3 (#45584)` + 流式 final 仍富文本 `7c0605b`** + **安全 fail-closed wave**：`fc46354` own-policy adapter 无 allowlist 时 fail-closed（WhatsApp/WeCom/Weixin/QQBot/Yuanbao）+ `3380563 /api/status` 受门 redact + `c6a992e _host_derived_api_key()` 注册标签链尾兜底 + `5908822/9514ddb` API key 不漏到非权威端点 + `4ead464 secure_parent_dir` 拒 chmod `/` 和顶层 + `5edb346` profile 下 write-deny `<root>/.env` + **Gateway auto-resume 重复 agent 防御 `6e2fd95/63097ee`**（`_AGENT_PENDING_SENTINEL` 占槽在 `create_task` 前 + 全路径回归测试）+ **JSONL 抄写降级 4 commit**（`state.db` 唯一存储）+ **Kanban 6 修**（sticky block + stale 不计 failure + respawn defer + idx 后置 + worker toolsets 钉 profile）+ **Billing `/credits` slash command `7ba5df0 (#44776)`**（`CreditsView` + 门户顶替）+ `c196269` 充值时不提示 + **Dashboard profile-scope 全量**（`62b4618 (#45598)` 会话+分析 + `88dbf95 (#44792)` Channels + 端点全包 `_profile_scope`）+ `28bf8fb` 任意 source 克隆 + `d206e1f` 本地文件浏览 home + `9102d4a` Win11 host + `a118b94` skill install `--yes` + **CLI 持久 approval scrollback `4474873 (#44702)` + Codex token 自愈 `aa07983/bd66e7e/311ff96`**（从 `~/.codex` 重导）+ **xAI 退役迁移 cluster**（`hermes_cli/xai_retirement.py:16 RETIREMENT_DATE="May 15, 2026"` + `hermes migrate xai` ruamel 圆滑改写 + doctor 表面 + 启动告警 + encrypted reasoning replay + 钉 x.ai 源 + xAI Web Search plugin 560 行 + opt-in TTS 停顿）+ **Bitwarden Secrets Manager `552e9c7 (#30035)`**（`agent/secret_sources/bitwarden.py 515 行` 懒 bws 安装 + SHA-256 校验 + `secrets_cli.py`）+ **Context Compression 重构波 11 commit**（保近期轮 + 自动聚焦近期用户 + flush 游标 clamp/snapshot + 末助手 reply 留 tail + `_SUMMARY_END_MARKER` 提至 `:92` + `COMPRESSED_SUMMARY_METADATA_KEY = "_compressed_summary"` 下划线前缀 wire 自剥 + `_MEDIA_DIRECTIVE_RE` 剥摘要器输入）+ **Perf wave**：`544c31b (#28866)` agent-loop 47% per-conv 函数调用削减 + `6bd4311 (#29006)` adaptive subprocess poll 省 ~195ms/工具调用 + `6cb9917 (#28957)` 压缩可行性检查延后 + **Models / Providers**：`957a8ff` Bedrock 受限 Claude 省采样参数 + `eed61a1` Gemini `systemInstruction` 加 role + `c8e5f34` 剥 google/gemini 自前缀 + `5acd185` Moonshot union 数组 + `4fd9397` Codex chatgpt.com 移 extra_headers + `bc060c7` 移 `claude-fable-5` + `643dc82` 自定义 provider 会话身份保真 + `e256f4a` 不恢复 bare billing provider + **Delegation 默认子代理时钟移除 `bba9b51 (#45149)`** + **MCP `5affecb (#44550)`** capability-gate `tools/list` + **Skills hub**：`2ee69d0 (#44500)` ClawHub 浏览预算 unlimited + identifier 去重 + youtube 经 uv + 排除依赖/venv + Pages 部署回退健康索引 + **Computer-use**：SOM/vision 路由 `auxiliary.vision` + app= 无匹配明示 + type_text MCP 名修正 + drag + **Approval `allow_permanent`** 端到端 TUI+Desktop + **Update / Installer**：post-pull 语法校验 + 自动回滚 + unmerged-index clear + 不弹 polkit + Windows hermes.exe 隔离 + **Platform**：`.xls/.doc/.ppt` 加 `SUPPORTED_DOCUMENT_TYPES` + Signal/SimpleX/Email/Teams DOCUMENT 兜底 — 验证基线 `/tmp/hermes-agent` clone @ `0a865e5` + 5 个并行 research agent 实证

- [[2026-06-11-update]] — **212 commits 跨日同步**（hermes-agent `a5d05cf30 → 6c752ca3a`，仍 v0.16.0 **未发新版**；约 131 fix / 25 feat / 9 chore / 6 test / 6 refactor / 3 style / 3 docs / 3 change / 1 revert / 1 perf / 15 merge / 5 misc；~48h 双日窗口）：**Cron Recipes → Automation Blueprints 改名 + 14 蓝图 catalog**（`1593ca540` 上线 → `e8b757845/e976faac7` 硬化 → `cb29e8a82` rebrand `cron/recipe_catalog.py → cron/blueprint_catalog.py 713 行` + `tools/recipes → tools/blueprints` + `/cron-recipe → /blueprint`（alias `/bp`） + `hermes://cron-recipe/ → hermes://blueprint/` + dashboard `/api/cron/recipes → /api/cron/blueprints`；`BlueprintSlot`/`AutomationBlueprint`/`fill_blueprint`/`blueprint_form_schema`/`blueprint_slash_command`/`blueprint_deeplink`/`blueprint_catalog_entry`；slot 4 type `time/enum/text/weekdays` + `WEEKDAY_PRESETS`；14 个 blueprint `morning-brief/important-mail/weekly-review/workday-start/custom-reminder/evening-winddown/news-digest/bill-renewal-watch/habit-checkin/hydration-move/meal-plan/learn-daily/gratitude-journal/on-this-day`；终结 in `cron.jobs.create_job` 无第二套 job 引擎；Electron `hermes://` URL scheme + single-instance lock）+ **Suggested Cron Jobs `9a09ea69f`**（`cron/suggestions.py 257 行` + `cron/suggestion_catalog.py 154 行` 4 个 catalog entry `daily-briefing/important-mail-monitor/weekly-review/standup-reminder`；4 来源 catalog/recipe/usage/integration；dedup+latch 按 key、pending cap、accept 走同一 `create_job`；`tools/blueprints.py:217 register_blueprint_suggestion`）+ **memory/skills `write_approval` 布尔门**（`96af61b6e (#38199)` 上线三态 `on|off|approve` → `095f526b1 (#43354)` 重构为 boolean `write_approval` 默 false + `_config_version 28→29` migration → `70d5d7e39 (#43452)` 修 inline prompt callback + gateway staging + `/skills` gateway review；`tools/write_approval.py 493 行` `write_approval_enabled/evaluate_gate` + `tools/memory_tool.py` / `tools/skill_manager_tool.py` 接入；gateway `/memory pending|approve|reject|mode` + `/skills pending|approve|reject|diff|mode`；`acd7932c0` 文档 cross-link to skills/configuration/slash-commands）+ **Dashboard 统一多 profile 管理 `875aa8f16 (#44007)` 1419+/310− 跨 21 文件**（`web_server.py +566 行` `profile` 参数 query/body 加到 `/api/config|env|mcp/servers|mcp/catalog|model/info|model/set` 全走 `_profile_scope()`；chat PTY `?profile=` 让 child `HERMES_HOME` 指向 profile dir；CLI `<profile> dashboard` 走机器 dashboard `--isolated` 保旧；前端 `ProfileProvider/ProfileSwitcher` 全局 selector URL-persisted `?profile=` mirrored 进 `fetchJSON`；`body.profile` > `?profile=`；amber `ProfileScopeBanner`）+ `73dd58499 (#44220)` MCP loop HERMES_HOME 上下文 propagate（OAuth token-store 跨 profile 修）+ `d986bb0c6 (#39084)` Profile Builder（model+skills+MCPs）+ `a09343cc9 (#44231)` SKILL.md editor on Skills 页 + cron modal attach-skill 选 + `fa7f24e89` Webhook enable from dashboard 页 + `9c051f57c (#44286)` Anthropic API Key 检 `ANTHROPIC_API_KEY` 链 + 隐藏 deprecated tool-progress env vars + `9c16ca879 (#44237)` model 分配标准化 + backup confirm modal + `c7bfc938d (#44374)` Config 页 header 显已切换 profile config.yaml 路径 + `63a421d4c (#42578)` `_require_token` 全部走 OAuth gate 修 cookie-auth 401 + `7df3aa34b (#43214)` `public_url` override 静默拒绝时 warn — **Coding-Context Posture `3e74f75e4 (#43316)`**（`agent/coding_context.py 731 行` `ContextProfile/RuntimeMode/GENERAL_PROFILE/CODING_PROFILE/_PROFILES/get_profile/resolve_runtime_mode`；4 域：System prompt operating brief + per-model edit-format tuning + skill index demote + Toolset collapse；activation `agent.coding_context: auto|focus|on|off`；subagent 自动继承；不 memoize）+ **Skill index demote 而非 hide**（`ee1a744ac (#44342)` API 改名 `hidden_skill_categories → compact_skill_categories`，`build_skills_system_prompt(hidden_categories=) → compact_categories=`，每个 skill 名字仍在 prompt 一行 names-only；deny-list 不变；`4d6a133a9 (#44387)` 一天后反转：demotion 只在 focus 模式下，auto/on 让 skill index 不变）+ `a4f179c50 (#44411)` GPT/Codex family steer 到 V4A 单文件编辑（OpenAI codex-rs `apply_patch` 是唯一 file editor）— **Parallel Web Search & Extract** `e0e257171` **1206+/98− 跨 15 文件**（keyless 免费 Search MCP `search.parallel.ai/mcp` Streamable-HTTP JSON-RPC client；keyed v1 REST `client.search/extract advanced_settings.full_content`；free path 带 attribution 'Parallel search/fetch'；web tool 无条件注册作为 backstop；`parallel-web 0.4.2→0.6.0`）+ `0a5762c78` clientInfo `mcp-web-client` 中性 identity 防 attribution + `93764b930/32a73010b` keyless web default 永远兜底 — **Bedrock `e24c935cf (#44293)`** IAM 拒绝 `InvokeModelWithResponseStream` 时自动 fallback 到 `converse()`（`agent/bedrock_adapter.py:211 is_streaming_access_denied_error` + `:1041` 调用 + `agent/chat_completion_helpers.py:1618/1635/2468/2471`；`_disable_streaming` 后续 turn 跳）— **Anthropic 交错 thinking 5 连**：`aaccaada2` 顺序保真 `anthropic_content_blocks` 端到端 verbatim copy（`thinking_1(signed) tool_use_1 thinking_2(signed) tool_use_2` 顺序 invalidate 签名修）+ `529bb1c3d` strip output-only SDK 字段 + `7a1eed826` redact replayed tool input + 加宽 thinking-replay 400 classifier + `86e10dd87` thinking-modified 400 路由 recovery + `9f95f72b9` strip api_messages in recovery + `efcbbde48` 改 in-memory only 不入 state.db — **SUMMARY_PREFIX 4 连**：`8f8cad7ec (#41607)` 抗陈旧任务执行 + `d5e2fbf24` historical context framing + `acb2954d8` carveout-era 冻 `_HISTORICAL_SUMMARY_PREFIXES` + **`6c752ca3a` HEAD 收紧 wording**（topic-overlap 三句塌缩成 WINS+discard 两句；`agent/context_compressor.py:43-69 SUMMARY_PREFIX` + `:70 LEGACY_SUMMARY_PREFIX` + `:78 _HISTORICAL_SUMMARY_PREFIXES`）— **OpenRouter `183d86b3e (#43436)`** reasoning_effort 改写 verbosity for adaptive Anthropic（`#42991` 反转后让 `agent.reasoning_effort` 不再 silent no-op；low/medium/high/xhigh/max passthrough）— **Streaming `615ad9792 (#43570)`** socket read timeout floor 到 stale-stream timeout（180/240/300s）防 Opus 在 125s 被撕碎 — **Params `19c07c403`** newer OpenAI 系列 `max_completion_tokens` 名匹配（gpt-4o/gpt-4.1/gpt-5/o1/o3/o4）+ `2ce3ae3d1` 错分类不误判 unsupported-param 为 overflow — **Model 选择**：`af978ecb1/243cada15` 昂贵模型确认 + typed gateway /model + `57c671499 (#43103)` curated Anthropic aliases 保留 + `5a4297a11` MiniMax M3 hardcode 1M — **Desktop registry-driven slash + /resume/handoff 一等公民 `3ffbdfbcc (#42351)` 1914+/478− 跨 31 文件** — **Desktop composer 解耦 + 草稿 localStorage 6 连**（`c710868fb/292192f7d/d7d281fa3/18d61bd06/3d14f01fd/fdc0d1956/65ddc7c4a` 防误覆盖 + per-session 附件）— **Desktop VS Code Marketplace 主题 + 可调终端 pane + Floating HUD**（`27a321157` install VS Code theme `.vsix` zlib 手写 zip reader + WCAG AA + `8f73d0d94 (#42521)` resizable terminal pane + `Ctrl+\`` toggle + `833410e02` ANSI palette + Cmd-K HUD + `ab5f1a1f1 (#43111)` Mac 风 `^Tab/^⇧Tab/^1-9` session 切换 + `b96bd4808 (#43219)` per-window chat + `d33965396 (#43188)` TUI session name in titlebar）— **Desktop 远端文件系统 `/api/fs/*` 5 连**（`51f47f9a9 web_server.py:1439-1514` + `db79e9013 desktop-fs.ts 84 行 facade` + `8878484f8 remote-picker.tsx 177 行 wire browsing` + `56a0f48ba/9121834b3` tighten + scope）+ **Desktop 远端 attach 5 连**（`4906dcfc2` stage OS drops to remote workspace `file.attach`/`image.attach_bytes` 覆盖每个 drop surface + `153060e20/b021497bc/891c9a682/29147afd6` 缩略图/spinner/race/16MB toast）— **Desktop misc 18 修**（`1770263cc (#43234)` default project dir + `0d3e2cc53 (#43487)` lineage dedup + `93a2f680f (#43496)` hide-all 模型可见性 + `743c55efa (#43541)` HTML5 backends 重 mount + `8505e9d66 (#44415)` spellcheck off + `484f484c2 (#44453)/38273676e` titlebar 拖动区 carve + `258d24039 (#43197)` thinking disclosure scope + `8bb652955 (#43147)` sidebar 重叠 + `8b84d8222 (#39639)` Enter from live editor + `93340fa3c (#40892)` profile switch terminal.cwd + `59ea2f98e (#42871)` Manage-profiles 溢出 + `6de3963e3 (#43702)` per-session model + `e37280355 (#43977)` model metadata refresh + `b1af653bf (#43618)` file tree paths + `4c797d0e2` hide Windows console children + `1e7316ced` sudo callback noninteractive + `c1308ebf3/e80754647` filled icons + `7803cbfbb/45e1689c0` Nous overlay HUD）— **Sessions 3 连**：`e96ca1a0d` empty session 退出/轮转时清理（`hermes_state.py:3697 delete_session_if_empty` + `cli.py:5863 _discard_session_if_empty`）+ `04b3f1953` archive 压缩 lineage + `218452b05 (#43149)` 从 malformed `sqlite_master` 恢复隐藏 sessions（`PRAGMA writable_schema=ON` + sqlite_master surgery）— **Agent binary @file 引用 actionable** `7ffc216bc` + **PDF/DOCX/audio attach note 改写**（`e7ae145ac` PDF/DOCX 不再 punting + `13650ab7f` 音频 + `4e9be3ee3` test 覆盖）— **Telegram 5 连**（`3b4c715e1` stripped-text fallback + tail-only delete guard + re-finalize skip live-validated + `590b3c0d7` partial overflow stream recover + `da818510e` stream consumer cancelled finalize + `d0e017bac (#44245)` voice/audio >20MB pre-download gate + `9ca969734 (#42090)` transcribe tuple unpack）— **Gateway 9 连**（`13f1efdd1 (#43968)` 终端 header 折叠 + `a8f404b29 (#40831)` launchd domain probe + `fa32af886/984e69ff6` restart 去重 + post-Telegram QR auto-restart + `5cf6e28a2 (#42675/#43236)` 容器重启 planned-stop marker auto-start + `f456f302d (#44267)` 临时 HERMES_HOME 拒写 service definition（修 7-小时 zombie 事件）+ `264ac72b6/cb2c13055` restart watcher env scrub POSIX/Windows + `6a30cfca8 (#37556)` post-delivery 前停 typing）— **Matrix `4717989c1 (#18505)`** room context 隔离 + handle_sync 显式 + allowed_users authz + i18n key + slash_commands mixin port — **Discord 7 连**：`c3464ecf4 (#44383)` runtime gateway task exit 恢复（salvage #39416）+ `020ef76cf/08b1c44a5` connect timeout cancel + `311900842/aea0b7397` voice-off 不断开 + `e5580f43c/4eadef18a` `DISCORD_ALLOWED_ROLES` 端到端 + MagicMock guard — **Skills hub 7 连**：`298bb93d3 (#43398)` live per-source 进度 `on_source_done` + `105625d65` overall_timeout 真 honor `ThreadPoolExecutor` 手动 shutdown wait=False cancel_futures=True + ClawHub `CATALOG_WALK_BUDGET_SECONDS` 不毒 cache + `eee1da45f (#43395)` cold start bound 到 page + `9caa12f4e` skill_view 按 frontmatter name 解析 + `0a593f132/d1383a6b1` `.env` HERMES_HOME-aware 跨 sibling skill + `fdc90346e (#43221)` red-team skills `godmode/obliteratus` 移到 optional-skills（claude-fable-5 95%→25% block rate） — **Plugins/Web 3 连**：`114e26573` 失败 sweep 不缓存（`_discovered=True` 改为重入守护 raise 时 reset）+ `93764b930/32a73010b` keyless web default 永远兜底 — **Cron 5 连**：`7d8d000b1 (#43956)` per-job profile 完整 revert（PR #28124）+ `acd4f34e6/f7a6d6a6a` provider "custom" → providers.custom + `702f4df19 (#41976)` 容器重启 ownership + `b4170f3ac (#43223)` no-skills job 不严检 script-inject 输出 — **CLI/TUI**：`8972a151a (#44265)` 状态栏 idle clock `✓ <elapsed>` + `2f1951234 (#43439)` 全平台 UTF-8 stdout/stderr `TextIOWrapper.reconfigure()` errors=replace + `b2043cf15/5508f4bc5` TUI/whatsapp-bridge UTF-8 decode + `f8fd30942 (#43320)/d03cdd63e (#43036)` one-shot finalize/cleanup + `0b5b7ddfd` /help quick commands + `3c489fda8` Slack 50-cap aliases — **Install/Update/Backup 7 连**：`899acfe42` winget 0x8A15002B 陈旧注册修 + 全 PM 后刷 PATH + `9662b76d5` merge 不覆盖 + `a5c32cdf3 (#42172)` 中断 venv 自愈 + `cedd9b6d4` SSH 跳 passive checks + `0edeee14c` 测试 + `dd40600e0/ed2b9e43c` backup SQLite 同位放置 + 嵌套 skill 目录纳入 — **Tooling 20 连**：`a72bb0375 (#38749)` Docker 缩减 + `5d8c44a39 (#30399/#42413)` Docker 预装 matrix + `464276228` Langfuse data URI redact + `47e77ae16` Curator atomic state + `dca11b665/e71d74682` MCP stdio argv + 启动状态 + `3edd09a46 (#44205)` WhatsApp stale bridge restart + `4cecb1a13/90f4b3040/3bfbb3f2a` npm audit / react-compiler eslint / TS 6 + CI typecheck + `72154ad87` CI uv cache + `f082b4ec5/07ac18590` CI exit-4 runner 健壮 + `af3c8b80b` pid-file race + `6110aed9b (#43669)` free model "Credit access paused" 抑 + `15813336c/e4a1b35a3` .env mode 保留 + `ea7981eba` webhook-disabled hint + `1c055a4c5` xAI Grok Build code + `6b330522e (#42641)` AGENTS.md Design Philosophy — **TTS Gemini**：`5718811de` persona prompt + `2c1920822` audio tag rewrite（`tools/tts_tool.py +143 行`）
- [[2026-06-09-update]] — **105 commits 跨日同步**（hermes-agent `754154a9c → a5d05cf30`，仍 v0.16.0 **未发新版**；约 56 fix / 17 feat / 11 test / 5 chore / 3 refactor / 2 docs / 1 ci / 1 style / 8 merge；~24h 单日窗口）：**Photon iMessage gRPC-native 收官**（`4e4d27875` 主轴 commit：删 aiohttp webhook receiver + HMAC 验签 + 5 个 `PHOTON_WEBHOOK_*` env + 3 CLI 子命令，改 `GET /inbound` NDJSON 长连接 + 25s heartbeat + consumer-state-machine；`plugin.yaml:4 0.1.0→0.2.0`）+ 20 commit 跟进硬化（`92dfd70d6 (#42732)` 生产硬化六件套：5 条 npm `overrides` 钳 protobufjs GHSA-66ff-xgx4-vchm + 4 个 @opentelemetry OTLP CVE / `crypto.timingSafeEqual` 替 `!==` token compare / 2 MiB body size cap / re-subscribe capped exponential backoff + jitter / `unhandledRejection` 处理 / dedup 按 insertion order 严格淘汰 4000 entries；`b3aef57f2` TYPE_CHECKING 包 httpx；`9fb83eaa2` spectrum-ts ^1.18.0 + npm install spectrum-ts@latest；`314af28e8 + dbf2470d4` 入站附件/语音 base64 inline 20MiB cap + voice MessageType；`4615e08d3 (#42397)` 出站 6 方法 override 经 spectrum-ts `attachment()/voice()` builder + cron multimedia + `validate_media_delivery_path` 二次校验；`fdf48c63c/e79e44af7/0d25cae04` 走 spectrum-ts `spectrumText/Reply/Typing` builder 后删 replyTo；`2130ef68b` `flattenGroups: true`；`064665688/92179352f/3b983e779` `PHOTON_HOME_CHANNEL` 支持裸 E.164 + DM GUID + `tools/send_message_tool.py:41 _PHONE_PLATFORMS` 加 photon + setup 自动 allowlist；`e9b26c7c8/84e4b4b9a/b58ff9345` 彩色 banner + `assignedPhoneNumber` 取号 + auth.json 持久化 phone numbers；`033765890` user API 全迁 Spectrum backend Basic auth + `DEFAULT_SPECTRUM_HOST="https://spectrum.photon.codes"`；`37561c214` device client_id `hermes-agent→photon-cli` 修 400 invalid_client + token 双重 validate；`637cf94be` `strip_markdown` + `_send_with_retry(max_retries=2, base_delay=2.0)` 指数退避）— **SimpleX 1.1.0 大演进**（`0c2e81df0` PR #27978 salvage：群组 `SIMPLEX_GROUP_ALLOWED` + rcvFileDescrReady→rcvFileComplete 延迟流程 `_pending_file_transfers` + 原生出站媒体 `send_image/video/document/voice` + `MEDIA:<path>` tag 自 reply 抽取按扩展名分发 + `HERMES_SIMPLEX_TEXT_BATCH_DELAY` 默 0.8s burst 批处理 + `SIMPLEX_AUTO_ACCEPT` 默 true contact request + `_pending_responses + _corr_counter` 关联追踪；不动 core）— **Plugins Hub TUI overlay `52f7e24a7`**（新 gateway RPC `plugins.manage` 入 `_LONG_HANDLERS` + **新建** `ui-tui/src/components/pluginsHub.tsx 238 行` + `/plugins` slash 无 arg 直开 overlay；键位 ↑↓/Enter/Tab/1-9,0/Esc）+ `b8eede7bd` `/plugins` slash 改写从 `PluginManager.list_plugins()` 改用 `_discover_all_plugins` 让 freshly-installed-but-not-enabled 可见 + `f6f573eba (#42963)` plugin install subdir `owner/repo/path/to/plugin` shorthand + `<url>.git/path` 自然边界 + `<url>#path` 显式 fragment + 防 traversal `_resolve_subdir_within` + `39b76d901 (#39859)` `optional-mcps` 入 wheel+sdist（`MANIFEST.in:3 graft` + per-entry data-files 防 glob flatten 冲突）— **Anthropic 默认现代 thinking 契约 `1febb0824 (#42991)`**（反转脆弱 version-substring allowlist 为 default-to-modern；`_LEGACY_MANUAL_THINKING_CLAUDE_SUBSTRINGS` 仅含 3.x/4.0/4.1/4.5/4-2025/Haiku 4.5；`_NO_XHIGH_CLAUDE_SUBSTRINGS` 仅 4.6 family；三 predicate 重写让 unknown Claude 默 adaptive+xhigh+无 sampling params；`_ANTHROPIC_OUTPUT_LIMITS["claude-fable"]=128_000`；OpenRouter `_anthropic_reasoning_is_mandatory()`）+ `46fedef07 (#43012)` OpenRouter Anthropic reasoning-mandatory 即使 enabled 也省（chat_completions 不回放 signed thinking blocks 致 tool-replay turn 上 OpenRouter 发 `thinking:{type:"disabled"}` 被 4.6+ 400）— **Fable 5 全链路加入**（`d7886da08` Anthropic 直连列表 `hermes_cli/models.py:329` + `ff9c110d5 (#42979)` OpenRouter + Nous 双列表 + `agent/model_metadata.py:144 claude-fable-5: 1000000` + `agent/anthropic_adapter.py:127 claude-fable output_limit 128_000` + `website/static/api/model-catalog.json:16,152 updated_at 2026-06-09T17:20:16Z`）+ `967c325da (#42986)` OpenRouter live `context_length` step-5f branch（`agent/model_metadata.py:1815-1835` 修 fable-5 等新 slug 落到 200K 默认；保留 Kimi-family 32k underreport guard）— **Curated models / Catalog**（`c4066091c (#42629)` 加 `poolside/laguna-m.1:free + nvidia/nemotron-3-ultra-550b-a55b:free` to OpenRouter free 块；`e687292eb (#42628)` Nous recommended-models 持久化 `$HERMES_HOME/cache/nous_recommended_cache.json` per-base map atomic write + Portal 失败 fallback；`54318c65b (#42614)` `seed_cache_from_checkout(project_root)` 在 `hermes update` 时从 checkout `website/static/api/model-catalog.json` 自动 seed disk cache）— **Observability**（`9f1c16a7f fix(langfuse)` `:849` 翻 gate 让 sanitized response (post_api_request 路径) 的 usage dict fallback 真正运行；`9d61076f8 + ecd4679d8 + 728612c29 fix(observability/nemo-relay)` plugins.toml lifecycle 三连：lazy reinit + direct fallback 保持 until init 成功 + try/finally 让 clear 失败也 re-arm；`021d1034d` adaptive config 对齐 `[components.config.tool_parallelism] mode="observe_only"`；`85852b71d (#42691)` preserve downstream errors via `_run_managed_with_downstream_preservation` + 按 `_DownstreamExecutionError` class-name + `.original` 属性 shape 探 + tolerant `str.startswith` match）— **Desktop reconnect/sleep/wake 恢复 cluster** `8d71c3891 (#43004)` `useRouteResume` 三 ref + `seenGatewayStateRef` 让 mount 不被误当 reconnect；`72f522d46 + be2f739e9` sleep/wake 后第一 `prompt.submit` 返 "session not found" 时自动 `session.resume` + 重发；`ba44de06d (#42998)` Electron download 自愈三步 escalation；`c4811c382 (#42946)` icon Apple grid pad；`c6dc2fcd2 (#42613)` profile delete 前先停后端防 Windows file lock；`d04616964 (#42537)` per-platform messaging sidebar sections + local-only recents + `orderByIds` 前缀新 session + 同步 flush 终端态 + `freshDraftReady` 区分 + Ctrl+N 清 quick-create profile + 移除 SmoothStreamingText 包装 — **Desktop file.attach RPC `dbbd1d4d0`**（remote gateway 非 image 附件上传：`@file:` ref 在 remote 模式时读 dataUrl 发 `file.attach`，gateway stage 到 `<workspace>/.hermes/desktop-attachments/` 返 workspace-relative ref；`DESKTOP_BACKEND_CONTRACT 1→2`）+ `09a6a2ddd (#42399)` Desktop 后台流式（3 个 Chromium switch + `backgroundThrottling: false`）+ `c1927d234` tsconfig ES2023 + `e0f6a35ac (#42515)` debug report 显式 URL only（`EXPLICIT_URL_RE` + `LinkifiedText explicitOnly`）— **TUI/CLI** `f8adefdeb fix(tui)` 新 `TERMINAL_CONFIG_ENV_MAP` at `hermes_cli/config.py:5156` + `apply_terminal_config_to_env()` 让 TUI/dashboard 子进程也走 classic CLI 同套 bridge；`520b59db1 + 4b073d090 fix(tui)` `_load_fallback_model()` 改 delegate to `hermes_cli.fallback_config.get_fallback_chain`；`ab98818e5 + d66bac5a1 + 714183530 + b5f8996cc` 修 native Windows confirm modal #33961（之前从 daemon thread `input()` 与 prompt_toolkit 主线程 stdin 锁死，`/reset` 卡 + Ctrl-C 吞）；`d1f23bb2d` 跨 **28 文件 85 个 call site** 加 `stdin=subprocess.DEVNULL` 修 TUI 模式下 subprocess 继承 stdin 致 race-read EOF 致 gateway 干净 exit on mid-tool-execution；`bddab61bc + 8bb60ff03` 新 `scripts/check_subprocess_stdin.py` regex scanner + 包成 pytest CI 守护；`2c1aaa9cb + dba6380ca` `noqa: subprocess-stdin` marker 让 `claude setup-token` 保留 inherited stdin；`a46462ec6 (#42435)` `hermes dashboard register` 显式 `--portal-url` 持久写 `.env` + 从 `--redirect-uri` 派生 `HERMES_DASHBOARD_PUBLIC_URL`(ORIGIN only) — **hermes-ink `1e5ff4a57 (#42527)`** 终端 raw-mode 退出时关 DEC mouse tracking (1000/1002/1003/1006) 防 SGR `35;col;row M` flood；re-entry setImmediate reassertTerminalModes — **Gateway-UI** `9351cbafa (#42616)` image_generate auto-deliver as native media（`_JSON_MEDIA_TOOL_PATH_FIELDS = ("host_image","image","agent_visible_image")` 优先级序）；`3705625b7 (#42576) + 8d99b5bc4 (#42729)` terminal command 渲为 bare fenced code block（无 language tag 因 Slack mrkdwn 把 "bash" 渲为字面 first line），gate on adapter `supports_code_blocks: bool`（7 adapter 设 True）；`6d2732e78` MarkdownV2 edit finalize（adapter `REQUIRES_EDIT_FINALIZE: bool`）+ `f4531feee` `_strip_mdv2` 顺序修先剥标准 `**bold**` 防半剥成 `*` — **Sessions/Dashboard** `639c1e363 feat(sessions)` 可选 `max_concurrent_sessions` cap 跨 CLI/TUI/dashboard/gateway，**新建** `hermes_cli/active_sessions.py 321 行` lease registry `$HERMES_HOME/runtime/active_sessions.json` + POSIX `fcntl.flock`/Windows `msvcrt.locking` + `psutil.create_time()` 抗 pid recycling，fail-open；JSON-RPC 4090 拒绝码；`52ae9d9f0 (#42455)` `hermes dashboard register` 凭 `HERMES_DASHBOARD_OAUTH_CLIENT_ID` stable key 幂等 — **Agent/Codex/API** `3a74b7521 + 57775e9e1 (#42741)` 识别 LM Studio/llama.cpp 把 ctx 报 token 但 prompt 报字符的措辞走 output-cap 路径；`cb4cc08b0` Codex app-server `thread/tokenUsage/updated` 通知接入 session accounting（之前丢失，session_* 不长、context compressor 不动）；**新建** `agent/codex_runtime.py:_record_codex_app_server_usage`；`b23184cad` api-server `_run_agent` executor 现 `set_session_vars(session_id=...)` 让 tools 看到正确 `HERMES_SESSION_ID` — **Terminal** `a38cc69bc (#42653)` POSIX `_SANE_PATH` 缺啥补啥而非用 `/usr/bin` 哨兵跳过（修 macOS launchd 已含 `/usr/bin` 缺 `/opt/homebrew/{bin,sbin}` 让 `gh`/`brew` 不可见）；新 `_path_env_key` 处 Windows case-variant `PATH/Path`；`76f89d66d (#42695)` test 锚定迁到 `TERMINAL_CONFIG_ENV_MAP` — **Yuanbao** `ffcd9d7ac` 媒体解析三阶段 middleware 拆分（QuoteContext → MediaResolve → PatchAnchors）+ 私聊 fallback 从 `_msg_content_cache` 恢复 local 路径不 re-download + 三源 first-occurrence dedup；`24a934295` test 加 missing `patch` import — **Skills-hub** `5e9d7a766 (#42347)` 修两 bug 让 GitHub rate-limit-zeroed catalog 不再上线：(1) `scripts/build_skills_index.py` write-before-check → 失败时 exit 2 不写 + `deploy-site.yml` 移除 `|| echo non-fatal` 让 step 严格致命；(2) `GitHubSource._list_skills_in_repo` 403 silent return → 共享 `_github_get` 重试 + 429 视作 rate-limit；`ClaudeMarketplaceSource` 持持久 `GitHubSource` 让 rate-limit state 跨 marketplace-index fetch + per-skill inspect 保留 — **Doctor** `732ababa1 + aa424e51a` 允许 `model.provider: custom:<name>` 的 vendor slug + `is_aggregator` 识别 — **Deps/Packaging/Nix** `f6416f50f (#40179)` `PyJWT[crypto] 2.12.1→2.13.0` + `urllib3>=2.7.0,<3` 清 4+2 CVE；`b5421f4ba (#40522)` `packaging==26.0` 入核心依赖（之前仅 transitively 到达，slim Docker 缺它致 hindsight `_meets_minimum_version` 返 False 让 append mode 关 #40503）；`5750d058f (#39881)` pytest-timeout `signal→thread` 跨 Windows；`a5d05cf30 (HEAD)` `nix/lib.nix` 跳过 `.#fix-lockfiles`（slow）；`02f878ec5 (#42856)` Windows native 数据目录文档纠正到 `%LOCALAPPDATA%\hermes`；`68a997fed` README 加 SEO 锚链 — **杂修+测试** `50ad191a8 (#42626)` `monkeypatch.setattr raising=False` 防 xdist race；`d6c11a457 (#42356)` 测试改按 args 而非共享计数防 race；4 条 AUTHOR_MAP（underthestars-zhy/m4dni5/ruangraung/mnajafian-nv/jooray）
- [[2026-06-08-update]] — **178 commits 跨日同步**（`c9863772 → 754154a9c`，仍 v0.16.0 **未发新版**；约 120 fix / 19 feat / 15 refactor / 12 test / 8 chore / 3 docs / 1 hardening）：**Photon Spectrum iMessage 全新平台插件**（12 commit；**新建** `plugins/platforms/photon/ 1981 行` 含 `adapter.py:200 class PhotonAdapter + :795 register()` 注册 `pii_safe=True / allow_update_command=True / emoji="📱"`；inbound aiohttp 8788 端口 `/photon/webhook` 验 HMAC-SHA256 `X-Spectrum-Signature` + 5 分钟时钟漂移拒绝 + `message.id` 去重；outbound 因 Photon 未公开 HTTP send 端点改 **Node sidecar** loopback 8789 跑 `spectrum-ts` SDK 共享 bearer token；`auth.py 581 行` RFC 8628 device-code 流；`cli.py 340 行` `hermes photon setup/status` + `630318e95` `login` 折入 `setup` + `d7f42e368` 全 channel parity gateway setup 集成 + `186651857` group-chat mention 门禁 `PHOTON_REQUIRE_MENTION + PHOTON_MENTION_PATTERNS`；多个 CodeQL clear-text-{logging,storage} 修补全清零）— **God-file 解耦运动 Phase 1-4**（15+ refactor 析出 ~12k LOC）：agent 内 `54870847c` **新建** `agent/turn_context.py 388 行 @dataclass TurnContext + build_turn_context()` 析出 run_conversation 前 ~470 行 prologue + `524453dab` **新建** `agent/turn_retry_state.py 68 行 @dataclass TurnRetryState` 集中 15 个零散 per-attempt one-shot guard + `55b83c3d9` **新建** `agent/turn_finalizer.py 428 行 finalize_turn()` 析出 post-loop tail（run_conversation 4204→3846 LOC、conversation_loop.py 4578→4220）；gateway/run.py 三 mixin（HEAD `:1163-1165,1866 class GatewayRunner(GatewayAuthorizationMixin, GatewayKanbanWatchersMixin, GatewaySlashCommandsMixin)`）：`a706a349b` **新建** `gateway/authz_mixin.py 426 行` 4 鉴权方法 + `1c68f6f81` **新建** `gateway/kanban_watchers.py 1064 行` 6 kanban 方法（1035 LOC）+ `619bd7827` **新建** `gateway/slash_commands.py 3393 行` 42 个 `_handle_*_command` 处理器（3200 LOC）合计 **4883 LOC** 自 run.py 移出；CLI 双轨：`b2e605324`+`4da45e872`+`568e12761`+`1a626470c` 建 `hermes_cli/subcommands/ 39 子文件 2857 LOC` 拆 main() 内 3297 行 argparse 树 + `a77efada5` **新建** `hermes_cli/model_setup_flows.py 2648 行` 析 18 `_model_flow_*` 向导（main.py 14050→11479）+ `0904bc7ea` **新建** `hermes_cli/cli_commands_mixin.py 2175 行` 32 slash-command handler（2077 LOC）+ `094aa85c3` **新建** `hermes_cli/cli_agent_setup_mixin.py 681 行` agent-construction 5 方法（cli.py 14139→13492）；全部 refactor byte-identical/MRO-resolved）— **Desktop 客户端/后端版本与更新按钮分离**（9 commit：`9e360681f` `/api/hermes/update/check` 加 best-effort `commits` list + `ed1e2533b` 新 `version-bar.ts` status-bar 同显 `client vX · backend vY` + drift flag + `64da518db` checkUpdates/applyUpdates 经 `connection.mode==='remote'` 改驱动后端 + `9c264555b` **新建** `update-copy.ts resolveUpdateCopy()` 显 "Backend update available" + `87ac7cac1` `_recent_upstream_commits` 从 `@{upstream}` 改 `origin/main` + `56be1a63a` 把合并 pill 拆 **client vX**（always）+ **backend vY (+N)**（remote-only）两 button + 删 version-bar.ts merge helper + 两套独立 atom + `81647458c` remote restart 后 poll backend 直答清 overlay）— **Desktop 主题 per-profile**（`9b1e0d6f7 (#42286)` `themes/context.tsx:25-60` 加 `PROFILE_SKINS_KEY/PROFILE_MODES_KEY/LAST_PROFILE_KEY` + `profilePref<T>()` factory，default profile 直走 legacy global 保单 profile 安装零变化）— **Desktop hover-reveal 折叠 sidebar #41670 + Shift+click status-bar zap 切全局 YOLO #41666 + viewport 不再追流式输出 #41414 + just-finished session 切走不消失 #42285 + scoped 事件 guard 收紧 #42178/#42359 + zoom 持久化 #41747 + messaging source folders 6 commit + 新 model provider 在 Settings 出现 #41545 + sleep/wake 后 stale backend revalidate + renderer bundle 缺失硬失败 #41729 + dist 解开 ASAR + Windows pack 时 Hermes.exe 锁 #42100**— **新 simplify-code skill**（`ace4b722d (#41691)`：**新建** `skills/software-development/simplify-code/SKILL.md 175 行` 灵感来自 Claude Code `/simplify`，3 narrow reviewer 经 `delegate_task` batch mode 并发，整 diff 不裂片；零核心修改全靠 orchestration）— **Windows ConPTY 接通 dashboard `/chat` tab**（`abcf996b1 (#42251)`：**新建** `hermes_cli/win_pty_bridge.py 179 行` POSIX PtyBridge drop-in 替代用 `pywinpty.PtyProcess` 替 ptyprocess/fcntl/termios；`hermes_cli/web_server.py:8285-8316` 平台分支让 `/api/pty` WebSocket handler 完全 platform-agnostic）— **Codex OAuth 帐户独立 pool entry**（`c78b3e1d3` 改创 `manual:device_code` pool entry 不再经 singleton `_save_codex_tokens` 覆盖前一个 #39236）+ **OpenRouter pool 自动侦测**（`9c9d9113a (#42263)` `hermes_cli/auth.py:1589-1605 resolve_provider()` 在 env 后 fallback `load_pool("openrouter").has_credentials()` 修隐形 401 #42130）+ **Approval 路径解析时机修**（`89d380261`+`b0efe1d64` HERMES_HOME 解析从 import 时改 `_normalize_command_for_detection()` 让 deferred-profile-resolution 工作）— **Vision 主动降级 list-type tool content**（`9513793ad (#41072)` `ProviderProfile` 加 `supports_vision_tool_messages: bool = True`，Xiaomi MiMo 设 False 免 400 round-trip）+ **honor custom_providers per-model `supports_vision`**（`41f071428 (#41036)` 扩 legacy list-style 路径）— **Compression 防泄漏四连**：cross-session `_previous_summary` 守 `8513a6aec`+`cca3b77a4` + background-review fork 关 compression `a77bc2c08 (#41708)` + topic+split 后 `/model`+`/reasoning` override 保 `2a10da3a1` #30479 + transcript 全装 tail budget 时无限 loop 修 `b18490b89` — **Memory 集群**：overflow 错语指令 in-turn 整合 `86c537d20 (#41755)` + end-of-turn sync 离 turn thread `aa6f2775f (#41945)`（broken Hindsight daemon 阻塞 298s 让 run_conversation 不返、agent 卡 running）+ `AIAgent.close()` 清 `_session_messages` `5e06c9ffe (#42123)` + gateway `_evict_cached_agent` 释 httpx client `e9c1e757f (#41974)` #29298 RSS 单调增 + 余下泄漏点 `3d029a53e`（~18MB/hr）— **Gateway 集群**：duplicate user message in state.db `8e4c447e5` + cascading interrupt hang `dd0d1222a`（interrupt close worker httpx 触 RemoteProtocolError 被误为 transient 重试每次停 300s）+ voice 在 agent 跑时也转录 `d55304c39` + busy_input FIFO `4d0f2bd24`/`fec5ca71d` #28503 + Telegram polling-conflict `get_running_loop` 替 `get_event_loop` `2e6286278 (#41716)` + DM session 凭 user_id 隔离 `540801336 (#41764)`（之前所有无 chat_id DM collapse 共享 key 让单 AIAgent 服多用户 history 渗血）+ `--replace` 老 PID 抗 SIGKILL 时中止 `e02f4c03c` #19471 + systemd `--replace` 去除 `4d18717b6 (#41892)`（Restart=always 下每次重启 self-kill loop）— **Auxiliary**：Codex Responses 共用转换器 `2789bf4e2 (#5709)`（让 replay 含 role=tool 不再泄入 Responses）+ 单次 same-target retry `02a4d6695 (#16587)` + Anthropic kwargs sanitize `399b8ee5f (#42155)`（Responses-only kwargs 在 api_mode-flip race 抵达 SDK 抛 non-retryable TypeError 杀整 turn）— **Plugins**：nested category 发现 `ccacfdbd6 (#41066)`（之前 flat iterdir 漏 `web/*/image_gen/*/browser/*/video_gen/*`）+ enable/disable mutation 补 `2b89afec7` + lazy-singleton 线程安全 helpers `47d5177a7 (#42150)`（fix honcho TOCTOU #24759）— **Install/Update**：`git fetch` scope 到目标 branch `e88116256`（数千 auto-generated branch 让 fetch 停几分钟）+ commit-less checkout 重 clone 不删 `fc0900d12`+`5d7abf911`+`a5c12f5f5 (#40998)`（改 rename 到 `<dir>.broken-<ts>` backup 永不 destructive）+ nix `addToSystemPackages` 时 config.yaml 改 0660 group-writable `4219a91df (#41940)` + NixOS node-gyp PYTHON env `15c99b437 (#40690)` — **Hindsight/ACP/Curator**：Hindsight 仅新回合 delta 上送 `09d66037f (#40605)` + ACP `_meta.hermes.sessionProvenance` 元数据 `777dc9da6 (#41724)` 让 client 不再需要解析 status text 猜 token drop 读 state.db + ACP interrupt sentinel 简化 `9b631e4ae`+`f5bd09af4`（共用 `INTERRUPT_WAITING_FOR_MODEL_PREFIX` 唯一 source-of-truth 防漂移；server.py 净 -17 LOC）+ Curator `PROTECTED_BUILTIN_SKILLS ({plan})` 守 `8e223b36e (#41817)`（之前 idle-archival 可 archive bundled `plan` skill 让 `/plan` 沉默返 Unknown command）— **Terminal/file-tools**：CWD-only override collapse 到 'default' 容器 `6459b3d99` + collapse 后 raw task_id 读 cwd `329c33dac`（之前 lookup 用 collapsed id 让 CWD-only override 的 cwd 被静默丢 env 在错根启动 exit 126）+ file-tools 拒收 sentinel `TERMINAL_CWD`，worktree edits anchor 在 live cwd 存在前 `e45b74583 (#41861)`— **Slack channel-only scope #15421 + Volcengine 工具名 XML 片段剥 #33007 + WeChat 输入指示器 ticket 600s 过期刷新 #38085 + ollama default_max_tokens 65536（修截到 128 token）+ custom endpoint 256K fallback 前查 `DEFAULT_CONTEXT_LENGTHS` + Profiles 跳 default 防重复扫**— **其余**：terminal tool 在 markdown 平台渲 ```bash 围栏块 gated on `BasePlatformAdapter.supports_code_blocks` capability #41215 / TUI `process.on('exit')` terminal-mode backstop #42165 / Windows PTY stdin str 替 bytes #41953 / api_server `/health` 报 hermes version #40620 / `hermes_time.reset_cache()` 实现 #41728 / `agent:start/end` hook context 加 `thread_id+chat_type` #41672 / TodoStore 内容长度+计数上限 / OSC 8 hyperlink 剥（Rich `[link=...]` raw payload 不再泄入 banner title）/ approval/clarify/sudo/secret modal 直绘不走 throttle #41098 / stream dropped mid-tool-call 不报 output truncation #42314 / TUI auto heap dump 防 disk fill（HERMES_AUTO_HEAPDUMP=1 闸 + HERMES_HEAPDUMP_MAX_BYTES cap）/ aiohttp/anthropic 额外 pin 与 lazy pin 对齐 + generalized regression guard #42335 / `498ae8029a (#41730)` delegate custom-endpoint 凭 endpoint identity 解（之前 parent+child 都 collapse 到 `provider="custom"` 让 child 把 delegated model 名发到错 endpoint）/ zombie subprocess reap（poll 后 wait）/ dist USER_OWNED_EXCLUDE 嵌套目录不再 drop / image_gen artifact path backend-visible expose

- [[2026-06-07-update]] — **~100 commits 单日爆发**（`c79e3fd0b → c9863772`，仍 v0.16.0；GitHub `compare` 接口报 108 commits / 290 文件 / 41 contributors，本日按 main 线性枚举得 97 独立 SHA；约 62 fix / 12 feat / 8 chore / 6 test / 6 docs / 1 refactor / 1 hardening；**未发新版**）：**Compression 时间锚定**（`d87f2939` `agent/context_compressor.py +32/-1` `_generate_summary()` 注两条 summarization prompt 路径都注 `_temporal_anchoring_rule` 把"打算 X" → "已于 YYYY-MM-DD X 完成"，日期经 `hermes_time.now()` 取 `YYYY-MM-DD`，时钟失败时 fail-open 省略而非阻塞；`tests/agent/test_context_compressor_temporal_anchoring.py 新建 +114`）— **Codex gpt-5.5 85% 压缩阈值**（`0524c9bd` 5 文件 +218：route-level override 仅 Codex OAuth 路由的 gpt-5.5 因硬钳 272K 窗口提升至 85%，OpenAI 直 API / OpenRouter / Copilot 同 slug 不动；`compression.codex_gpt55_autoraise` 旗默 true 可关）— **Trajectory 配对保护**（`f10a3309` `trajectory_compressor.py +81/-10` 新 `_is_boundary_clean` + `_snap_boundary` 让压缩边界绝不切开 gpt+tool 块，对 RL 训练数据生成尤要紧；+121 测试）— **Onboarding 结构化 profile-build**（`cb3e41e2` 5 文件 +174：**新建** `agent/onboarding.py +60` 含 `profile_build_directive()` 要求 agent 必先征同意/严禁沉默读已连接账户/经 memory `target="user"` 持久化；`gateway/run.py +31/-1` 首条消息 swap intro；`hermes_cli/config.py +6` 新 `onboarding.profile_build` 默 `"ask"` 可设 `"off"`；+73 测试）— **Desktop 统一 overlay 设计系统**（`f033b7d8` **33 文件**：所有 overlay raw `<button>` 统一到共享 `Button` 组件 + 共用 `shadow-nous` + `--stroke-nous` 发丝边线 + outline 1px inset ring + `BrandMark` 替 sparkle + `LogView/ErrorState/SearchField/OverlaySplitLayout` 跨 feature 复用；随附 `d165933 docs(desktop): add DESIGN.md` 成文规范）— **Desktop 工具后端完整 GUI 配置**（`20fd0bde` `settings/constants.ts +103/-2` 把 10 TTS provider/6 STT provider/6 Terminal backend 全写成 `ENUM_OPTIONS` 下拉 + 每后端字段 voice_id/language/model/device + `enumOptionsFor` helper；i18n ja/zh-hant/zh 同步 +41 各；与 `9752434 feat(desktop): run tool backend post-setup installs from GUI` 配合 GUI 直驱 `hermes tools post-setup` install hook + tail log）— **Desktop+Gateway 远程媒体中转**（`16786f3b` 11 文件：新 `image.attach_bytes` 端点 base64 上传 + magic-byte 嗅探 + 25 MB cap / 新 `pdf.attach` 经 `pdftoppm` 150 DPI 渲 PNG + 50 MB/25 页双 cap / 新 `GET /api/media` 经 base64 data URL 返 Gateway 图限白名单扩展名 + 限定 media root；Desktop `use-prompt-actions.ts +47/-4` 检测 remote 即走 `image.attach_bytes` 替本地路径；`media.ts +22` `isRemoteGateway/gatewayMediaDataUrl`）— **Desktop per-session 回合计时器**（`9d726807` 5 文件 +142/-8：状态栏单一全局 `$turnStartedAt` 改 per-session `ClientSessionState.turnStartedAt`，全局 atom 仅映当前 focused session；修后台仍跑回合切换显示从零的假象）— **Dashboard 字体独立选择器**（`9e631098` 11 文件：新建 `web/src/themes/fonts.ts` curated 目录含 Inter/IBM Plex/Atkinson Hyperlegible/Fraunces/Source Serif/JetBrains Mono 等 11+ + `ThemeSwitcher.tsx +119/-4 FontSection` 与主题完全正交 survives theme switches；后端 `web_server.py +46` 持久化；i18n + 文档同步）— **三平台 fail-closed 安全波**（`f6f3636 Discord` + `410cb743 Slack` + `3fa15b33 Feishu`：组件按钮 / 卡片 actions / 审批+slash-confirm 一律默认拒绝，必须 explicitly 命中用户白名单 / 角色白名单 / `*_ALLOW_ALL_USERS` opt-in 之一；Discord `_component_check_auth +19/-18` + 93/-17 测试，与 Telegram/Matrix 既有契约对齐）— **approval denylist shell-escape 剥除**（`621bf3a` 2 文件 +11/-2 关 #36846/#36847：`tools/approval.py:_normalize_command_for_detection +4` 比对前剥两类伪装 `\\([^\n])` 反斜杠 + `''|""` 空字面量；`tui_gateway/server.py +7/-2` 之前 `except ImportError: pass` 沉默关安全检查改 fail-closed 返 error 5001；+`1a4010e1` 回归）— **skill_view 路径穿越拦截**（`56f833e` 2 文件 +95/-1 关 #40566：新 `tools/skills_tool.py:_skill_lookup_path_error +57/-1` 三检 POSIX/Windows 绝对路径 + `..` traversal 段 + Windows 盘符；`skill_view()` 入口立即调用；38 测试）— **memory tool shadow 拒绝**（`fe8920d` 2 文件 +73/-1 关 #40902：`MemoryManager.add_provider` 与 `_HERMES_CORE_TOOLS` 比对，命中即拒绝进入路由表防 `clarify/delegate_task` 被劫持；`get_all_tool_schemas` 应用同过滤；built-ins always win 让 dispatch 劫持结构上不可能）— **Bitwarden zip-slip 防御** (`8f7567c`) + **api-server REST cron prompt 扫**（`0c48b71` 2 文件 +113：`POST /api/jobs` 与 `PATCH /api/jobs/{id}` 接 prompt 时调 `_scan_cron_prompt()` 与 agent tool 共用同一扫描器，defense-in-depth/parity 而非首道边界）— **SEARXNG_URL config-aware 自动侦测**（`7df81d05` HEAD merge #41482：新 `_env_value()` 先查 `get_env_value()` 后回落 `os.getenv()`；`_has_env` 切走该助手，让 config-only 设的 URL 也进 auto-detect cascade；+`2ee8c98 fix(web): honor Hermes config-aware SEARXNG_URL lookup`）— **URL 非 ASCII percent-encode**（`333f01b` PR #41430）— **Skills `browse` 全目录**（`1892e22` 1 文件 +8/-5 关 #41413：`hermes_cli/skills_hub.py:_PER_SOURCE_LIMIT["hermes-index"]` 5 000 → 1 000 000 让 ~90k catalog 全可见 5.4k → 全量）— **Kanban scratch 父清扫补漏**（`9405cd0` 主 `defer scratch workspace cleanup when task has active children #33774` + `76f0178` 补 2 文件 +89：非 scratch 子（`dir`/`worktree`）完成时清理函数 early-return 前**先跑** parent sweep，修永久 scratch 工作区泄漏）— **TUI SSH cwd 保留**（`0e0d704` 2 文件 +91/-11：新 `_terminal_task_cwd()` non-local backend 把 cwd 保留远端路径，`/home/user/workspace` 在 local 不存在但在远端是合法上下文；+`f9ea492` 测试）— **Pillow 提核心依赖**（`b13ab0b` `pyproject.toml +14/-8`：理由 oversized image >5MB/8000px 重缩在 vision-embed 时关键 + lazy-install 在 prompt_toolkit 持终端时锁死 #40490 + 纯 wheel 无系统库；`[vision]` extra 收为空 back-compat alias；+`5b55f4f` uv.lock 同步 + `c3d750c` + `d47f919` 把教训外推到所有 lazy-install 路径强制 `prompt=False`）— **自定义 provider v11→v12 迁移保留 models 映射**（`887295b` 3 文件 +134/-17：新 `_custom_provider_entry_to_provider_config` 保 `models`(含 per-model `context_length`)/`context_length`/`rate_limit_delay`/`discover_models`/`extra_body`/`api_mode→transport`/`model→default_model`，修之前迁移把 per-model context_length 元数据一刀切丢）— **`model.default_headers` 三处一致生效**（`a216ff8` agent 主路径 + `ffe6652` aux 路径 #40033 + `591e6fb` computer_use 视觉路由）— **Kimi 严格 thinking ⊕ reasoning_effort**（`ce4e74b` + `03392b6` opencode-go 同契约 + `e18f14d` 测试对齐）— **Windows 强化**（`210f4e7`+`8720023`+`5dee40f` powershell.exe 绝对路径 / `b08662b` stderr Unicode handler / `fc086da` JOB breakaway + status --deep / `fe0b3f2` watcher Popen 重试 / `f3af489 install.sh:` root-owned npm cache 提示 #39688 / `bec0796`+`fe2942a` color-emoji fallback #40364）— **MCP 已 ready 跳过 preflight**（`af08c43` 关 #40604）+ **cua-driver MCP stdio 重连**（`365437e`）— **杂修**：qqbot WebSocket 100% CPU spin（`3eeca46`）/ ffmpeg TimeoutExpired（`3289d4a`）+ subprocess.run/proc.wait 超时守护（`7223f22`）/ `int(os.getenv())` malformed env 守护（`2912d94` 关 #40598）/ Simplex display name（`490c486`）/ Honcho env fallback（`e2cc24e`）/ image-gen Codex 改 gpt-5.5（`bb53edc`）/ OpenRouter 输出 cap 解析（`1fb99b1`）/ `force_fresh_nous_tier` 仅 keyword（`44c0c2d`+`eb70ab8`）/ Desktop bootstrap install.sh GitHub 404 fallback（`02aad08`）/ 裸 URL autolinker 不再吞末尾 emphasis `*`（`349a3f6` 关 #41093）/ 同 provider 内换 model 保留 base_url（`0507e46` 关 #41121）/ destructive-slash 取消返 bool（`136dae7` 关 #40583）/ `/model` 切换 per-session 不泄进程级 env（`5a3092b` 关 #41120）/ Desktop session 列表按 profile 过滤（`b55ac45`）+ Gateway startup 与 turn 持久化（`330ca45`）/ TUI `liveSessionCount` 只变值时 patch（`8992955`）/ hook payload `SimpleNamespace` import 修（`40cea4d`）/ Home Assistant 从 `/update` allowlist 退出（`f4a73ab` 配合 v0.16 已迁插件）/ Bitwarden 无 TTY 早报清晰错（`89040e0`）/ OSV `npx --package` honor（`c0424b0`）/ uninstall Chat GUI 不动 agent（`5b43bf72`）/ install/TUI/workspace 安装作用域 6 连（`4bf5202`+`0416f85`+`675fb10`+`1c0437d`+`7db7a94`+`f2e8234`）/ 6 docs 修补（`b2b4d97` `d17c953` `fda66c4` `fd4c8b4` `f5c3fc3` `2d099fe`）/ 5 chore(release) 作者映射（`6701c61` `3763355` `a317e54` `fa8fd51` `4b9862e`）

- [[2026-06-06-update]] — **75 commits 跨日同步**（`66a6b9c930 → c79e3fd0b`，**v0.15.1 → v0.16.0 "The Surface Release"**，874 commits · 542 PRs · 170 contributors · 399 issues closed 自 v0.15.0；约 47 fix / 15 feat / 6 refactor / 5 test / 1 chore / 1 docs）：**v0.16.0 发布**（`3c231eb39` `pyproject.toml:10` + `hermes_cli/__init__.py:17`，"native desktop app + browser admin panel + remote-gateway connect + Simplified Chinese desktop UI + leaner default skill set + NVIDIA/skills trusted tap + fuzzy model picker + /undo"） — **`/version` 全栈贯通**（CLI/TUI/Gateway/Desktop，`hermes_cli/commands.py:218-219` + `cli.py:9076` + `gateway/run.py:10942-10946` reuse `format_banner_version_label()` 含 git SHA；Slack `/q` alias 被新 `/version` 占槽顶出去仍可 `/hermes q` 触达） — **`/usage` 用量感知 Credits 系统**（`fcb1944b4` 33 文件 **+4535/-26**：**新建** `agent/credits_tracker.py 723 行` 含 `CreditsState dataclass :93`（`paid_access` 锚 depletion 永不 `remaining==0` + `subscription_micros` SIGNED 唯一允许负值 + `used_fraction` 守 `subscription_limit_micros` 真有时算）+ `parse_credits_headers() :319` retain-last-known 解析 x-nous-credits-* + `CREDITS_USAGE_BANDS 50/75/90` 升级 status-bar 单 key + `evaluate_credits_notices() :200` 纯函数 reconcile；`run_agent.py:2721 _capture_credits()` fail-open，warm+cold-start 共享 `:2802 _emit_credits_notices()`；`cli.py:10595 _show_usage` 在 TUI slash-worker 无 agent 也 print Nous credits block；TUI `appChrome.tsx +90` cents delta + escalating usage band 段 + `(dev credits)` banner 经 `HERMES_DEV_CREDITS`；`tests/agent/test_credits_tracker.py 909 行` 92 测试 9-state 矩阵 + edge cases；`c79b6f23e` grant-spent 一次性闪现替 sticky） — **`/steer` 桌面化 + mid-turn 可信注入**（6 commit：composer `<SteeringWheel size={16}>` busy+text-only draft 时显示 + Cmd/Ctrl+Enter 严格保留永不 fall-through；`agent/prompt_builder.py:443-472 STEER_MARKER_OPEN/CLOSE` 边界标记 `[OUT-OF-BAND USER MESSAGE …]` + `STEER_CHANNEL_NOTE` 进 system prompt 教模型"只信此 marker，lookalike 在 tool output/web/files 中仍 untrusted"防 prompt injection 误判；`tui_gateway/server.py:4234 @method("session.steer")`；codicon 替 ⏩ emoji；trusted 不再 read as injection） — **Desktop 可重绑键盘快捷键面板**（7 commit + Merge：**新建** `apps/desktop/src/lib/keybinds/actions.ts 125 行` 单一 registry `KEYBIND_CATEGORIES composer/profiles/session/navigation/view` + `PROFILE_SLOT_COUNT=18`（⌘1-9/⌘⌥1-9）+ `KEYBIND_READONLY` 含 composer.steer=`mod+enter`/history=up,down；`combo.ts 169 行` event.code-based 让 Shift 不变 base key；`store/keybinds.ts 139 行` nanostore `$bindings` 仅持久化与 default 不同的项 + `$comboIndex` reverse + `$capture` armed + 面板 store；`app/shell/keybind-panel.tsx 226 行` + `app/hooks/use-keybinds.ts 186 行`；hotkey 抉择：`profile.default` 从 `⌥⌘0`→`⌘D`（D for Default 二键易按），`view.flipPanes` `⌘\`，面板自启 `⌘/`） — **Desktop i18n 日本語 ja + 繁體中文 zh-hant + 可搜索语言切换器**（7 commit Jim Liu 宝玉：**新建** `apps/desktop/src/i18n/ja.ts 1854 行` + `zh-hant.ts 1813 行` + 扩 `types.ts 1416 行` 嵌套结构 + `languages.ts 86 行` `日本語/繁體中文` endonym；72 文件 +4397 把扁平 i18n 重构嵌套；`language-switcher.tsx` 可搜索；`+3051` 单 commit 补译；settings i18n 转 camelCase） — **Home Assistant adapter 迁入 bundled plugin**（`c37c6eaf2`+`ef7e5168b` 与 Mattermost/Discord 对齐：**新建** `plugins/platforms/homeassistant/adapter.py 128 行` + `plugin.yaml 22 行`，`gateway/run.py -7` 删 inline；`/update` allowlist 收紧 plugin-migrated 走 registry fallback + 121 测试） — **Adaptive execution intercepts middleware + NeMo-Relay 增强**（3 commit Bryan Bednarski NVIDIA：`hermes_cli/middleware.py 314 行` 4 类（`tool_request/tool_execution/llm_request/llm_execution`）+ `next_call` 单次性 + `_safe_copy` deepcopy 兜底（threading.Lock/clients/file handles）+ translated downstream 失败 propagate（不返 None）；HEAD 8 处 Hermes call site `agent/conversation_loop.py:1243,1375` + `agent_runtime_helpers.py:1637,1806` + `tool_executor.py:184,227` + `model_tools.py:987,1114`；`plugins/observability/nemo_relay/__init__.py 836 行` 加 `adaptive_enabled/adaptive_mode` 字段 + `register_middleware("llm_execution"/"tool_execution", ...)`；**新建** `docs/middleware/README.md 260 行`；+614 测试） — **Dashboard Skills hub 浏览器重做**（`56236b16e` 4 文件 **+1281/-72** + `e6de6dd55`）：4 新端点 `/api/skills/hub/{sources,preview,scan,search}` 在 `hermes_cli/web_server.py:6790-6957` connected hubs + trust tier + GitHub rate-limit + featured + installed map + 不安装 preview SKILL.md + quarantine + scan_skill + should_allow_install 安全 verdict + per-source counts；`web/src/pages/SkillsPage.tsx 1442 行` HubBrowser landing 不再空白页 + trust-color cards + Detail dialog 读真 SKILL.md + 视觉安全扫描 — **Dashboard 工具后端完整 GUI 配置**（`2bf0a6e76` 7 文件 +866）：CLI 新子命令 `hermes tools post-setup <KEY>` 非交互 + `tools_config.py:1171 valid_post_setup_keys()` 校验防注入；3 新端点 `PUT /api/tools/toolsets/{name}/env :7706` 写 `~/.hermes/.env` + `POST .../post-setup :7764` spawn 跑 install hook + tail log；**新建** `web/src/components/ToolsetConfigDrawer.tsx 448 行` 全生命周期 enable/disable/pick provider/save key/run install hook+log tail — **Desktop 暴露 `hermes model` 全 37 provider + stale-aux pin 警告**（`e8c837c92`+`b91aade17` 8 文件 +411 + 7 文件 +217：`/api/model/options` 加 `include_unconfigured + picker_hints + canonical_order`；`apps/desktop/src/app/settings/model-settings.tsx +174` inline "paste key → Activate" 流；switch main 返 `stale_aux` 让 Desktop 显 banner + "Reset all to main"，永不自动清 pin —— 专门便宜 aux 是合法配置 surface-and-offer 非 nuke） — **macOS 26 launchd 降级**（`59c273ba3`+`360630733`：`hermes_cli/gateway.py:3002 _launchd_domain() user/<uid>` 替 `gui/<uid>`（后者返 error 125）+ `:3015 _LAUNCHCTL_DOMAIN_UNSUPPORTED_CODES = {5, 125}` 持续不支持时 `:3051 _spawn_detached_gateway()` 真 detach 用 `nohup` workaround，logs→gateway.log，PID→gateway.pid 让 stop/status/restart 继续工作；184 测试） — **Composer Arrow 历史 + 队列融合 + drop-shadow 删除 + IME compositionend flush**（8 commit：**新建** `composer-input-history.ts 158 行` + 147 测试；queue 非空时 ArrowUp 编辑 queue 末项；TUI `message.complete` 边界 force-send 一次；删 `shadow-composer` 平衡 Apple HIG；`onCompositionEnd` flush 修 IME #39614 + 三测中/日/韩） — **About panel 显活版本 + macOS updater repair + OAuth path 修**（3 commit：seed `applicationVersion: resolveHermesVersion()` 经 `__init__.py` 读 + click handler `showAboutPanelFresh` 每次开重解 in-place update reflect；updater helper paths.rs 路径解析；抽 `oauth-net-request.cjs 20 行` + 34 测试避受限 `origin` header） — **FTS5 ":" 脱毒**（`d1771114e` `hermes_state.py:2708-2739 _sanitize_fts5_query` Step 2 在 strip 字符集 `[+{}():\"^]` 加 `:`（col:term 在单 content 列上 `TODO: fix` 抛 no such column 被吞为零结果）+ 25 测试） — **delegate content-block 处理**（`f83918c31`+`f8a241e10` `_stringify_tool_content` 拍平 content-block 数组 + error block 识别） — **CLI 更新时隔离 hermes.exe**（`ebed881d4` 抽 `_run_quarantined_install` 让 dep-verification repair 也走 quarantine + `_detect_concurrent_hermes_instances` 用 `proc.parents()` 整祖先扫只排 shim 父进程；`:12836` 扫 stale `hermes.exe.old.*`） — **Desktop log 大小封顶 + 级联轮转**（`abbf05024`+`146e77684` `apps/desktop/electron/main.cjs:265-268 MAX=10MB/BACKUP=3/DISCARD=40MB` mirror Python RotatingFileHandler；DISCARD 上 ceiling 直接删 multi-GB boot-loop 巨档自愈用户磁盘） — **Middleware `next_call` 单次性 + deepcopy 兜底 + translated 错误保留** — **Install Windows npm TLS 证书侦测**（`fd234bad6 install.ps1 +40 Show-NpmCertHint` 给 `NODE_EXTRA_CA_CERTS`/`strict-ssl` 提示 修企业 MITM 误读 #38016） — **Gateway plain text while busy 默认 interrupt**（`54e7b74f7` 关 #38390/#31588：`busy_input_mode` 重夺单源真理，`busy_text_mode` 沦为遗留 override；所有默认 fallback `queue → interrupt`） — **杂项**：dashboard cron 派单读已配置 platforms / send-message ntfy topic 当显式 / rg/grep search error guard 接 partial matches +266 / image-gen artifact 路径暴露 backend-visible + 抽 `map_cache_path_to_container` 共享 helper / profile alias 显示反向查 wrapper dir / skill 详情对话框间距 / `/q` test 因 `/version` 占槽更新

- [[2026-06-05-update]] — **289 commits 跨日同步**（`6fff744158 → 66a6b9c930`，仍 v0.15.1；163 fix / 45 feat / 21 style / 16 chore / 10 docs / 7 test / 7 refactor / 2 perf / 1 revert / 1 polish；日产能 06-03:57 / 06-04:176 / 06-05:53 / 06-06:3）：**Desktop 多 Profile 革命** — **Arc 风格 Profile Rail + 跨 Profile 会话聚合**（`b94b3622b` 52 文件 +2517：`profile-color.ts 71 行` 确定性 hash hue + 12 swatches + `store/profile.ts 299 行` 5 nanostore atom + `profile-switcher.tsx 491 行` dnd-kit + ContextMenu + `hermes_state.py:3189 session_count(exclude_children)` + `web_server.py:1637 get_profiles_sessions`）+ **并发多 Profile Gateway Sockets**（`4891f9ae7` 12 文件 +585：`store/gateway.ts 290 行 primaryGateway + secondaries map + 1s..15s backoff` 一 profile 一持久 socket，approval/sudo prompts per-session 暂存不 hijack foreground）+ **每 Profile 独立远端 Gateway 主机**（#39778，`connection-config.cjs 254 行 + 测试 63 行` + `profileRemoteOverride` 纯函数 + `resolveRemoteBackend` 4 级 precedence override→env→global→local）+ **Profile rail 14 follow-up**（rename/delete/drag-sort/长按选色/fluid haptic/单 profile +/"All" +/cell-by-cell 步进/x 轴锁/session-id SQL-bounded 搜索）— **Desktop 全局 Cmd+K 调色板 + UI 大扫除**（`ac9de2e80` + 15 commit：cmdk-based palette + Cmd+P 别名 + 共享 SearchField/ErrorState/Badge + controlVariants + boxless text 按钮 + kill focus rings + flatten appearance settings）— **Desktop i18n 简体中文 zh-Hans + 配置持久化**（`4a1907bd1` 36 文件 +4226 + `1d9c3ebae` 21 文件 +830：**新建** `apps/desktop/src/i18n/` 2431 行（`types.ts 610 + en.ts 744 + zh.ts 873 + languages.ts 56 含 endonym 简体中文`）+ 24 屏改 t 对象 + Appearance Language 下拉 + 从 Hermes config 读让 Desktop↔Dashboard↔CLI 共享语言）— **Dashboard Auth 三新 provider**：**BasicAuthProvider 用户名/密码**（**新建** `plugins/dashboard_auth/basic/__init__.py 491 行` 无 IdP 无 DB + scrypt + stateless HMAC）+ **SelfHostedOIDCProvider**（**新建** `plugins/dashboard_auth/self_hosted/__init__.py 736 行` Authentik/Keycloak/Zitadel/Authelia/Auth0/Okta/Google — discovery + PKCE S256 + ID token RS256 验证 vs JWKS）+ **`hermes dashboard register`**（`hermes_cli/main.py:12403,15712-15754` 子命令含 issuer/client-id）+ **Desktop username/password 远端 gateway 登录** + **pluggable non-redirect password 登录**+ **Verify chain 一 provider 错不短路**— **Discord 语音频道软件混音器**（`8a9ded5b2` **新建** `plugins/platforms/discord/voice_mixer.py 378 行 :148 class VoiceMixer(discord.AudioSource)` 48kHz/2ch/int16 LE/20ms 一安装永不 stop，ambient idle bed + verbal acks/TTS 覆盖并 duck — Grok-voice 感）— **Telegram QR 入门 CLI + Dashboard 双面**（`6bf55a473` **新建** `hermes_cli/telegram_managed_bot.py 358 行` + 323 测试，扫码即入门告别 BotFather token 复制；Dashboard 同 QR 入口）— **Feishu 视频会议邀请**（`feishu.py:1635,2481,3372` 订阅 `vc.bot.meeting_invited_v1` + **新建** `feishu_meeting_invite.py 212 行`）— **Gateway `max_tokens` 全链路传播修复**（#20741，4 commit：`gateway/run.py:1205-1232,2621,2719` + `hermes_cli/runtime_provider.py:477 _lift_max_output_tokens` 5 处 call site + `cli.py +14` CLI 路径 `HERMES_MAX_TOKENS` env override + `test_max_tokens_propagation.py 178 测试`，precedence `HERMES_MAX_TOKENS > model.max_tokens > custom_providers max_output_tokens`）— **`discover_models` 在 named-custom 流接上**（2 commit，custom_providers section 4 + `hermes model named-custom` 不再静默忽略）— **Update 非交互默认 stash/restore**（`72eb42d9e` revert #38542/#39568：`config.py:2287 non_interactive_local_changes` 新 key 默认 stash + `_config_version 26→27` + `main.py:8378 _discard_stashed_changes` 永不 `reset --hard` 用 stash+drop 保 ignored 路径生存；`install.ps1 +76` Windows 同步）— **Update Windows 卫生 4 连**（ensure_uv 穿越更新 + 不返 _UvResult / desktop 不与自身后端 race / handoff retry / venv "move aside" 防 .pyd 锁）— **hermes-ink SGR 鼠标分片根因修**（`f35432354` supersedes #29337/#17701/#18113/#26781/#28463/#35512：`tokenize.ts +40` flush state-aware xterm.js 纪律 multi-byte sequence 中不 emit + one-tick truncation valve；`6efc7eda5 -205 行` 死正则 sink 全删；`725290db6` fuzz 加固）— **TUI cgroup-aware V8 heap cap**（`memoryMonitor.ts:20-50` 从 `getHeapStatistics().heap_size_limit` 派生 critical ~88% / high ~70% 真 ceiling 替硬编码 2.5GB）— **State `PRAGMA wal_checkpoint(TRUNCATE)`**（`hermes_state.py:597,617-651` 防 WAL 无限增长）— **Gemini 原生 `maxOutputTokens` + 去 OpenAI extra_body 污染**（7 文件 +277；Fireworks/Mistral 同向 strip extra_content）— **`perl/ruby -i` 在配置/env 路径上落闸**（`approval.py:446-454` 三形式全捕与 `sed -i` 闸对齐）— **react-router-dom 7.17.0 GHSA-8x6r-g9mw-2r78** + Nix npmDepsHash + fetchNpmDeps — **Web SSRF 检查移出 event loop**（`tools/url_safety.py:355 async_is_safe_url`）— **Vision ProviderProfile flag 取代 models.dev 目录** + 像素维度上限（高分辨率小字节也 cap）— **MiniMax 视频 `type=video` block + M3 默认 aux 对齐前沿** + **Grok-4.3 陈旧 256k cache 失效**— **Cron 顺序任务非阻塞 + 解耦完成 + MCP 清扫 + 陈旧 output 复验**（`scheduler.py:161-195 _sequential_pool` 持久 single-thread + 复验 #37721 + cron list null repeat 容忍）— **Docker wave**（hindsight-client 烤进镜像 / libolm-dev for python-olm / chown gateway tree / `--user <uid>` 拒绝+guidance / Unraid uid 例外 / config migration on boot / persistent 模式 out-of-band 删恢复 / 孤儿容器清 / Docker 内 update-check 不报 phantom commits behind / LF entrypoints / .dockerignore Python 模式）— **Skills 内置集瘦身 + `environments:` relevance gate**（#39028：内置 skill 删过气 + frontmatter cli/tui/desktop/gateway/* 决定**何时实际激活进 prompt**）— **Auth `set active_provider` 三入口**（xai/qwen-oauth/google-gemini-cli 后再"auth 加完没生效"）— **MCP `${ENV}` discovery probe 解析 + shutdown 守护扩 BaseException**— **Install `requires-python<3.14` + `UV_PYTHON` 钉 venv + PEP 639 SPDX license + FHS symlink node/npm + shallow clone + .bootstrap-marker gitignore**— **Gateway 杂项硬化** 15 条（dm_policy=pairing 不视为开放 / `/update` 完成通知保留 / root-run `$HOME` 投递 / session_reset race 清 zombie agent slot / Telegram sealed overflow chunk 显式 finalize / shared-key loop 嵌套 platform 桥接 / 非 UTF-8 status/pid 文件容忍 / kanban dispatcher 守 malformed config + 空 summary / Slack `thread_ts` / Line MessageType / Google Chat OAuth client secret / api-server json.loads 守 SQLite corrupt / Windows native default Hermes home + 锚 cwd / Discord interactive views 超时视觉过期 / strip session token from subprocess env）— **Delegation `max_spawn_depth` 取消上限**（`config.py:1679-1682` floor 1 无 ceiling 拥抱 orchestrator-of-orchestrators 自由编排）— **Models catalog**（`qwen/qwen3.7-plus` 入 Nous+OR / `deepseek-v4-flash` 作 Nous silent default 替 flagship + guard test 钉死 / Desktop 每 provider 50 model 默认）— **杂项** OpenViking memory URI `/agent/{agent}/` 段修 + memory 用户安装 provider 父包注册 + memory pip fallback + SSH WinError 1314 symlink copy2 / Prompt 自知识扩 docs+skill / CLI resume relaunch 在 session 起始 cwd + rich [dim] / dashboard orphaned embedded-chat 回收 / /model picker provider-models prewarm 后台 / termux frontend npm scope / 25+ AUTHOR_MAP + 10+ docs
- [[2026-06-03-update]] — **131 commits 跨日同步**（`c47b9d126 → 6fff744158`，仍 v0.15.1；69 fix / 12 feat / 8 test / 6 chore / 5 docs / 1 refactor / 1 polish / 1 perf / 1 ci）：**Observability observer-grade telemetry hooks + NeMo-Relay 插件**（#29722 salvage 26 文件 +3187：`hermes_cli/plugins.py:128 VALID_HOOKS` 新加 `api_request_error`/`subagent_start` 共 21 hook + `has_hook()` 闸 hot path 零开销 + **新建** `plugins/observability/nemo_relay/__init__.py 568 行` + `plugin.yaml 20 行` + `README.md 368 行` + `docs/observability/README.md 316 行`；`827f25142 perf` 进一步把 per-tool emit 也闸，懒派生 status/error 字段）— **`hermes portal` 三 commit 一站化**（`da4f407e5` 人类可读 onboarding 别名 + `cd188b814` 真正走 `hermes_cli/setup.py:2723-2790 _run_portal_one_shot` → `_model_flow_nous` 27-model curated picker + `26a57467a` SystemExit 防御 + `9ba7e5b1b` retry hints 对齐）— **Desktop xAI Grok 升级一等 OAuth provider**（`dd5e97bd7`，5 文件 +434：**loopback PKCE 流** `hermes_cli/web_server.py:3160-3163 flow=loopback` + `:3750 _start_xai_loopback_flow` +222 行 + 6 后续修复包 cancel signal worker / Copilot review / source_label 修 / OAuth catalog 顺序）— **Desktop 状态栏 inline model picker**（`ea4fe1563`，11 文件 +788：Cursor 风格下拉替 modal，`model-menu-panel.tsx 289 行` + `model-edit-submenu.tsx 248 行` hover-Edit reasoning+fast + `model-visibility-dialog.tsx 148 行` localStorage edit-models + 共享 dropdown primitives + nanostores 防 rebuild）— **Desktop session/scroll/sticky/clamp wave**（~12 commits：in-flight new chats 不消失/不匿名 + thread-virtualizer scroll anchor 三连 `e67ab2e04`/`3aa24e261`/`c930a49ce` 与 native scroll anchoring 双修冲突 + `0caa23788` IME composing Enter 不再 split + viewport resize 不再误 disarm sticky-bottom `lastClientHeightRef` + sticky human 4 commit clamp ~2 行 + `--sticky-human-top` 0.75rem token + 自定义 zoom 0.1 step 替 0.2 + clipboard image 去重 items/files 双附）— **Desktop slash/`@` 菜单 + Stop 真中断 + 后台 bleed**（`188e52db9`/`49f1b9e4b` `triggerKeyConsumedRef` 替 timing-fragile guard 修 Esc 重开 + Arrow 不能 cycle 过头几个 + `a23728dfc` Stop 按钮 `shouldAutoDrainOnSettle()` 抽出 + `28f1590b7` keyed-by-sessionId map staging 防后台 bleed）— **Desktop macOS 自更新 clean rebuild**（`a1cda2410` `update.rs` 头插 PATH + `--target-app` + ditto install stage + `AtomicBool` race guard；`bootstrap.rs` `open <App>.app`；`d3b1e4300` `swap_in_new_bundle()` 严格不变式永不留断臂 target；`810e5864d` `stream: LogStream` 结构化替 `stderr:` literal + `1971b1052` 4 处 emit_log signature 修；`746618217` 已存在 macOS 安装收编 + 自动重定位 `/Applications` + Dock 钉一次；`1daecfa4b` Dock 图钉 `pathToFileURL` + type 15；`21e172b94` macOS helper 麦克风 entitlement；`46e513ef5` Linux chrome-sandbox 4755）— **Desktop 远端显示禁 GPU**（`6a2909fe5` `apps/desktop/electron/bootstrap-platform.cjs +51 detectRemoteDisplay` + `HERMES_DESKTOP_DISABLE_GPU` 双向 override + `d0ea4caf7` WSLg vGPU 本地渲染例外）— **Dashboard System 检查后再更新**（`c5d199ead` `GET /api/hermes/update/check :1168` 6h cache + `POST /api/hermes/update :1140` + System 页 badge/按钮/ConfirmDialog + 5 端点测试）— **Dashboard Profiles 富化 + Channels env 去重**（`7fb8a6b5c` active-profile 切换/角色描述自动生成/per-profile 模型/gateway-running badges；messaging 凭据从 Keys/Env 隐藏改 Channels 唯一；"messaging" → "Gateway" i18n）— **Dashboard auth WS origin + PTY internal credential**（`dd28f2ac9` packaged Hermes Desktop `file://` 在 OAuth-gated 不再被 Origin guard 403 + `fd1ec8033` `_build_gateway_ws_url()` 补 gated 分支 PTY child 用进程内 mint-once credential 修嵌入 chat 在每个 gated 部署都不能用 + `e114b31ed` 7 分支单测）— **Dashboard PTY 维度 WSL2 钳制**（`966630563` `cols=131072 rows=1` 钳到 `[1,2000]×[1,1000]` 防 `struct.error`）— **Gateway ResponseStore + 未挂 adapter 泄 fd 修**（`4b06c98fe` reconnect 3 失败路径漏 2 fd（db+WAL）~12h 撑爆 2560 fd ulimit #37011 + `01eaba706` Copilot review 接）— **Gateway `/background` 媒体按类型路由**（`082025abc` TTS→voice/video→send_video/image→send_image_file 复用 `should_send_media_as_audio`）— **Gateway schtasks locale 解码**（`973decc05` Windows 中文 console 不再 `UnicodeDecodeError`）— **Web-server event channel state 从模块全局搬 app.state**（`cbc82511e` `_lifespan()` 在 FastAPI 启动正确 loop 创建 dict+lock 修 "attached to a different loop"）— **Doctor `HERMES_MAX_ITERATIONS` env 影子检测**（`6ee046a72` `hermes_cli/doctor.py:894-922 env_ghost` 关 #17534 - bridge 在更早 config-parse 错时 bail 让 ghost 默默胜出）— **Kanban 速率限制非永久阻塞**（`4c544b633` `EX_TEMPFAIL=75` 哨兵 `hermes_cli/kanban_db.py:158-161` + 冷却 `:189-196 HERMES_KANBAN_RATE_LIMIT_COOLDOWN_SECONDS` 默认 5min + `:4821 rate_limited` field + `:4873-4884` reap + `:5429` worker drain）— **Matrix `!` bang 命令别名 + 死代码修**（`0022e94d7`/`a1264e996` underscore→hyphen 容忍 emit + skill command slash-prefixed 比较 + reply-fallback 后再规范化）— **MCP banner `disabled` 不再 `failed`**（`1d90b2398` `enabled:false` 改 dim）— **TUI 持久化 trail OOM 修**（`e76d8bf5a` `ui-tui/src/lib/text.ts:2-5 VERBOSE_TRAIL_MAX_CHARS=800` 12 行 preview 替 16KB live-render budget 持久化 trail 掉 17×（12.2MB→0.7MB @800 msgs）+ `memoryMonitor.ts` 真实 V8 heap ceiling 阈值；`e02a6038a` TUI `/save` 写 `get_hermes_home()/sessions/saved/` 含 system prompt；`dfba3f3e5` 右键复制清选区）— **Aux Nous-routed self-heal**（`ab2472e69` `agent/auxiliary_client.py:2495 _is_model_not_found_error` + `:1624 _refresh_nous_recommended_model` + 两 call site，长寿命进程缓存 Portal 推荐 model 被 catalog 掉时不再 404 至重启）— **CLI `display.interface` 可配置默认界面**（`d6b0c23f8` 加 `--cli` 显式覆盖 + 两 resolver `_resolve_use_tui :1823`/`_wants_tui_early :145` 无依赖早期）— **Debug 含 `desktop.log`**（`1b302a047` `hermes_cli/logs.py +2` 注册 + `debug.py +32` summary+share+`hermes logs desktop`）— **Packaging `locales/` 进 wheel/sdist/Nix**（`c349eca82` 关 #23943/#27632/#35374 `agent/i18n.py:88 _locales_dir HERMES_BUNDLED_LOCALES override` 封闭安装不再渲染原始 i18n key）— **Refactor 单一 managed-uv 路径**（`4df280d51` **新建** `hermes_cli/managed_uv.py 228 行` 5 函数；删 install.sh 61 行 `ensure_fts5` 升级路径）— **Workspace lockfile 整合 + Node ≥20.19/22.12**（`a51a7b9b9` 单一根 lockfile + Nix 共享 + Desktop 内容 hash build stamp 替 `--force-build`/mtime + `1dca7c620` Node 版本预检 + `214b7e070` install.ps1 处理 `core.autocrlf` 脏 worktree + `205ed71ba` npm audit 6 修）— **Docker TUI launcher 用 prebuilt bundle**（`9272e4019` `HERMES_TUI_DIR` 修嵌入 dashboard Chat tab 在托管镜像 502 因 root lockfile workspace 失配；`d9f7e7ac8` 首次启动 seed `gateway_state.json` from `HERMES_GATEWAY_BOOTSTRAP_STATE=running`）— **Cron skill bundles 展开**（`ae5b2de2f`）— **Setup browser/TTS picker 默认指向免费本地后端**（`b28dd3417` 不再 Enter 即走 inline Nous Portal device-code 付费）— **Submodule + RELEASE 清理**（`43fd63b4b` 7 文件 -35 + `047e7cf36` 残留 + `64202200a` 15 RELEASE_v*.md + `.gitignore`）— **杂修**（`192020992 + 5fca754ee` `HERMES_DESKTOP_CHILD_PID` 端到端关 #37532 + `6ed9a2de8` desktop WS origin 远端 bind + `a429a2a0b` CI nix flake check + `bb1c8b6f1` honcho prewarm 去 flake + 5 docs 远端 backend 自含 + `--tui` 标志 + session token 钉法）
- [[2026-06-02-update]] — **104 commits 跨日同步**（`b9646276f → c47b9d126`，仍 v0.15.1；43 fix / 23 feat / 7 test / 6 refactor / 5 docs / 5 chore / 1 ci）：**Dashboard 第二轮强化** — **Channels 页面**（#37211 一站式 20+ messaging 平台配置，`web/src/pages/ChannelsPage.tsx +429` + `App.tsx:81,135,176` + `api.ts:476-490,871-913`）+ **完整管理面板 v2**（#36736，`web_server.py +665` MCP catalog/system stats/webhook 启停/skills hub 搜索 6 类端点）+ **`nous-blue` 主题 + bulk sessions + schedule picker**（#37383，41 文件 +3299，LENS_5I 移植 + 14 i18n + `schedule.ts +382`）+ **Dashboard auth refresh-token 轮转**（#37247，AT 缺席 + RT 在不再硬 401）— **Gateway 结构化 stream-event 协议**（#37250，8 文件 +740：**新建** `gateway/stream_events.py` 171 行 7 frozen dataclass + `gateway/stream_dispatch.py` 132 行 `GatewayEventDispatcher` + `base.py:1873,1934,1955` 三 adapter hook + Telegram draft MarkdownV2 对齐告别 raw→formatted 跳变）+ **Per-platform streaming 默认值**（#37303，`config.py:1365-1368 telegram=on/discord=off`，deep-merge 不覆盖用户值，dashboard 自动暴露 toggle）— **TUI 单一 `/model` + 统一 Sessions 浮层**（#37112，`/provider` 别名废 + `activeSessionSwitcher.tsx :48-394` 合 `/resume`/`/sessions`/`/session`/`/switch`，armed-delete 按 session id 防轮询重排错杀）— **Agent `runtime_cwd` 单一真源**（8 commit：**新建** `agent/runtime_cwd.py` 62 行 5 函数 ContextVar override + isdir guard 不对称，修 #24882/#24969/#27383/#29265 多年 cwd 不进 system prompt bug 簇）— **Model picker 模糊搜索**（3 commit：**新建** `web/src/lib/fuzzy.ts` 192 行 + 镜像 `ui-tui/src/lib/fuzzy.ts` 177 行，subsequence 评分 + `fuzzyRank<T>()` 泛型 ranker 返 positions 给 `<mark>` 高亮）— **Desktop session 卫生/archive/media streaming**（#37099，26 文件 +1000：lazy DB row 不泄 "Untitled" + `hermes_state.py:267 archived` 列 + Electron `hermes-media://` 协议 Range 支持去 16MB cap + 9 后续修复 cancellable install / sidebar 搜索 / `--build-only`/`--force-build` content-hash build stamp / GUI triage 38 文件 +1638 / project folder session 稳定基于 `runtime_cwd`）— **Skills hub browse cap**（#37143，`skills_hub.py:343` 补 `"hermes-index": 5000` 修 silent 50-item cap + Identifier 列复制 + source links + 类别 430→173 整理）— **Cron skill 不可见 unicode 净化**（#37245，`cronjob_tools.py:184-210 _strip_invisible_unicode` 返 `(cleaned, removed)` 保留 emoji ZWJ + 净化后**再扫**让真注入仍能匹）— **MCP HTTP fast-fail**（2 commit，`mcp_tool.py:1480-1553 _preflight_content_type()` ≤5s HEAD/GET 检 HTML 抛 `NonMcpEndpointError` 不 retry）— **Kanban workspace 继承**（#37172/#37182，`kanban_db.py:4355-4408` decompose 取根 workspace 不强制 scratch + `HERMES_KANBAN_TASK` env）— **File-safety sandbox-mirror 双层 guard**（#32213/#32407，`agent/file_safety.py:498-640` 新 `classify_sandbox_mirror_target` + `classify_container_mirror_target` 防 docker 后端写 SOUL.md 两份分歧副本）— **Telegram 观测群媒体缓存 → 平台无关原语**（2 commit，`gateway/platforms/base.py:1313-1366 cache_media_bytes()` 任何 adapter 复用）— **Gateway extract-stripped 工具响应恢复**（#29346 2 commit，`base.py:4082-4126` pre-extract 快照 + 文本被剥空时从 post-`extract_media` body 恢复 + `:4333 response_delivery_dropped` ERROR 守不变式）— **Weixin `asyncio.wait_for` 替 aiohttp ClientTimeout**（2 commit，`weixin.py:370-414`，修 cron `run_coroutine_threadsafe()` "context manager should be used inside a task" 错）— **MiniMax-M3 ≤204,800 stale cache 主动失效**（#36726，`model_metadata.py:1130 _model_name_suggests_minimax_m3()` + `:1554-1567`）— **Bluebubbles 群消息 mention gating**（97 行 + 132 测试）— **xAI 视频按 modality 路由**（8 文件 +167）— **Gateway 杂项**（WeCom env-only allowlist + WhatsApp dm_policy 开放 + s6 心跳缺 sleep 回落 + 重启通知清理 9 文件 +378）— **Docker/CI**（python3-venv / docker -w / arm64 registry cache / dashboard docker update guidance）— **Installer**（macOS "Hermes" launcher #37516 + commit pinning opt-in）— **TUI status bar 10 连** + **Model picker 多修**（OpenAI curated 不被 OR 幻影 + gemini-3.5-flash + gemini-3-flash-preview + Codex OAuth 路径）— **CLI 小修**（queued notes 安全前置 multimodal + Honcho fail open + Simplex idle ws 不误重连）
- [[2026-06-01-update]] — **58 commits 跨日同步**（`eb3cf9750 → b9646276f`，仍 v0.15.1；36 fix / 16 feat / 4 chore / 1 revert / 1 merge）：**三大特性集中合入** — **Hermes Desktop App**（#20059 PR 合并，442 文件 +114,155：Electron 主进程 `apps/desktop/electron/main.cjs` + `bootstrap-runner.cjs` Python 子进程 + `backend-probes.cjs` /api/status 健康探针 + `hardening.cjs` CSP；Vite React renderer `apps/desktop/src/`；Tauri Windows installer `apps/bootstrap-installer/` bundle Python+Git 免管理员；CLI `hermes_cli/main.py:14716 desktop` 子命令 + 别名 `gui`；`HERMES_DESKTOP_REMOTE_URL`/`_TOKEN` 远程后端模式跨 WSL2/Windows GUI 边界）、**Dashboard 全管理面板**（#36704，9 文件 +3,189：4 新页面 `McpPage`/`PairingPage`/`WebhooksPage`/`SystemPage`，`web_server.py` +786 新增 17 个 `/api/{mcp,pairing,webhooks,credentials,memory,gateway,ops}` 端点；继承既有 dashboard_auth OAuth 闸门）、**`/undo [N]` 三层落地**（#21910 SaguaroDev + #36699 Teknium：state primitives `hermes_state.py:288 messages.active` + `:2426 rewind_to_message` + `:2513 restore_rewound` 软删 + audit；memory hook `on_session_switch(*, rewound=False)` `agent/memory_provider.py:175`；CLI `cli.py:7106 undo_last(n, prefill)`；TUI command.dispatch prefill；Gateway `gateway/run.py:8022` + `SessionStore.rewind_session` + 16 locale 同步 `undo.removed{turns}`）+ **Curator inactivity 修剪 + 全 skill 用量遥测**（`agent/curator.py:53` 软 archive + days-since-last-use 向前计算；`tools/skill_usage.py:326` 新 `usage_report()`/`provenance()` 解耦观测与生命周期；hub 安装永不被修剪，#36701）+ **Blank-slate skills**（`install --no-skills` + `hermes profile create --no-skills` + 运行时 `hermes skills opt-out`/`opt-in` + marker `.no-bundled-skills`，`tools/skills_sync.py:43,464,753`，#36228）+ **Setup 行内解释**（"Quick Setup (Nous Portal) — free OAuth login, no API keys" vs "Full setup — BYO keys"，#36227）+ **Free tool pool entitlement** 解耦付费/免费/BYO 三档（`hermes_cli/nous_account.py:64,111,152` + `nous_subscription.py:889,903` per-tool checklist setup，#36153）+ **MiniMax-M3 1M context**（`DEFAULT_CONTEXT_LENGTHS['minimax-m3']=1_000_000`，#36214）+ **Model picker group-layer description**（`47d2d0589`/`84d82453a`/`c9a28dfb0`）+ **安全：Agent 写 `~/.hermes/config.yaml` 双闸门**（工具层 `tools/file_tools.py:256-287` + 终端层 `tools/approval.py:139-170,413` `tee|>|>>|cp|mv|sed -i/--in-place` 全闸 + 9 regression test）+ **Skills guard 良性内容 + `.skillignore`/`.clawhubignore` 蜜罐**（`SKILL.md` 永不可忽略，`tools/skills_guard.py +137`）+ **Gateway MEDIA 标签 4 连**（JSON/code-block/blockquote/inline-code 排除 + mask-as-locator 模式 `6c73e8ffa` "masking is a locator, not a text rewrite"）+ **Gateway 服务重启通知清理**（`b14e15c48` 9 文件 +378）+ **Streaming broken-stream 不再误报输出截断**（`agent/conversation_loop.py +64`，#36705）+ **Desktop/Dashboard 后续 9 修复**（self-update tracks main 而非 GUI 分支 / macOS desktop build 'desktop' stage / lazy session.create 缺 desktop_contract / PATCH /api/sessions/{id} rename 缺端点 / 本地构 macOS 自更后可重启 / drop files anywhere in chat 区 / docker `/update` endpoint 守护 / OAuth `/api/*` not next= roundtrip / 浅色模式代码注释加深）+ **Docker s6 / 启动权限 10 连**（HERMES_UID/GID 校验防 stage2-hook 提权 + s6 /init image + tini compat shim + Playwright headless_shell 发现 + non-root 不 drop privs）+ **Windows GA 去 early-beta 文案**（#36093）+ **WSL 终端维度钳制 revert**（误伤合法宽屏 tmux，#36096）+ **`os.fchmod` Windows 防护**（HEAD `b9646276f`，atomic_json_write）
- [[2026-05-31-update]] — **141 commits 跨日同步**（`689ef5e2 → eb3cf9750`，v0.15.1 维护窗口，无新发布；97 fix / 11 feat / 4 perf / 1 security / 1 revert）：**Setup 重构 Quick Setup→Nous Portal + Full Setup happy-defaults + curses picker 迁移**（`hermes_cli/setup.py:3010,3064`，#35723/#35776）+ **`/compress here [N]`** 用户选择压缩边界（**新模块** `hermes_cli/partial_compress.py` 235 行 + seam-alternation guard，#35048）+ **`hermes prompt-size`** 诊断命令（`hermes_cli/prompt_size.py:141`，#35276）+ **Kanban 文件附件**（`task_attachments` 表 + 25MB 上传 + 13 测试，#35395）+ **Kanban `goal_mode` 卡片**（包 worker 进 `/goal` 循环 + `goal_max_turns` budget，#35710）+ **read_file 紧凑 gutter ~14% token 节省**（删 `HERMES_READ_GUTTER` 逃生口，#35368/#35532）+ **write_file/patch 原子化**（temp + rename，`_atomic_write` line 772，#35252）+ **UTF-8 BOM 处理**（`_UTF8_BOM` line 127-143，#35278）+ **SQLite FTS5 优雅降级**（`hermes_state.py:452 _sqlite_supports_fts5` + uv-managed Python 确保 FTS5）+ **CVE-2026-48710 Starlette BadHost pin** `>=1.0.1`（#35118）+ **进程标题 'hermes'**（setproctitle + prctl/pthread_setname_np fallback）+ **Model picker 多 endpoint 合并 + catalog 1h TTL + deepseek-v4-flash**（#35227/#35756/#35659）+ **Streaming cumulative-resend 修复 → 6h 后 revert**（#35718 → #35860）+ **Anthropic thinking-signature 在 orphan-strip 后降级**（Opus 4.8 extended-thinking 签名）+ **Telegram DM topic** 合成通知保留 + `_get_dm_topic_info` 改类级别解析（`4259bab7d` + HEAD `eb3cf9750`）+ **WhatsApp/WeChat 文本去抖批处理** + **Compressor 四连**（stale handoff prefix 剥离 + 未答问题算 Active Task + 删冲突 resume + preflight 钳制，#35344）+ **MCP 三连**（stdio 子孙经 `killpg(pgid)` 回收 `tools/mcp_tool.py:2270` + 非阻塞启动 `hermes_cli/mcp_startup.py` + asyncio.sleep）+ **Browser CDP DOM-node 序列化降级 `returnByValue=false`**（#35385）+ **Vision 4 MB embed cap 提前** + **/voice 经 SSH 探 PulseAudio socket** + **Nous Tool Gateway 始终展示 + 选中即 OAuth**（#35792）+ **TUI `/agents` delegation 提示** + WSL 终端维度钳制 + **Turn-Completion Explainer**（`agent/conversation_loop.py:4493-4540`）+ **安全 wave**（mutation footer 路径中和 #35584 + media-delivery 多层 denylist + Discord mention 不再脱敏 + `_HERMES_GATEWAY` 防自指令循环 #30719 + dashboard chat WS `--insecure` 非环回放行）+ **File-tools 相对路径锚定 + 终端 cwd 持久化 `_resolve_command_cwd`** + **spawn_via_env 防双 compound-rewrite 包裹** + **LSP Windows .cmd shim** + **State 模型切换持久化到 DB** + **8 条 update 簇**（uv tool upgrade / pipx / Windows launcher-shim / pyproject-init drift）+ **`/stop` 跨参与者** + **pending_watchers 分批 100 + LRU 封顶** + **httpx pool timeout 重试** + **嵌套 `gateway.platforms` 块** + **send_message 识 email** + **`/status` token 标签精化**
- [[2026-05-29-update]] — **275 commits 跨日同步**（`963d22c → 689ef5e2`）：**v0.15.0 + v0.15.1 双发布**、**claude-opus-4-8** 模型 + **gemini-3.5-flash / step-3.7-flash** 进 OpenRouter+Nous 列表、**工具渐进式披露扩展到 MCP + 插件**（`tools/tool_search.py`）、**MCP mTLS 客户端证书**（#33721 `tools/mcp_tool.py:573-625`）、**web_crawl 工具与 provider crawl 管线全面移除**（#33824，23 文件）、**`hermes sessions optimize` + FTS5 段合并**（`hermes_state.py:3267 optimize_fts` / `:3306 vacuum`）、**Kanban 可靠性 wave**（`POST /runs/{run_id}/terminate` + per-profile 并发上限 + SQLite 抗撕裂写 + close-FD-after-connect + corrupt-DB 内容寻址备份）、**`sync_turn(..., messages=...)` 暴露 completed-turn 消息给 MemoryProvider**、**pluggable `ContextEngine` ABC**（`agent/context_engine.py:32`）、**Skills 目录扩张**（skills.sh 858→19,932、ClawHub 200→20k+、NVIDIA tap，新增 `antigravity-cli`/`grok` 可选 skill）、**Krea 2 进入 FAL 目录**（与既有 `plugins/image_gen/krea/` 直连后端并存）、**FAL 视频经 Nous 网关**、**Docker s6 持久化 wave**（#20561）、**安全 wave**（Nous 仅 JWT、AWS 子进程凭据剥离、code-exec 审批旁路回归簇、`API_SERVER_KEY`、docker stop/kill DANGEROUS_PATTERNS）
- [[2026-05-27-update]] — **645 commits 跨日同步**（`556bf7c5c → 963d22c`，hermes 默认远端分支改名 `master → main`）：**Dashboard OAuth 鉴权闸门 Phase 0-7 整体落地**（`hermes_cli/dashboard_auth/` 10 文件 1868 行 + Nous OAuth Provider 582 行 + WS 单次性 ticket + `register_dashboard_auth_provider` 第 8 个 PluginContext hook）+ **Honcho AI-native 跨会话用户建模 MemoryProvider**（5 文件 5158 行 + identity-mapping single/multi/hybrid wizard + `pinUserPeer ↔ pinPeerName` 别名链 + 15 个 honcho fix）+ **Krea 图像生成 Provider 插件**（`plugins/image_gen/krea/` 548 行，Krea 2 Medium/Large）+ **security-guidance 插件 25 条 dangerous-pattern 警告**（Apache-2.0 Anthropic fork，#33131）+ **TUI Session Orchestrator**（in-TUI 多 session 同屏 635 行 + tui_gateway +221）+ **API Server 三连**（session CRUD + GET /v1/skills + /v1/toolsets）+ **Windows 原生支持收官**（UTF-8 stdio shim / psutil PID / Scheduled Task / install.ps1 / 79+63 skill platforms frontmatter / Playwright autoinstall）+ **Docker wave**（Node 22 LTS 多 stage + chown 链 + s6 env 转译）+ **Codex 14 修复**（drop `responses.stream()` helper + null/large/encrypted_content recovery）+ **xAI 模型退役迁移工具**（`hermes migrate xai [--apply]` + ruamel round-trip + doctor 提示）+ **xAI Web Search provider 插件**（第 8 个 web provider）+ **Telegram 19 修复**（in-place status edit + DM topic thread 修复 + heartbeat 原地编辑 + 静默 chatter）+ **性能 wave**（agent-loop -47% + terminal poll -195ms + cold start -19s + load_config_readonly）+ **Bitwarden EU + 自托管 server URL**（#31378）+ **BrowseShSource 第 8 个 skill catalog**（Browserbase 200+ 站点）+ **`hermes update --branch` + post-pull syntax-validate auto-rollback** + **Nix #messaging / #full 包变体**
- [[2026-05-26-update]] — **37 commits 跨日同步**（`b62af47 → 556bf7c5c`：**Promptware 防御**（共享威胁模式库 `tools/threat_patterns.py` 252 行 + Memory load-time scan + 高风险工具结果 `<untrusted_tool_result>` 分隔符，#32269）+ **Nous-approved MCP 目录 + 交互式选择器**（`hermes_cli/mcp_catalog.py` 776 行 + `mcp_picker.py` 322 行 + `optional-mcps/{n8n,linear}`，#30870）+ **Skills Hub 健康检查 + 新鲜度徽章 + 4h watchdog cron**（#32345）+ 3 个新可选 skill（`web-pentest` / `openhands` / `code-wiki`）+ Patch 三连（缩进/CRLF/失败升级，#507/#32273）+ Cron 扫描器二级分裂（strict vs loose，#32339）+ Skill install 拒符号链接 + Dashboard 插件资源 suffix-allowlist + env denylist（#32277）+ Markdown link scheme + WeCom defusedxml + AGENTS.md scope + Telegram DM topic 6 连 + Anthropic API-key skip OAuth + Gateway /model coerce + qwen3.7-max + TTS [pause] dedup）
- [[2026-05-25-update]] — **175 commits 跨日同步**（`186bf25 → b62af47`：Docker `s6-overlay` PID 1 **BREAKING** + 容器化运行时监管 + 安全 wave 3（symlink/`.env`/`/proc/*`/YOLO frozen/GHSA 二次合规/7 处凭据持久化）+ `hermes security audit` OSV.dev + CLI 冷启 -63% Bitwarden 磁盘 cache + TTS/STT plugin hook + `openai-api` 新 provider + CredentialPool 周配额轮换 + 子 Agent busy-mode 抗中断 + Mattermost 迁移 plugin + MCP OAuth 无头 paste-back + Codex TTFB watchdog + partial-stream `finish_reason=length` 续传 + Aux 主模型 fallback 统一 + Nous 401 指导 + `/resume` 编号 + `/q` 改 `/queue`）
- [[2026-05-24-update]] — **84 commits daily delta**（安全 wave 2：17 webhook/dashboard/平台审批授权 + `register_auxiliary_task()` plugin API + 跨 Profile 软护栏 + Streaming 三连可见性 + ntfy 第 23 平台 + Kanban `promote` `--ids` + Skills `audit --deep` AST + Bitwarden EU）
- [[2026-05-23-update]] — 49 commits daily delta（Nous Portal one-shot、Kanban DB 抗污染、Memory drift guard、审批沉默契约、TLS FD 三层防御、Plugin RCE 第二段、Telegram/WhatsApp/QQBot）
- [[2026-05-22-update]] — 2,578 commits（v0.12.0 → v0.14.0）
- [[2026-05-20-update]] — v0.14.0（~2,480 commits）
- [[2026-05-19-update]] — 2,418 commits（v2026.4.23 → v2026.5.16）
- [[2026-05-18-update]] — v0.14.0（285 commits）
- [[2026-05-17-update]] — v2026.5.16（988 commits）
- [[2026-05-16-update]] — v2026.5.16（2,890 commits since v2026.4.23）
- [[2026-05-15-update]] — v0.13.0（579 commits）
- [[2026-05-14-update]] — 1,533 commits（v0.12.0 + v0.13.0 + post-release）
- [[2026-05-13-update]] — v0.12.0 → v0.13.0（595 commits）
- [[2026-05-12-update]] — v0.12.0 + v0.13.0（~2,015 commits）
- [[2026-05-11-update]] — v0.12.0 + v0.13.0 + post-release
- [[2026-05-10-update]] — v0.12.0 + v0.13.0（1,960 commits）
- [[2026-05-09-update]] — v0.13.0 (v2026.5.7) + post-release（1,100 commits）
- [[2026-05-07-update]] — v0.12.0 + v0.13.0（1,960 commits）
- [[2026-05-07-v0.13.0]] — v0.13.0 release notes
- [[2026-05-06-update]] — v0.12.0（211 commits）
- [[2026-05-05-update]] — v0.12.0（99 commits）
- [[2026-05-04-update]] — v0.12.0 (v2026.4.30) + 71 post-release commits
- [[2026-05-02-update]] — v2026.4.30（1,338 commits）— The Curator Release
- [[2026-04-30-update]] — v2026.4.30
- [[2026-04-30-v0.12.0]] — v0.12.0 release notes
- [[2026-04-29-update]] — 182 commits (v2026.4.23)
- [[2026-04-18-update]] — 410 commits post-v0.10.0
- [[2026-04-17-update]] — 641 commits (v0.10.0)
- [[2026-04-10-update]] — 293 commits
- [[2026-04-09-update]] — 59 commits
