---
title: 2026-06-16 单日同步 — hermes-agent cffd6e3c8 → d1ecebc（65 commits, ~28h 单日窗口；Desktop composer model picker 替代 statusbar + 新开 compact 子窗口 + watch window subagent streaming + 远端 gateway artifact 鉴权下载 + 全局 remote profile REST 路由 + memory_notifications 三态 off/on/verbose + verbose skill change 预览 + session:compress hook event + prompt context_file_max_chars 可配 + 截断警告 contextvar 隔离 + tool_progress_grouping 重命名+style→grouping + Codex pool-only rate-limit 不弹 relogin + xAI 默认 grok-build-0.1 + 模型 picker live+curated 合并+curated-first + validate_requested_model 兜底 curated + base_url 透传 + 自定义 provider dedup 保留 + 无 api_key 仍探针 /v1/models + 文件工具支持远端 file.attach + Honcho 网关身份树 gateway-gated 替换部署形状 + skills 跳 references/templates/assets/scripts 支持目录 + shop-app 改 CLI shop skill v1.0.1 + OpenViking skill 输入清洗 → 全 provider 共用 strip_skill_scaffolding + Telegram rich 默认开+rich-reply 跨重启恢复 via send-time index + Slack /debug via /hermes + Mattermost notify-only flat-fallback + WhatsApp/email send_image 接 metadata + Photon Spectrum project id 收敛 + auxiliary auto 任务用主 fallback chain + Electron pack 失败镜像 redownload + Codex pool 速率限制不弹 relogin + AUTHOR_MAP 5 条 salvage + GLM-5.2 入 OpenRouter+Nous 列表）
created: 2026-06-16
updated: 2026-06-16
type: changelog
tags: [changelog, update, v0.16.0-maintenance, desktop-composer-model-picker, desktop-compact-session-window, desktop-scratch-window-chrome, desktop-session-sync-broadcastchannel, desktop-secondary-window-chrome, desktop-watch-window-subagent-streaming, desktop-remote-gateway-artifact-download, desktop-global-remote-profile-route, desktop-per-model-effort-fast-presets, desktop-composer-sticky-session-state, desktop-collapse-date-pinned-snapshots, desktop-external-provider-disconnect-terminal, desktop-browser-slash-local-only, desktop-thread-top-padding, desktop-explorer-refresh-group-hover, desktop-electron-pack-mirror-redownload, memory-notifications-three-mode, display-skill-notifications-verbose, prompt-context-file-max-chars-configurable, prompt-truncation-warnings-contextvar, hooks-session-compress-event, hooks-event-callback-bridge, gateway-tool-progress-grouping-rename, gateway-tool-progress-style-introduced, gateway-can-edit-by-grouping, gateway-send-image-metadata-kwarg, auth-codex-pool-only-rate-limit, auth-codex-rate-limited-code, codex-pool-not-missing-credentials, xai-default-grok-build-01, models-live-curated-merge, models-validate-fallback-curated, models-curated-first-ordering, models-base-url-passthrough, models-fetch-models-base-url-resolution, models-no-apikey-probe-v1-models, inventory-keep-user-defined-providers, openrouter-glm-5-2, nous-glm-5-2, auxiliary-main-fallback-chain-for-auto, auxiliary-resolve-fallback-entry, skills-shop-app-to-cli-shop, skills-shop-v1-0-1, skills-ignore-support-docs, skills-skill-support-paths-frozenset, skills-recursive-delete-tree-escape-guard, memory-strip-skill-scaffolding-shared, memory-extract-user-instruction-helper, openviking-sanitize-skill-memory-input, telegram-rich-default-enabled, telegram-rich-eligible-helper, telegram-rich-reply-recovery-index, gateway-rich-sent-store, slack-debug-via-hermes, mattermost-notify-only-flat-fallback, mattermost-thread-local-delivery-hygiene, mattermost-thread-root-resolver, photon-project-id-unify, photon-spectrum-project-id, honcho-identity-tree-gateway-gated, honcho-pin-user-peer-canonical, honcho-migrate-pin-peer-name, honcho-pin-non-agent-only, honcho-runtime-peer-prefix, tui-isolate-session-create-race-test, dashboard-register-idempotent-followup, salvage-46895-remote-gateway-artifact, salvage-47011-global-remote-profile-route, salvage-47245-browser-slash-local, salvage-47060-subagent-watch-window, salvage-47266-electron-mirror-redownload, salvage-44431-honcho-identity-tree, salvage-46244-desktop-explorer-refresh, salvage-46951-new-session-window, salvage-46959-composer-model-selector, salvage-46857-live-curated-merge, salvage-46890-telegram-bot-api-10-1, salvage-47235-auxiliary-fallback-chain, salvage-47248-slack-debug-route, salvage-47230-tui-session-race-isolation, salvage-46929-skills-recursive-delete-guard, salvage-46968-delegate-task-background-forward, salvage-47391-glm-5-2, author-map-5-salvage]
sources:
  - hermes-agent/main cffd6e3c8..d1ecebc
total_commits: "65 个 commit（59 个 non-merge + 6 个 merge；~28h 单日窗口；committer dates 2026-06-15T20:35 UTC → 2026-06-16T20:40 UTC）；注：cffd6e3c8..HEAD 总 git revlist 141 commits 中约 76 个为前期 sync 已覆盖的旧 PR salvage（committer date ≤ 2026-06-15 20:35 UTC、author date 横跨 2026-04-14~2026-06-13），其中含 photon spectrum-ts/iMessage gRPC、yuanbao media pipeline middleware、agent char-based output-cap overflow、honcho identity-tree 各 sub-PR 等；本期只重点记录真正新增的 65 commit 内容"
---

# 2026-06-16 单日同步：hermes-agent main `cffd6e3c8 → d1ecebc`

> **基线**：wiki `286ce13` 对齐 hermes `cffd6e3c8d0bf6a180ed19a919fab05f44def311`（2026-06-15T20:35 UTC，"Merge pull request #46078 from xxxigm/fix/discord-slash-command-100-cap"，v0.16.0 维护未发新版）
> **目标**：hermes-agent/main HEAD `d1ecebcbfd8c7f2b942fd9cc425cea028e34111c`（2026-06-16T20:40 UTC，"fix(desktop): re-download Electron binary via mirror when pack fails (#47266) (#47276)"）
> **跨度**：约 28 小时（committer dates `2026-06-15T20:35 UTC → 2026-06-16T20:40 UTC`，单日窗口）
> **版本**：仍 **v0.16.0**（`pyproject.toml:10 version = "0.16.0"` + `hermes_cli/__init__.py:17-18 __version__ = "0.16.0"` + `__release_date__ = "2026.6.5"` 不变；本期为 fix / feat / test / chore / docs / refactor 混合，未发新版）
> **commit 类型分布**（截止日 2026-06-15T20:35 UTC 之后窗口口径）：**25 fix / 18 feat / 6 chore / 5 test / 4 docs / 1 refactor / 6 merge**（合计 65；前期 sync 已覆盖的旧 PR salvage 不重复计入）
> **diff 体量**（整个 `cffd6e3c8..d1ecebc` 整段）：**201 文件改动，+9874 / −2064** 行
> **验证基线**：`/tmp/hermes-agent` clone @ `d1ecebc` 全 checkout，所有 commit hash / file:line / 路径 / 函数名 / 行数经 `grep -n` / `git show <sha>` / `Read` 实证；4 个并行 research agent 分主题分担实证（A: Desktop UI 17 commit，B: Memory / Display / Gateway / Hooks / Prompt / Auth 15 commit，C: Models / xAI / Skills / Honcho 18 commit，D: Messaging Telegram / Slack / Mattermost / Photon / WhatsApp / AUTHOR_MAP 13 commit）

---

## 章节索引

1. [⭐ Desktop — Composer model picker 替代 statusbar + 新 compact session 窗口家族 + watch window 子代理流（PR #46951+#46959+#47060 等 12 commit）](#1-desktop-composer-window-family)
2. [Desktop — 远端 gateway artifact 鉴权下载 + 全局 remote profile REST 路由 + Electron pack 镜像 redownload（PR #46895+#47011+#47266 4 commit）](#2-desktop-remote-and-pack)
3. [Desktop — Right-sidebar explorer refresh 始终可点 + 外部 provider 终端断开 + /browser 限本地 gateway（PR #46244+#47245 3 commit）](#3-desktop-misc)
4. [⭐ Memory / Display — `display.memory_notifications` 三态 off/on/verbose + `display.skill_notifications` verbose 预览 + skill scaffolding 全 provider strip（3 feat + 2 fix）](#4-memory-display-notifications)
5. [⭐ Hooks — `session:compress` 事件 + `event_callback` 网关桥（feat #38199 后续 1 feat + 1 test）](#5-hooks-session-compress)
6. [Prompt — `context_file_max_chars` 可配 + 截断警告 contextvar 隔离（2 commit）](#6-prompt-context-file-truncation)
7. [Gateway — `tool_progress_style` 引入 → `tool_progress_grouping` 改名 + `can_edit` 按 grouping 分（2 commit）](#7-gateway-tool-progress)
8. [Auth — Codex pool-only rate-limit 状态保留（不再误报 "missing credentials"）（PR + AUTHOR_MAP 2 commit）](#8-auth-codex-pool-ratelimit)
9. [Models — xAI 默认 grok-build-0.1（2 commit） + live+curated catalog 合并 / curated-first / `validate_requested_model` 兜底 / base_url 透传 / 无 api_key 仍探针（PR #46857 等 8 commit）](#9-models)
10. [Models — 自定义 provider dedup 保留 + GLM-5.2 入 OpenRouter+Nous（PR #47391 等 2 commit）](#10-models-extra)
11. [Auxiliary — auto 任务沿主 fallback chain（PR #47235 1 commit）](#11-auxiliary-fallback)
12. [Skills — shop-app 改 CLI 化 shop skill v1.0.1 + skills 跳 references/templates/assets/scripts 支持目录（4 commit）](#12-skills)
13. [Messaging Telegram — rich 默认开 + 流式 final 仍 rich + send-time 索引恢复 rich-reply（PR #46890 等 3 commit + 1 test）](#13-telegram)
14. [Messaging — Slack `/debug` via `/hermes` + Mattermost notify-only flat-fallback + WhatsApp/email `send_image(metadata=)`（PR #47248 等 3 commit）](#14-messaging-misc)
15. [Photon — project id 收敛为 `spectrumProjectId`（PR 1 commit + docs）](#15-photon-project-id)
16. [⭐ Honcho — Gateway-gated 身份树替换部署形状提问（PR #44431 多 commit + 2 docs 跟进）](#16-honcho-identity-tree)
17. [TUI 测试隔离 — session-create no-race 测试不再被 shard-sibling 干扰（PR #47230 1 commit）](#17-tui-test-isolation)
18. [AUTHOR_MAP — 5 条 salvage（x1erra / chimpera / cyb0rgk1tty / Joe Rinaldi Johnson / zimigit2020 / MrDiamondBallz）](#18-author-map)

---

## 1. desktop composer window family

> **背景**：本期 Desktop 围绕"composer 化"重写模型选择与新会话窗口两个流程，配合 watch window 子代理实时流。

### 1.1 Compact session window 家族（PR #46951，3 commit + 1 merge）

`98c2941 feat(desktop): open new sessions in compact windows` —— 在主进程引入 secondary window，IPC + 可重绑快捷键打开"草稿"会话窗口。
- `apps/desktop/electron/main.cjs:5087` `spawnSecondaryWindow({ sessionId, watch, newSession })`
- `:5148` `createSessionWindow` 包 `spawnSecondaryWindow`，`:5155` 新 `createNewSessionWindow` 调 `spawnSecondaryWindow({ newSession: true })`
- `apps/desktop/electron/preload.cjs:9` `openNewSessionWindow: () => ipcRenderer.invoke('hermes:window:openNewSession')`
- `apps/desktop/src/store/windows.ts:9` `NEW_SESSION_WINDOW_FLAG='1'`，`:33` `isNewSessionWindow()`，`:110-114` `openNewSessionInNewWindow()`
- `apps/desktop/src/lib/keybinds/actions.ts:69` `{ id: 'session.newWindow', defaults: ['mod+shift+n'] }`
- `apps/desktop/src/app/hooks/use-keybinds.ts:136` 调度
- i18n: `en.ts:192`、`zh.ts:188`

`0f75e99 feat(desktop): trim scratch window chrome` —— 草稿窗口隐藏 titlebar 工具组 / statusbar / intro。
- `apps/desktop/src/app/shell/app-shell.tsx` 初引 `hideTitlebarControls = isNewSessionWindow()`（后在 44e5848 扩为 `isSecondaryWindow()`）`:86`
- statusbar `:194` `!isSecondaryWindow()` 门
- `apps/desktop/src/app/chat/index.tsx:127`（`ChatHeader` 早返）+ `:311`（secondary 跳过 `showIntro`）
- `apps/desktop/src/components/assistant-ui/thread-list.tsx:140` 加 `secondaryWindow`/`secondaryTitlebarGap` 后折叠到 `secondaryTitlebarGap`，`:257` 内联 `--sticky-human-top`

`67233d1 fix(desktop): sync new sessions across windows` —— BroadcastChannel 跨渲染进程通知 sidebar 刷新。
- **新文件** `apps/desktop/src/store/session-sync.ts`（25 行）：`CHANNEL='hermes:sessions'` + `broadcastSessionsChanged()` + `onSessionsChanged(handler)`
- `apps/desktop/src/app/desktop-controller.tsx:468-475` 订阅（跳过 secondary 窗口）
- `apps/desktop/src/app/session/hooks/use-message-stream.ts:644` title commit 后广播
- `apps/desktop/src/app/session/hooks/use-session-actions.ts:476` 乐观创建后广播

### 1.2 Composer model picker（PR #46959，4 commit + 1 merge）

`c92a95a feat(desktop): move model selector from statusbar to composer` —— 模型选择器从 statusbar 移到 composer，引入 `ModelPill`。
- **新文件** `apps/desktop/src/app/chat/composer/model-pill.tsx`（72 行）：`ModelPill` 订阅 `$currentModel/$currentProvider/$currentFastMode/$currentReasoningEffort`；用 `formatModelStatusLabel`；`modelMenuContent` 缺失时回退 `setModelPickerOpen(true)`
- `apps/desktop/src/app/chat/composer/controls.tsx:85` 渲染 `<ModelPill disabled model={state.model} />`
- `apps/desktop/src/app/chat/composer/types.ts:27` 加 `modelMenuContent?: ReactNode` 进 `ChatBarState.model`
- `apps/desktop/src/app/shell/hooks/use-statusbar-items.tsx` 删旧 `model-summary` statusbar 项（约 50 行）

`989d5d0 fix(desktop): declutter date-pinned model snapshots in the picker` —— picker 折叠日期 pinned 快照行。
- `apps/desktop/src/lib/model-status-label.ts:71` `base = base.replace(/-\d{8}$/, '')` 进 `modelDisplayParts`
- `apps/desktop/src/store/model-visibility.ts:54-57` `collapseModelFamilies` 中：若 `/-\d{8}$/.test(model) && present.has(model.replace(...))` → 跳过 snapshot
- 测试 `model-status-label.test.ts:13`、`model-visibility.test.ts:82`：`claude-opus-4-5-20251101` → `claude-opus-4-5`

`0e81d2f feat(desktop): per-model effort/fast presets in the picker` —— 模型级别 effort/fast 偏好不写回 gateway 持久化 `agent.reasoning_effort`。
- **新文件** `apps/desktop/src/store/model-presets.ts`（86 行）：`STORAGE_KEY='hermes.desktop.model-presets'`、`modelPresetKey(provider,model)='${provider}::${model}'`、`$modelPresets` atom、`getModelPreset`、`setModelPreset`、`applyModelPreset({effort,fast}, ctx)` —— 无 `ctx.sessionId` 时 no-op，避免写穿网关 effort 全局
- `apps/desktop/src/app/shell/model-edit-submenu.tsx` 重写：新 props `effort`/`model`/`provider`；`patchReasoning` 先 `setModelPreset(provider, model, { effort: next })`，仅 active session 时改 session；`toggleFast` 在 base model 记 `{ fast }`，仅 active 时换 model
- `apps/desktop/src/app/shell/model-menu-panel.tsx:23` 导入 `applyModelPreset`，`:125` `selectFamily` 后调

`cb6b412 refactor(desktop): make composer model picker sticky session state` —— composer picker 改 sticky session state（localStorage）。
- `apps/desktop/src/store/session.ts:18-23` 四个新 localStorage key：`hermes.desktop.composer.{model,provider,reasoning-effort,fast}`
- `:221-225` atom 从 `storedString/storedBoolean` 初始化
- `:268-285` `setCurrentModel/Provider/ReasoningEffort/FastMode` 写入持久化
- `apps/desktop/src/app/session/hooks/use-model-controls.ts:44-67` `refreshCurrentModel(force=false)` 仅空时 / `force` 时 seed，不再覆盖 session；`:84-110` `selectModel` 删 `persistGlobal`，无 active session 早返（纯 UI 经 `session.create` 透传）
- `apps/desktop/src/components/model-picker.tsx:80` 删 `persistGlobal` checkbox；签名简化为 `{provider, model}`
- 后端：`tui_gateway/server.py` `session.create` 接 override

`80e4b89 feat(desktop): tighten composer model picker interactions` —— picker 用 React Context 关闭。
- `apps/desktop/src/app/shell/model-menu-panel.tsx:45` `export const ModelMenuCloseContext = createContext<() => void>(() => {})`
- `:63` `closeMenu = useContext(...)`，`:218-225` `activate` 选中后 `closeMenu()`
- `apps/desktop/src/app/chat/composer/model-pill.tsx:38` `useState(false)` 加 `open`，包 `<ModelMenuCloseContext.Provider value={() => setOpen(false)}>`
- `!currentModel.trim()` 时显示 `<GlyphSpinner spinner="braille" />`
- `apps/desktop/src/app/chat/composer/controls.tsx:88-103` steer 占 mic 槽（`canSteer ? steer : DictationButton`）

`7d938cc fix(desktop): keep live model switch metadata truthful` —— live 切换后系统 prompt 重建 + 注入显式 `[System: ... changed to ...]` 标记。
- `agent/conversation_loop.py:303` `if stored_prompt and _stored_prompt_matches_runtime(agent, stored_prompt)`
- `:378` `_stored_prompt_matches_runtime(agent, prompt)` 比对 `Model:`/`Provider:` 行 vs runtime
- `tui_gateway/server.py:1684` `_persist_live_session_system_prompt(session)` 经 `db.update_system_prompt` 重建写
- `:1705` `_append_model_switch_marker(session, model, provider)` 史历追加 `[System: The active model for this chat has changed to {model} via provider {provider}…]`
- `:2166-2168` 接 `_apply_model_switch` 在 `_persist_live_session_runtime` 后

`dd0e3e0 fix(desktop): tighten thread content top padding` —— `thread-list.tsx:140` 主窗 pad 由 `pt-[calc(var(--titlebar-height)+1.5rem)]` 改 `pt-[calc(var(--titlebar-height)-0.5rem)]`。

### 1.3 Watch window subagent 流（PR #47060，1 commit + chrome 扩展）

`44e5848 feat(desktop): stream subagent activity into watch windows (#47060)` —— 父代理把子代理 token 流转发到 watch window；窗口 chrome 与 secondary 共用。
- `tools/delegate_tool.py:870-876` 进度中继转发 `subagent.text`（预览）
- `:1638-1647` `_relay_child_text(delta)` + `child.run_conversation(..., stream_callback=_relay_child_text)`
- `tui_gateway/server.py:907-913` `_start_agent_build` 在 `session.get("lazy") and _child_run_active(...)` 时跳过（lazy watch 保 lazy）
- `:2878-2884` `_on_tool_progress` 对 `subagent.text` 跳过父 emit
- `:2945-2951` `_mirror_subagent_to_child` 把 `subagent.text` → child sid 的 `message.delta`（`subagent.start` 退化为单次 header）
- `:4248-4258` lazy `session.resume` 在 `_child_run_active` 时早返空 history（WSL race 修）
- 窗口 chrome：`apps/desktop/src/app/chat/index.tsx:124-127` `ChatHeader` 在 `isSecondaryWindow()` 早返（从 `isNewSessionWindow` 扩展）
- `apps/desktop/src/app/shell/app-shell.tsx:83-86` `hideTitlebarControls = isSecondaryWindow()`
- `apps/desktop/src/components/assistant-ui/thread-list.tsx:140` `secondaryWindow = isSecondaryWindow()`，`:147` `secondaryTitlebarGap = 'calc(var(--titlebar-height) + 0.75rem)'`（空格 load-bearing），`:265-270` 不透明拖条 `<div … className="absolute inset-x-0 top-0 z-10 h-(--titlebar-height) bg-background [-webkit-app-region:drag]" />`

---

## 2. desktop remote and pack

### 2.1 远端 gateway artifact 鉴权下载（PR #46895，1 commit）

`c6b0eb4 fix(desktop): open remote-gateway artifacts via authenticated download (#46895)` —— Desktop 打开远端 gateway 上的 artifact / 图片不再用裸 `file://`，而走带 token 的 `/api/files/download` GET。
- `apps/desktop/src/lib/media.ts:62` `mediaExternalUrl(path)` 远端感知；`:67` `isRemoteGateway()` 分支构 `${conn.baseUrl}/api/files/download?path=…&token=…`
- `apps/desktop/src/app/artifacts/index.tsx:128-133` `artifactHref` 把 `file://` 与 `/` 路径都过 `mediaExternalUrl`
- `hermes_cli/web_server.py:253` `_QUERY_TOKEN_API_PATHS = frozenset({"/api/files/download"})`
- `hermes_cli/web_server.py:1425` `@app.get("/api/files/download")`：`Content-Disposition: attachment`、auth-gated、**不在** `PUBLIC_API_PATHS` 中
- 测试 `apps/desktop/src/lib/media.remote.test.ts`、`tests/hermes_cli/test_web_server_files.py`（60 行）

### 2.2 全局 remote profile REST 路由（PR #47011，1 commit）

`0441b7f fix(desktop): route global remote profile REST calls (#47011)` —— global-remote 模式下 OAuth provider REST 调用自动带 `?profile=`，避免穿到默认 profile。
- `apps/desktop/electron/connection-config.cjs:175` `pathWithGlobalRemoteProfile(path, profile, opts)` —— global-remote + 无每-profile override 时追加 `?profile=`；`:283` 导出
- `apps/desktop/electron/main.cjs:66` 引入，`:5619` 在 `hermes:api` handler 调用
- 后端：`hermes_cli/web_server.py` OAuth provider 路由按 profile scope，新增 `tests/hermes_cli/test_web_oauth_dispatch.py`（133 行）

### 2.3 Electron pack 失败镜像 redownload（PR #47266，1 commit）

`d1ecebc fix(desktop): re-download Electron binary via mirror when pack fails (#47266) (#47276)` —— `npm run pack` 失败时尝试镜像 redownload Electron，仍失败时跳过 pack。
- `hermes_cli/main.py:5113` `_electron_dist_binary(project_root)`（按 OS：mac `Electron.app/.../Electron`、win `electron.exe`、linux `electron`）
- `:5130` `_electron_dist_ok` 检 binary 在
- `:5144-5194` `_redownload_electron_dist(project_root, env, *, mirror=None)` 清残留 dist + `path.txt`，注 `ELECTRON_MIRROR` 跑 `node install.js`
- `:5459-5463` 首次重试在 dist 缺失时调 `_redownload_electron_dist(PROJECT_ROOT, env)`
- `:5489-5493` 镜像重试用 `https://npmmirror.com/mirrors/electron/`；redownload 仍失败时跳过最终 `npm run pack`
- 并行 `Restore-ElectronDist` / `Test-ElectronDist` helper 进 `scripts/install.ps1` 与 `scripts/install.sh`
- 测试 `tests/hermes_cli/test_gui_command.py`（+155 行）

---

## 3. desktop misc

### 3.1 Right-sidebar explorer refresh 始终可点（PR #46244，1 merge commit）

`9d2ec8d Merge PR #46244 fix(desktop): keep file tree refresh clickable` —— 文件树 header refresh 按钮不再被 sibling 元素挡。
- `apps/desktop/src/app/right-sidebar/index.tsx:134` `HEADER_ACTION_LABEL_REVEAL` 从 `peer-hover/project-label` 改 `group-hover/project-header` + `group-focus-within/project-header`
- `:219` `RightSidebarSectionHeader` div 现在挂 `group/project-header` class
- 测试新增 +75 行 `index.test.tsx`

### 3.2 外部 provider 终端断开（1 commit）

`a0ec4f5 feat(desktop): disconnect external (CLI-managed) providers` —— 外部 CLI 托管 provider 提供 `disconnect_command`，Desktop 引导用户在 terminal pane 执行。
- `apps/desktop/src/app/right-sidebar/store.ts:16` `$terminalInjection = atom<null|string>(null)`，`:21-30` `runInTerminal(command)` 置 takeover + injection
- `apps/desktop/src/app/right-sidebar/terminal/use-terminal-session.ts:680-700` `useEffect` 在 `status === 'open'` 时订阅、写 `${command}\r` 至 `hermesDesktop.terminal.write`、写完清 atom
- `apps/desktop/src/app/settings/providers-settings.tsx:34` `GroupLabel` helper，`:174` 第二个 "Other providers" 段；`:218` `terminalDisconnect = !canDisconnect && Boolean(provider.disconnect_command) && canRunInTerminal()`
- `:325-339` `handleTerminalDisconnect` 调 `runInTerminal(command)`
- `apps/desktop/src/types/hermes.ts` 加 `disconnect_command`
- 后端 `hermes_cli/web_server.py` 返回 per-OS `disconnect_command`

### 3.3 `/browser` 限本地 gateway（PR #47245，1 commit）

`a68ac0c feat(desktop): allow /browser connect on a local gateway (#47245)` —— `/browser` slash 在 Desktop 全面打开，但远端模式下报"本地 gateway only"。
- `apps/desktop/src/lib/desktop-slash-commands.ts:33` 加 `'browser'` 进 `DesktopActionId`
- `:107-112` `/browser` spec：`surface: action('browser')`
- `:152` 从 terminal-only blocklist 移除
- `apps/desktop/src/app/types.ts:49-53` 新 `BrowserManageResponse { connected?, url?, messages?[] }`
- `apps/desktop/src/app/session/hooks/use-prompt-actions.ts:1148-1219` `browser` handler：`$connection.get()?.mode === 'remote'` → "local gateway only"；否则 `connect|disconnect|status` 调 `requestGateway<BrowserManageResponse>('browser.manage', { action, session_id, url? })`
- 同 PR 加 skill 删除 tree-escape 守 `tools/skill_manager_tool.py` `_validate_delete_target`

---

## 4. memory display notifications

> **背景**：本期新增"显示层"通知配置族：把 background memory 写入与 skill 增删改用三态 off/on/verbose 暴露给用户，并把 `_strip_skill_scaffolding` 从 openviking-only 提到全 provider 共用。

### 4.1 `display.memory_notifications` 三态（`20b1f4f` + `98ae286`）

`20b1f4f feat(memory): configurable background memory update notifications` —— 三态 `off/on/verbose`。
- `agent/agent_init.py:303` 初始化 `agent.memory_notifications` 属性
- `agent/background_review.py:626` 调用 `summarize_background_review_actions(notification_mode=...)`；解析 mode 并构建 Memory `➕/✏️/➖` 预览（add/replace 120 char，remove 60 char）；dispatch 在 line 326+ 附近
- 网关解析 `gateway/run.py:14805-14812` 读 `display.memory_notifications` 并写到 agent

`98ae286 feat(display): document and test memory_notifications setting` —— 默认值 + 文档 + 测试。
- `hermes_cli/config.py:1441` `DEFAULT_CONFIG["display"]` 加 `"memory_notifications": "on"`
- 文档 `website/docs/user-guide/features/memory.md:248-267` 新增 `## Background review notifications (display.memory_notifications)` 段
- 测试 `tests/run_agent/test_background_review.py:390,397,404,414,425`：`test_memory_notifications_off_returns_nothing` / `on` / `verbose` / `default_is_on` / `skill_patch_off_silent_verbose_shows_diff`

### 4.2 `display.skill_notifications` verbose 预览（`4cf9d80`）

`4cf9d80 feat(display): verbose skill change notifications with content previews` —— skill_manage 工具产出可被 background_review 用的"变更体"。
- `tools/skill_manager_tool.py:616` `_create_skill` 加 `_change:{description}`
- `:667` `_edit_skill` 同上，message 改 `"updated (full rewrite)"`
- `:764` `_patch_skill` 加 `_change:{old,new}` 截到 200
- 消费者 `agent/background_review.py` 渲染 `📝 Skill '{name}' patched: "old" → "new"` / `created:` / `rewritten:`
- 转发 `_summarize_background_review_actions` 进 `run_agent.py:1411`

### 4.3 Skill scaffolding 全 provider 共用（`c2c55c4` + 前置 `e3adbb5`）

`e3adbb5 fix(openviking): sanitize skill memory input` —— 先在 openviking 插件内引入 `_derive_openviking_user_text()`。
- `plugins/memory/openviking/__init__.py:71` `_derive_openviking_user_text()` 定义
- `:548` `queue_prefetch` 调用、`:588` `sync` 调用

`c2c55c4 fix(memory): strip skill scaffolding for all providers, not just openviking` —— 提取共享 helper，全 memory provider 都先 strip。
- 新规范解析器 `extract_user_instruction_from_skill_message()` 在 `agent/skill_commands.py:58`
- 标记常量 `_SKILL_INVOCATION_PREFIX`、`_SINGLE_SKILL_MARKER`、`_BUNDLE_MARKER`、`_BUNDLE_USER_INSTRUCTION`、`_BUNDLE_FIRST_SKILL_BLOCK`、`_RUNTIME_NOTE` 在 ~44-53 行
- `MemoryManager._strip_skill_scaffolding` 静态方法在 `agent/memory_manager.py:435`，由 `prefetch_all`（458）/ `queue_prefetch_all`（485）/ `sync_all`（544）调
- openviking 在本 commit 之后 delegate：`plugins/memory/openviking/__init__.py:42` `from agent.skill_commands import extract_user_instruction_from_skill_message`、`:81` 调用
- 新测试 `tests/agent/test_memory_skill_scaffolding.py`

---

## 5. hooks session compress

`e76e7b5 feat(hooks): session:compress event_callback for MemPalace sync` —— 新事件 `session:compress` + 网关桥 `_event_callback_sync`。
- `init_agent` 接 `event_callback: Optional[Callable[[str, dict], None]]` 进 `agent/agent_init.py:198`，赋值 `:430`
- 触发位 `agent/conversation_compression.py:611-618`，payload `{platform, session_id, old_session_id, compression_count}`
- 网关桥 `gateway/run.py:14374` `_event_callback_sync` 调 `_hooks_ref.emit(event_type, context)` 经 `asyncio.run_coroutine_threadsafe`
- agent 接线 `gateway/run.py:14747` `agent.event_callback = _event_callback_sync`
- `AIAgent.__init__` 在 `run_agent.py:388` 透传 kwarg

`28f9247 test(hooks): cover session:compress event; drop dead import` —— 删 `from urllib.parse import urlparse, parse_qs, urlunparse` 死引；测试。
- `tests/run_agent/test_compression_boundary_hook.py:165` 新 class `TestSessionCompressEvent`
- `:213` `test_event_emitted_on_compression` 断 `compress_events` 与 `ctx["old_session_id"]`/`compression_count`
- `test_no_callback_is_safe`、`test_callback_exception_does_not_break_compression`

---

## 6. prompt context file truncation

### 6.1 `prompt.context_file_max_chars` 可配（`f6a42b1`）

- 默认 `"context_file_max_chars": 20_000` 在 `hermes_cli/config.py:1110`
- 读 `_get_context_file_max_chars()` 在 `agent/prompt_builder.py:962`
- 用 `_truncate_content(max_chars=None)` 在 `:1516`
- 警告 `"increase context_file_max_chars or trim the file!"` 在 `:1522`
- `build_system_prompt`（`agent/system_prompt.py`）经 `drain_truncation_warnings()` 推 `agent._emit_status()`
- 文档 `configuration.md:614,620`、`context-files.md:112,174,188`、`prompt-assembly.md:198`

### 6.2 截断警告 contextvar 隔离（`6ebc449`）

`fix(prompt): isolate truncation warnings per context` —— 多 agent 并发不再串。
- `agent/prompt_builder.py:11` `import contextvars`
- `:984` `_truncation_warnings = contextvars.ContextVar("context_file_truncation_warnings", default=None)`
- helper `_record_truncation_warning`（~`:991`）+ 重写 `drain_truncation_warnings()`（`:998`）
- 测试 `tests/agent/test_prompt_builder.py` `test_warnings_isolated_across_contexts`

---

## 7. gateway tool progress

> **顺序**：`fc956b9` 先引入 `tool_progress_style`（`accumulate` vs `separate`），同日 `6373aba` 改名为 `tool_progress_grouping`，HEAD 上以 grouping 为准。

`fc956b9 feat: add tool_progress_style config (accumulate vs separate)` —— 原 key。
- `_GLOBAL_DEFAULTS["tool_progress_style"]` 进 `gateway/display_config.py`
- `_normalise` 分支处理 `accumulate|separate`
- `gateway/run.py` `can_edit = progress_style != "separate"`
- HEAD 上该 key 已被 `tool_progress_grouping` 取代，本 commit 价值在引入语义

`6373aba feat(gateway): rename to tool_progress_grouping, add config/docs/tests` —— 改名 + 默认值 + 测试 + 文档。
- `gateway/display_config.py:35` `_GLOBAL_DEFAULTS["tool_progress_grouping"]`
- `:242` `_normalise` 保 `accumulate|separate` 域
- 解析调 `gateway/run.py:13688`
- `gateway/run.py:14027` `can_edit = progress_grouping != "separate"`
- `hermes_cli/config.py:1498` `DEFAULT_CONFIG["tool_progress_grouping"]: "accumulate"`
- 测试 `tests/gateway/test_display_config.py:454-510` `TestToolProgressGrouping`
- 文档 `website/docs/user-guide/messaging/index.md:323-327`

---

## 8. auth codex pool ratelimit

`9a59ad7 fix(auth): preserve Codex pool-only rate-limit state` —— Codex 池里 `last_status="exhausted"` 且 code 429/quota/rate_limit 时，认证状态为 `rate_limited` 而非 `missing credentials`，UI 不再弹 relogin。
- 新分类器 `_codex_pool_rate_limit_status()` 在 `hermes_cli/auth.py:3875`，扫 `auth_store["credential_pool"]["openai-codex"]`
- `resolve_codex_runtime_credentials` 在 `:3809-3826` 抛 `AuthError(code=CODEX_RATE_LIMITED_CODE, relogin_required=False)`
- `get_codex_auth_status()` 在 `:6003-6014` 返 `{logged_in: True, rate_limited: True, error_code: CODEX_RATE_LIMITED_CODE, reset_at}`
- 常量 `CODEX_RATE_LIMITED_CODE = "codex_rate_limited"` 在 `:723`

`1ac76a9 chore: add MrDiamondBallz to release AUTHOR_MAP` —— 同时间 release map salvage。
- `scripts/release.py:52` `"264773240+MrDiamondBallz@users.noreply.github.com": "MrDiamondBallz"`，插在 `benegessarit` 与 `Kenmege` 之间

---

## 9. models

### 9.1 xAI 默认 grok-build-0.1（`bbc842d` + `f4ef70f`）

`bbc842d feat(xai): default to grok-build-0.1` —— 内置 fallback + picker 默认改 grok-build-0.1。
- `hermes_cli/models.py:113` `"grok-build-0.1"` 进 `_XAI_STATIC_FALLBACK` 头
- `:121` `_XAI_TOP_MODEL = "grok-build-0.1"`
- `plugins/web/xai/provider.py:49` `DEFAULT_MODEL = "grok-build-0.1"`
- `hermes_cli/model_setup_flows.py:520` `_prompt_model_selection` fallback 改 `"grok-build-0.1"`
- 保留 grok-4.3 在 `models.py:114`

`f4ef70f docs(xai): update default model references to grok-build-0.1` —— 文档同步。
- `plugins/web/xai/provider.py:22` 例 `model: "grok-build-0.1"`
- `website/docs/user-guide/features/web-search.md` + zh-Hans + `website/docs/guides/xai-grok-oauth.md` 同步改
- 聚合目录（Nous/OpenRouter）不动

### 9.2 Live + curated catalog 合并（PR #46857，3 commit + 1 merge）

`630b438 fix(models): merge live API results with curated static catalog in generic provider path` —— 通用 provider 路径同时合并 live API 与 curated 静态目录。
- `hermes_cli/models.py:2263-2280` Anthropic 分支：`curated = list(_PROVIDER_MODELS.get("anthropic", []))` → `merged = list(curated)` → 大小写 insensitive `merged_lower` set 追加 live-only ID
- 同 pattern 在 fetch_models 调用点 `~models.py:2376` 泛化
- 测试 `tests/hermes_cli/test_provider_live_curated_merge.py`（113 行）

`ee7b8a4 fix(models): validate_requested_model falls back to curated catalog when live API omits model` —— live `/v1/models` 不返某 model 时改用 curated。
- `hermes_cli/models.py:3585` `def validate_requested_model(...)`
- `:3947-3962` `"Model not in live /v1/models — check the curated catalog before rejecting"` 分支返 `{"accepted": True, "recognized": True, "message": "Note: ... exists in the curated catalog — accepted."}`

`658ac1d fix(models): keep curated-first ordering in live+curated merge; use pure-catalog helper in validation` —— 排序保 curated-first + 验证用纯目录 helper。
- `hermes_cli/models.py:1762` 新 `_model_in_provider_catalog(name_lower, providers)`
- `:3952` 验证器调 `_model_in_provider_catalog(requested_for_lookup.lower(), _provider_keys(normalized))`（无网）
- `:1861, 1896` 其他 catalog 成员检查走该 helper
- 合并代码在 `:2263-2280` 保 curated 在前再追加 live-only

### 9.3 `model.base_url` 透传 fetch_models（`1b962f0` + `db44af0`）

`1b962f0 fix(models): pass model.base_url to fetch_models in /model picker` —— `/model` picker 把模型注册时的 `base_url` 传 `fetch_models`。
- `providers/base.py:162-217` `fetch_models(*, api_key, base_url, timeout)` 解析顺序 `models_url → base_url → self.base_url + "/models"`
- `hermes_cli/models.py:2374` `live = _p.fetch_models(api_key=api_key, base_url=base_url or None)`
- 子类 override：`plugins/model-providers/custom/__init__.py:42-52`、`openrouter/__init__.py:50-73`
- 新测试 `tests/providers/test_fetch_models_base_url.py`（140 行）

`db44af0 test(model-picker): cover two overlapping user-defined custom providers` —— 跨过滤 dedup 回归（守 `b7fa62c`）。22 行加进 `tests/hermes_cli/test_inventory.py`

### 9.4 无 api_key 仍探针（`1039e90` + `7493de7`）

`1039e90 fix(model-switch): probe /v1/models for providers without api_key` —— section-3 在无 api_key 时仍探针。
- `hermes_cli/model_switch.py:1741-1758` 注释 `"With an api_key: always probe… Without an api_key AND no explicit models: probe anyway"`
- `should_probe = bool(api_url) and discover and (bool(api_key) or not has_explicit_models)`
- 调 `fetch_api_models(api_key, api_url)`

`7493de7 test(model-switch): cover section-3 no-auth probe; map chimpera author` —— 78 行新测试 `tests/hermes_cli/test_user_providers_model_switch.py`；`scripts/release.py:420` `"11052595+chimpera@users.noreply.github.com": "chimpera"`

---

## 10. models extra

`b7fa62c fix(inventory): keep user-defined custom providers in model dedup` —— 用户自定义 provider 不被聚合 dedup 误杀。
- `hermes_cli/inventory.py:175-195` 先扫 `is_user_defined` 行收 `user_models`，主循环再 `if row.get("is_user_defined"): continue` 才调 `_is_aggregator(slug)`
- 回归测试 `tests/hermes_cli/test_inventory.py`

`b2da39a feat: add z-ai/glm-5.2 to OpenRouter and Nous model lists` —— 新 model。
- `hermes_cli/models.py:64` `("z-ai/glm-5.2", "")` 进 `OPENROUTER_MODELS`
- `:187` `"z-ai/glm-5.2"` 进 `_PROVIDER_MODELS["nous"]`（在 glm-5.1 之前）
- `:263` `"glm-5.2"` 进 zai picker
- 重生成 `website/static/api/model-catalog.json`

---

## 11. auxiliary fallback

`4858942 fix(auxiliary): honor main fallback chain for auto tasks (#47235)` —— auto 任务在主 provider 死时按用户配置的 fallback chain 走。
- `agent/auxiliary_client.py:3106` `_fallback_entry_api_key`
- `:3117` `_resolve_fallback_entry`（委托 `resolve_provider_client`）
- `:3135` `_try_main_fallback_chain` 读 `get_fallback_chain(load_config())` 迭代，跳已失败 / main / 不健康
- `_resolve_auto` 接 `task` 参；在 "Step 2" 插用户 fallback（`:3320-3333`）
- `resolve_provider_client` 加 `task: Optional[str] = None`（`:3459`）

---

## 12. skills

### 12.1 shop-app → shop CLI skill v1.0.1（PR 4 commit）

`5094325 feat(skills): replace shop-app with CLI-based shop skill (v1.0.1)` —— shop-app 退场，新 CLI-based shop skill。
- `optional-skills/productivity/shop/SKILL.md:2-7` `name: shop`、`version: 1.0.1`、`prerequisites.commands: [curl, node]`
- 引用 `catalog-mcp.md`（236 行）、`direct-api.md`（278 行）、`safety.md`（36 行）、`legal.md`
- 旧 `productivity/shop-app/SKILL.md` 删

`d7668aa chore(skills/shop): tighten description to ≤60 chars, credit contributor`
- `optional-skills/productivity/shop/SKILL.md:3` `description: "Shop catalog search, checkout, order tracking, returns."`
- `:5` `author: Joe Rinaldi Johnson (joerj123), Hermes Agent`

`e236bb8 docs(skills): regenerate shop skill page after shop-app rename`
- 删 `website/docs/optional/productivity/productivity-shop-app.md`（354 行）
- 新 `productivity-shop.md`（238 行）
- 更新 `website/docs/reference/optional-skills-catalog.md` 与 `website/sidebars.ts`

`cf52370 chore(release): AUTHOR_MAP entry for Joe Rinaldi Johnson` —— `scripts/release.py:59` `"joe.rinaldijohnson@shopify.com": "joerj123"`

### 12.2 Skills 跳支持目录（`9137b86`）

`9137b86 fix(skills): ignore support docs in skill discovery` —— `references/templates/assets/scripts` 不参与 skill 发现。
- `agent/skill_utils.py:50` `SKILL_SUPPORT_DIRS = frozenset(("references", "templates", "assets", "scripts"))`
- `agent/skill_utils.py:73` `def is_skill_support_path(path)`
- `agent/skill_utils.py:68` `should_exclude_skill_path` 用 OR 结合 `is_skill_support_path` 与既有 `EXCLUDED_SKILL_DIRS`
- `tools/skills_tool.py:84, 1027, 1032, 1043, 1050, 1077` 接 `_is_skill_support_path` 守 `skill_view` 查找与发现遍历

---

## 13. telegram

### 13.1 Bot API 10.1 rich edit（PR #46890，`a6364bf`）

`a6364bf fix(telegram): edit streamed previews in place as rich (Bot API 10.1)` —— 富文本默认开 + 流式预览原地 editMessageText 仍渲染富。
- `gateway/platforms/telegram.py:428` `_rich_messages_enabled` 默认翻 `True`
- `:984` `_needs_rich_rendering()`：富专用于 table / task-list / `<details>` / block-math
- `:1006` `_rich_eligible()` 共享核心（去 `expect_edits` 门）
- `:1257` `async def _try_edit_rich(...)` editMessageText + rich_message 路
- `:2680-2681` `if finalize and self._rich_eligible(content): rich_result = await self._try_edit_rich(...)`

### 13.2 Rich-reply 跨重启恢复 via send-time index（`01ae9b8` + `8ed16a7`）

`01ae9b8 fix(telegram): resolve replies to rich (sendRichMessage) messages` —— rich 消息内容无 Telegram 原文时，从本地 send-time 索引查回。
- **新文件** `gateway/rich_sent_store.py`：`:23 _MAX_ENTRIES=1000`、`:36 def record(...)`、`:68 def lookup(...)`、JSON store 在 `~/.hermes/state/rich_sent_index.json`
- `gateway/platforms/telegram.py:1248-1249` 发送后 record `(chat_id, message_id) -> content`
- `:6718-6720` 当 text+caption 都空时 `reply_to_text = rich_sent_store.lookup(str(chat.id), reply_to_id)`

`8ed16a7 test(telegram): rich-reply recovery via send-time index` —— 107 行测试覆 record-on-send + lookup-on-reply + 优先级。
- `tests/gateway/test_telegram_rich_messages.py:795` `test_rich_reply_records_and_recovers_text`
- `:825` `test_rich_reply_lookup_miss_leaves_text_none`
- `:839` `test_rich_reply_native_quote_wins_over_lookup`
- `:854` `test_rich_reply_caption_wins_over_lookup`

`3f80bca chore(release): AUTHOR_MAP entry for x1erra (Sierra)` —— `scripts/release.py:94` `"stevenn.damatoo@gmail.com": "x1erra"`

---

## 14. messaging misc

### 14.1 Slack `/debug` via `/hermes`（PR #47248，`4d470b3`）

`4d470b3 fix(slack): route /debug via /hermes to restore Telegram-parity` —— Slack 单字 `/debug` 走 `/hermes debug` 与 Telegram 对齐。
- `hermes_cli/commands.py:1057` `_SLACK_VIA_HERMES_ONLY = frozenset({"credits", "debug"})`（加 `debug`），check 点 `:1107`

### 14.2 Mattermost notify-only flat-fallback（`16fc717` + `5a0e0d3`）

`16fc717 fix(mattermost): harden delivery hygiene` —— 重试发送 / 最终回复保 `notify=True` metadata。
- `gateway/platforms/base.py:80` `def _mark_notify_metadata(metadata)` 浅克隆置 `notify=True`
- 应用点 `:3899, :4005, :4055`（重试发送）+ `:4282 _final_thread_metadata = _mark_notify_metadata(_thread_metadata)`（最终回复）

`5a0e0d3 fix(mattermost): preserve thread-local delivery hygiene` —— thread root 解析 + 仅 notify=True 时 fallback to flat。
- `plugins/platforms/mattermost/adapter.py:155` `async def _thread_root_for_send` 从 reply_to/metadata 解析 root
- `:170` `def _last_post_failure_is_broken_thread_root` 在 400/404 + body 标识时识别 broken root
- `:181` `async def _post_preserving_thread` 仅当 `metadata.get("notify")` + broken-root signal 时 flat-fallback
- 接到 `:352, :534, :575, :663` text/image/file/document 发送路径

### 14.3 WhatsApp / email `send_image(metadata=)`（`e65d74b`）

`e65d74b fix(gateway): accept metadata kwarg in WhatsApp/email send_image` —— 媒体 metadata kwarg 全 adapter 契约对齐。
- `gateway/platforms/whatsapp.py:843-862` `async def send_image(... metadata: Optional[Dict[str, Any]] = None)`；`:862` `super().send_image(..., metadata)`
- `gateway/platforms/email.py:675-683` 同
- 新契约测试 `tests/gateway/test_media_metadata_contract.py`（80 行）+ `_accepts_metadata()` helper 扫所有 adapter

---

## 15. photon project id

`5b3fa26 fix(photon): unify project identifiers and update documentation for Spectrum provisioning` —— `project.id == spectrumProjectId` 全统一；删 `get_project()` 与 `spectrum: true` 切换（始终 provisioned）。
- `plugins/platforms/photon/auth.py:15-17` 文档：post-unify `project.id == spectrumProjectId`
- `:168` `def load_dashboard_project_id()` 优先 `spectrum_project_id` 而非 legacy `dashboard_project_id`
- `:185` entry-key fallback 链
- `get_project()` 删
- `create_project` 不再发 `spectrum: true`（始终 provisioned）

`db01910 chore(release): map cyb0rgk1tty noreply email for AUTHOR_MAP` —— `scripts/release.py:419` `"214562553+cyb0rgk1tty@users.noreply.github.com": "cyb0rgk1tty"`

`925b0d1 chore: add zimigit2020 to release AUTHOR_MAP` —— `scripts/release.py:95` `"evansrory@gmail.com": "zimigit2020"`

---

## 16. honcho identity tree

> **背景**：本期 Honcho 网关身份映射经 PR #44431 merge 重写：把"部署形状"的提示流换成"网关身份树"的菜单流；以 `pinUserPeer` 作正典 key、`pinPeerName` 降为 deprecated 别名。

合并 commit：`d2b34e8 Merge pull request #44431 from erosika/feat/honcho-identity-tree` —— 子 commit 按时序：

`bb5cb32 refactor(honcho): canonicalize identity-mapping on pinUserPeer, migrate legacy key`
- `plugins/memory/honcho/cli.py:380-391` `_migrate_pin_peer_name()` pop `pinPeerName` 写 `pinUserPeer`
- 克隆路径 `cli.py:51,56` 包 pinUserPeer + legacy 迁移

`d7dfeed feat(honcho-setup): replace deployment-shape prompt with gateway-gated identity tree`
- `plugins/memory/honcho/cli.py:394-408` `_gateway_platforms()` 懒导 `gateway.config.load_gateway_config` 返已连平台或 `None`
- `cli.py:681-707` 无网关时自动 skip；否则菜单 `[1] just me / [2] me + others / [3] only others / [s] skip / [e] edit raw keys`
- `cli.py:724-735` orphan-guard：un-pin 会孤悬 peerName 历史时重提
- 各分支 `cli.py:741, 754-758, 766-769` 清洗后写 `pinUserPeer` / `userPeerAliases` / `runtimePeerPrefix`

`99feb03 docs(honcho): demote pinPeerName to deprecated alias`
- `plugins/memory/honcho/README.md:140-144` 加 pinUserPeer / userPeerAliases / runtimePeerPrefix 表行
- pinPeerName 降为 deprecated 别名注

`23a7458 docs(website): cover gateway identity mapping in Honcho feature page`
- `website/docs/user-guide/features/honcho.md:160` 加 "Gateway Identity Mapping" 节
- `:174` 解析器顺序
- `:177` orphan 警告
- `:181` deprecated 别名

`2708c33 docs(honcho): anonymize example peer name to alice` + `1544813 chore(honcho): replace example Telegram UID with placeholder` —— 隐私去标识，例 `alice` + UID `7654321` 跨 README / cli.py / 测试 / docs

后续两个 docs 跟进（已 merge 进 main 后再补的 follow-up，但本 commit 在 cffd6e3c8 与 PR #44431 merge 之间）：

`6dde7d4 docs(memory-providers): cover gateway identity mapping for Honcho`
- `website/docs/user-guide/features/memory-providers.md:98` 加 pinUserPeer 行进 config table
- `:211` "every non-agent gateway user collapses to peerName" 进 Gateway Identity Mapping 子段

`c7513df docs(honcho): clarify pinUserPeer pins only non-agent users`
- `plugins/memory/honcho/README.md:140` 改为 "every gateway runtime user collapses…" 再 reframe 为 non-agent + pin-overrides-aliases
- `plugins/memory/honcho/cli.py:458` 菜单提示词改 "pinUserPeer — pin all gateway users to your peer?"
- `website/docs/user-guide/features/honcho.md:168` "every non-agent gateway user collapses to your peer. Pin overrides all aliases"；交叉引用 `session.py:335` 解析器优先级

---

## 17. tui test isolation

`2483200 test(tui): isolate session-create no-race test from shard-sibling leakage (#47230)` —— TUI session 创建无竞争测试不被同 shard 兄弟泄漏污染。
- `tests/test_tui_gateway_server.py:4901-4942` `test_session_create_no_race_keeps_worker_alive`
- 把 `server._sessions` 快照进 `_saved_sessions`，`server._sessions.clear()`，`finally` 还原
- `agent_ready.wait(timeout=2.0)` → `10.0`，并断言 `built` 返回值（之前默默吞掉 timeout）
- 服务端引用点 `server.py:1011` `'replaced'` check

---

## 18. author map

本期 5 条 AUTHOR_MAP salvage，全部进 `scripts/release.py`（不是 `tools/release/AUTHOR_MAP.json`，hermes-agent 把 AUTHOR_MAP 维护在 `scripts/release.py` 的 Python dict 中）：

| commit  | line | email → handle |
|---|---|---|
| `3f80bca` | `scripts/release.py:94` | `stevenn.damatoo@gmail.com` → `x1erra` |
| `7493de7` | `scripts/release.py:420` | `11052595+chimpera@users.noreply.github.com` → `chimpera` |
| `1ac76a9` | `scripts/release.py:52` | `264773240+MrDiamondBallz@users.noreply.github.com` → `MrDiamondBallz` |
| `db01910` | `scripts/release.py:419` | `214562553+cyb0rgk1tty@users.noreply.github.com` → `cyb0rgk1tty` |
| `cf52370` | `scripts/release.py:59` | `joe.rinaldijohnson@shopify.com` → `joerj123` |
| `925b0d1` | `scripts/release.py:95` | `evansrory@gmail.com` → `zimigit2020` |

---

## 跨页索引（受影响 wiki 概念页）

- **[[memory-system-architecture]]** — `display.memory_notifications` 三态 + `_strip_skill_scaffolding` 共享 helper
- **[[skills-system-architecture]]** — skill 支持目录 frozenset + 工具 patch/edit/create `_change:` 体 + shop CLI 化
- **[[skills-and-memory-interaction]]** — verbose skill change 预览
- **[[hook-system-architecture]]** — `session:compress` 事件 + `event_callback` 网关桥
- **[[prompt-builder-architecture]]** — `context_file_max_chars` 配 + contextvar 截断警告
- **[[gateway-session-management]]** — `tool_progress_grouping`（取代 `tool_progress_style`）
- **[[messaging-gateway-architecture]]** — Slack `/debug via /hermes` + Mattermost notify-only flat-fallback + WhatsApp/email send_image metadata kwarg
- **[[provider-plugin-system]]** — `fetch_models(base_url=)` 签名 + live+curated 合并 + curated-first
- **[[smart-model-routing]]** — auxiliary auto 走主 fallback chain
- **[[credential-pool-and-isolation]]** — Codex pool-only rate-limit 不弹 relogin
- **[[multi-agent-architecture]]** — delegate watch window subagent 流（`subagent.text` 父端跳过 emit / 子端 `message.delta`）
- **[[browser-tool-architecture]]** — `/browser` Desktop 本地 gateway 限

---

## 验证说明

- HEAD = `d1ecebcbfd8c7f2b942fd9cc425cea028e34111c`
- baseline = `cffd6e3c8d0bf6a180ed19a919fab05f44def311`
- 4 个并行 research agent 分主题分担实证：A（Desktop UI 17 commit）/ B（Memory / Display / Gateway / Hooks / Prompt / Auth 15 commit）/ C（Models / xAI / Skills / Honcho 18 commit）/ D（Messaging Telegram / Slack / Mattermost / Photon / WhatsApp / AUTHOR_MAP 13 commit）
- 所有 file:line 来自 `d1ecebc` 时刻的 `/tmp/hermes-agent` checkout，函数名 / 常量 / class 名经 `git show` / `Read` 实证
- `cffd6e3c8..HEAD` 在 git 上共 141 commit，但其中约 76 commit 的 committer date ≤ 2026-06-15 20:35 UTC（author date 横跨 2026-04-14 ~ 2026-06-13），属于之前 sync 已覆盖的旧 PR salvage（photon spectrum-ts/iMessage gRPC、yuanbao media middleware、agent char-based output-cap overflow、honcho identity-tree 各 sub-PR 等），本期不重复记录；本期 65 commit 全部为 committer date ∈ (2026-06-15 20:35 UTC, 2026-06-16 20:40 UTC] 的新内容
