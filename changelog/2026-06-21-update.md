---
title: 2026-06-21 单日同步 — hermes-agent 5a53e0f0 → 87ab373（179 commits, ~26h 单日窗口；v0.17.0 维护）— ⭐ Compression 9 commit（protect_first_n 衰减 + goal/platform/会话索引跨 rotation 保留 + summary mid-flight 中断保护 + fallback 三重 ask 去重 + 空 content 视为失败 + 小 ctx 模型 85% 阈值 + TUI /compress 前后摘要 + 模型切换 preflight-compress 预警 + 重复压缩告警走 _emit_status）+ ⭐ Kanban 8 commit + lifecycle hooks（reclaim 锁感知 + auto_decompose 实时 + bounded init lock 10s + 工作器 TERMINAL_CWD + 单写者 dispatch lock + 三新 plugin hook claimed/completed/blocked + 无路径 worktree 锚 board.default_workdir + 实物化复用 linked worktree）+ ⭐ Security 9 commit（IPv6 scope-ID URL 旁路 + WebSocket 空 peer fail-closed + kanban markdown 白名单消毒 + restore_quick_snapshot 路径穿越 + kanban 工具 payload secret redact + config show/set api_key 屏蔽 + read_file 设备别名硬化 + symlink 守卫锚 task cwd + 拒绝写入 read_file 行号显示文本）+ ⭐ Gateway/API 跨多主题（流终态 fresh-final 尊重适配器 + cron 命名空间 sys.path[3] + 卫生 400→5000 + 卫生无法旋转保留转录 + image_generate JSON-payload 路径入历史去重集 + busy 模式文本 FIFO 不再 newline-merge + 在飞转录 drain-timeout flush + 入站媒体下载封顶 128MiB + Windows 未映射 gateway 更新后重启 + stateless HTTP path 不再静默承诺 async + 可配并发 run 上限 + active_agents 轮边界 publish + /api/status surface gateway_busy/drainable + parse_active_agents 三处统一）+ ⭐ Telegram 9 commit（Bot API 10.1 rich 默认 off + CJK 不走 rich + 表格不强换行 + slash 富文本保留换行 + chunk 标记移出代码栅栏 + 原生 rich echo reply 文本恢复 + 流式 final 经 editMessageText 重新索引 + finalize 前暂停 typing + 最终回复后 typing indicator 不残留）+ ⭐ Cron 6 commit（ticker BaseException 不死 + 心跳感知状态 + 迁移时区偏移修复防双触发 + action='run' 立即执行 + DM-topic 走 DeliveryRouter + 直播适配器交付确认可靠 + model.default 解析 fail-fast）+ Plugins/Session lifecycle（ctx.profile_name 无会话 profile 访问 + finalize_turn cleanup 三段守 + AIAgent.close 会话 SQLite finalize + 后台 review fork 退出 + 委派级联不删父会话 + force-quit/signal 持久化）+ SendResult.error_kind 7 类（too_long/bad_format/forbidden/not_found/rate_limited/transient/unknown）+ i18n Spanish 完整 + Desktop About 加 Update now + 模型可见性跨 provider + Hindsight 健康 grace 可配 + Hindsight root 跳 local_embedded + MCP "unknown method" ping keepalive + Photon Envoy overflow + sidecar 死亡探测 + 16+ docs commit（多语言+SKILL.md+插件钩子+会话生命周期）+ AUTHOR_MAP 10+ salvage
created: 2026-06-21
updated: 2026-06-21
type: changelog
tags: [changelog, update, v0.17.0-maintenance, compression-protect-first-n-decay, effective-protect-first-n, compression-goal-migrate, migrate-goal-to-session, compression-rotation-rollback, compression-platform-forward, aux-interrupt-protection, aux-interrupt-protected, compression-fallback-single-ask, historical-task-heading, compression-empty-content-failure, compression-min-context-auto-trigger, min-ctx-trigger-ratio-085, compute-threshold-tokens, tui-compress-before-after, summarize-manual-compression, model-switch-preflight-warning, context-switch-guard, emit-status-compression-warning, kanban-reclaim-claim-lock-aware, release-stale-claims-lock-gate, enforce-max-runtime-lock-gate, detect-crashed-workers-lock-gate, kanban-auto-decompose-live, resolve-auto-decompose-settings, kanban-bounded-init-lock, init-lock-timeout-10s, kanban-worker-terminal-cwd, kanban-single-writer-dispatch, dispatch-tick-lock, dispatch-result-skipped-locked, kanban-task-lifecycle-hooks, kanban-task-claimed, kanban-task-completed, kanban-task-blocked, fire-kanban-lifecycle-hook, kanban-worktree-board-anchor, default-workdir-anchor, ensure-git-worktree, kanban-linked-worktree-reuse, is-linked-worktree-checkout, resolve-worktree-workspace, ipv6-scope-id-strip, url-safety-fail-closed, websocket-empty-peer-fail-closed, ws-client-is-allowed, kanban-markdown-allowlist, sanitize-markdown-html, sanitize-markdown-attrs, restore-quick-snapshot-traversal, snapshot-id-validate, manifest-path-traversal, kanban-payload-redact, redact-sensitive-text-force, config-show-api-key-redact, redact-config-value, secret-config-keys-frozenset, read-file-device-alias-walk, is-blocked-device-path-normpath, anchor-symlink-guard-task-cwd, refuse-line-numbered-writeback, looks-like-read-file-line-numbered, gateway-fresh-final-adapter-decline, prefers-fresh-final-streaming, has-prefers-hook, cron-namespace-sys-path-3, gateway-hygiene-hard-message-5000, hygiene-hard-message-limit-config, gateway-hygiene-preserve-transcript, hyg-rotated-or-in-place-guard, gateway-image-generate-dedup, collect-history-media-paths, json-media-tool-path-fields, gateway-busy-fifo-followups, queue-or-replace-pending-event, gateway-shutdown-flush-inflight, finalize-shutdown-agents, gateway-media-download-cap, max-inbound-media-bytes-128mib, cache-image-from-bytes-cap, gateway-windows-unmapped-respawn, pause-windows-gateways-for-update, looks-like-gateway-command-line, api-server-supports-async-delivery, api-server-async-delivery-supported, hermes-session-async-delivery, api-server-max-concurrent-runs, resolve-max-concurrent-runs, gateway-active-agents-turn-boundary, persist-active-agents, derive-gateway-busy, derive-gateway-drainable, parse-active-agents, telegram-rich-messages-opt-in-off, telegram-cjk-no-rich, telegram-tables-no-hard-break, telegram-slash-rich-newlines, telegram-chunk-markers-outside-fences, telegram-rich-echo-reply-recovery, telegram-rich-final-edit-message-text, telegram-typing-pause-before-finalize, telegram-typing-indicator-not-lingering, cron-ticker-base-exception, cron-heartbeat-status, cron-tz-offset-repair, cron-action-run-immediate, cron-dm-topic-delivery-router, cron-live-adapter-confirmation, cron-model-default-fail-fast, plugin-ctx-profile-name, agent-finalize-turn-cleanup-guard, agent-end-session-on-close, collect-delegate-child-ids-parent-filter, cli-persist-active-session-before-close, tui-finalize-session-flush, send-result-error-kind, send-error-kinds, classify-send-error, i18n-spanish-complete, locales-es-yaml, readme-es-md, contributing-es-md, security-es-md, desktop-update-now-button, model-visibility-cross-provider, hindsight-daemon-health-grace, hindsight-skip-root-local-embedded, mcp-ping-keepalive-unknown-method, photon-envoy-overflow-retry, photon-sidecar-death-reconnect, photon-typing-cooldown, codex-app-server-tool-progress, codex-note-to-tool-progress, whatsapp-jid-normalize, whatsapp-bridge-dir-hermes-home, bedrock-prompt-cache-pricing, bedrock-cross-region-prefix-strip, browser-ssrf-container, terminal-env-guard, auth-load-pool-non-destructive, prune-env-sources-flag, backup-memory-provider-external, memory-provider-backup-paths, external-prefix-archive, chat-completions-timestamp-strip, install-windows-83-temp-long-path, process-poll-read-only, poll-observed-set, drain-skip-poll-observed, agent-copilot-default-headers, file-device-base-task-cwd, model-switch-failed-swap-noop, model-switch-token-calibration-reset, model-switch-tool-output-budget-scale, config-api-base-alias-base-url, email-missing-config-non-retryable, email-imap-smtp-host-validate, approval-honor-interrupt-blocking, gateway-respect-fresh-final-decline, gateway-cron-namespace-collision-fix, desktop-windows-update-restart-loop, hermes-state-curator-fix, agent-repeat-compression-warning, salvage-50313-config-redact, salvage-50346-ctx-profile-name, salvage-50349-kanban-lifecycle-hooks, salvage-50353-init-lock-bound, salvage-50358-auto-decompose-live, salvage-50366-reclaim-lock-aware, salvage-50373-windows-unmapped-respawn, salvage-50375-model-switch-noop, salvage-50381-desktop-update-loop, salvage-50385-base-url-alias, salvage-50342-error-kind, salvage-50341-hindsight-grace, salvage-50319-api-server-async, salvage-50312-shutdown-flush, salvage-50331-single-writer, salvage-50325-backup-external, salvage-50312-inflight-transcript, salvage-50194-hygiene-5000, salvage-50297-empty-content-summary, salvage-50007-concurrent-cap, salvage-50028-mcp-unknown-method, salvage-50090-windows-unmapped, salvage-50115-preflight-warning, salvage-50136-tool-budget-scale, salvage-50137-calibration-reset, salvage-50100-model-visibility, salvage-50186-update-now-button, salvage-50342-send-result-error-kind, salvage-46686-tui-compress-summary, salvage-46070-slash-rich-newlines, salvage-38835-codex-tool-progress, salvage-23767-token-calibration, salvage-22773-dm-topic-router, salvage-14690-min-ctx-trigger, salvage-13121-shutdown-flush, salvage-11996-protect-decay, salvage-8697-approval-interrupt, salvage-40715-email-missing-config, salvage-49997-memory-single-op-recover, salvage-50245-config-redact, author-map-salvage-multiple]
sources:
  - hermes-agent/main 5a53e0f0..87ab373
total_commits: "179 个 commit（156 non-merge + 23 merge；~26h 单日窗口 committer dates 2026-06-20T16:24 -0700 → 2026-06-21T13:56 -0700；266 文件 +17145/−1079；约 70 fix / 16 feat / 13 test / 6 chore / 16 docs + 23 merge）"
---

# 2026-06-21 单日同步：hermes-agent main `5a53e0f0 → 87ab373`

> **基线**：wiki `89e3e86` 对齐 hermes `5a53e0f0f487d3d383e2a7b2eae8f260e9bf1090`（2026-06-20T11:38 -0700，"fix(compression): abort on auth failure instead of rotating into a degraded session"，v0.17.0 维护未发新版）
> **目标**：hermes-agent/main HEAD `87ab373`（2026-06-21T13:56 -0700，"test(url-safety): cover IPv6 scope-ID strip + fail-closed in URL guards"，**v0.17.0 维护**未发新版）
> **跨度**：约 26 小时（committer dates `2026-06-20T16:24 -0700 → 2026-06-21T13:56 -0700`，主活跃集中在 2026-06-20 末 ~ 2026-06-21 全天）
> **版本**：仍 **v0.17.0**（`pyproject.toml:10 version = "0.17.0"` + `hermes_cli/__init__.py:17-18 __version__ = "0.17.0"` + `__release_date__ = "2026.6.19"` 不变；本期为 v0.17.0 第二个维护性合并日，无版本号 bump）
> **commit 类型分布**：**约 70 fix / 16 feat / 13 test / 6 chore / 16 docs / 1 refactor + 23 merge**（合计 179）
> **diff 体量**：**266 文件改动，+17145 / −1079** 行
> **验证基线**：`/tmp/hermes-agent` clone @ `87ab373` 全 checkout，所有 commit hash / file:line / 函数名 / 行数经 `git show <sha> --stat` / `Grep -n` / `Read` 实证；4 个并行 research agent 分主题分担（A: Security 9 commit / B: Kanban 8 commit + lifecycle hooks / C: Compression 9 + Gateway/API 13 commit / D: Telegram 9 + Cron 6 + Misc 30+ commit），全部返回 file:line + 函数名 / 常量名 / 类名 + 行数实证

---

## 章节索引

1. [⭐ Compression cluster — `protect_first_n` 衰减 + goal/platform/session 索引跨 rotation 保留 + summary 中断保护 + fallback 三重 ask 去重 + 空 content 失败 + 小 ctx 模型 85% 阈值 + TUI `/compress` 前后摘要 + 模型切换 preflight 预警 + 重复压缩告警走 `_emit_status`（9 commit）](#1-compression-cluster)
2. [⭐ Kanban cluster — reclaim 锁感知 + `auto_decompose` 实时 + bounded init lock 10s + 工作器 TERMINAL_CWD + 单写者 dispatch lock + 三新 plugin hook + worktree 板 default_workdir 锚 + 实物化 linked worktree（8 commit + 1 feat）](#2-kanban-cluster)
3. [⭐ Security cluster — IPv6 scope-ID URL 旁路 + WebSocket 空 peer fail-closed + kanban markdown 白名单消毒 + `restore_quick_snapshot` 路径穿越 + kanban 工具 payload secret redact + config show/set api_key 屏蔽 + `read_file` 设备别名硬化 + symlink 守卫锚 task cwd + 拒绝行号 writeback（9 commit）](#3-security-cluster)
4. [⭐ Gateway / API server cluster — 流终态 fresh-final 尊重适配器 + cron 命名空间 sys.path[3] + 卫生 400→5000 + 卫生无法旋转保留转录 + `image_generate` JSON-payload 路径入历史去重集 + busy 模式文本 FIFO + 关机 in-flight 转录 flush + 入站媒体下载 128MiB cap + Windows 未映射 gateway 重生 + stateless HTTP 不再静默承诺 async + 可配并发 run 上限 + active_agents 轮边界 publish + `/api/status` 加 gateway_busy/drainable（13 commit）](#4-gateway-api-cluster)
5. [⭐ Telegram cluster — Bot API 10.1 rich 默认 off + CJK 不走 rich + 表格不强换行 + slash 富文本保留换行 + chunk 标记移出代码栅栏 + native rich echo reply 文本恢复 + 流式 final 经 editMessageText 重索引 + finalize 前暂停 typing + 最终回复后 typing indicator 不残留（9 commit）](#5-telegram-cluster)
6. [⭐ Cron cluster — ticker `BaseException` 不死 + 心跳感知状态 + 迁移时区偏移修复防双触发 + `action='run'` 立即执行 + DM-topic 经 DeliveryRouter + 直播适配器交付确认可靠 + `model.default` 解析 fail-fast（6 commit）](#6-cron-cluster)
7. [Session/Agent lifecycle — `ctx.profile_name` 无会话 profile 访问 + `finalize_turn` 三段 cleanup 守 + `AIAgent.close()` SQLite 行 finalize + 后台 review fork 退出 finalize + 委派级联不删父会话 + force-quit/signal 持久化（7 commit）](#7-session-lifecycle)
8. [Misc clusters — `SendResult.error_kind` 7 类 + i18n Spanish 完整 + Desktop About 加 Update now + 模型可见性跨 provider + Hindsight 健康 grace + Hindsight root 跳 + MCP "unknown method" ping + Photon 修复 + Codex app-server tool-progress 桥接 + Bedrock prompt-cache 计价 + WhatsApp JID 规范 + 文件设备符号链接守卫 cwd 锚 + browser SSRF 容器化 + 16+ docs commit + AUTHOR_MAP salvage](#8-misc-cluster)

---

## 1. Compression cluster

**单日 9 commit 集中修复压缩管线长期遗留 P1 bug**（保留任务/重复回复/空摘要/小 ctx 模型不触发等），整组都进 `agent/context_compressor.py` 和 `agent/conversation_compression.py`。

### 1.1 `14ef631` — `protect_first_n` 衰减让早期轮不化石化 (#11996)

- `agent/context_compressor.py:1935-1950` 新 `_effective_protect_first_n()` 方法
- `agent/context_compressor.py:1975` `_protect_head_size()` 改返回 `head + self._effective_protect_first_n()`
- **行为**：第一次压缩时仍按 `protect_first_n` 复制（默认保留前 N 个非系统消息以保留原始任务框架），之后 `compression_count >= 1 or self._previous_summary` 触发衰减返回 0，避免早期用户轮在多代会话间永生 + 受保护头无限增长
- 测试：`tests/agent/test_context_compressor.py` +33 行

### 1.2 `7ace96b` — goal/platform/session 索引跨 rotation 三状态丢失修复

引用 commit body："three state-loss bugs at the compression rotation boundary, fixed together because they all live in the same ~80-line rotation block"

- **#33618 持久 `/goal` 不跟随 rotation**：`hermes_cli/goals.py:282` 新 `migrate_goal_to_session(old_session_id, new_session_id, reason)` move-not-copy（父行归档为已清除，恰好一行 active goal）；`agent/conversation_compression.py:646-650` rotation 后调
- **#33906/#33907 子会话 `create_session` 失败导致游离 phantom id**：`agent/conversation_compression.py:613-640` 失败时 `agent.session_id = old_session_id` 回滚 + `agent._session_db.reopen_session(old_session_id)`，不再让对话停留在 state.db 无记录的幻影 id
- **#27633 compaction 边界 `on_session_start` 通知缺 `platform` kwarg**：`agent/conversation_compression.py:694-701` 加 `platform=getattr(agent, "platform", None) or "cli"` 转发，匹配 `agent_init.py` 初始 session-start 调用
- 测试：`tests/agent/test_compression_rotation_state.py` +129 行 + `tests/hermes_cli/test_goals.py` +41 行

### 1.3 `2f3177a` — summary 调用受中断保护

- `agent/auxiliary_client.py:121` 新 `_aux_interrupt_protection = threading.local()`
- `agent/auxiliary_client.py:128-141` `aux_interrupt_protection()` ctx 管理器（reentrant，主模型重试递归安全）
- `agent/context_compressor.py:1588` `with aux_interrupt_protection(): response = call_llm(...)`
- **行为**：Codex Responses aux 流轮询线程中断 flag 时尊重该 flag —— 网关收到用户中断时不再 abort 压缩摘要 LLM 调用导致退化为静态 "summary unavailable" 标记；超时仍触发（挂住的调用必须死），其他 aux 任务（vision/web_extract/title_generation）仍可中断
- 修 #23975；测试：`tests/agent/test_compression_interrupt_protection.py` +95 行

### 1.4 `1f874df` — fallback 摘要不再把最新用户问题三重渲染

- `agent/context_compressor.py:1281-1285` 把 active_task 渲染收敛到一处
- `agent/context_compressor.py:1311-1315` In-Progress / Pending 段改 "Unknown / None recoverable from deterministic fallback" + 明确 "historical context only" 注脚
- **行为**：LLM 摘要失败的确定性 fallback 之前把最新用户问题（`active_task = "User asked: '<ask>'"`）以 **三种** heading 渲染（Historical Task Snapshot / Historical In-Progress State / Historical Pending User Asks），让模型把已答的问题当未解决重答并把那个复活的 ask 当作活动轮，埋掉真正新发的 post-compaction 用户消息；改为只在 Task Snapshot 出现一次
- 修 #49307（answer repetition + new-instruction loss, P1）；测试 +34 行

### 1.5 `b6a4638` — 空 content 摘要响应视作失败 (#50297)

- `agent/context_compressor.py:1604-1609` empty content 不再 coerce 为 `""` 存储，改 `raise RuntimeError("Context compression LLM returned empty content ...")`
- `agent/context_compressor.py:1632` 窄化 `except RuntimeError` → 仅 `"no llm provider configured"` 字面进 600s no-provider cooldown，其他 empty/invalid-response RuntimeError 进 main-model fallback
- **行为**：OpenAI-兼容代理（cmkey.cn / one-api Anthropic 通道）返回格式正确 HTTP 200 但 content 为 null/空白时不再静默替换压缩轮为空摘要，避免 in-progress 上下文全丢
- 修 #11978/#11914；测试 +65 行

### 1.6 `3509be7` — 小 ctx 模型 85% 阈值触发 (#14690)

- `agent/context_compressor.py:697` 新常量 `_MIN_CTX_TRIGGER_RATIO = 0.85`
- `agent/context_compressor.py:699-724` 新 `_compute_threshold_tokens()` 静态方法
- `agent/context_compressor.py:659` + `:769` 两调用点（`__init__` + `update_model`）走共享助手
- **行为**：压缩阈值是 `max(context_length * threshold_percent, MINIMUM_CONTEXT_LENGTH=64000)`；当 floor 达到或超过窗口（如恰 64K ctx 模型 `max(32000, 64000)=64000`）时 `should_compress()` 永不触发；现在 `floored >= context_length` 时返回 `min(int(context_length * 0.85), context_length - 1)`，64K-ctx 模型在 ~54.4K 触发而非永不触发
- 修 #14690；测试 +38 行

### 1.7 `d0de460` — TUI `/compress` 显示前后摘要 (#46686)

- `tui_gateway/server.py:9822-9865` slash `compress` 分支
- **行为**：CLI（`_manual_compress`）和 gateway（`slash_commands`）都已调 `summarize_manual_compression` 显 "Compressed: N → M messages / ~X → ~Y tokens"，唯独 TUI slash 路径返回空串；现在 snapshot `_before_messages`/`_before_tokens` 经 `estimate_request_tokens_rough` 然后 `_compress_session_history` 然后 snapshot after 返回 `summarize_manual_compression(...)` 完整格式化

### 1.8 `04730f3` + `1ca2972` — 模型切换 preflight-compress 预警 + 一致 TUI warning field

- 新文件 `hermes_cli/context_switch_guard.py`（镜像 `model_cost_guard` 模式）+ `_merge_warning(result, text)` 辅助
- **行为**：会话中模型切换若将触发 preflight 压缩，之前无任何预警；现 `ModelSwitchResult.warning_message` 加 `" | "` 拼接路由进 `cli.py` / `gateway/slash_commands.py` / `tui_gateway/server.py`；`1ca2972` 还把 preflight-warning 错误从 swallow 改 log + 统一 TUI warning 字段

### 1.9 `41e0c10` — 重复压缩告警走 `_emit_status` (#36908)

- `agent/conversation_compression.py:728-735` `notify_compression_complete()` 把 `agent._compression_warning = _cc_msg; agent._emit_status(_cc_msg)`
- **行为**：之前用 `agent._vprint`（CLI only），TUI/gateway 经 `status_callback` 收不到；改 `_emit_status` 后 `replay_compression_warning`（:263）可重发；匹配 `:127-128` / `:241-242` 兄弟 emit 点

---

## 2. Kanban cluster

**单日 8 fix + 1 feat 大修任务板**，覆盖 reclaim 锁竞态、热配置、跨进程锁、工作器 cwd、单写者、生命周期 hook 全栈。

### 2.1 `d164ed0` — reclaim 锁感知防 task/run 状态失同步 (#50366)

- `hermes_cli/kanban_db.py:3499-3504` `release_stale_claims` —— RESET SQL 加 `AND claim_lock IS ?`（snapshot 锁）
- `hermes_cli/kanban_db.py:5786-5791` `enforce_max_runtime` —— RESET 加 `AND worker_pid = ? AND claim_lock IS ?`
- `hermes_cli/kanban_db.py:6086-6091` `detect_crashed_workers` —— 同上
- `hermes_cli/kanban_db.py:3570-3576` 手动 `reclaim_task` —— `AND claim_lock IS ?(prev_lock)` 平价
- **行为**：之前 RESET SQL 仅 `WHERE status='running'`，再次 claim 后新 lock 会被旧 reclaimer 覆写；现在 snapshot 锁（+ worker_pid 已知时）锁定，stale reclaim no-op（`rowcount=0`）不再让 task vs `task_run` 行状态失同步

### 2.2 `1f4c5ae` — `auto_decompose` 切换无需重启 (#50358)

- `gateway/kanban_watchers.py:26-55` 新 `_resolve_auto_decompose_settings()` 每次读 `kanban.auto_decompose` + `auto_decompose_per_tick` fresh from config；读错 fail safe (False, 3)；`per_tick >= 1` 钳制
- `gateway/kanban_watchers.py:1029-1031` 每 tick closure `_read_auto_decompose_settings()`
- `gateway/kanban_watchers.py:1131-1133` 调度器 loop 每次迭代调

### 2.3 `84ba83b` — 跨进程 init lock 10s 边界 (#50353)

- `hermes_cli/kanban_db.py:1191-1192` 新常量 `_INIT_LOCK_TIMEOUT_SECONDS = 10.0` + `_INIT_LOCK_POLL_SECONDS = 0.05`
- `hermes_cli/kanban_db.py:1228-1309` `_cross_process_init_lock` —— 之前无界阻塞 `flock(LOCK_EX)`；现在重试非阻塞 acquire（POSIX `LOCK_EX|LOCK_NB` / Windows `msvcrt.LK_NBLCK`）直到 deadline = monotonic+10s 然后 WARNING + yield **无锁**（幂等 init 作 backstop）
- `hermes_cli/kanban_db.py:1619-1634` `connect()` 快速路径 —— 若 `resolved in _INITIALIZED_PATHS` 只走轻量进程内 `_INIT_LOCK` 不再取文件锁
- Windows 改 `LK_LOCK → LK_NBLCK`（:1262, :1356）

### 2.4 `9630ec6` — 工作器 `TERMINAL_CWD` 钉到任务工作区 (#50348)

- `hermes_cli/kanban_db.py:7376-7389` `_default_spawn()` 加 `env["TERMINAL_CWD"] = workspace` 在已有 `HERMES_KANBAN_WORKSPACE` set 之后，由 `workspace and os.path.isabs(workspace) and os.path.isdir(workspace)` 守
- **行为**：之前 TERMINAL_CWD 继承自 gateway；现钉到任务工作区，修 `file_tools._resolve_base_dir` (#41312) 和 `build_context_files_prompt` 的 AGENTS.md 查找 (#34619)

### 2.5 `e217fd4` — Kanban 任务生命周期 plugin hook 三新 (#50349)

新 hook 名（注册于 `hermes_cli/plugins.py:192-194` 的 `VALID_HOOKS`）：
- **`kanban_task_claimed`** — `claim_task` 在 `hermes_cli/kanban_db.py:3277-3283`（dispatcher 进程，commit 后 spawn 前）
- **`kanban_task_completed`** — `complete_task` 在 `hermes_cli/kanban_db.py:3951-3958`（worker 进程，携 `summary`）
- **`kanban_task_blocked`** — `block_task` 在 `hermes_cli/kanban_db.py:4383-4390`（携 `reason`）

辅助 `_fire_kanban_lifecycle_hook(event, task_id, **fields)` 在 `hermes_cli/kanban_db.py:107-129` —— 经 HERMES_HOME 经 `get_active_profile_name()` 解 profile_name，吞所有异常（best-effort），仅在 write txn commit **之后** 调用以确保 plugin 观察到的是持久状态 + 慢/挂住的 callback 永不持有 SQLite 写锁。文档 `website/docs/guides/build-a-hermes-plugin.md:600-608`。

### 2.6 `e581740` — 单写者 dispatch lock 防 orphan-dispatcher DB 损坏 (#50331)

- `hermes_cli/kanban_db.py:1312-1394` 新 `_dispatch_tick_lock` ctx mgr —— 非阻塞、board-scoped flock 在 `<db>.dispatch.lock` sibling（POSIX `fcntl.LOCK_EX|LOCK_NB` / Windows `msvcrt.LK_NBLCK`）
- `hermes_cli/kanban_db.py:5369` 新字段 `DispatchResult.skipped_locked: bool`
- `hermes_cli/kanban_db.py:6573-6623` `dispatch_once` 包 `_dispatch_once_locked` 在 `with _dispatch_tick_lock(db_path) as held: if not held: return DispatchResult(skipped_locked=True)`
- **行为**：之前一旦绕过 `_guard_supervised_gateway_conflict` 就无跨进程守护；现在同一 `kanban.db` 上两个 dispatcher 永不能并发跑 reclaim/spawn/写

### 2.7 `15cfc28` — 无路径 worktree 任务锚 `board.default_workdir`

- `hermes_cli/kanban_db.py:5062-5103` `_resolve_worktree_workspace` —— `task.workspace_path` 未设时 resolve `board_default = read_board_metadata(board_slug).get("default_workdir")` 实物化 `<repo>/.worktrees/<task.id>`；缺/非绝对/非 git 仓库时显式 `ValueError`，永不静默落到 `Path.cwd()`
- `hermes_cli/kanban_db.py:5147-5154` `resolve_workspace` docstring 文档化

### 2.8 `d79f67f` — 实物化并复用 linked worktree

- `hermes_cli/kanban_db.py:5032-5059` 新 `_ensure_git_worktree(repo, target, branch)` —— 运行 `git -C <repo> worktree add [-b <branch>] <target> [HEAD]`；复用短路：若 `target` 存在且 `_git_common_dir(target) == _git_common_dir(repo_root)` 则不再 add（防嵌套）
- `hermes_cli/kanban_db.py:5006-5011` `_is_linked_worktree_checkout` —— 经 `_git_dir(path) != _git_common_dir(path)` 检测
- `hermes_cli/kanban_db.py:5062-5130` `_resolve_worktree_workspace` —— 处理 4 case (a) 无路径→board 锚 (b) 请求路径已是 linked worktree→复用读当前分支 (c) 请求路径 == repo root→视为锚实物化 `<repo>/.worktrees/<task.id>` (d) 请求路径在仓库内→在该路径创建；分支默认 `wt/<task.id>`
- `hermes_cli/kanban_db.py:6886-6903` `_dispatch_once_locked` 直接调 `_resolve_worktree_workspace` 取回 resolved 分支后经 `set_workspace_path` + `set_branch_name` 持久化
- **行为**：之前 `workspace_kind="worktree"` 仅元数据 —— 每个 worktree 任务都跑在主 checkout；现在 `<repo>/.worktrees/<task.id>` 真有 linked worktree 在分支 `wt/<task.id>` 上，rerun 经 git-common-dir 复用

---

## 3. Security cluster

**单日 9 安全 commit**，覆盖 URL safety / WebSocket / 路径穿越 / HTML 注入 / 密钥泄露 / 设备 alias 全栈。

| # | sha | 标题 | 文件 | 改动 |
|---|---|---|---|---|
| 3.1 | `ed96669` + 测试 `87ab373` | IPv6 scope-ID URL 旁路 | `tools/url_safety.py:285-287` `is_always_blocked_url()` + `:359-365` `is_safe_url()` | `ip_str.split('%')[0]` 剥除 scope-ID（`%eth0`）再 `ipaddress.ip_address()`；`is_safe_url` 不可解析地址改 fail-closed（`return False` + `logger.warning`），不再 `continue` 跳过 |
| 3.2 | `ed3d12a` | WebSocket 空 peer fail-closed | `hermes_cli/web_server.py:10992` `_ws_client_reason()` + `:11019` `_ws_client_is_allowed()` | `ws.client is None` 或 `ws.client.host == ""` 时 helper 之前 fail-open；现 `:11013` 返 `f"missing_or_empty_peer bound={...}"` + `:11055` 返 `False`；调度器 `_ws_request_reason`/`_ws_request_is_allowed`（:11110/:11115）经 4 endpoint |
| 3.3 | `5b45fb2` | Kanban markdown HTML 白名单消毒 | `plugins/kanban/dashboard/dist/index.js:337` `MARKDOWN_ALLOWED_TAGS` + `:354` `sanitizeMarkdownAttrs` + `:368` `sanitizeMarkdownHtml` + `:387` `MarkdownBlock` 包装 `dangerouslySetInnerHTML` | 之前 markdown→HTML 直接 raw 经 `dangerouslySetInnerHTML`；现走白名单（a/code/em/h1–h4/li/p/pre/strong/ul），`<a>` 要求 `https?://` 或 `mailto:` + 加 `target=_blank rel="noopener noreferrer"`，`<pre>` 仅保留 `class="hermes-kanban-md-code"` |
| 3.4 | `ae46699` + 测试 `87615f4` | `restore_quick_snapshot` 路径穿越 | `hermes_cli/backup.py:907` snapshot_id 校验 + `:916` snap_dir traversal 校验 + `:936/:943` manifest src/dst traversal 校验 | (a) `snapshot_id` 含 `/`/`\`/`.`/`..` 或空 → 拒绝 (b) `snap_dir.resolve().relative_to(root.resolve())` (c) 每条 manifest `rel` 双校验 `src.resolve().relative_to(snap_dir.resolve())` + `dst.resolve().relative_to(home.resolve())` —— 之前缺该校验，manifest 含 `../../outside.txt` 可逃 HERMES_HOME |
| 3.5 | `3b56d3a` | Kanban 工具 payload 密钥 redact | `tools/kanban_tools.py:36` `import redact_sensitive_text` + `:488` `_handle_complete` + `:621` `_handle_block` + `:709` `_handle_comment` | summary（:492）/result（:494）/metadata JSON round-trip（:497）/block reason（:624）/comment body（:712）全经 `redact_sensitive_text(..., force=True)` 后再入 kanban DB，防 API key/token 漏入后续 worker prompt；测试 `tests/tools/test_kanban_redaction.py` +191 行 |
| 3.6 | `a18bae6` | `config show/set` `api_key` 屏蔽 (#50245 #50313) | `hermes_cli/config.py:6439` 新常量 `_SECRET_CONFIG_KEYS` frozenset + `:6459` 新 `redact_config_value()` + `:6556` `show_config()` Model 行包 model dict + `:6827` `set_config_value()` echo 屏蔽 | `print()` 之前绕过 logging redactor，不透明 token（如 Cloudflare `cfut_...`）逃 vendor 正则；新递归结构化 masker（case-insensitive key 匹配 `api_key`/`token`/`secret`/`password` 等，深度限 20）经 `agent.redact.mask_secret()` 屏蔽 |
| 3.7 | `9078b4b` | `read_file` 设备别名硬化 | `tools/file_tools.py:286` `_is_blocked_device_path()` + `:305` `_is_blocked_device()` + `:316-340` normpath + symlink 多跳 | 之前 expanduser-only 比对然后一次 `realpath`；现 `os.path.normpath(os.path.expanduser(filepath))` 折叠 `/dev/../dev/zero` 和 `/dev/./urandom` + 走最多 20 个 symlink 跳，每跳 `_is_blocked_device_path(target)` 检（`os.readlink`）+ 最终 realpath 校验，捕 `/dev/stdin` 别名在 resolve 到终端特定路径前 |
| 3.8 | `def3f63` | 设备 symlink 守卫锚 task cwd | `tools/file_tools.py:305` `_is_blocked_device(filepath, base_dir=None)` 签名扩展 + `:312-315` 相对路径 prefix `base_dir` + `:856-857` `read_file_tool` 计算 `device_base` 并传入 | 之前相对工作区 symlink 解析对 Python 进程 cwd；现锚到 task base（TERMINAL_CWD 工作区），让 `/dev/../dev/stdin` 在 I/O 前被捕获，与 `read_file` 实际打开路径同 |
| 3.9 | `71274f2` + 测试 `16899ae` | 拒绝 `read_file` 行号 writeback | `tools/file_tools.py:663` 新 `_looks_like_read_file_line_numbered_content()` + `:698` `_is_internal_file_tool_content()` + `:1263-1267` `write_file_tool` 守 | 改名（`_is_internal_file_status_text` → `_is_internal_file_tool_content`） + 加显示文本检测；要求 ≥2 个编号行、≥60% 比例、连续行号递进（单个 `1|value` 字面通过）；命中返 `"Refusing to write internal read_file display text as file content. Strip read_file line-number prefixes or reconstruct the intended file contents before writing."`；后续测试 commit 更新断言为统一 `"internal read_file display text"` 消息 |

---

## 4. Gateway / API server cluster

**13 commit 跨多主题**，包括 transcript 持久性、媒体处理、并发上限、可观测性。

### 4.1 `b5b8a4c` — 流终态 fresh-final 尊重适配器 decline 防双投递

- `gateway/stream_consumer.py:1459-1464` 检测 `_has_prefers_hook`（`type(self.adapter)` + `__dict__` 双查防 MagicMock auto-attr false-positive）
- `gateway/stream_consumer.py:1466-1474` finalize gate —— `if _has_prefers_hook` 则仅 `_prefers_fresh` 决定；时间 fallback 仅 `not _has_prefers_hook and self._should_send_fresh_final()`
- **行为**：之前 fresh-final 纯按时间阈值触发（即使 Telegram `prefers_fresh_final_streaming = False`），新 Rich Message 与已上屏的 legacy MarkdownV2 preview 重叠双显（修 #47048 table + bullet 双渲染）

### 4.2 `4c206b9` + `79f2978` — 插件 cron 命名空间 `sys.path[3]` 修正

- `plugins/platforms/discord/adapter.py:101` + `plugins/platforms/raft/adapter.py:39`（commit 1）
- `plugins/platforms/slack/adapter.py:37` + `plugins/platforms/telegram/adapter.py:66` + `plugins/platforms/whatsapp/adapter.py:181`（commit 2 扩 fix）
- **行为**：顶层 `sys.path.insert(0, ...)` `parents[2]`（= `plugins/`）shadow 真 `cron/` 包；修为 `parents[3]`（仓库根）

### 4.3 `03563da` — 会话卫生硬消息上限 400→5000 (#50194)

- `gateway/run.py:9077` `_hyg_hard_msg_limit = 5000` + `:9112` `_comp_cfg.get("hygiene_hard_message_limit")`
- `hermes_cli/config.py:1262` 默认改 5000
- **行为**：网关 pre-compression 卫生 valve 之前在任何会话超 400 消息时强制压缩，与 token 用量无关；大-ctx（1M+）模型做许多短而密集轮的健康会话在 ~16% token 用量时碰 400 触发，压缩摘要的 stale Active Task 会漏入下轮；阀实际目的是打破死循环（API 调用对超大会话连断 → 无 token 用量数据 → token 阈值永不触发 → 转录无限增长），是 count 基底底；改 5000 仍远低于死循环但不再触发合法长对话；仍可经 `compression.hygiene_hard_message_limit` 配置

### 4.4 `4c349e8` — 卫生压缩无法旋转时保留转录

- `gateway/run.py:9287-9321` 加 `if _hyg_rotated or _hyg_in_place:` 守
- **行为**：网关 Session Hygiene 自动压缩之前在 throwaway hygiene agent 无法旋转会话时摧毁原始转录（#21301, P1）—— `_hyg_agent` 无 `session_db` 故 `_compress_context` rotate 块（gated on `agent._session_db`）跳过，session_id 不变但 `rewrite_transcript()` 仍**无条件**调用，用 head+summary 替换全原始转录 → 每次卫生 compaction 永久数据丢失；现在 rewrite 仅在 rotation 或 in-place 成功时调，否则保留转录 + warning log；token/count 簿记移入守内

### 4.5 `8ac5e90` — `image_generate` JSON-payload 路径入历史去重集

- `gateway/run.py:1022` 新常量 `_JSON_MEDIA_TOOL_PATH_FIELDS = ("host_image", "image", "agent_visible_image")`
- `gateway/run.py:1119` 新 `_collect_history_media_paths()` 抽取
- `gateway/run.py:15712` `_history_media_paths: set = _collect_history_media_paths(agent_history)`
- **行为**：压缩后每轮 agent 重发已交付的生成图（#46627）—— auto-append fallback 消息收缩时重扫全历史（压缩安全路径），去重对 `_history_media_paths`；该集之前仅扫工具结果 text 内 `MEDIA:` 标签；`image_generate` 在 JSON payload 字段（host_image/image/agent_visible_image）返路径而非 MEDIA 标签 → 生成图路径永不入去重集 → 边界后重发；现在收集器双覆盖

### 4.6 `c11c510` — busy 模式文本 FIFO 不再 newline-merge

- `gateway/run.py:4154` `_queue_or_replace_pending_event`
- `gateway/run.py:4326` 新中断模式 follow-up 调用点（+ 现有 FIFO 站点 :7873/:7881/:7899/:7915）
- **行为**：agent 忙时用户发多条文本 follow-up，中断模式 + steer-fallback 路径之前经 `merge_pending_message_event(merge_text=True)` newline-join 连续 TEXT 为单个 pending 轮（#43066 sub-bug 2）—— 改路由经 `_queue_or_replace_pending_event` 让每条 follow-up 得自己下轮 slot 按到达顺序，仍保留 photo-burst/album merge 媒体语义；纯队列模式之前已用 FIFO，本提交让中断/steer-fallback 路径对齐

### 4.7 `d19aabb` — 关机 drain-timeout 强 flush 在飞转录 (#50312)

- `gateway/run.py:4675` `_finalize_shutdown_agents`，关机路径 `:6700` 调
- `gateway/run.py:4693` `_flush = getattr(agent, "_flush_messages_to_session_db", None)`
- **行为**：drain-timeout escalation 强中断的轮永不到达 `turn_finalizer.finalize_turn`（state.db flush 的唯一点），其在飞 tool 轮仅活在内存 `_session_messages`，重启后 `load_transcript()` 静默丢即前轮；现在 shutdown 经 `_flush_messages_to_session_db` flush（幂等 identity-tracked），优雅完成的 agent 重 flush 无操作；resume_pending / fresh-tool-tail 分支 in `_handle_message_with_agent` 已能处理 pending tool result tail；修 #13121；测试 `tests/test_13121_shutdown_inflight_transcript_flush.py` +243 行

### 4.8 `93ea9b0` — 入站媒体下载尺寸封顶防 OOM

- `gateway/platforms/base.py:582-605` config 读 `_max_inbound_media_bytes`
- `gateway/platforms/base.py:684` `cache_image_from_bytes` + `:919` `cache_video_from_bytes`
- **行为**：入站 image/audio/video payload 之前无尺寸限制全 buffer 进程内存再写缓存，Discord Nitro 500MB 大上传或入站消息含远端大文件 URL 可让 RAM 飙升 OOM 杀网关；现 cache_image/audio/video_from_bytes 拒绝超大 + cache_image/audio_from_url stream-check `Content-Length` + 累计；Discord adapter 前置 check `att.size`；可配 `gateway.max_inbound_media_bytes`（128 MiB 默认 / 0 禁）

### 4.9 `bb77a8b` — Windows 未映射 gateway 更新后重生 (#50090 #50373)

- `hermes_cli/main.py:8380` `_pause_windows_gateways_for_update` + `:8438-8483` 未映射快照 loop + `:8520-8545` 重放
- 守 `gateway.status.looks_like_gateway_command_line`（`hermes_cli/main.py:8447-8453` + `hermes_cli/gateway.py:641`）
- **行为**：未映射 Windows gateway 之前更新时被强杀 + 手动重启提示；现 `psutil` argv snapshot 经 `looks_like_gateway_command_line` 严格匹配后经 detached watcher 重放（`unmapped_relaunched += 1`）

### 4.10 `7a131f7` — Stateless HTTP 不再静默承诺 async 交付 (#50319)

- `gateway/platforms/base.py:760` `supports_async_delivery: bool = False`
- `gateway/session_context.py` 新 `async_delivery_supported` + `HERMES_SESSION_ASYNC_DELIVERY` contextvar
- `gateway/platforms/api_server.py:3669` `_bind_api_server_session`（调用点 `:3729 _run_agent` + `:4000 _run_sync`）
- `tools/terminal_tool.py` + `tools/delegate_tool.py` 加 `notify_unsupported` / sync fallback
- **行为**：API-server 路由之前静默 no-op `terminal(notify_on_complete=True)` 和 `delegate_task(background=True)`；现在 per-adapter `supports_async_delivery=False` 经 contextvar 传播；terminal 返 `notify_unsupported` 说明 + 跳 watcher；`delegate_task` 退化为同步执行；两入口经单一 chokepoint 绑定

### 4.11 `e499d69` — 可配并发 run 上限防 DoS (#50007)

- `gateway/platforms/api_server.py:797-836` `_resolve_max_concurrent_runs`
- `:3652` gate 取 `self._max_concurrent_runs`
- `:3853` 文档
- **行为**：硬编码 `_MAX_CONCURRENT_RUNS = 10` 仅在 `/v1/runs` 强；现统一可配上限（`gateway.api_server.max_concurrent_runs`，默 10，0 禁）跨 `/v1/chat/completions` + `/v1/responses` + `/v1/runs`；上限到达返 OpenAI 风格 429 + `Retry-After`

### 4.12 `51a338a` + `0ee7546` + `b577f25` + `4d7bb38` — `active_agents` 轮边界 publish + `/api/status` surface busy/drainable + 三处统一 parse

- **`51a338a`**：`gateway/run.py:3668` `_persist_active_agents` —— 之前仅 lifecycle 转换写 `gateway_state.json`；现轮边界写（claim 入站 `:5267`、claim 启动恢复 `:8445`、`_release_running_agent_state` 释放 `:13585`）；仅传 `active_agents`（其他 `_UNSET`）让 `write_runtime_status` read-merge-write 保留其他字段
- **`0ee7546`**：`gateway/status.py:645` 新 `derive_gateway_busy` + `:670` `derive_gateway_drainable`；消费方 `api_server.py:1150-1174 /health/detailed` + `web_server.py:1867-1923 /api/status` —— `/api/status` 之前无 busy/drainable 信号，现加 `active_agents`/`gateway_busy`/`gateway_drainable`/`restart_drain_timeout`
- **`b577f25`**：`gateway/status.py:624-636` 新 `parse_active_agents` + `web_server.py:154-157 _resolve_restart_drain_timeout` 委托 `hermes_cli.gateway._get_restart_drain_timeout` —— 之前内联 drain-timeout 优先链含空字符串 bug；`/api/status` 钳制而 `/health/detailed` 暴露原始 → 现统一
- **`4d7bb38`**：`gateway/status.py:598 write_runtime_status` 写边钳制经 `parse_active_agents`（非数值不再抛）+ `hermes_cli/gateway.py:4638-4640` draining-status 第三 coercion 站点

### 4.13 关联文档

- `8e4d2fd docs(plugins): document acting from hooks via ctx.profile_name + dispatch_tool (#50352)` —— 配合 §7.1 新 `ctx.profile_name`

---

## 5. Telegram cluster

**9 commit 修 Bot API 10.1 rich messaging 边角 + typing 状态机**：

| # | sha | 文件 | 改动 |
|---|---|---|---|
| 5.1 | `6183e8c` | `hermes_cli/config.py:2161` `"rich_messages": False` 默认 + `plugins/platforms/telegram/adapter.py:500` `_rich_messages_enabled = self._coerce_bool_extra("rich_messages", False)` | rich messages 改默认 off；当前 Telegram 客户端可能渲染 Bot API 10.1 rich 为空/不支持气泡且难以复制为纯文本，对命令片段和移动 handoff 比 legacy MarkdownV2 更差；可经 `platforms.telegram.extra.rich_messages: true` opt-in |
| 5.2 | `ea056b0` | `plugins/platforms/telegram/adapter.py` | CJK 文本绕过 rich：检测 CJK 字符存在时回 legacy MarkdownV2，避免 rich 对中日韩的渲染缺陷 |
| 5.3 | `a966932` | `plugins/platforms/telegram/adapter.py` | 表格 exempt 自 rich newline 硬换行：避免 markdown 表格分行 |
| 5.4 | `31e59fe` | `plugins/platforms/telegram/adapter.py` | slash-command 富文本输出保留换行（#46070），不再 collapse |
| 5.5 | `796f618` | `plugins/platforms/telegram/adapter.py` | chunk 标记移出 code fence：分块标志不再被代码栅栏吞 |
| 5.6 | `29e5e12` | `plugins/platforms/telegram/adapter.py` | 自 native rich echo 恢复 reply 文本：rich 模式下 reply 上下文 hydrate |
| 5.7 | `c1f11f8` | `plugins/platforms/telegram/adapter.py` | 流式 rich final 经 editMessageText 也索引：之前仅 sendRichMessage final 索引 message_id |
| 5.8 | `09a96ba` | `gateway/run.py` | 在流 finalize 前暂停 Telegram typing indicator：finalize 期间气泡仍闪 typing 让用户以为还在写 |
| 5.9 | `565b7c8` | `plugins/platforms/telegram/adapter.py` | 最终回复后 typing indicator 残留修复：发送 stop-typing RPC |

---

## 6. Cron cluster

**6 commit 修调度可靠性**：

| # | sha | 标题 | 改动 |
|---|---|---|---|
| 6.1 | `07424da` | ticker `BaseException` 不死 + 心跳感知状态 | ticker loop 之前 `except Exception` 漏 `BaseException`（KeyboardInterrupt/SystemExit）；改吞 BaseException + 维护 heartbeat 时戳让 `/api/status` 报准确 alive/stuck |
| 6.2 | `f1f36b3` | 迁移时区偏移修复防双触发 | 迁移期间时区偏移计算错误导致同一 cron job 触发两次；修复偏移基底 |
| 6.3 | `65d7c7f` | `action='run'` 立即执行 | 之前 `/api/cron/jobs/<id>` `action='run'` 标记 next_fire = now 后等下次 ticker；现立即执行 |
| 6.4 | `4cc28aa` | Telegram DM-topic cron 经 DeliveryRouter (#22773) | DM-topic 目标之前直接通过 chat-id 投递不知道 topic 路由；改走 `DeliveryRouter` 让 topic 信息保留 |
| 6.5 | `d548908` | 直播适配器交付确认可靠 (#38922 #47056 #43014) | 直播适配器 send 后无 ack/确认 → 失败静默；改加显式 confirmation 步骤 |
| 6.6 | `73b9226` | `model.default` 解析 + 缺 model fail-fast | 之前 missing model 静默回 GPT-4 fallback；现 resolve `model.default` 经显式链 + 缺失抛 |

---

## 7. Session / Agent lifecycle

### 7.1 `9d883ac` — `ctx.profile_name` 无会话 profile 访问 (#50346)

- `hermes_cli/plugins.py` 加 22 行 + `tests/hermes_cli/test_plugins.py` 加 35 行
- **行为**：plugin 之前无从 `PluginContext` 读 active profile name；workaround 用 `ctx._manager._cli_ref` 仅在交互式 CLI 有效，在 gateway 和 kanban-spawned worker（`hermes -p <profile> chat -q ...`）`_cli_ref` 为 None；新 `ctx.profile_name` 包 `hermes_cli.profiles.get_active_profile_name()` 经 HERMES_HOME，在任何执行上下文工作零依赖 `_cli_ref`

### 7.2 `9f67ba1` — `finalize_turn` 三段 cleanup 守 (#50009)

- `agent/turn_finalizer.py:142-168` + `:385-386`
- **行为**：`_save_trajectory`、`_cleanup_task_resources`、`_persist_session` 三段现各自 try/except；失败追加到 `_cleanup_errors` 经 `result["cleanup_errors"]` surface，max_iterations 时部分 `final_response` 总能返

### 7.3 `b17180d` + `9e4fe32` — `AIAgent.close()` finalize SQLite + 后台 review fork 退出

- `agent/agent_init.py:1108` `init_agent` 默 `agent._end_session_on_close = True`
- `run_agent.py:3261-3265` `AIAgent.close()` step 7 调 `session_db.end_session(session_id, "agent_close")` 当 flag True，subagent/cron close 路径不再泄露 `ended_at IS NULL` 行
- `gateway/run.py:4723/:16376` `_should_emit_long_running_notification` + heartbeat loop —— 停 stale `running: delegate_task` 气泡当 executor 完成 / session slot 重绑
- `gateway/run.py:9258` 会话卫生 helper 设 `_end_session_on_close = False`
- `agent/background_review.py:584` `_run_review_in_thread` 设 `review_agent._end_session_on_close = False`，fork（钉父活会话 id）不再 finalize 仍活的父行

### 7.4 `56255f8` — 委派级联不删父会话

- `hermes_state.py:69-95` `_collect_delegate_child_ids()` 用父 id seed `found` 集合 + 过滤返回，循环 `_delegate_from` 链不再把父收为自己的后代

### 7.5 `99233fa` + `c7e8854` — force-quit/signal 持久化

- `cli.py:11702` `HermesCLI._persist_active_session_before_close()` —— flush `agent._session_messages` 经 `agent._persist_session`；`:14432` 在 SQLite `end_session()` 前调
- `tui_gateway/server.py:383-453` `_finalize_session()` —— 调 `agent._persist_session(snapshot, ...)` 优先 `agent._session_messages` 而非 stale `session["history"]`；before close 调 `invoke_hook("on_session_end", interrupted=True, ...)`
- `tui_gateway/entry.py:140-142` `_log_signal()` SIGHUP/SIGTERM 显式 `_shutdown_sessions()` 在 `sys.exit(0)` 前作为 atexit 之上保险

---

## 8. Misc cluster

### 8.1 `c0409a8` — `SendResult.error_kind` 7 类 (#50342)

- `gateway/platforms/base.py` 加 `SEND_ERROR_KINDS` + `classify_send_error()`（too_long / bad_format / forbidden / not_found / rate_limited / transient / unknown）+ `SendResult.error_kind` 可选字段（默 None，完全 backward compat）
- `plugins/platforms/telegram/adapter.py` send() 失败时 populate `error_kind`；`message_too_long` 保留现有 error token + 加 `error_kind='too_long'`
- 纯加：现有 degrade-and-deliver 路径（MarkdownV2→plain-text fallback、overflow split、retry classification）无行为变化；22 新 test + 210 adapter regression test 绿

### 8.2 `2609bcc` — i18n Spanish 完整

- 新文件 `README.es.md`（220 行）+ `CONTRIBUTING.es.md`（602 行）+ `SECURITY.es.md`（322 行）+ `locales/es.yaml` Matrix section 修复 + `README.md` 加 Spanish badge
- 47 i18n test 通过含 catalog key parity + placeholder parity

### 8.3 `65a477f` — Desktop About 加 Update now 按钮 (#50186)

About 面板加 Update now 按钮触发 in-app 更新检查（macOS/Windows/Linux 三平台）

### 8.4 `8666fd7` + 测试 `461fcc0` — 模型可见性对话框保留其他 provider hide-all

跨 provider 模型可见性切换：之前在一 provider 切默认展开会清其他 provider 的 hide-all；现保留

### 8.5 `5e3e89c` — Hindsight embedded daemon 健康 grace 可配 (#50341)

新 config 项控制 hindsight embedded daemon 健康 grace 期 timeout

### 8.6 `7bc6f18` — Hindsight 跳 local_embedded 在 root 下

root 用户运行时跳 local_embedded daemon（典型 Docker 容器场景，否则 daemon 无法 chmod/seal 自身）

### 8.7 `472c068` + 测试 `7b9a0b3` — MCP "unknown method" ping keepalive fallback (#50028)

MCP server 对 ping 方法返 "unknown method" 字面错误时，之前视作连接死并 reconnect；现检测该短语作为 keepalive fallback 信号继续保持连接

### 8.8 `9578e52` + `2a45423` + 测试 `e267237` — Photon 三修

- `9578e52`：探测意外 sidecar 死亡并触发 reconnect
- `2a45423`：Envoy overflow 错误归类为可重试 + 加 typing cooldown 防紧密重试 spam
- 测试覆盖 overflow retry / typing cooldown / sidecar-crash detection

### 8.9 `2f4f23f` — Codex app-server item/started → Telegram tool-progress 桥接 (#38835)

- `agent/codex_runtime.py:28` 新 `_codex_note_to_tool_progress()` —— 把 `item/started` 通知映射到 `(tool_name, preview, args)` 对 command/file/MCP/dynamic tool call
- `agent/codex_runtime.py:263-282` `run_codex_app_server_turn` —— 加 `_on_codex_event` closure 接为 `on_event=` 入 `CodexAppServerSession`，`agent.tool_progress_callback('tool.started', ...)` 现在本路由也触发

### 8.10 `e0498bd` — Bedrock prompt-cache token 计价 (#50307)

- `agent/usage_pricing.py:451-490` `_OFFICIAL_DOCS_PRICING` 四 Bedrock Claude 行加 `cache_read_cost_per_million` (input 的 0.1x) + `cache_write_cost_per_million` (1.25x)
- `:595` 新 `_normalize_bedrock_model_name()` 剥除 `us./global./eu./ap./jp.` 跨区域前缀；`_lookup_official_docs_pricing` (`:648`) 调用让跨区域 profile 命中裸 key 不再 unknown

### 8.11 `a4b1554` — WhatsApp 裸号→JID 规范化

- `gateway/whatsapp_identity.py:77` `to_whatsapp_jid()` —— 把裸号（如 `+50766715226`）转 `50766715226@s.whatsapp.net` JID，`@g.us`/`@lid`/etc 直通
- `plugins/platforms/whatsapp/adapter.py:730/791/826/938/956/1244` `send_message/edit/media/typing/get_chat_info` 和 `_standalone_send` —— 所有出站 bridge call 用 `to_whatsapp_jid()` wrap `chat_id`，Baileys `jidDecode` 不再 crash

### 8.12 `491579f` — WhatsApp bridge dir 经 HERMES_HOME mirror

- `gateway/platforms/whatsapp_common.py:374` `resolve_whatsapp_bridge_dir()` —— 探测安装树写访问，无（如 `/opt/hermes` Docker）时 `shutil.copytree` 镜像 `scripts/whatsapp-bridge` 进 HERMES_HOME
- `hermes_cli/main.py:2530-2531` `cmd_whatsapp` + `plugins/platforms/whatsapp/adapter.py:272-273` `__init__` 走共享 helper

### 8.13 `4467c22` — chat-completions 剥除 timestamp

- `agent/transports/chat_completions.py:175/:205` `ChatCompletionsTransport.convert_messages` —— sanitize trigger 加 `"timestamp" in msg`，per-message strip 加 `msg.pop("timestamp", None)`，严格 OpenAI 兼容 provider 不再 400 on leaked timestamp 字段

### 8.14 `ac83365` + `e74033b` — Windows install 8.3 短 %TEMP% 展长

- `scripts/install.ps1:125-132` —— `foreach ($tmpVar in @('TEMP','TMP'))` 顶层调 `ConvertTo-LongPath` 一次性 re-`Set-Item` `Env:TEMP`/`Env:TMP`；Tee-Object/Node/Electron stages 不再被 `FIRST~1.LAS` 风格路径噎住

### 8.15 `6f5f58e` + `41ba90f` — `poll()` 只读 + CLI drain dedup (#10156)

- `tools/process_registry.py:1038-1046` `poll()` 内移除 `self._completion_consumed.add(session_id)`，status 检查不再压制 gateway/tui watcher 投递
- `gateway/run.py:13213-13216` agent-triggered completion 注入 —— 注释 + `is_completion_consumed` 守对齐
- `tools/process_registry.py:187` 新 `_poll_observed: set`
- `tools/process_registry.py:926/:937/:953` `_drain_should_skip()` + `drain_notifications()` —— CLI drain 跳事件 session ∈ `_completion_consumed ∪ _poll_observed`
- `tools/process_registry.py:1077` `poll()` 退出 `_poll_observed.add(session_id)`；剪枝 `:1433/:1441/:1451-1453`

### 8.16 `3e354b6` — agent 保留 copilot 路由 headers

- `agent/agent_init.py:812` + `:867` 路由客户端和 fallback 客户端 header 捕获 —— 在 `_custom_headers` 和 `_default_headers` 之间插入 `default_headers`（公共属性，被 `copilot_default_headers` at :773 用），Copilot 路由/fallback 客户端不再丢 headers
- `run_agent.py:3576` `_create_httpx_client` —— `api.githubcopilot.com` base URL 短路裸 `httpx.Client()`，避免破 Copilot 路由的 socket-keepalive 选项

### 8.17 `6984026` — browser SSRF 守在容器化终端

- `tools/browser_tool.py:621-643` `_is_local_backend()` —— 加查 `TERMINAL_ENV`；terminal backend 为 docker/modal/daytona/ssh/singularity 时返 False（视为非本地，启 SSRF 守）

### 8.18 `8a506ed` — `load_pool()` 对 env-seeded creds 非破坏

- `agent/credential_pool.py:2065-2087` `_prune_stale_seeded_entries()` —— 新 `prune_env_sources` kwarg（默 True）；`env:*` 行仅在 flag 显式请求时可剪
- `:2196-2200` `load_pool()` 传 `prune_env_sources=False`，缺 `MINIMAX_API_KEY` 的进程不再为其他进程删除持久 `env:MINIMAX_API_KEY` 行

### 8.19 `587b5b9` — backup 捕获 HERMES_HOME 外 memory provider 状态 (#50325)

- `agent/memory_provider.py:299` 新可选 `MemoryProvider.backup_paths()` hook 返额外绝对路径（默 `[]`）
- `hermes_cli/backup.py:132/:135/:357-369/:586-587` —— 新 `_EXTERNAL_PREFIX="_external/"` + `_collect_memory_provider_external_paths()`；backup walk 把 home-anchored 路径归档至 `_external/`；import 经 home-relative traversal 守恢复
- `plugins/memory/honcho/__init__.py:194`、`plugins/memory/hindsight/__init__.py:624`、`plugins/memory/openviking/__init__.py:1681` provider override 声明 `~/.honcho` / `~/.hindsight` / `~/.openviking`

### 8.20 `99f3072` + `1e0b3a2` + `1965d56` — 模型切换 3 修 (#50375 #23767)

- `99f3072`：失败原地 swap 必须 no-op 而非死会话
- `1e0b3a2`：模型切换时重置 stale token calibration
- `1965d56`：工具输出预算按模型上下文窗口缩放

### 8.21 `824c9d3` — `model.api_base` → `model.base_url` 别名 (#50385)

config alias，让旧 `model.api_base` 配置自定义 provider 时仍工作

### 8.22 `f726908` — Desktop/Windows 防 in-app 更新级联进 backend 重启循环 (#50381)

in-app 更新触发 Desktop self-update → backend 检测 binary 变化 → 重启 gateway → 重新触发更新检测；改打破链

### 8.23 `a9c8025` — approval 阻塞 gateway 等候中尊重中断 (#8697)

阻塞 gateway approval wait 期间用户中断（如 ESC）之前被吞；现 honor interrupt 让用户能取消挂起 approval

### 8.24 `f79e0a7` + `b7f6cb9` — Email 配置硬化 (#40715)

- `f79e0a7`：缺 config 标记 non-retryable + 拒绝空白 env vars
- `b7f6cb9`：从 config resolve IMAP/SMTP host 并连接前验证

### 8.25 16+ docs commit

- `defeda8 docs: sync documentation with current implementation`
- `2008a96 docs: align contributor test checklist with wrapper`
- `72e4cca docs(config): correct MCP docs path in cli-config.yaml.example`
- `b1ab5a8 docs(antigravity-cli): add delegation patterns + output/bounding caveats`
- `9f507a0 docs: remove file tools TBD placeholder`
- `225dcf8 docs(.env.example): add HF_BASE_URL placeholder`
- `85f108e test(cron): document consent-first self-learning suggestions`
- `bc85f61 docs: document per-event extra keys in shell-hook wire protocol`
- `c02648c fix(docs): align slash-command and docker docs`
- `98ecd0b docs(mcp): fix stale ~0.75s discovery-wait reference in late-refresh docstring`
- `b337afd docs(cli): fix broken terminal-backend guide link in setup wizard`
- `95d970a docs: sharpen software-development skills`
- `74b5cc7 docs(spotify): document 6-month re-auth cycle + add client-level invalid_grant test`
- `242962e docs(providers): clarify vllm qwen reasoning output`
- `fe5c8d2 fix(docs): document curl, xz-utils, and g++ as Linux prerequisites`
- `fa53e36 docs(hooks): document manual shell hook allowlisting`
- `f80088f docs: add missing Prerequisites/How to Run sections to SKILL.md template`
- `eec9c1d docs(agents): clarify background delegation durability`
- `063155e docs(hooks): document subagent_start plugin hook`
- `df4015b docs: session lifecycle documentation`
- `38756f2 docs(docker): document gateway tool-loop hard stops`
- `cc30e0b docs(config): document auxiliary task fallback_chain`
- `5eb158e docs(hermes-agent skill): document project context files and their discovery rules`
- `97563ab fix: warn on line-oriented newline search patterns`
- `eb9a002 docs: clarify search_files newline regex behavior`
- `6403ed0 docs(session-search): document source-first retrieval limits`
- `1eb2959 docs(.env.example): add missing ELEVENLABS_API_KEY placeholder`
- `46cc034 docs(skills): add hermes-agent verification rule`
- `a9602d2 docs(skill): document context_length auto-detection resolution chain`
- `abfbd61 fix(docs): regenerate skill docs to fix stale cross-links, add tool-search to sidebar`
- `e1a717a docs: add Open Scaffold MCP workflow`
- `f6275a5 docs(contributing): add "search first" guidance to cut duplicate PRs`
- `9e4348f docs(windows): document uv.exe AV false positive`
- `2b08a42 docs(README.zh-CN): update Windows install from 'not supported' to native PowerShell`
- `31bdb60 docs(skills): fix himalaya CLI arg order and download flag`
- `4711936 fix(docs): remove non-existent conversation_entity setting from homeassistant troubleshooting`
- `5d05415 Expand .gitignore example` + `094d9cb Update docs to clarify requirement for gitignore`
- `9bd5003 fix(spotify): quarantine dead tokens on terminal refresh failure`
- `37fa3c5 docs(kanban-worker): document kanban_complete artifacts deliverable param`
- `d7737bf docs(ui-tui): fix file paths, add billing command, update file map`
- `b6f03ab docs(ui-tui): add billing.step_up.verification event + perfPane.tsx to README`

### 8.26 AUTHOR_MAP salvage 10+ 条

`e921c4f devorun`、`f43c616 devsart95`、`d6cb69a sweetcornna`、`35752fc szzhoujiarui-sketch + rayjun`、`d5f0e73 #49544`、`7a8c4fe #48422`、`e5e173e/fcdefb4 docs PR salvage cluster`、`296b290 de1tydev (#10158)`、`7d9f6a2 #48678`、`2213ea9 whatsapp read-only bridge`、`0a2b712 chat-completions timestamp`

### 8.27 其他单次修复

- `c6a0929 Merge` + `ed8f789 Merge` —— 合并 model-switch 二修和 context-aware tool-budget 二修
- `5aec00f Merge` —— gateway busy readout salvage
- `f57ff7a Merge` —— cron tz offset salvage
- `f6a504d Merge` —— cron run immediate salvage
- `3051a16 Merge` —— Telegram DM-topic salvage
- `02a3288 Merge` —— delivery-confirm salvage
- `9f4c0b2 Merge` —— cron ticker liveness salvage
- `8ca38d3 Merge` —— model-visibility cross-provider salvage
- `44d552e Merge` —— model-switch preflight warning salvage
- `4b7f9a4 test(matrix): make voice-detection tests hermetic against mention gating (#49946)`
- `67523fa test(web_server): make profile-wrapper alias test OS-aware`
- `e580706 test(web_server): add integration tests for desktop boot handshake fix`
- `475e81d fix(web_server): use run_in_executor for gateway pre-warm and drain-timeout`
- `6bbacc2 fix(desktop): make cold-start port-announcement deadline tolerant`
- `170ef24 fix(doctor): audit WhatsApp bridge at its resolved (HERMES_HOME) dir (#49890)`
- `c6bf6bd fix(memory): recover from missing old_text on single-op replace/remove (#49997)`

---

## 总结

- **核心变化**：v0.17.0 进入快速维护节奏 —— 单日 179 commit 中 70 fix 集中收敛压缩管线、Kanban 锁/工作器/生命周期、安全旁路、网关 transcript 持久性和媒体处理；16 feat 含 Kanban lifecycle hooks 3 hook + `ctx.profile_name` plugin API + `SendResult.error_kind` 7 类 typed 错误 + i18n Spanish 完整 + 模型切换 preflight 预警 + Telegram rich opt-in default off
- **架构方向**：plugin 生态进一步扩展（Kanban lifecycle hooks 让 plugin 观察任务转换 / `ctx.profile_name` 无会话 profile 访问 / `SendResult.error_kind` 平台中立错误分类）；transcript 持久性补全多个之前漏掉的关闭路径（drain-timeout / force-quit / signal / agent.close）
- **安全姿态**：URL safety IPv6 旁路、WebSocket fail-closed、backup 路径穿越、kanban 密钥泄露、config api_key 屏蔽、file 设备别名硬化 9 commit 同日打补
- **稳定性**：压缩管线 9 commit 解决多个长期 P1（#11978/#11914/#11996/#14690/#23975/#27633/#33618/#33906/#33907/#36908/#46686/#49307）
