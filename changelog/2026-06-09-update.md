---
title: 2026-06-09 跨日同步 — hermes-agent 754154a9c → a5d05cf30（105 commits，~24h；Photon **gRPC-native iMessage** 收官 + **Plugins Hub TUI overlay** + **Anthropic 默认现代 thinking 契约** + **Fable 5 全链路加入** + **max_concurrent_sessions cap** + **Desktop sleep/wake + websocket reconnect 恢复** + **TUI subprocess stdin EOF 全面修补**）
created: 2026-06-09
type: changelog
tags: [changelog, update, v0.16.0-maintenance, photon-grpc-native, photon-spectrum-ts-1.18.0, photon-attachment-inline, photon-voice, photon-cve-overrides, photon-spectrum-builders, photon-home-channel-e164, photon-token-double-validate, photon-spectrum-backend-migration, simplex-1.1.0, simplex-groups, simplex-auto-accept, simplex-text-batching, plugins-hub-tui-overlay, plugins-manage-rpc, plugins-subdir-install, plugins-optional-mcps-packaging, anthropic-default-modern-thinking, anthropic-fable-5, openrouter-anthropic-reasoning-mandatory, openrouter-live-context-length, models-nous-recommended-disk-cache, models-laguna-m1, models-nemotron-3-ultra, models-catalog-disk-seed, langfuse-sanitized-response-usage, nemo-relay-plugins-toml-lifecycle, nemo-relay-tool-parallelism-mode, nemo-relay-downstream-preserve, observability-direct-fallback-preserve, desktop-rebind-after-reconnect, desktop-sleep-wake-recovery, desktop-file-attach-rpc, desktop-electron-mirror-fallback, desktop-profile-delete-backend-release, desktop-recents-per-platform-sections, desktop-orderByIds-prepend-fresh, desktop-route-resume-self-heal, desktop-background-stream, desktop-tsconfig-es2023, desktop-debug-report-explicit-urls, desktop-icon-padded, tui-fallback-chain-canonical, tui-terminal-config-bridge, tui-subprocess-stdin-devnull, tui-subprocess-stdin-guard, tui-oauth-setup-token-inherit-stdin, hermes-ink-mouse-tracking-teardown, cli-native-windows-confirm-modal, cli-portal-url-persist, cli-public-url-from-redirect-uri, gateway-image-generate-auto-deliver, gateway-terminal-fenced-block, gateway-markdownv2-edit-finalize, telegram-strip-mdv2-bold-order, dashboard-register-idempotent, sessions-max-concurrent-cap, active-sessions-lease-registry, agent-char-based-output-cap, codex-app-server-token-usage, codex-runtime-canonical-usage, api-server-session-context-bind, terminal-sane-path-completion-posix, terminal-config-env-map, yuanbao-quote-media-middleware, yuanbao-patch-anchors-middleware, skills-hub-rate-limit-fail-loud, skills-hub-write-after-health-check, doctor-custom-vendor-slugs, doctor-named-custom-aggregator, deps-urllib3-pyjwt-cve, deps-packaging-core-dep, pytest-timeout-thread-method, nix-skip-fix-lockfiles, windows-localappdata-data-dir, readme-website-links-seo]
sources:
  - hermes-agent/main 754154a9c..a5d05cf30
total_commits: "105 个 commit（97 个 non-merge + 8 个 merge 节点；约 24h 单日窗口）"
---

# 2026-06-09 跨日同步：hermes-agent main `754154a9c → a5d05cf30`

> **基线**：wiki `853db95` 对齐 hermes `754154a9c2faaff9e00932fa3c9e32b3ed936fb4`（2026-06-08T13:32 -0700，"fix(tests): retry per-file pytest subprocess once on exit-4 when the file exists"，v0.16.0）
> **目标**：hermes-agent/main HEAD `a5d05cf30e8f5d79689ff8cee3ed806a6ca1c00e`（2026-06-09T16:47 -0400，"fix(nix); don't run .#fix-lockfiles"）
> **跨度**：约 27 小时单日窗口（author dates 2026-06-07T00:06 +0900 → 2026-06-09T16:47 -0400，含数条 historic salvage / merge intermediate）
> **版本**：仍 **v0.16.0**（`pyproject.toml:10 version = "0.16.0"` + `hermes_cli/__init__.py:17-18 __version__ = "0.16.0"` + `__release_date__ = "2026.6.5"` 不变；本日全为 feat/fix/test/refactor/chore/docs，未发新版）
> **commit 类型分布**：约 **56 fix / 17 feat / 11 test / 5 chore / 3 refactor / 2 docs / 1 ci / 1 style / 8 merge / 1 misc**
> **验证基线**：`/tmp/hermes-agent` blobless clone + `git checkout -f a5d05cf30` 全 checkout @ HEAD，所有 commit hash / file:line / 路径 / 函数名经 `grep -n` / `git show <sha> --stat` / `Read` 实证

---

## 章节索引

1. [Photon iMessage — gRPC-native 收官 + 20 commit 后续硬化（架构主轴）](#1-photon-grpc-native)
2. [Photon iMessage — 入站/出站附件全打通 + 语音](#2-photon-attachments)
3. [Photon iMessage — spectrum-ts API 收敛（4 commit）](#3-photon-spectrum-builders)
4. [Photon iMessage — Home channel / E.164 寻址 + Setup 体验 + Spectrum backend 迁移](#4-photon-setup-and-addressing)
5. [SimpleX — 1.1.0 大演进：群组 + 原生附件 + 文字批处理 + auto-accept](#5-simplex-1-1-0)
6. [Plugins — TUI Plugins Hub overlay + `/plugins` 改写 + subdir install + optional-mcps 打包修](#6-plugins-cluster)
7. [Anthropic — 默认现代 thinking 契约 + Fable 5 全链路 + OpenRouter reasoning-mandatory 守护](#7-anthropic-and-models)
8. [Models / OpenRouter — 实时 context_length + Nous 推荐磁盘缓存 + laguna-m.1 / nemotron-3-ultra + catalog disk seed](#8-models-curated)
9. [Observability — Langfuse sanitized response usage 恢复 + NeMo-Relay plugins.toml lifecycle 四连](#9-observability-cluster)
10. [Desktop — websocket reconnect 重绑 + sleep/wake 恢复 + Electron mirror fallback + per-profile 后端释放](#10-desktop-recover-cluster)
11. [Desktop — file.attach RPC（远程网关非 image 附件）+ 后台流式 + per-platform 边栏 + ES2023 + 调试报告显式 URL](#11-desktop-features-cluster)
12. [TUI / CLI — fallback chain canonical + terminal config env bridge + native Windows confirm modal + stdin EOF 全面修](#12-tui-cli-cluster)
13. [hermes-ink — 终端 raw-mode 退出时关 mouse tracking 防 SGR flood](#13-hermes-ink)
14. [Gateway-UI — image_generate auto-deliver + bare fenced code blocks + MarkdownV2 edit finalize](#14-gateway-ui-cluster)
15. [Sessions / Dashboard — max_concurrent_sessions cap + dashboard register idempotent](#15-sessions-dashboard)
16. [Agent / Codex / API — char-based output-cap overflow + Codex app-server token usage + api-server session ctx 绑定](#16-agent-codex-api)
17. [Terminal — POSIX `_SANE_PATH` 完整化 + TERMINAL_CONFIG_ENV_MAP test 锚定](#17-terminal-cluster)
18. [Yuanbao — 媒体解析三阶段 middleware 拆分（QuoteContext / MediaResolve / PatchAnchors）](#18-yuanbao)
19. [Skills-hub — 退化 index fail-loud + GitHub 429 识别 + ClaudeMarketplace 持久 GitHubSource](#19-skills-hub)
20. [Doctor — 命名 custom provider vendor-slug 接受 + aggregator 识别](#20-doctor)
21. [Deps / Packaging / Nix / Install / Docs — urllib3/PyJWT CVE + packaging 核心依赖 + pytest thread-timeout + Windows %LOCALAPPDATA% + Nix skip fix-lockfiles + SEO 链接](#21-deps-and-misc)
22. [Tests / Author maps / Merge nodes](#22-tests-author-merges)
23. [验证基线](#23-verification)

---

<a id="1-photon-grpc-native"></a>
## 1. Photon iMessage — gRPC-native 收官 + 20 commit 后续硬化（架构主轴）

上一日（`2026-06-08-update`）已铺基 Photon Spectrum 平台插件初始落地（aiohttp webhook receiver + Node sidecar outbound + HMAC-SHA256 验签 + CodeQL 清零）。本日把它**整体推到 gRPC-native 持久连接形态**——零 webhook、零公网 URL、零签名密钥，与 Discord/Slack 同款。

### 1.1 `4e4d27875` `feat(photon): gRPC-native iMessage channel (no webhook)` — 主轴 commit

`2026-06-08T16:03 -0700`，1317+/1170−，跨 12 文件。

**删除：aiohttp webhook receiver + 验签函数 + 5 个 env + 3 个 CLI 子命令 + 95 行测试**

- `_start_webhook_server` / `_stop_webhook_server` 整段块 + `_handle_webhook` / `web.Application` / `web.AppRunner` 全删
- HMAC-SHA256 验签 `verify_signature(body, timestamp_header, signature_header, signing_secret)` 删除（旧实现验 `X-Spectrum-Signature: v0={hex}` 对 `v0:{ts}:{body}`，±5min drift）
- 常量 `_DEFAULT_WEBHOOK_PORT=8788 / _DEFAULT_WEBHOOK_PATH=/photon/webhook / _DEFAULT_WEBHOOK_BIND=0.0.0.0 / _TIMESTAMP_DRIFT_SECONDS=300` 全删
- `plugin.yaml` 中 `PHOTON_WEBHOOK_SECRET / PHOTON_WEBHOOK_PORT / PHOTON_WEBHOOK_PATH / PHOTON_WEBHOOK_BIND / PHOTON_API_HOST` 删除
- CLI `webhook register/list/delete` 三个子命令整段删除（commit 注释：*there is no webhook to register, so there are no webhook subcommands*）
- 测试 `tests/plugins/platforms/photon/test_signature.py`（95 行）整文件删除
- 旧 `_dispatch_inbound` 中靠 `";+;"` 字符串嗅探 chat_type 的逻辑删除（改由 sidecar 显式传 `space.type`）

**新增：入站 gRPC 持久流 — Python 端**

- `plugins/platforms/photon/adapter.py:158 class PhotonAdapter` docstring 重写：*"Bidirectional bridge to Photon Spectrum via the Node spectrum-ts sidecar. Inbound: consume the sidecar's `/inbound` gRPC stream. Outbound: loopback POSTs to the sidecar's control channel."*
- 新增方法 `_inbound_loop`、`_on_inbound_line`，由 `connect()` 拉起 background task：`self._inbound_task = asyncio.get_event_loop().create_task(self._inbound_loop())`
- `_inbound_loop`：长连接 `GET http://127.0.0.1:8789/inbound` + `X-Hermes-Sidecar-Token` + `timeout=None`，空行视为 heartbeat，`backoff = min(backoff*2, 30.0)` 指数退避重连
- `_dispatch_inbound(event)` 重写消费 sidecar 输出的统一事件 schema（adapter.py:432 docstring 完整给出）
- 新增 dedup 注释：*"The gRPC stream is at-least-once, and a sidecar reconnect can replay"*（adapter.py:80）；`_DEDUP_MAX_SIZE = 4000 / _DEDUP_WINDOW_SECONDS = 48 * 3600`

**新增：入站推送 — sidecar Node 端**（`plugins/platforms/photon/sidecar/index.mjs`）

- 顶部 banner 重写（index.mjs:1-40）：*"Spawned by `plugins/platforms/photon/adapter.py` to bridge BOTH directions of messaging to Photon's Spectrum platform via the `spectrum-ts` SDK"*
- 新协议端点 `GET /inbound -> 200 NDJSON stream; one JSON event per line, blank lines are heartbeats. One consumer at a time.`
- consumer 状态机：`consumerRes` / `consumerWaiters` / `waitForConsumer() / setConsumer() / clearConsumer()` / `deliver(line)` 阻塞写直到 consumer 接上（`while no consumer is attached so a backlog isn't pulled-and-lost before the gateway connects`）
- `handleInbound(req, res)` — `Content-Type: application/x-ndjson`，25s heartbeat（`setInterval(() => res.write("\n"), 25000)`），fresh connection 顶掉旧 consumer
- `normalizeContent(content)` 把 spectrum-ts 内部对象 → 干净 JSON
- `normalizeEvent(space, message)` → `{ messageId, platform, space: {id, type, phone}, sender: {id}, content, timestamp }`
- 主循环：`for await (const [space, message] of app.messages)` 过滤 `message.direction !== "inbound"`（自己 outbound 的回声），其余 deliver

**管理面 / 凭证转向**

- `cli.py` setup 从 4 步变 5 步（HEAD `cli.py` 步骤标号 `[1/5]..[5/5]`；commit 注释 *CLI: 6-step setup, webhook subcommands removed*；其后 `e9b26c7c8 + 84e4b4b9a` 调整最终为 5 步）
- 新增 `photon_auth.find_project_by_name() / ensure_spectrum_enabled() / regenerate_project_secret() / register_user_if_absent() / get_imessage_line() / DEFAULT_PROJECT_NAME`
- `project_id` 语义改写：「`project_id` 是项目的 `spectrumProjectId`，不是 dashboard id」 — `adapter.py:172-173` 注释明确：*"project_id here is the project's spectrumProjectId — the value the spectrum-ts SDK authenticates with"*
- `store_project_credentials()` 持久化到 `~/.hermes/.env`（`PHOTON_PROJECT_ID = spectrumProjectId, PHOTON_PROJECT_SECRET`，*runtime creds persist to ~/.hermes/.env like every other channel*）+ `~/.hermes/auth.json` 下 `credential_pool.photon_project`（dashboard id, spectrum id, name）
- `plugin.yaml:4` 版本 `0.1.0 → 0.2.0`
- `plugin.yaml` 描述重写：*"Both directions run over the `spectrum-ts` SDK's long-lived gRPC stream via a small supervised Node sidecar — inbound messages arrive on the SDK's `app.messages` stream (no webhook, no public URL, no signing secret)"*
- `sidecar/package.json` `dependencies.spectrum-ts: ^1.17.1`（首次绑定 1.x 主版本，后续 `9fb83eaa2` 升 ^1.18.0）

### 1.2 `92dfd70d6 fix(photon): production hardening for the gRPC-native iMessage channel (#42732)` — 生产硬化六件套

`2026-06-09T16:12 +0100` Philip D'Souza

**CVE override**（`sidecar/package.json:17-23`）：
```json
"overrides": {
  "protobufjs": "8.6.1",
  "@opentelemetry/otlp-transformer": "0.218.0",
  "@opentelemetry/otlp-exporter-base": "0.218.0",
  "@opentelemetry/exporter-trace-otlp-http": "0.218.0",
  "@opentelemetry/exporter-logs-otlp-http": "0.218.0"
}
```
commit message：`npm audit` 标 7 high-severity 传递依赖 CVE（**GHSA-66ff-xgx4-vchm** protobufjs code injection + 过期 @opentelemetry OTLP exporter），来自 `spectrum-ts -> @photon-ai/otel`；npm 建议的修复会把 spectrum-ts 降级到指向已下线 spectrum host 的版本 — 改 `overrides` 钉补丁版本，不动 spectrum-ts。最终 `npm audit -> 0`。

**Constant-time token compare**（`sidecar/index.mjs:415-421`）：
```javascript
const _tokenBuf = Buffer.from(sharedToken);
function tokenOk(header) {
  if (typeof header !== "string") return false;
  const h = Buffer.from(header);
  return h.length === _tokenBuf.length && crypto.timingSafeEqual(h, _tokenBuf);
}
```
替换 `req.headers["x-hermes-sidecar-token"] !== sharedToken`（commit *"was `!==`, timing-attackable"*）。新增 `import crypto from "node:crypto"`（line 43）。

**Body size cap**（`sidecar/index.mjs:297-309`）：`const MAX_BODY_BYTES = 2 * 1024 * 1024;` 在 `readBody(req)` 中累计 `size += chunk.length`，超 2 MiB `req.destroy()` 抛 `"request body too large"`。

**Re-subscribe loop with capped exponential backoff + jitter**（`sidecar/index.mjs:258-289`）：把原来一锤子 `for await (const [space, message] of app.messages)` 包进 `for (;;) { try {...} catch (e) {...} await new Promise(r => setTimeout(r, backoff + Math.random()*backoff*0.2)); backoff = Math.min(backoff*2, 30000); }`。健康流量到达时 `backoff = 1000` 重置（line 267）。

**`process.on("unhandledRejection")`** 处理器（`sidecar/index.mjs:537-542`）。

**Dedup hard size bound**（`adapter.py:415-430`）：原实现仅在 `len > _DEDUP_MAX_SIZE` 时按过期时间裁剪 — *"a burst of unique ids within the window grew it without limit"*。新实现按 insertion order 严格淘汰：
```python
if msg_id in seen:
    del seen[msg_id]  # refresh insertion order
seen[msg_id] = now
if len(seen) > _DEDUP_MAX_SIZE:
    for old in list(seen.keys())[: len(seen) - _DEDUP_MAX_SIZE]:
        del seen[old]
```

### 1.3 `b3aef57f2 refactor(photon): use TYPE_CHECKING for httpx import and fix client ref`

`adapter.py:42-54`：`try/except ImportError` 包 httpx 改为 `TYPE_CHECKING` 双分支——类型检查器看到 httpx 永远在场，每个 use site 类型干净；运行时仍可在缺 httpx 时 fail-soft。同 commit 在 `connect()` 中把 `await self._http_client.aclose()` 改为捕获局部 `client` 引用以避免在错误路径上 `self._http_client = None` 后 race。同时把面向用户的标签从 `"Photon iMessage"` 改为 `"iMessage via Photon"`（`adapter.py:914` + `plugin.yaml:2`）。

### 1.4 `9fb83eaa2 fix(photon): bump spectrum-ts to ^1.18.0 and always install latest on setup`

`sidecar/package.json:15` `spectrum-ts: ^1.17.1 → ^1.18.0` + `cli.py:339` `_install_sidecar()` 把 `npm install` 改为 `npm install spectrum-ts@latest` — commit *"a plain `npm install` would stay pinned to whatever the committed lockfile already resolved"*。

---

<a id="2-photon-attachments"></a>
## 2. Photon iMessage — 入站/出站附件全打通 + 语音

### 2.1 `314af28e8 feat(photon): download and inline inbound attachments`

把入站从「附件只能给出元数据 marker」升级为「字节 base64 inline 全送到 Hermes，cache 到 image/audio/document 缓存，模型直接看到媒体」。

- **sidecar 端**（`sidecar/index.mjs:57-58`）：新常量 `MAX_INLINE_ATTACHMENT_BYTES = Number(process.env.PHOTON_MAX_INLINE_ATTACHMENT_BYTES) || 20 * 1024 * 1024`
- `normalizeContent()`（`index.mjs:219-230`）拆分 text/attachment/voice 三分支；attachment/voice 调 `normalizeBinaryContent()`
- `normalizeBinaryContent()`（`index.mjs:168-217`）：调 `content.read()` 拿 Buffer → `Buffer.from(buf).toString("base64")` 内联到 `meta.data` + `meta.encoding = "base64"`；over-cap 只发 metadata；read fail log 后继续，**永不破坏 inbound loop**
- **Python 端**（`adapter.py`）：`_dispatch_inbound()` 新增 `media_urls: List[str] = []` + `media_types: List[str] = []`，attachment 分支调 `_cache_inbound_attachment(content, name, mime)` 拿回本地路径；text 设为短 marker `"(attachment)"`；cache 失败回落到 `[Photon attachment received: {name} ({mime})]` marker
- 新函数 `_cache_inbound_attachment`（`adapter.py:977-1026`）按 mime 路由到 `cache_image_from_bytes / cache_audio_from_bytes / cache_document_from_bytes`；image cache 抛 `ValueError`（HEIC magic 等）则降级到 document cache
- 新 mime-ext 映射常量 `_IMAGE_EXT_BY_MIME` + `_AUDIO_EXT_BY_MIME` — mirrors BlueBubbles iMessage channel
- `MessageEvent` 新增 `media_urls=media_urls, media_types=media_types` 传给 gateway 的 image-routing
- `cli.py` 同时引入 `from hermes_cli.colors import Colors, color`，删除两个交互 first_name/email prompt

### 2.2 `dbf2470d4 feat(photon): Add voice message support to Photon adapter`

`2026-06-08T21:29 -0700` underthestars-zhy

- `sidecar/index.mjs:219-230`：`normalizeContent` 接受 `content.type === "attachment" || content.type === "voice"`；voice 把 `content.duration` 透传到 `meta.duration`（line 176-178）
- `adapter.py:486-517`：`ctype in {"attachment", "voice"}` 分支，`is_voice = ctype == "voice"`，`mtype = MessageType.VOICE if is_voice else _attachment_message_type(mime)`，name 默认 `"voice"`，cached 时 `text = "(voice)"`，fallback marker 形如 `[Photon voice received: <name> (<mime>, duration: <s>s)]`
- `_cache_inbound_attachment` 新增 `force_audio: bool = False` kwarg（`adapter.py:976`）：voice 类型时无论 mime 都走 audio cache，扩展名默认 `.m4a`
- 当 mime 缺失时 voice fallback `media_types` 用 `audio/mp4`（`adapter.py:497`）

### 2.3 `4615e08d3 feat(photon): wire outbound media via spectrum-ts attachment() (#42397)`

Photon 服务端打开了 attachment send（Ray Sun / photon-nousresearch），插件追上 BlueBubbles iMessage 出站媒体能力。

- **sidecar**（`sidecar/index.mjs:454-488`）：新 `/send-attachment` 端点，构 builder：
  ```javascript
  const opts = {};
  if (name) opts.name = name;
  if (mimeType) opts.mimeType = mimeType;
  const builder = kind === "voice"
    ? voice(path, Object.keys(opts).length ? opts : undefined)
    : attachment(path, Object.keys(opts).length ? opts : undefined);
  const result = await space.send(builder);
  ```
  spectrum-ts 会从扩展名推导 name/mimeType，所以只在 Hermes 显式传时覆盖（*"so a known-good inference isn't clobbered with an empty string"*）。caption 走单独 `space.send(spectrumText(caption))` 跟在媒体之后（*"iMessage delivers the caption as a separate bubble"*）。
- **adapter**（`adapter.py:676-759`）：override `send_image / send_image_file / send_voice / send_video / send_document / send_animation` 六个方法
  - URL 类（`send_image / send_animation`）先 `cache_image_from_url` 落地再交 sidecar；URL fetch 失败回退到 super 文本发送
  - File 类直接调 `_sidecar_send_attachment(chat_id, path, ...)`
  - `send_voice` 传 `kind="voice"`
  - `send_animation` 委托 `send_image`（*"iMessage renders GIFs inline as ordinary image attachments"*）
- 新私有方法 `_sidecar_send_attachment`：**defense-in-depth** 调 `BasePlatformAdapter.validate_media_delivery_path(str(path))` 重新校验路径；`mimetypes.guess_type` 推 mime；POST `/send-attachment`
- **cron** `_standalone_send`（`adapter.py:1034-1107`）：原先只发文本，新版按 `media_files: List[Tuple[path, is_voice]]` 逐个 POST `/send-attachment`，每条做 `validate_media_delivery_path` 校验；`is_voice` 决定 `kind`；text 先发以保持对话顺序

---

<a id="3-photon-spectrum-builders"></a>
## 3. Photon iMessage — spectrum-ts API 收敛（4 commit）

四个串行 commit 把 sidecar 从「直接 `space.send(string)` + 自己的 reply 协议」迁到 spectrum-ts 文档化的 content builder。

### 3.1 `fdf48c63c fix(photon): wrap text sends with spectrumText helper`

`sidecar/index.mjs` 顶部 import 加 `text: spectrumText`；`space.send(text)` → `space.send(spectrumText(text))`，caption fallback 也用 `spectrumText(caption)`。

### 3.2 `e79e44af7 fix(photon): use spectrum-ts reply builder for threaded messages`

import 加 `reply: spectrumReply`。新增 helper `maybeReplyContent(space, builder, replyTo)`：调 `space.getMessage(replyTo)` 拿到 target，包成 `spectrumReply(builder, target)`；fallback 到普通 send。`/send` 和 `/send-attachment` 都改用 `maybeReplyContent`，替换原来的 `space.send(builder, { replyTo })` 选项形式——后者在 spectrum-ts 中并非正确 API。

### 3.3 `0d25cae04 fix(photon): remove reply-to support and fix typing API`

走一步退两步：实测 spectrum-ts 的 reply 在 iMessage 路径不可靠，干脆全删 `replyTo` 参数。`adapter.py` 的 `send / send_image / send_image_file / send_voice / send_video / send_document` 全部去掉 `reply_to=` 透传（保留参数但忽略）；`_sidecar_send` 签名 `(self, space_id, text)`；request body 不再含 `replyTo`。sidecar 同样删 `maybeReplyContent`、`/send` body 缩到 `{spaceId, text}`、`/send-attachment` 缩到 `{spaceId, path, name, mimeType, caption, kind}`。

同时修正 typing API：原实现按 `space.startTyping / space.typing / space.setTyping` 三套试探，新实现统一走 `space.send(spectrumTyping(state))`，import 加 `typing: spectrumTyping`，端点 body `{spaceId, state: "start" | "stop"}`，新增 `stop_typing(chat_id)` Python 方法（`adapter.py:769-775`）。

### 3.4 `2130ef68b fix(photon): Enable group flattening in Spectrum config`

`sidecar/index.mjs:96`：`Spectrum({ projectId, projectSecret, providers: [imessage.config()], options: { flattenGroups: true } })` — 一行，把群组结构展平以让 inbound 事件直接出现，不嵌套到 group root。

---

<a id="4-photon-setup-and-addressing"></a>
## 4. Photon iMessage — Home channel / E.164 寻址 + Setup 体验 + Spectrum backend 迁移

### 4.1 `064665688 fix(photon): support E.164 and DM GUID targets for home channel`

允许 `PHOTON_HOME_CHANNEL` 接受三种形式：Spectrum space id / bare E.164（`+15551234567`）/ Photon DM chat GUID（`any;-;+15551234567`）。

- `sidecar/index.mjs:59-61`：
  ```javascript
  const DM_CHAT_GUID_RE = /^any;-;(\+\d{6,})$/;
  const E164_RE = /^\+\d{6,}$/;
  const MAX_KNOWN_SPACES = 2048;
  ```
- `knownSpaces` Map（line 105）+ `rememberKnownSpace(id, space)` / `phoneTargetFromSpaceId(spaceId)` / `rememberInboundSpace(space, message)` 三个 helper（line 107-132）：inbound message 到达时把 `space.id` / `message.space.id` / 推得的 phone 三套 key 全 cache（LRU 上界 2048）
- `resolveSpace`（`index.mjs:378-413`）：先 `knownSpaces.get(spaceId)` cache hit 直接返回；否则若是 E.164/DM-GUID 走 `imessage(app).user(phone) → im.space(user)`；都不行 throw（注释明确：*"spectrum-ts exposes no API to rehydrate an arbitrary opaque space id"*）
- `plugin.yaml:65-66`：`PHOTON_HOME_CHANNEL` 描述从 `"Default Spectrum space id for cron / notification delivery"` 改为 `"Default Photon target for cron / notification delivery: Spectrum space id, DM GUID, or bare E.164 phone number"`
- `tools/send_message_tool.py:41`：`_PHONE_PLATFORMS = frozenset({"photon", "signal", "sms", "whatsapp"})` —— 加 photon 让 `send_message` 把 E.164 字符串当作明确目标而非 channel-name lookup

### 4.2 `92179352f feat(photon): auto-configure allowlist and cron channel on setup`

`cli.py:269-293` 新增 `_autoconfigure_access(phone: str)`：setup 注册用户后调用 — 若 `PHOTON_ALLOWED_USERS` / `PHOTON_HOME_CHANNEL` 未设，自动写入用户的 E.164（已设则跳过）。借 `hermes_cli.config.get_env_value / save_env_value`。Commit 解释：*"otherwise the gateway denies the operator's own inbound messages ('Unauthorized user') and has no default space for cron delivery"*。同时在 sidecar `resolveSpace` 中首次加入 bare E.164 → DM space 的路径（被后续 `064665688` 扩展为含 GUID）。

### 4.3 `3b983e779 fix(photon): add home channel env seed and simplify space resolution`

`adapter.py:136-152`：`_env_enablement()` 在已有 `{project_id, project_secret}` 基础上新增 home_channel seed：
```python
home = os.getenv("PHOTON_HOME_CHANNEL", "").strip()
if home:
    seed["home_channel"] = {
        "chat_id": home,
        "name": os.getenv("PHOTON_HOME_CHANNEL_NAME", "Home"),
    }
```
docstring：*"The special `home_channel` key is handled by the core plugin hook and becomes a proper `HomeChannel` on `PlatformConfig`"*。

sidecar 的 `resolveSpace` 同时大幅简化——去掉对 `app.space / app.spaces.get / im.space({id})` 多种 fallback shape 的试探，只保留 `knownSpaces` cache + phone-target 路径；group space 若 cache 丢了只能等下一条 inbound message re-warm（`index.mjs:405-412` 注释明示这一约束）。`space.send` 返回值简化为 `result?.id || null`。`sidecar/package-lock.json` 新增 1781 行（首次完整 lockfile 提交）。

### 4.4 `e9b26c7c8 style(photon): Colorize iMessage number box in setup output`

`cli.py:234-240` 把 setup 末尾的「Your agent's iMessage number」框装上 `Colors.GREEN`，号码本身 `Colors.GREEN + Colors.BOLD`。

### 4.5 `84e4b4b9a fix(photon): use per-user assigned line for agent iMessage number`

shared-line plan 上 `/lines` 没专属条目，号码真正来源是 user object 的 `assignedPhoneNumber`（dashboard "TEXTS ON" 列）。

- `auth.py:830-842`：新函数 `user_assigned_line(user)` —— 返回 `user["assignedPhoneNumber"]` 或 None
- `cli.py:182-231` 重构 setup 步骤 4/5：先 `agent_number = photon_auth.user_assigned_line(user)`；只在缺失时才回落到 `photon_auth.get_imessage_line(token, dashboard_id)`
- 删除 `line.get("status")` 输出（原来打印 `(active)` 等状态）

### 4.6 `b58ff9345 feat(photon): persist and display user phone numbers in status`

把 operator 号码 + assigned line 持久化到 `~/.hermes/auth.json` 下 `credential_pool.photon_user`，让 `hermes photon status` 离线时也能展示。

- `auth.py:211-232`：`store_user_numbers(*, phone_number, assigned_phone_number, user_id, dashboard_project_id)`
- `auth.py:845-862`：`load_user_numbers() -> Tuple[Optional[str], Optional[str]]`
- `auth.py:865-911`：`refresh_user_numbers(project_id, project_secret)` —— 从 dashboard 拉新值，不 provision
- `auth.py:914-928`：`_configured_operator_phone()` —— 缺持久化时从 `PHOTON_HOME_CHANNEL` / `PHOTON_ALLOWED_USERS` 推断 E.164
- `auth.py:print_credential_summary` 加两行：`my number: <phone>` + `assigned number: <assigned>`
- `cli.py:243-252`：setup 完成时调 `store_user_numbers`
- `cli.py:309-319`：`_refresh_status_numbers()` — `status` 子命令入口先调用

### 4.7 `033765890 fix(photon): migrate user API calls to Spectrum backend`

把 `list_users / find_user_by_phone / create_user / register_user_if_absent / refresh_user_numbers` 全部从 Dashboard API（Bearer token）迁到 Spectrum API（Basic auth：`base64(project_id:project_secret)`）。

- `auth.py:71` 新常量 `DEFAULT_SPECTRUM_HOST = "https://spectrum.photon.codes"`
- `auth.py:278-279` `_spectrum_host()` 解析（env `PHOTON_SPECTRUM_HOST`）
- `auth.py:286-288` `_basic(project_id, project_secret)` —— 构 `Authorization: Basic <base64>`
- `auth.py:291-312` `_response_error_detail(resp)` + `_raise_for_status(resp, action)` 结构化错误
- `auth.py:737-746` `list_users` — `GET {spectrum_host}/projects/{id}/users/` Basic auth
- `auth.py:747-755` `find_user_by_phone`
- `auth.py:758-794` `create_user` — `POST` Spectrum；body 加 `"type": "shared"`；`send_invite=True` 时 log debug *"send_invite is ignored by Spectrum shared-user creation"*
- `cli.py:196-201` 调用签名相应改为 `register_user_if_absent(spectrum_id, secret, ...)` 不再传 dashboard token
- `plugin.yaml:49-52` 新增可选 env `PHOTON_SPECTRUM_HOST`

### 4.8 `37561c214 fix(photon): use allowlisted device client_id + validate token before save`（Ray Sun，salvaged from #34467 by @yanxue06）

两件事：

**device client_id 切换**：Hosted Photon 在 device-code 端点上 allowlist 注册过的客户端，未注册的 `client_id` 直接 `400 invalid_client`。`auth.py:68-69`：
```python
DEFAULT_CLIENT_ID = "photon-cli"
DEFAULT_SCOPE = "openid profile email"
```
替换原 `"hermes-agent"`。Commit：*"Verified live: request_device_code() now returns 200 + a valid user_code where 'hermes-agent' returned 400 invalid_client."*

**Token 双重校验**：device-flow 回的 token 可能过 Better Auth session lookup 但被 project API 拒。新增：
- `auth.py:57-58` `class PhotonDashboardAuthError(RuntimeError)`
- `auth.py:170-175` `@dataclass(frozen=True) class _DeviceTokenCandidate(source, token)`
- `auth.py:426-456` `_device_response_token_candidates(body, *, headers)` —— 同时尝试 `access_token / accessToken / session.access_token / data.access_token / data.accessToken / set-auth-token header`（去重、`Bearer ` 前缀清洗）
- `auth.py:499-528` `validate_photon_token(token)` —— 先打 `/api/auth/get-session`（拿到 user 对象才通过），再打 `/api/projects/`（401/403 抛 `PhotonDashboardAuthError`），两者都过才认
- `auth.py:531-560` `_validated_dashboard_token(candidates)` —— 遍历 candidates 试 validate
- `auth.py:589-597`（`login_device_flow` 中）poll 拿到 token 后包成 candidate，调 `_validated_dashboard_token` 验证，验过才 `store_photon_token`

### 4.9 `637cf94be fix(photon): strip markdown and add send retry logic`

`adapter.py:63`：顶部 `from gateway.platforms.helpers import strip_markdown`
`adapter.py:666`：`send()` 改 `return await self._sidecar_send(chat_id, self.format_message(content))`
`adapter.py:785-786`：`format_message(content) -> str: return strip_markdown(content)` —— iMessage 是纯文本
`adapter.py:788-852` `_send_with_retry(chat_id, content, reply_to, metadata, max_retries=2, base_delay=2.0)`：失败时 `is_network = result.retryable or self._is_retryable_error(error_str)` 判定；指数 `delay = base_delay * (2 ** (attempt - 1))`；重试 `max_retries` 次后 fallback 发一次 `text[:MAX_MESSAGE_LENGTH]` 截断版

### 4.10 累计：Photon 文件改动汇总（HEAD 现状）

| 文件 | HEAD 行数 | 本周期净改动 |
|---|---|---|
| `plugins/platforms/photon/plugin.yaml` | 76 | `0.1.0 → 0.2.0`，删 4 webhook env，加 `PHOTON_SPECTRUM_HOST`；label 改 `iMessage via Photon` |
| `plugins/platforms/photon/adapter.py` | 1162 | 删 aiohttp webhook、加 gRPC 入站消费 loop、attachment 缓存、6 个 send_* override、`_standalone_send` 多媒体、`format_message`/`_send_with_retry` |
| `plugins/platforms/photon/auth.py` | 1065 | spectrum-host helper、Basic auth、user API 迁移、token 双重 validate、`PhotonDashboardAuthError`、user numbers persistence |
| `plugins/platforms/photon/cli.py` | 387 | webhook 子命令删除、5 步 setup、`_autoconfigure_access`、`_refresh_status_numbers`、彩色 banner |
| `plugins/platforms/photon/sidecar/index.mjs` | 542 | `GET /inbound` NDJSON、`/send-attachment` + `normalizeBinaryContent`、knownSpaces cache、`spectrumText/Reply/Typing` builders、constant-time token、body cap、re-subscribe loop、`flattenGroups`、`MAX_INLINE_ATTACHMENT_BYTES` |
| `plugins/platforms/photon/sidecar/package.json` | 24 | `^0.1.0 → ^1.18.0` spectrum-ts、5 条 `overrides` |
| `plugins/platforms/photon/sidecar/package-lock.json` | 1781 (新建) | 首次完整 lockfile |
| `tests/plugins/platforms/photon/test_signature.py` | 删除 | webhook 验签随 4e4d27875 一并去除 |
| `tests/plugins/platforms/photon/test_outbound_media.py` | 新建 255 行 | 覆盖 6 个 send_* override |
| `tools/send_message_tool.py` | — | `_PHONE_PLATFORMS` 加 `"photon"` |

---

<a id="5-simplex-1-1-0"></a>
## 5. SimpleX — 1.1.0 大演进：群组 + 原生附件 + 文字批处理 + auto-accept

### `0c2e81df0 feat(simplex): groups, native attachments, text batching, auto-accept`（984+/222−）

Author: Juraj Bednar (PR #27978 salvage, 经 `c3420d91a chore: add jooray to AUTHOR_MAP`)。整 commit 完全在 `plugins/platforms/simplex/` 内部完成，**不动 core**。`plugin.yaml:4` 版本 `1.0.x → 1.1.0`。

| 子主题 | 关键 file:line |
|---|---|
| **群组支持**：`SIMPLEX_GROUP_ALLOWED` 逗号分隔 group ID 或 `*`，默认 disabled | `adapter.py:162-168 group_allowed_str / group_allow_from`；inbound chat_id=`group:<id>`，chat_type=group；群发用结构化 `/_send #<id> json [...]`（commit *"the bracket #[<id>] form is parsed as display-name lookup and silently drops"*） |
| **入站文件/语音 rcvFileDescrReady 延迟流程** | `adapter.py:184 self._pending_file_transfers: Dict[int, dict] = {}`；`adapter.py:410-440` rcvFileDescrReady 立即 `/freceive` + 暂存原 message；`adapter.py:442+` rcvFileComplete 时 pop 出来 replay；voice notes → `MessageType.VOICE` |
| **原生出站媒体** | `send_image`（925）+ `send_image_file`（977）+ `send_video`（990）+ `send_document`（1001）+ `send_voice`（1033），全部按 numeric ID 通过 `/_send … json [...]` 寻址 |
| **`MEDIA:<path>` 标签处理** | `adapter.py:805-845` `re.findall(r"MEDIA:(\S+)", content)`；按扩展名（`_is_audio_ext` 集合新增 `.opus`，line 130）分发到 `send_voice / send_document / send_image` |
| **文字 burst 批处理** | `HERMES_SIMPLEX_TEXT_BATCH_DELAY` 默认 0.8s；`adapter.py:195 _pending_text_batches + _pending_text_batch_tasks`；disconnect 时 task.cancel() |
| **Auto-accept contact requests** | `SIMPLEX_AUTO_ACCEPT` 默认 true；`adapter.py:155-160`；`adapter.py:395-403` `if resp_type == "contactRequest" and self.auto_accept: await self._send_command(f"/accept {contact_req_id}")` |
| **杂** | `MAX_MESSAGE_LENGTH = 8000`（从 16000 缩小，line 75）+ `HEALTH_CHECK_STALE_THRESHOLD = 300.0`（从 120 放宽）；`_redact_id(contact_id)` log 脱敏（line 96）；`_send_command` 关联 ID 追踪 `_pending_responses: Dict[str, asyncio.Future] + _corr_counter`（line 188）；WebSocket cleanup `close_timeout=10` + 缩小 reconnect catch 到 `ConnectionClosed`（line 302, 326） |

`tests/gateway/test_simplex_plugin.py:+21`，`website/docs/user-guide/messaging/simplex.md:+36` 同步更新。`plugin.yaml` 描述补充三个新 env。

---

<a id="6-plugins-cluster"></a>
## 6. Plugins — TUI Plugins Hub overlay + `/plugins` 改写 + subdir install + optional-mcps 打包修

### 6.1 `b8eede7bd fix(cli): /plugins shows installed-but-not-enabled plugins`

`/plugins` slash 之前读 live `PluginManager.list_plugins()`，只见 *loaded* plugin；freshly-installed-but-not-enabled 显 "No plugins installed."

`cli.py:7303-7366` 重写——改用 `hermes plugins list` 的同套 disk-discovery primitive：
- 导入 `_discover_all_plugins / _get_disabled_set / _get_enabled_set / _plugin_status` from `hermes_cli.plugins_cmd`（`cli.py:7310-7315`）
- 过滤掉 bundled entries（`e[3] != "bundled"`）at `cli.py:7325`；bundled 一行 summary
- empty-user 路径打印 install hint + 可选 `"{bundled_count} bundled plugins available — see: hermes plugins list"`（`cli.py:7328-7333`）
- state glyph：`{"enabled": "✓", "disabled": "✗"}.get(state, "○")`（`cli.py:7347-7348`）
- 末尾 footer：`"  Enable/disable: hermes plugins enable/disable <name>"`（`cli.py:7364`）

### 6.2 `52f7e24a7 feat(tui): interactive Plugins Hub overlay for enable/disable`

仿 SkillsHub。

**Gateway RPC `plugins.manage`** at `tui_gateway/server.py:9252-9328`：
- 加入 `_LONG_HANDLERS` at `tui_gateway/server.py:178`（与 `browser.manage / cli.exec / session.branch` 并列，worker 视为 disk-/config-I/O method）
- Action `"list"` 返 `{"plugins": [...], "user_count": N, "bundled_count": M}`（`server.py:9293-9303`）
- Action `"toggle"` 读 `name + enable`，调 `dashboard_set_agent_plugin_enabled(name, enabled=enable)`（`server.py:9306-9324`）；返 `{ok, unchanged, name, plugin}`
- 每行带 `name / version / description / source ("bundled"|"user") / status`，`status` 来自 `_plugin_status(name, enabled, disabled, key=key)`
- 错误码：4017 unknown action / 4019 toggle missing name / 5026 toggle failure

**TUI overlay 通路**：
- `OverlayState.pluginsHub: boolean` at `ui-tui/src/app/interfaces.ts:96`
- `overlayStore.ts:13,24,26,52` 注入 `pluginsHub: false` + 加入 `$isBlocked` + `resetFlowOverlays` 保留
- `useInputHandlers.ts:154-155` Esc handler
- `appOverlays.tsx:14,129,179-182` import + `hasAny` guard + 渲染 `<PluginsHub gw={gw} onClose={...} t={theme} />` in `FloatBox`

**Slash `/plugins`** at `ui-tui/src/app/slash/commands/ops.ts:655-681`：
- 无 arg → `patchOverlayState({ pluginsHub: true })`
- 任意 sub 落到 `slash.exec` 与 `hermes plugins` 一致

**`PluginsHub` 组件** at `ui-tui/src/components/pluginsHub.tsx`（238 行，**新建**）：
- `VISIBLE = 12 / MIN_WIDTH = 44 / MAX_WIDTH = 96`（`pluginsHub.tsx:10-12`）
- `GLYPH = { disabled: '✗', enabled: '✓' }`，其他 `○`
- `Scope = 'all' | 'user'` Tab 切（default `'user'`）；empty-user fallback all 永不空
- `toggle(row)` 推 `enable = row.status !== 'enabled'` 发 `plugins.manage {action:'toggle', enable, name}`
- 键位：↑↓ 选 / Enter|Space toggle / Tab scope swap / 1-9,0 quick-toggle / Esc|q close
- scope label：`"{userCount} user plugin(s) · +{bundledCount} bundled (Tab)"` (user) / `"all {rows.length} plugins"` (all)

### 6.3 `f6f573eba feat(plugins): install from a subdirectory within a repo (#42963)`

支持 monorepo 的子目录安装，三种标识：
- `owner/repo/path/to/plugin` shorthand
- `<url>.git/path/to/plugin` 自然 `.git/` 边界
- `<url>#path/to/plugin` 显式 fragment（适用任何 scheme，包括 `file://` 和 ssh）

`_resolve_git_url` 返 `tuple[str, Optional[str]]` at `hermes_cli/plugins_cmd.py:138`：
- URL 标识：显式 `#` fragment 先解（`plugins_cmd.py:163-167`）；自然 `.git/` 边界次之（`168-174`）；else `(identifier, None)`（`175`）
- shorthand：`parts = identifier.strip("/").split("/")`；`len(parts) >= 2` → `(f"https://github.com/{owner}/{repo}.git", subdir or None)`（`177-183`）

`_resolve_subdir_within(clone_root, subdir)` at `plugins_cmd.py:192-218`：
- 双端 resolve，guard `candidate != clone_root and clone_root not in candidate.parents` → `PluginOperationError("Plugin subdirectory '...' escapes the repository.")`（`200-207`）
- 三个 failure mode：`..` traversal / 绝对路径 / symlink-out（测试 `test_rejects_dot_dot_escape / test_rejects_absolute_path_escape / test_rejects_symlink_escape`）

`_install_plugin_core` at `plugins_cmd.py:421-472`：
- `git_url, subdir = _resolve_git_url(identifier)`（`430`）
- clone 到 `tmp_clone = Path(tmp) / "plugin"`（`437, 445`）
- 之后：`if subdir: tmp_target = _resolve_subdir_within(tmp_clone, subdir) else: tmp_target = tmp_clone`（`463-467`）
- default name 是 subdir leaf 或 `_repo_name_from_url(git_url)`（`470-472`）

`cmd_install` at `plugins_cmd.py:537` 打印 `(subdir: {_subdir})`；`dashboard_install_plugin` at `plugins_cmd.py:1542` 同。Web UI hint at `web/src/i18n/en.ts:365` 与 placeholder at `web/src/pages/PluginsPage.tsx:243` 同步。

### 6.4 `39b76d901 fix(packaging): ship optional-mcps catalog in wheel and sdist (#39859)`

`optional-mcps/` catalog 既未进 wheel 也未进 sdist，pip / Homebrew / Nix 安装后 `hermes mcp catalog` / dashboard 空白（`get_optional_mcps_dir() -> _get_packaged_data_dir("optional-mcps")` 返 `list_catalog() = []`）。

- `MANIFEST.in:3` `graft optional-mcps`（与 skills / optional-skills / locales 并列）
- `pyproject.toml:300-301` 两条 per-entry data-files：`"optional-mcps/linear" = ["optional-mcps/linear/manifest.yaml"]` + `"optional-mcps/n8n" = ["optional-mcps/n8n/manifest.yaml"]`
- `pyproject.toml:288-299` 注释解释：data-files 把 globs flatten 到单个 target dir，共享 `optional-mcps/*/*` glob 会让所有 manifest 撞同一 `optional-mcps/manifest.yaml`；每 entry 必须自己 target
- 新测 `test_optional_mcps_manifests_ship_in_both_wheel_and_sdist` 锁定

---

<a id="7-anthropic-and-models"></a>
## 7. Anthropic — 默认现代 thinking 契约 + Fable 5 全链路 + OpenRouter reasoning-mandatory 守护

### 7.1 `1febb0824 fix(anthropic): default new Claude models to the modern thinking contract (#42991)`

反转脆弱的 version-substring allowlist 为 default-to-modern（镜像 `_get_anthropic_max_output` 的 "default to newest"）。Unknown Claude 模型自动得到 adaptive / xhigh / no-sampling-params 契约；只有显式 legacy 列表保留 manual budget-thinking。Non-Claude Anthropic-Messages 模型（minimax / qwen3 / GLM 等）自动保留 manual 路径。

**常量 / 助手** in `agent/anthropic_adapter.py`：
- Header 注释 at `anthropic_adapter.py:76-93` 文档化设计意图
- `_LEGACY_MANUAL_THINKING_CLAUDE_SUBSTRINGS` at `:97-105` — `claude.lower()` 子串匹配：`claude-3 / claude-opus-4-0/-4.0/-4-1/-4.1 / claude-sonnet-4-0/-4.0 / claude-opus-4-2025 / claude-sonnet-4-2025 / claude-opus-4-5/-4.5 / claude-sonnet-4-5/-4.5 / claude-haiku-4-5/-4.5`
- `_NO_XHIGH_CLAUDE_SUBSTRINGS` at `:110-113` — 仅 `claude-opus-4-6/-4.6, claude-sonnet-4-6/-4.6`（4.6 family adaptive 但不收 `xhigh`，仍收 sampling params）
- `_is_claude_model(model)` at `:116-117` — case-insensitive `"claude"` 子串

**Predicates 重写**：
- `_supports_adaptive_thinking(model)` at `:242-253` — non-Claude False；否则 `not any(v in m for v in _LEGACY_MANUAL_THINKING_CLAUDE_SUBSTRINGS)`
- `_supports_xhigh_effort(model)` at `:256-270` — 非 adaptive False；else `not any(v in m for v in _NO_XHIGH_CLAUDE_SUBSTRINGS)`。4.7+/fable/future named → True；4.6 → False
- `_forbids_sampling_params(model)` at `:273-289` — non-Claude False；4.6 family False；otherwise `not any(v in m for v in _LEGACY_MANUAL_THINKING_CLAUDE_SUBSTRINGS)` — unknown Claude → True（现代契约）

**Output limits** + **OpenRouter profile guard**：
- `_ANTHROPIC_OUTPUT_LIMITS` at `anthropic_adapter.py:126-128` 加 `"claude-fable": 128_000`
- `plugins/model-providers/openrouter/__init__.py:21-29` 新 `_ANTHROPIC_REASONING_OPTIONAL_SUBSTRINGS`（同 `_LEGACY_MANUAL_THINKING_CLAUDE_SUBSTRINGS` shape）
- `:32-44` 新 `_anthropic_reasoning_is_mandatory(model)` — Anthropic 且不在 optional 列表则 True。Unknown Anthropic → mandatory
- `:13-20` 头注释解释 4.6+ 包括 named 模型（如 fable）拒任何 disable form 返 HTTP 400 的原因

### 7.2 `46fedef07 fix(openrouter): never send reasoning field for adaptive Anthropic models (#43012)`

`#42991` 只在 *disabling* 时省略 reasoning。但 reasoning-mandatory 模型在 *enabled* 时也 400 在每个 tool-continuation turn 上：chat_completions 不回放 signed thinking blocks，所以前一个 assistant tool_call 不带 thinking，OpenRouter 把 "reasoning requested but history has none" 解析为 `thinking: {type: "disabled"}` — 被模型拒收。第一回合 OK；每个 tool call 之后全死（HTTP 400 不可重试）。

**新逻辑** at `plugins/model-providers/openrouter/__init__.py:118-140`：
```python
if _anthropic_reasoning_is_mandatory(model):
    pass  # omit reasoning entirely → adaptive default
elif reasoning_config is not None:
    extra_body["reasoning"] = dict(reasoning_config)
else:
    extra_body["reasoning"] = {"enabled": True, "effort": "medium"}
```
注释 at `:120-134` 列举两种 failure shape；测试 `test_reasoning_omitted_for_mandatory_anthropic_even_when_enabled` 覆盖 4 种 reasoning shape 全 → 无 `reasoning` key。

### 7.3 `d7886da08 add Fable 5 to model list for Anthropic provider`（PR #43041，`49dd776d8` 合入）

`hermes_cli/models.py:329` Anthropic 直连列表顶部新增 `"claude-fable-5"`，位于 `claude-opus-4-8 / -4-7 / -4-6` 前。

### 7.4 `ff9c110d5 feat(models): add anthropic/claude-fable-5 to openrouter + nous curated lists (#42979)`

在两个 curated picker 列表 `claude-opus-4.8` 之上加 Fable 5：
- `OPENROUTER_MODELS`: `("anthropic/claude-fable-5", "")` at `hermes_cli/models.py:36`
- `_PROVIDER_MODELS["nous"]`: `"anthropic/claude-fable-5"` at `hermes_cli/models.py:158`
- `website/static/api/model-catalog.json:16, 152` 同步；`updated_at: 2026-06-09T17:20:16Z`

### 7.5 `967c325da fix(models): read OpenRouter live context_length before hardcoded catch-all (#42986)`

OpenRouter-routed slugs 缺失 models.dev（如新鲜的 `anthropic/claude-fable-5`）会落到通用 `DEFAULT_CONTEXT_LENGTHS["claude"] = 200_000` catch-all 而漏报 1M 窗。原有 step-6 OR-live fallback 被 `not effective_provider` 把守，但 OpenRouter selection 设 `effective_provider="openrouter"`（由 base URL 推），因此该分支对每个 OR 模型都是死代码。

**新 step-5f branch** at `agent/model_metadata.py:1815-1835`：
- Guard `if effective_provider == "openrouter":`（`:1825`）
- `fetch_model_metadata()` 查 `entry.get("context_length")`（`:1826-1829`）
- 返 `isinstance(or_ctx, int) and or_ctx > 0 and not (or_ctx == 32768 and _model_name_suggests_kimi(model))` —— 保留已知 Kimi-family 32k underreport guard

`agent/model_metadata.py:144-145` 注册：
```python
"claude-fable-5": 1000000,
"claude-fable": 1000000,
```

---

<a id="8-models-curated"></a>
## 8. Models / OpenRouter — 实时 context_length + Nous 推荐磁盘缓存 + laguna-m.1 / nemotron-3-ultra + catalog disk seed

### 8.1 `c4066091c feat(models): add laguna-m.1 + nemotron-3-ultra to curated OpenRouter list (#42629)`

两条新 free-tier slug at `hermes_cli/models.py`：
- `("poolside/laguna-m.1:free", "free")` at `:77`
- `("nvidia/nemotron-3-ultra-550b-a55b:free", "free")` at `:80`

与既有 `openrouter/elephant-alpha / owl-alpha / tencent/hy3-preview:free / nvidia/nemotron-3-super-120b-a12b:free / inclusionai/ring-2.6-1t:free` 并列（`:74-81`）。

### 8.2 `e687292eb feat(models): persist Nous recommended-models to disk; fall back on Portal failure (#42628)`

新 per-base 磁盘缓存 at `$HERMES_HOME/cache/nous_recommended_cache.json` 用于 Portal `/api/nous/recommended-models`：

- `_nous_recommended_disk_path()` at `models.py:772-775`
- `_read_nous_recommended_disk(base)` at `:778-796` — disk shape `{"<base>": {"data": {...}, "ts": <epoch>}}`
- `_write_nous_recommended_disk(base, data)` at `:799-827` — merge into existing map，atomic `os.replace(tmp, path)`
- `fetch_nous_recommended_models` at `:830-889`：live 成功写双层（mem + disk）；live 失败且 mem 空则读 disk；都无返 `{}`；key 用 `(portal_base_url or "https://portal.nousresearch.com").rstrip("/")` 让 staging/prod 不撞

### 8.3 `54318c65b feat(models): seed model-catalog disk cache from checkout on update (#42614)`

`seed_cache_from_checkout(project_root)` at `hermes_cli/model_catalog.py:359-387`：
- 读 `Path(project_root) / "website" / "static" / "api" / "model-catalog.json"`
- `_validate_manifest(data)` 校验
- atomic `_write_disk_cache(data)`
- `reset_cache()` 让 in-process 副本下次重载
- 失败 / 缺失返 False 非致命

**两 update path 都接** in `hermes_cli/main.py`：
- `_cmd_update_impl`（git pull）at `main.py:8402-8416`：`"Code updated!"` 后调用，成功 print `"  ✓ Model catalog cache refreshed from checkout"`
- `_update_via_zip`（Docker / no-git）at `main.py:5836-5838`：相同 helper

---

<a id="9-observability-cluster"></a>
## 9. Observability — Langfuse sanitized response usage 恢复 + NeMo-Relay plugins.toml lifecycle 四连

### 9.1 `9f1c16a7f fix(langfuse): restore usage/cost when post_api_request sends a sanitized response`

`on_post_llm_call` 以前用 `if response is not None:` 提 usage。新 `post_api_request` 路径 `response` 是 sanitized dict（无 `.usage` 属性），usage 单独以 dict 传 — `getattr(response, "usage", None)` 总 None，每个 gateway turn token/cost 静默丢失（traces 显 usage 0 / cost 0）。

- Gate 翻 at `plugins/observability/langfuse/__init__.py:849`：`if getattr(response, "usage", None) is not None:`
- `:857-` fallback `elif isinstance(usage, dict) and usage:` 现真正跑：从 summary dict 拿 `input_tokens / output_tokens / cache_read_tokens / ...` 翻译成 Langfuse-convention key（`"input"`, `"output"`, `"cache_read_input_tokens"`, `"cache_creation_input_tokens"`）
- `:840-848` 注释明文档化回归
- 真实 response object（post_llm_call / legacy 路径）继续走原 branch

### 9.2 `9d61076f8 fix: flush plugin-config OpenInference when the final session closes`

加 session-rotation-safe 生命周期给 nemo-relay 的 `plugins.toml` 初始化；在 `plugins.toml` 已占 ATIF 时禁用 direct ATIF fallback 防 teardown 双 export：

- `_configure_plugins_toml` at `plugins/observability/nemo_relay/__init__.py:75-88`：`initialize(...)` 包 `_resolve_awaitable(...)`（async-safe）；任何异常返 False + debug log
- `_clear_plugins_toml` at `:90-101`：调 `_resolve_awaitable(clear())`，标 `_plugin_config_initialized = False`
- finalize 钩 at `:235-239`：`if self._plugin_config_initialized and not self.sessions: self._clear_plugins_toml()` — 只在最后一个 active session 结束才清
- ATIF double-export guard：新 `_observability_exporter_enabled(plugins_config, exporter_name)` at `:710-` 走 `_enabled_component_config(plugins_config, "observability") → [exporter_name]`

### 9.3 `ecd4679d8 fix(observability): preserve direct fallback until plugin-config init succeeds`

`plugins.toml` 初始化失败时，必须保留 direct env-var fallback：

- `_atof_subscriber_name = "hermes.nemo_relay.atof"` at `:69`
- 构造器 at `:63-73`：set `_plugin_config_needs_reinit = False`，然后 `if not self._plugin_config_initialized: self._activate_direct_fallbacks()`
- `_activate_direct_fallbacks()` at `:103-105`
- `_maybe_reinitialize_plugins_toml()` at `:107-115` — `_plugin_config_needs_reinit and not _plugin_config_initialized` 时重试；成功清 direct ATOF + 取消 reinit flag；失败走 fallback
- `_plugins_toml_owns_exporter(exporter_name)` at `:117-121`
- `_configure_atof` at `:142-155` 现 idempotent + 用常量名
- `_clear_atof()` at `:157-166` — 调 `deregister(self._atof_subscriber_name)` 然后 `atof_exporter = None`
- `ensure_session` 现先调 `_maybe_reinitialize_plugins_toml()`；per-session ATIF exporter gate 改为 `self.settings.atif_enabled and not self._plugins_toml_owns_exporter("atif")`
- finalize 加 `elif self.settings.plugins_config and not self.sessions: self._plugin_config_needs_reinit = True`

### 9.4 `728612c29 fix(observability): recover after plugin-config clear failure`

`_clear_plugins_toml` at `:90-101` 包 try/finally：
```python
try:
    _resolve_awaitable(clear())
finally:
    self._plugin_config_initialized = False
    self._plugin_config_needs_reinit = bool(self.settings.plugins_config)
```
之前 state flag 只在成功时翻，`clear()` `RuntimeError` 会让下次 session 既跳 reinit 又跳 fallback。回归测 `test_nemo_relay_plugin_retries_plugins_toml_after_clear_failure` 锁定。

### 9.5 `021d1034d fix(nemo-relay): align adaptive config with tool_parallelism mode`

与上游 NeMo Relay contract 对齐：`[components.config.tool_parallelism] mode = "observe_only"` 代替 `[components.config] mode = "route"`。

- `_Settings.adaptive_mode` default `"observe" → "observe_only"` at `:48`
- `_adaptive_mode(config)` at `:696-707` 优先 `config["tool_parallelism"]["mode"]` over legacy 顶层 `config["mode"]`
- README at `plugins/observability/nemo_relay/README.md` 全部 example migrate；E2E example 加 callout：`nemo-relay==0.3` 不支持

### 9.6 `85852b71d fix(nemo-relay): preserve downstream errors in adaptive execution (#42691)`

NeMo Relay 的 managed adaptive execution 把失败 callback 包成 internal runtime error，把原 provider/tool exception（连带 retry-classification signal 如 `status_code`）藏在 wrapper 后。

**共享 scaffolding** `_run_managed_with_downstream_preservation(...)` at `:288-323`：
- `raw_response` dict 承载成功；`callback_error / downstream_error` 捕异常
- `_impl(next_payload)` 调 `next_call(normalize_payload(next_payload))`；异常 stores `callback_error = exc` + `downstream_error = _original_downstream_error(exc)`，re-raises
- 外层 try at `:317-322`：catch managed-execute 异常；`downstream_error is not None and _is_relay_wrapped_callback_error(exc, callback_error):` → `raise downstream_error`，否则 re-raise Relay's

**Wrapper-shape detection**：
- `_original_downstream_error(exc)` at `:844-850` — 按 class-name + `.original` 属性 shape 探（`exc.__class__.__name__ == "_DownstreamExecutionError"`），不 import 私类
- `_is_relay_wrapped_callback_error(exc, callback_error)` at `:853-865` — 容错 `str.startswith` match `f"internal error: {callback_error.__class__.__name__}: {callback_error}"`

`execute_llm`（`:325-363`）+ `execute_tool`（`:366-`）共享去重；新 import `from collections.abc import Callable` at `:12`。

---

<a id="10-desktop-recover-cluster"></a>
## 10. Desktop — websocket reconnect 重绑 + sleep/wake 恢复 + Electron mirror fallback + per-profile 后端释放

### 10.1 `8d71c3891 fix(desktop): rebind sessions after websocket reconnect (#43004)`

reconnect 后 cached runtime id 可 stale，stuck on 死 id。

- `useRouteResume` 三 ref：`lastPathnameRef / seenGatewayStateRef / wasGatewayOpenRef`（`apps/desktop/src/app/session/hooks/use-route-resume.ts:58-60`）
- `gatewayBecameOpen` 要 `seenGatewayStateRef.current && !wasGatewayOpenRef.current && gatewayOpen` 不把 mount 误当 reconnect（`:65-69`）
- "skip if already active" guard 允许 reconnect re-resume：`if ((gatewayBecameOpen || !alreadyActive) && shouldResume && !creatingSessionRef.current) { void resumeSession(routedSessionId, true) }`（`:108-114`）
- Gateway side: `live.session` lookup 现传 transport 让 live payload 带正确 binding（`tui_gateway/server.py:3844-3856`）

### 10.2 `72f522d46 fix(desktop): recover session after sleep/wake gateway restart`（+ `be2f739e9` 测试覆盖）

Sleep/wake 清空 gateway in-memory session table，Desktop 持 stale `activeSessionId`，第一次 `prompt.submit` 返 "session not found"。

`apps/desktop/src/app/session/hooks/use-prompt-actions.ts:493-520`：
- `try/catch` 包 submit
- 命中 `/session not found/i.test(firstMsg) && selectedStoredSessionIdRef.current` 时 `requestGateway<{ session_id: string }>('session.resume', { session_id: selectedStoredSessionIdRef.current })`
- 设 `activeSessionIdRef.current = recoveredId`，re-submit `prompt.submit` 一次
- 其他错或缺 stored id 直接 re-throw

`apps/desktop/src/app/session/hooks/use-prompt-actions.test.tsx` 三测：resume+retry / 非 session-not-found passthrough / 无 stored id 无 resume。

### 10.3 `ba44de06d fix(install): self-heal a stuck Electron download (#42998)`

三条 desktop build 路径（install.sh / install.ps1 / `hermes desktop`）都按三步 escalate `npm run pack`：先 GitHub，然后清 corrupt `electron-*.zip` + stale `*-unpacked` 重试，最后 fallback 经 `ELECTRON_MIRROR=https://npmmirror.com/mirrors/electron/`。user-pinned `ELECTRON_MIRROR` 永远尊重。

- `hermes_cli/main.py:5229-5253`：第三 escalation 块
- `scripts/install.sh:2293` `clear_electron_build_cache()`、`:2352` `_desktop_pack()`、`:2369` `DESKTOP_ELECTRON_FALLBACK_MIRROR`、`:2437-2459` 三步 escalation
- user-facing 失败信息加：`"If the log shows Electron download retries, rebuild via a mirror: ELECTRON_MIRROR=<mirror-base-url> hermes desktop --force-build"`

### 10.4 `c6dc2fcd2 fix(desktop): release profile backends before delete (#42613)`

`DELETE /api/profiles/<name>` 现先停匹配的 backend 再删 profile 目录，避免 Windows file lock 卡 delete。

- `apps/desktop/electron/main.cjs:4301-4327` `waitForBackendExit(child, timeoutMs=5000)`：超时则 Windows `forceKillProcessTree(child.pid)` / 否则 SIGKILL
- `:4514-4528` `teardownPoolBackendAndWait(profile)`
- `:4536-4561` `profileNameFromDeleteRequest(request)`：URL-decode、trim、lower、normalize `default`
- `:4563-4576` `prepareProfileDeleteRequest(request)`：拒 `default`，要求 `PROFILE_NAME_RE` 匹配；若删 primary 则翻 active 到 `default` 再 teardown
- 入口 IPC handler `:5193` `await prepareProfileDeleteRequest(request)`（在 `ensureBackend` 前）
- Python side `hermes_cli/profiles.py:1010-1071` `delete_profile` 捕 rmtree error 入 `remove_error`，完成 `active_profile` clear 后 re-raise `RuntimeError(f"Could not remove profile directory {profile_dir}: ...")`

### 10.5 `c4811c382 fix(desktop): pad app icon to Apple grid so dock size matches peers (#42946)`

资产 only。替换 `icon.icns / icon.ico / icon.png` + `apple-touch-icon.png` 为 Apple grid 标准 padding（~824px body on 1024px canvas）。dock icon 来自 `apple-touch-icon.png`（经 `app.dock.setIcon()`）所以只 pad bundle `.icns` 不够。

---

<a id="11-desktop-features-cluster"></a>
## 11. Desktop — file.attach RPC + 后台流式 + per-platform 边栏 + ES2023 + 调试报告显式 URL

### 11.1 `dbbd1d4d0 feat(desktop+gateway): remote-gateway file attachments via file.attach`

加 `file.attach` JSON-RPC sibling 给 `image.attach_bytes / pdf.attach`。Non-image `@file:` ref 现 work against remote gateway：bytes 上传，gateway stage 到 `<workspace>/.hermes/desktop-attachments/`，返 workspace-relative `@file:` ref。

**Desktop side**：
- `readFileDataUrlForAttach(filePath)` 调 `window.hermesDesktop?.readFileDataUrl(filePath)`（`apps/desktop/src/app/session/hooks/use-prompt-actions.ts:107-118`）
- `syncImageAttachmentsForSubmit` 改名扩展为 `syncAttachmentsForSubmit`，处 `kind === 'image' | 'file'`。file remote 模式读 dataUrl 调 `requestGateway<FileAttachResponse>('file.attach', { session_id, path, name, data_url })`（`:230-336`）
- optimistic message sync 后 rewrite refs（`rewriteOptimistic`）让 transcript 显 resolved ref 而非 local 路径（`:410-422, 487`）
- 新 interface `FileAttachResponse` at `apps/desktop/src/app/types.ts:30-42`：`attached, message, path, ref_path, ref_text, uploaded, name`
- `REQUIRED_BACKEND_CONTRACT` `1 → 2`（`apps/desktop/src/store/updates.ts:88-92`）

**Gateway side**：
- `DESKTOP_BACKEND_CONTRACT = 2` at `tui_gateway/server.py:1997`
- `@method("file.attach")` at `tui_gateway/server.py:5786-5823`：params `session_id, path, data_url, name`，返 `{attached, name, path, ref_path, ref_text: f"@file:{_format_ref_value(ref_path)}", uploaded}`。错误码 4015 missing path/data_url，5028 stage failure
- helper：`_format_ref_value(value)`（`:5635-5655`）、`_attachment_ref_path(session, target)`（`:5658-5665`）、`_desktop_attachment_dir(session)`、`_sanitize_attachment_name(name)`、`_unique_attachment_path(root, filename)`、`_resolve_gateway_attachment_path(raw)`、`_decode_attachment_data_url(data_url)`（接受任何 media type，与 image-mime-only `_decode_attach_base64` 不同）
- `_stage_session_file_attachment(...)` 三 case 逻辑 at `:5745-5783`：in-workspace → no copy；outside-workspace gateway-visible → copy；not on gateway → decode data_url

### 11.2 `c1927d234 fix(desktop): set tsconfig lib/target to ES2023`

`apps/desktop/tsconfig.json:3,5`：`"target": "ES2023"` + `"lib": ["DOM", "DOM.Iterable", "ES2023"]`（之前 ES2022）。Desktop renderer 用 `Array.prototype.findLast / findLastIndex`（ES2023）。

### 11.3 `09a6a2ddd fix(desktop): stream the transcript while the window is backgrounded (#42399)`

Chromium 在 backgrounded/occluded renderer 上暂停 `requestAnimationFrame` + 限 timer。

- 三个 process-level Chromium switch 加在 `app ready` 之前（`apps/desktop/electron/main.cjs:132-134`）：`disable-renderer-backgrounding / disable-backgrounding-occluded-windows / disable-background-timer-throttling`
- 主 BrowserWindow `webPreferences.backgroundThrottling: false` at `:4784`

### 11.4 `d04616964 fix(desktop): local-only recents, per-platform sidebar sections, and Ctrl+N regressions (#42537)`

六段补丁：

**per-platform messaging sections / local-only recents**：
- 新常量 `LOCAL_SESSION_SOURCE_IDS` + `MESSAGING_SESSION_SOURCE_IDS`（19 个 messaging 平台）at `apps/desktop/src/lib/session-source.ts:40-66`
- `isMessagingSource(source)` at `:71-75`、`handoffOriginSource(handoffState, handoffPlatform)` at `:89-104`（仅 completed 且非 local 才返）
- 新 atoms at `apps/desktop/src/store/session.ts:85-100`：`$messagingSessions`、`MESSAGING_SECTION_LIMIT = 100`、`$messagingPlatformTotals`、`$messagingTruncated`
- Controller fetch `refreshMessagingSessions` at `apps/desktop/src/app/desktop-controller.tsx:303-321`、per-platform pager `loadMoreMessagingForPlatform(platform)` at `:323-345`
- 边栏：`messagingGroups` memo at `apps/desktop/src/app/chat/sidebar/index.tsx:545-582`，渲染 at `:932-970` 带 `<PlatformAvatar>` + `SidebarLoadMoreRow`
- `SessionInfo` 加 `handoff_platform / handoff_state / handoff_error`（`apps/desktop/src/types/hermes.ts:300-308`）；`SidebarSessionRow` 显 origin-platform badge

**sidebar 顺序修（新 session 浮顶）**：
- `orderByIds`（`:143-167`）prepend fresh：`return fresh.length ? [...fresh, ...ordered] : ordered`
- `reconcileOrderIds`（`:170-185`）镜像

**同步 flush 终端/注意力转换**：
- `useSessionStateCache` at `:150-175` 现在 `!state.busy || state.needsInput` 时同步 flush，取消 in-flight RAF 先

**route-resume self-heal**（pre-#43004 状态）：
- `stuckOnRoutedSession = routedSessionId !== selectedStoredSessionIdRef.current && !freshDraftReady`

**Ctrl+N profile reset**：
- `useKeybinds` at `:108-114` 先 `$newChatProfile.set(null)` 再 `deps.startFreshSession()`

**markdown stream 改动**：
- `MarkdownTextImpl` 不再包 `SmoothStreamingText`，仅 `<DeferStreamingText><MarkdownTextSurface /></DeferStreamingText>`（`apps/desktop/src/components/assistant-ui/markdown-text.tsx:494-500`）

### 11.5 `6a8dda171 + e0f6a35ac fix(desktop): render debug-report paste URLs as real clickable links (#42515)`

System messages 现经 `LinkifiedText` 加 `explicitOnly` 模式，只 match 显式 `http(s)://` / `www.` URL，让 `/debug` 报告中的 `agent.log` 之类不被误识别。

- `EXPLICIT_URL_RE = /(?:https?:\/\/|www\.)[^\s<>"'`]+[^\s<>"'`.,;:!?)]/gi` at `apps/desktop/src/lib/external-link.tsx:19`
- `LinkifiedText({ className, explicitOnly = false, pretty = true, text })` at `:270-277`
- `SystemMessage` at `apps/desktop/src/components/assistant-ui/thread.tsx:923, 934` 换 `<LinkifiedText className="whitespace-pre-wrap" explicitOnly pretty={false} text=... />`

---

<a id="12-tui-cli-cluster"></a>
## 12. TUI / CLI — fallback chain canonical + terminal config env bridge + native Windows confirm modal + stdin EOF 全面修

### 12.1 `f8adefdeb fix(tui): apply terminal backend config before launch`

TUI/dashboard 子进程现也走 classic CLI 同款 `terminal.*` config-to-env bridge，但不付 `cli.py` 启动副作用。Bridge 现住 `hermes_cli.config`：

- `TERMINAL_CONFIG_ENV_MAP` at `hermes_cli/config.py:5156-5184` —— 全 terminal.* key 到 env var twin：`backend → TERMINAL_ENV`、`modal_mode → TERMINAL_MODAL_MODE`、`cwd → TERMINAL_CWD`、`timeout → TERMINAL_TIMEOUT`、`lifetime_seconds → TERMINAL_LIFETIME_SECONDS`、docker/singularity/modal/daytona images、ssh fields、container CPU/memory/disk/persistent、`docker_volumes`、`docker_env`、`docker_mount_cwd_to_workspace`、`docker_extra_args`、`docker_run_as_host_user`、`docker_persist_across_processes`、`docker_orphan_reaper`、`sandbox_dir`、`persistent_shell`
- `terminal_config_env_var_for_key(key)` at `:5193-5198` 剥 `terminal.` 前缀查表
- `apply_terminal_config_to_env(env=None, config=None, override=None)` at `:5201-5239` 读 `read_raw_config()` 决定 config.yaml authoritative 与否；JSON-encode list/dict via `_terminal_env_value`
- `set_config_value` at `:6131-6133` 现共享同 helper，删旧 `_config_to_env_sync` dict
- `_launch_tui` at `hermes_cli/main.py:1825-1832` 与 `_resolve_chat_argv` at `hermes_cli/web_server.py:8573-8580` 现 spawn 前调 `apply_terminal_config_to_env(env=env)`，wrap try/except 仅 debug log 不挡 launch

### 12.2 `520b59db1 + 4b073d090 fix(tui): canonical get_fallback_chain + map author / preserve fallback provider chain`

TUI agent factory 现走 shared `get_fallback_chain` helper（与 `HermesCLI.__init__` 和 `gateway/run.py` 一致）。之前 TUI `cfg.get("fallback_providers") or cfg.get("fallback_model")` 二选一发散。

- `_load_fallback_model()` at `tui_gateway/server.py:2592-2603`：thin delegate `from hermes_cli.fallback_config import get_fallback_chain; return get_fallback_chain(_load_cfg())`
- `_agent_fallback_model(agent)` at `:2606-2612`：先 `agent._fallback_chain`（空 list 视为故意）→ `agent._fallback_model` → `_load_fallback_model()`
- `_background_agent_kwargs` 现传 `"fallback_model": _agent_fallback_model(agent)` at `:2649`
- `_make_agent` 传 `fallback_model=_load_fallback_model()` at `:2908`

### 12.3 `ab98818e5 + d66bac5a1 + 714183530 + b5f8996cc` — native-Windows confirm modal（issue #33961）

destructive-slash confirm modal 之前在 `sys.platform == "win32"` 上短路 fallback 到 raw `input()`。daemon thread 内 `input()` 与 prompt_toolkit 主线程 stdin 拥有权死锁，`/reset` 卡 + Ctrl-C 被吞。

- native Windows 现走 modal 同 Linux/macOS via `self._app.loop.call_soon_threadsafe`。短路移除；新 docstring "Platform note (Windows — issue #33961)" at `cli.py:6221-6240`
- 新 nested `_stdin_fallback()` closure at `cli.py:6254-6262`：`win32 and not in_main_thread` 时返 None（clean cancel），否则 `self._prompt_text_input("Choice [1/2/3]: ")`
- fallback 用于两 case：`not in_main_thread and app_loop is None`（`:6265`）和 `not _run_on_app_loop(_setup_modal)`（`:6306`）
- `_DESTRUCTIVE_SKIP_TOKENS = frozenset({"now", "--yes", "-y"})` 在 `cli.py:8268, 8295` 改作 general non-interactive escape hatch
- 测试 `tests/cli/test_slash_confirm_windows.py` 重写到新 contract（parametrized `_run_on_daemon` harness for linux/win32）+ 真 helper-chain 测试

### 12.4 `d1f23bb2d fix: prevent TUI gateway stdin EOF crash across all TUI-context subprocess calls`

TUI 模式下 gateway 子进程与 Node 父进程经 stdin JSON-RPC 通信。任何继承 stdin 的 `subprocess.run/Popen` 都可能 race-read EOF，gateway 干净 exit on mid-tool-execution（同 #14036 / PR #39257 根因）。本 commit 在 **28 个文件 85 个 call site** 加 `stdin=subprocess.DEVNULL` 跨 `agent/ / tools/ / plugins/ / tui_gateway/`。CLI 代码、tests、scripts、gateway process management 不在范围。

举例：
- `tui_gateway/server.py`：`_git_branch_for_cwd` git 调用（`:1041-1057`）、`pdftoppm` PDF rasterize（`:5573`）、shell exec in JSON-RPC handler（`:9045-9052`）、`_list_repo_files` git ls-files（`:7295-7321`）、tool-spawned subprocess（`:6845-6911`）
- `tools/terminal_tool.py:2436-2444`：docker / apptainer / singularity preflight 双探针都加 `stdin=subprocess.DEVNULL`
- 涉及 plugins：byterover / hindsight / honcho / discord / discord/voice_mixer / google_meet/audio_bridge
- 涉及 tools：checkpoint_manager / code_execution_tool / env_probe / environments/{docker,singularity} / lazy_deps / process_registry / skills_hub / tirith_security / transcription_tools / tts_tool / voice_mode
- 涉及 agent：anthropic_adapter / context_references / lsp/install / secret_sources/bitwarden / skill_preprocessing / transports/codex_app_server

### 12.5 `bddab61bc ci: add subprocess stdin= regression check for TUI-context code`

新工具 `scripts/check_subprocess_stdin.py`：扫 `agent/ / tools/ / plugins/ / tui_gateway/`（`TUI_CONTEXT_DIRS`），跳 `tests/ / scripts/ / skills/ / optional-skills/ / hermes_cli/ / gateway/ / cron/`（`SKIP_DIRS`）。matcher regex `subprocess\.(run|Popen)\s*\(["\'a-zA-Z_\[\(]` 不命中注释/docstring 提及。`input=` calls 跳过。exit 0 clean / 1 violations。

### 12.6 `8bb60ff03 test: add pytest guard for subprocess stdin= in TUI-context code`

`tests/tools/test_subprocess_stdin_guard.py` 把 `scripts/check_subprocess_stdin.py` 包装成 pytest，CI 第一时间挂掉漏 stdin= 的新 subprocess。

### 12.7 `2c1aaa9cb fix: keep interactive OAuth setup-token inheriting stdin`

`stdin=subprocess.DEVNULL` 一刀切把 `run_oauth_setup_token()` 的交互式 `claude setup-token` 登录憋死。修：

- adapter at `agent/anthropic_adapter.py:1223-1228`：comment "Run interactively — stdin/stdout/stderr inherited so the user can complete the OAuth login prompt. Must keep inherited stdin; the TUI-EOF concern does not apply to an interactive login the user explicitly invokes. noqa: subprocess-stdin"，然后 `subprocess.run([claude_path, "setup-token"])`（无 kwargs）
- guard at `scripts/check_subprocess_stdin.py`：`EXEMPT_MARKER = "noqa: subprocess-stdin"`，matcher 在 call 文本或上面四行注释中找到即豁免

### 12.8 `dba6380ca + 18ead8827`

`dba6380ca` 测试守 OAuth setup-token 保持 interactive + marker 豁免；`18ead8827` `tests/tools/test_docker_environment.py:107-114` 让 docker `version` preflight 期望 `"stdin": subprocess.DEVNULL`。

### 12.9 `a46462ec6 fix(cli): persist custom --portal-url to .env on dashboard register (#42435)`

**Portal URL persistence**：
- `custom_portal_supplied = bool(isinstance(portal_override, str) and portal_override.strip())` 与 resolved `portal_base_url` 解耦（`hermes_cli/dashboard_register.py:271-275`）
- 写入决策 at `:354-372`：custom 时 `should_write_portal = existing_portal != portal_base_url`（总写）；否则旧保守 `not existing_portal and portal_base_url.rstrip("/") != "https://portal.nousresearch.com"`

**Dashboard public URL from `--redirect-uri`**：
- `urlparse` 取 ORIGIN（scheme://netloc），仅当 scheme 是 http/https 且 netloc 非空 at `:394-403`
- 持久 ORIGIN 不带 callback path（`dashboard_auth/routes._redirect_uri` 自重建）
- `_print_post_register_hint` 加 `public_url=""` 参数 + 打印 `"HERMES_DASHBOARD_PUBLIC_URL=" + str(public_url)`（`:184-198`）

---

<a id="13-hermes-ink"></a>
## 13. hermes-ink — 终端 raw-mode 退出时关 mouse tracking 防 SGR flood

### `1e5ff4a57 fix(hermes-ink): disable mouse tracking on raw-mode teardown to stop SGR leak (#42527)`

raw-mode teardown 关了 modifyOtherKeys / kitty keyboard / focus reporting / bracketed paste，但留 DEC mouse tracking（1000/1002/1003/1006）asserted。raw mode 关后无 reader，terminal 把 hover report 当文本回显（`"35;col;row M flood over the prompt"` bug）。

- teardown 现在 `DBP` 后、`stdin.setRawMode(false)` 前写 `DISABLE_MOUSE_TRACKING`（`ui-tui/packages/hermes-ink/src/ink/components/App.tsx:343-351`）
- import 加 `DISABLE_MOUSE_TRACKING` 与既有 DEC 常量并列 at `:33`
- 再入路径用 Ink 实例 re-assert terminal modes，`setImmediate` 让它落在 alt-screen enable write 之后（`:312-327`）：`setImmediate(() => { instances.get(this.props.stdout)?.reassertTerminalModes() })`。`reassertTerminalModes` gate on `altScreenActive` + idempotent
- 与 `handleSuspend()`（`:498-503`，在 SIGSTOP 前已写 `SHOW_CURSOR + DFE + DISABLE_MOUSE_TRACKING`）对齐

---

<a id="14-gateway-ui-cluster"></a>
## 14. Gateway-UI — image_generate auto-deliver + bare fenced code blocks + MarkdownV2 edit finalize

### 14.1 `9351cbafa fix(gateway): auto-deliver image_generate output as native media (#42616)`

`image_generate` 返 JSON artifact `{"image": "/abs/path.png"}` 不带 `MEDIA:` tag，gateway 自动 append 路径（只识 `text_to_speech` MEDIA: tag）从不送 — image 交付静默依赖模型在回复中重述路径。

- producer allowlist 加 `"image_generate"`：`_AUTO_APPEND_MEDIA_TOOL_NAMES = {"text_to_speech", "text_to_speech_tool", "image_generate"}`（`gateway/run.py:691-695`）
- 新常量 `_JSON_MEDIA_TOOL_PATH_FIELDS = ("host_image", "image", "agent_visible_image")`（`:702`）—— 优先级序的 JSON 字段
- 提取 loop in `_collect_auto_append_media_tags`（`:766-787`）：tool == `image_generate` 且 content 无 MEDIA: 时 `json.loads` tool result，require `payload.get("success")`，遍历 `_JSON_MEDIA_TOOL_PATH_FIELDS`，第一个 match `_TOOL_MEDIA_RE.fullmatch(f"MEDIA:{path}")` 且不在 `history_media_paths` 的，append `f"MEDIA:{path}"` 到 `media_tags`

### 14.2 `3705625b7 feat(gateway): render terminal commands as bare fenced code blocks in chat (#42576)` + `8d99b5bc4` cap in non-verbose mode (#42729)

Terminal-tool progress 在 markdown-capable gateway 上现以 fenced code block 渲染全命令（verbose）或单行 capped fence（all/new），gate on adapter `supports_code_blocks` capability。fence **无** language tag — Slack mrkdwn 把 ```` ```bash ```` 中的 "bash" 当字面 first code line。

- `supports_code_blocks: bool = False` at `gateway/platforms/base.py:1804`。设 True：telegram（`:349`）、slack（`:320`）、whatsapp（`:245`）、matrix（`:423`）、weixin（`:1141`）、feishu（`:1412`）、discord plugin（`plugins/platforms/discord/adapter.py:577`）
- progress callback 最终 block-builder at `gateway/run.py:13017-13043`：`_code_block_full = f"{emoji} {tool_name}\n\`\`\`\n{_cmd_full}\n\`\`\`"`；`_code_block_short` 把首行 cap 到 `get_tool_preview_max_len()`（默认 40）；gate on `getattr(_progress_adapter, "supports_code_blocks", False) and tool_name == "terminal" and isinstance(args, dict) and isinstance(args.get("command"), str) and args["command"].strip()`
- verbose 发 full block（`:13046-13049`）；非 verbose all/new 发 short（`:13072-13073`）

context：`421226e40`（紧前一条）REVERT 了原 `bash` fence #41215（怕 group chat 全命令外泄）；`3705625b7` 用 bare fence 再恢复 default；`8d99b5bc4` cap 非 verbose mode。当前 tree 是后两条形态。

### 14.3 `6d2732e78 fix(gateway): apply MarkdownV2 formatting on progress message edits`

adapter 设 `REQUIRES_EDIT_FINALIZE=True` 时，tool-progress edit 现传 `finalize=True` 让 `format_message()` 在 edit 命中平台前应用。

- `gateway/run.py:13186-13187`：`if getattr(adapter, "REQUIRES_EDIT_FINALIZE", False): kwargs["finalize"] = True` 加入 `edit_message` kwargs
- `REQUIRES_EDIT_FINALIZE: bool = False` on base adapter（`gateway/platforms/base.py:2265`）；Telegram（`gateway/platforms/telegram.py:361`）+ DingTalk 设 True（`gateway/platforms/dingtalk.py:173` via property）

之前初始 `send()` 走 MarkdownV2 但后续 `edit_message()` 跳，raw markdown（如 fenced block）在 Telegram 显为纯文本。

### 14.4 `f4531feee fix(telegram): improve MarkdownV2 edit fallback and fix _strip_mdv2 bold handling`

`edit_message(finalize=True)` MarkdownV2 parse error 时静默 fallback 改为 log + `_strip_mdv2()` 干净纯文本 fallback。

- `gateway/platforms/telegram.py:2213-2222`：`logger.warning("[%s] MarkdownV2 edit failed, falling back to plain text: %s", self.name, fmt_err)` 然后 `_plain = _strip_mdv2(content) if content else content`，调 `edit_message_text(..., text=_plain)`
- `_strip_mdv2` 现在先 strip 标准 markdown `**bold**` 再 strip MarkdownV2 `*bold*` 防 `**` 半剥成 `*`。顺序 at `:184-187`：
  1. `re.sub(r'\\([_*\[\]()~\`>#\+\-=|{}.!\\])', r'\1', text)` 反斜杠 unescape
  2. `re.sub(r'\*\*([^*]+)\*\*', r'\1', cleaned)` 标准 markdown 粗体（**新**）
  3. `re.sub(r'\*([^*]+)\*', r'\1', cleaned)` MarkdownV2 粗体
  4. `re.sub(r'(?<!\w)_([^_]+)_(?!\w)', r'\1', cleaned)` 斜体
  5. `re.sub(r'~([^~]+)~', r'\1', cleaned)` 删除线
  6. `re.sub(r'\|\|([^|]+)\|\|', r'\1', cleaned)` spoiler

---

<a id="15-sessions-dashboard"></a>
## 15. Sessions / Dashboard — max_concurrent_sessions cap + dashboard register idempotent

### 15.1 `639c1e363 feat(sessions): add optional max session cap`（Robin Fernandes）

可选并发会话上限跨 CLI / TUI / dashboard chat / messaging gateway。cap 是 best-effort 单主机/单 profile runtime 守护；本地 lease registry 无法读/锁则 fail open。

**Config**：
- `cli-config.yaml.example:531-536` 文档化顶层 `max_concurrent_sessions: null`
- `hermes_cli/config.py:808-810` `DEFAULT_CONFIG["max_concurrent_sessions"] = None`
- `gateway/config.py:59-92` `_coerce_optional_positive_int(value, key)`：拒 bool，接受 None/0/负 = "disabled"，warn 垃圾
- `gateway/config.py:534` `GatewayConfig.max_concurrent_sessions: Optional[int] = None`；`to_dict()` 暴露（`:640`）
- `gateway/config.py:686-696` `GatewayConfig.from_dict()` 读 `max_concurrent_sessions`（顶层优先）+ `gateway.max_concurrent_sessions` fallback
- `gateway/config.py:814-820` `load_gateway_config()` 读双层

**Lease registry**（新文件 `hermes_cli/active_sessions.py`）：
- `coerce_max_concurrent_sessions()` at `:24` + `resolve_max_concurrent_sessions(config)` at `:56` 实现优先级（顶层 over gateway.*）
- `active_session_limit_message(active_count, max_sessions)` at `:73` —— 单一 source `"Hermes is at the active session limit (N/M). Try again when another session finishes."`
- Registry 存 `get_hermes_home() / "runtime" / "active_sessions.json"` + `.lock` 兄弟（`:80-89`）
- `_FileLock` at `:92-142`：POSIX `fcntl.flock` / Windows `msvcrt.locking`
- `_pid_alive(pid, process_start_time)` at `:188-209`：`psutil.Process(pid).create_time()` 抗 pid recycling
- `try_acquire_active_session(*, session_id, surface, config, metadata=None)` at `:234-295`：返 `(ActiveSessionLease, None)` 成功 / `(None, limit_message)` cap-reached。cap disabled 时返 no-op enabled-False lease 让 caller 无条件 release
- `release_active_session()` at `:298` + `active_session_registry_snapshot()` at `:314`

**Gateway 集成** in `gateway/run.py`：
- `_active_session_leases: Dict[str, Any] = {}` at `:1937`（与 `_running_agents_ts` 并列）
- `_get_max_concurrent_sessions()` at `:3394`
- `_active_session_limit_message(session_key)` at `:3403` —— fast in-process check 在 file lock 之前
- `_claim_active_session_slot(session_key, source)` at `:3419`：包 `try_acquire_active_session`，`surface=f"gateway:{platform}"` + `metadata: platform/chat_id/user_id`
- inbound message handler at `:7298` 在 `_running_agents[_quick_key] = _AGENT_PENDING_SENTINEL` 之前 claim slot，cap-reached 直接 early return limit message
- `_release_running_agent_state` at `:12048-` release lease；shutdown at `:5808` 显式 release 所有

**CLI 集成** in `cli.py`：
- `HermesCLI.__init__:3465` `self._active_session_lease = None`
- `_claim_active_session(surface, *, stderr)` at `:3494` + `_release_active_session()` at `:3522`，atexit 注册
- `HermesCLI.run()` at `:10540` 和 single-query at `:13328` 都 claim；rejection exit 1

**TUI 集成** in `tui_gateway/server.py`：
- `_claim_active_session_slot(session_key, *, live_session_id, surface="tui")` at `:348` + `_release_active_session_slot(session)` at `:369`
- `_finalize_session` at `:384` release lease
- `session.start`（`:3320`）/ `session.resume`（`:3537`）/ `session.branch`（`:4242`）都 claim；rejection 返 JSON-RPC `4090`

**测试**（5 个新文件 / 1 增补）+ 文档 at `website/docs/user-guide/configuration.md:1420-1437`。

### 15.2 `52ae9d9f0 feat(dashboard): make hermes dashboard register idempotent (#42455)`（benbarclay）

重跑 `hermes dashboard register` 现更新已存 record 而非建副本。stable key：先前持久化的 `HERMES_DASHBOARD_OAUTH_CLIENT_ID`。

`hermes_cli/dashboard_register.py`：
- `_register_self_hosted_client(...)` at `:93` 签名加 `existing_client_id: Optional[str] = None`；`name` 变 `Optional[str]`。body（`:117-127`）`name` 空时省略；`client_id` 仅在 `existing_client_id` set 时含
- `cmd_dashboard_register(args)` at `:230`：
  - `:269-280` 读 `existing_client_id = get_env_value("HERMES_DASHBOARD_OAUTH_CLIENT_ID")`，blank 归 None
  - `:282-290` name 选择：显式 `--name` 赢；否则 re-run 保留；否则 auto-generate
  - `:308` 传 `existing_client_id=existing_client_id`
  - `:314-321` 区分 "Updated dashboard" vs "Registered dashboard"

测试 `tests/hermes_cli/test_dashboard_register.py:169` 新 `TestIdempotentRerun(TestHappyPath)` 7 case。

---

<a id="16-agent-codex-api"></a>
## 16. Agent / Codex / API — char-based output-cap overflow + Codex app-server token usage + api-server session ctx 绑定

### 16.1 `3a74b7521 fix(agent): recover from char-based output-cap overflow (#42741)` + `57775e9e1` test 覆盖

xxxigm。识别 LM Studio / llama.cpp 把 context window 报成 token 但 prompt 报成 **字符** 的措辞，例如 `"maximum context length is 65536 tokens. However, you requested 65536 output tokens and your prompt contains 77409 characters"`。原 parser 把这个误路由到 prompt-too-long / compression（输入已 fit 进窗 compression 帮不了）而非降 `max_tokens`。

`parse_available_output_tokens_from_error()` in `agent/model_metadata.py`：
- 识别新措辞 as output-cap error at `:973-983`：加 `or` branch 匹配 `"maximum context length" in error_lower and "requested" in error_lower and "output tokens" in error_lower`
- 从 chars 估 input（~3 chars/token，保守）at `:1014-1028`：新 regex `r'maximum context length is (\d+)\s*token'` + `r'prompt contains (\d+)\s*character'`；返 `_ctx - (_chars + 2) // 3`

测试 `tests/test_output_cap_parsing.py:28-64` 三 case：char-based 65536-tok / 77409-char 例 → `39733`、invariant `available + ceil(chars/3) <= ctx`、prompt > window → None。

### 16.2 `cb4cc08b0 fix(codex): record app-server token usage in session accounting`（JP Lew）

Codex app-server 不在 `turn/completed` 上放 token usage；分开发 `thread/tokenUsage/updated` 通知。Hermes 在丢——既 `session_*` counter 不长，context compressor 也不动。

- `agent/codex_runtime.py:28-40` `_coerce_usage_int()` 助手 clamp 到 non-negative int（拒 bool、收 float/str）
- `agent/codex_runtime.py:43-173` `_record_codex_app_server_usage(agent, turn)` 翻译 Codex bucket 名（`inputTokens / cachedInputTokens / outputTokens / reasoningOutputTokens / totalTokens`）为 Hermes canonical accounting via `CanonicalUsage + estimate_usage_cost`；Codex app-server 不暴露 cache-write，所以 `cache_write_tokens=0` on this runtime（`:89`）；总 increment `session_api_calls`（`:57`）即使 Codex 省 usage block；持久化 `api_call_count=1` via `_session_db.update_token_counts`（`:61-69, 158`）
- `agent/codex_runtime.py:270` 从 `run_codex_app_server_turn()` 调用，并 `**usage_result`（`:322`）merge 入返回 dict

`agent/transports/codex_app_server_session.py`：
- `TurnResult` dataclass 加 `token_usage_last / token_usage_total / model_context_window`（`:75-77`）
- `_apply_token_usage_notification(result, note)` at `:811-832` 捕 `thread/tokenUsage/updated`（pending drain at `:507` + 主 loop at `:543`）

测试 `tests/agent/transports/test_codex_app_server_session.py:199-230` + `tests/run_agent/test_codex_app_server_integration.py:87-136`（断言 prompt=100 / completion=25 / reasoning=5 / `context_compressor.context_length == 200000`）。

### 16.3 `b23184cad fix(api-server): bind request session context for tools`（helix4u）

api-server `_run_agent` executor 从未装 request session-context contextvar，tools（与 `_make_run_env` 衍生子进程）看到 stale `HERMES_SESSION_ID`（来自 OS env）。

- `gateway/platforms/api_server.py:3510-3554` wrap executor body in `set_session_vars(platform="api_server", chat_id=session_id or "", session_key=gateway_session_key or session_id or "", session_id=session_id or "")` + `try/finally: clear_session_vars(tokens)`
- `gateway/session_context.py:109` `set_session_vars(..., session_id: str = "")` 新参数
- `gateway/session_context.py:131` `_SESSION_ID.set(session_id)` 加 token list
- `gateway/session_context.py:162` `_SESSION_ID` 加 `clear_session_vars` reset loop

测试 `tests/gateway/test_session_api.py:78-122 test_run_agent_binds_api_session_context_for_tool_env` 证 contextvars override pre-set `HERMES_SESSION_ID=stale-session` 抵达 `tools.environments.local._make_run_env`。

---

<a id="17-terminal-cluster"></a>
## 17. Terminal — POSIX `_SANE_PATH` 完整化 + TERMINAL_CONFIG_ENV_MAP test 锚定

### 17.1 `a38cc69bc fix(terminal): complete sane PATH entries on POSIX (salvage of #35614) (#42653)`

修 macOS launchd / gateway 会话 — `PATH` 已含 `/usr/bin` 但缺 `/opt/homebrew/{bin,sbin}`，Homebrew CLI (`gh`/`brew`) 对 terminal tool 不可见。原代码用 `/usr/bin` 作哨兵命中即跳过注入。

`tools/environments/local.py`：
- `_SANE_PATH` 不变 at `:297-300`：`"/opt/homebrew/bin:/opt/homebrew/sbin:/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin"`
- `_append_missing_sane_path_entries(existing_path: str)` at `:303-348`：POSIX 重写 caller PATH — strip 空 entry（leading/trailing/double `:` = CWD in POSIX shells）、collapse 重复 first-occurrence-wins、append `_SANE_PATH` 中缺失的 entry。Windows no-op
- `_path_env_key(run_env)` at `:351-366`：POSIX 返 `"PATH"`；Windows 扫 env case-variant key（`Path` vs `PATH`）让写入保留 caller 大小写
- `_make_run_env(env)` at `:384-386` 现 `path_key = _path_env_key(run_env); if path_key is not None: run_env[path_key] = _append_missing_sane_path_entries(run_env.get(path_key, ""))` 替旧 `/usr/bin` 启发

显式 **不** 与 `tools/browser_tool._merge_browser_path` 合并（语义不同：prepend vs append、`os.path.isdir` 过滤、动态候选集）。

### 17.2 `76f89d66d fix(test): track TERMINAL_CONFIG_ENV_MAP after env-sync consolidation (#42695)`

terminal-config env bridging 合到 canonical `TERMINAL_CONFIG_ENV_MAP` 后，AST scanner `test_terminal_config_env_sync.py` 仍找已消失的 `_config_to_env_sync = {...}` 字面量，挂 `AssertionError`。

`tests/tools/test_terminal_config_env_sync.py:89-103` `_save_config_env_sync_keys()` 改读 `hc_config.TERMINAL_CONFIG_ENV_MAP` 直接（除 `cwd` 另处理）。canonical map at `hermes_cli/config.py:5156`；`terminal_config_env_var_for_key()` at `:5193` 是 bridge；`set_config_value` at `:6132` 调它。module docstring + error message 指 `TERMINAL_CONFIG_ENV_MAP`。

---

<a id="18-yuanbao"></a>
## 18. Yuanbao — 媒体解析三阶段 middleware 拆分（QuoteContext / MediaResolve / PatchAnchors）

### 18.1 `ffcd9d7ac refactor(yuanbao): consolidate media resolution into dedicated pipeline middlewares`（loongzhao）

把原 `DispatchMiddleware._dispatch_inbound_event()` 内 inline 的 quote-media-resolve + observed-media-backfill + anchor-patching hoisting 到独立 middleware（pipeline 阶段可见 + 可独立测试）。

`gateway/platforms/yuanbao.py`：

- 新 regex at `:164`：`_YB_LOCAL_MEDIA_RE = re.compile(r"\[(\w+):[^\]]*?(/[^\]]+?)\s*\]")` — 匹配已 patched local-media anchor
- `QuoteContextMiddleware` at `:2195`：`_extract_quote_context()` 现 instance method，仅返 `(reply_to_message_id, reply_to_text)`（旧 static 返 3 个）。media-ref extraction 移入 `_extract_media_refs_from_transcript(ctx)`
- `MediaResolveMiddleware` at `:2269`：
  - `_resolve_ybres_refs(adapter, refs, *, log_prefix)` at `:2518` — 共享核 resolve `[(rid, kind, filename), ...]` list（删旧 `_resolve_by_resource_id` 间接）
  - `_resolve_quote_media(adapter, quote_media_refs)` at `:2606` — wrapper
  - `_collect_quote_local_media(ctx)` at `:2619` — **私聊** fallback 从 `_msg_content_cache` 恢复已 local 路径（不 re-download）
  - `_collect_observed_media(adapter, source)` 委托 `_resolve_ybres_refs(..., log_prefix="observed-media")` at `:2601`
  - `handle()` 三源 first-occurrence dedup（`_add_unique_pairs` helper, `:2678-2686`）：(1) own media (2) quote media if `reply_to_message_id` set（或 `_collect_quote_local_media` for DM）(3) group-observed backfill 仅 `chat_type == "group"` 且无 quote
  - placeholder re-check 用 `own_count`（`:2716`）而非全 URL，保留原语义
- `PatchAnchorsMiddleware` 新 at `:2725-2755` —— 在 `MediaResolveMiddleware` 填 `ctx.media_urls` 后把 `[kind|ybres:RID]` anchor 替本地路径。`_patch`（`:2735-2754`）仅 `/` 开头才 sub，生成 `[image: /path]` 或 `[file: label → /path]`
- `InboundPipelineBuilder` at `:2867` `_DEFAULT_STAGES` 现在 `MediaResolveMiddleware` 后 `DispatchMiddleware` 前包 `PatchAnchorsMiddleware`
- `InboundContext.media_urls/media_types` docstring at `:947-952` 文档化 3 源 dedup contract

净 diff：`yuanbao.py +446/-283`；`tests/test_yuanbao_pipeline.py +571/-283`。

### 18.2 `24a934295 test(yuanbao): add missing patch import to pipeline tests`

`tests/test_yuanbao_pipeline.py:23` `from unittest.mock import AsyncMock, MagicMock, patch`（之前漏 patch，25 个 call site 全 `NameError`）。

---

<a id="19-skills-hub"></a>
## 19. Skills-hub — 退化 index fail-loud + GitHub 429 识别 + ClaudeMarketplace 持久 GitHubSource

### `5e9d7a766 fix(skills-hub): stop shipping a degenerate index when GitHub taps collapse (#42347)`（Teknium）

两个相互放大的 bug 让 GitHub-API-rate-limit-zeroed catalog 上线（OpenAI / Anthropic / HuggingFace / NVIDIA / gstack / Claude Marketplace / Well-Known tab 全消失；ClawHub / skills.sh / LobeHub / browse.sh 幸存）：

**Bug 1 — write-before-check**：`scripts/build_skills_index.py` 在 health check 之前已写 `OUTPUT_PATH`，github floor (30) trip 时已落盘 degenerate 文件。`deploy-site.yml` 用 `|| echo non-fatal` 吞 exit，`extract-skills.py` 读 partial index。

`scripts/build_skills_index.py`：
- `:300-313` 从 `getattr(source, "is_rate_limited", False)` 收集 `rate_limited_sources`，early WARNING
- `:374-405` health-check 失败时 print rate-limit 信息后 **exit 2 不写**（`:397-405` 注释明示已修 bug）
- `:407-419` 仅成功后才写 `OUTPUT_PATH`

`.github/workflows/deploy-site.yml:62-77`：`|| echo "Skills index build failed (non-fatal)"` 移除，step 严格致命。

**Bug 2 — silent rate-limit return**：`GitHubSource._list_skills_in_repo` 403 时返 `[]` 不 retry 不 flag。

`tools/skills_hub.py`：
- 新共享 `_github_get(url, *, params=None, headers=None, timeout=15.0, max_retries=3)` on `GitHubSource`（`:651-723` 区域）：5xx + 传输错重试指数退避，403/429 看 `Retry-After / X-RateLimit-Reset`，终极 exhaustion 经 `_check_rate_limit_response` flag `_rate_limited`。`429` 现也视作 rate-limit 信号（`:643`）
- `_list_skills_in_repo`（约 `:550`）：经 `_github_get` 而非裸 `httpx.get`
- `_fetch_file_content`（约 `:848`）：同
- `ClaudeMarketplaceSource`（约 `:2448`）现持持久 `GitHubSource` 让 rate-limit 状态跨 marketplace-index fetch + per-skill `inspect()` call 保留；`is_rate_limited` property 代理 `self.github.is_rate_limited`；marketplace.json fetch（`fetch_marketplace_index` 区，`:2519+`）现经 `self.github._github_get`

新测 `tests/scripts/test_build_skills_index_health.py`：
- `test_degenerate_crawl_exits_nonzero_and_writes_no_file`
- `test_healthy_crawl_writes_index_with_all_sources` —— `{"github", "claude-marketplace", "well-known"} <= sources`

---

<a id="20-doctor"></a>
## 20. Doctor — 命名 custom provider vendor-slug 接受 + aggregator 识别

### 20.1 `732ababa1 fix(doctor): allow vendor slugs for named custom providers`

`hermes doctor` 把合法的 `model.provider: custom:hpc-ai` + `model.default: deepseek/deepseek-v4-flash` flag 成 broken 因 vendor-slug allowlist 只含 bare `"custom"`。

- `hermes_cli/doctor.py:743` `provider_policy_id = str(provider_for_policy or "").strip().lower()` 提前
- `:756-759` slug-warning gate 加 `and not provider_policy_id.startswith("custom:")`

`hermes_cli/providers.py:493-499` `is_aggregator(provider)` 现 normalize 后 `provider_norm.startswith("custom:")` 全返 True（命名 custom provider 总 fronting OpenAI-compatible aggregator）。

测试 `tests/hermes_cli/test_doctor.py:543 test_run_doctor_accepts_vendor_slugs_for_named_custom_provider` + `tests/hermes_cli/test_model_switch_custom_providers.py:68`。

### 20.2 `aa424e51a refactor(doctor): fold custom-provider vendor-slug check into one predicate`

无行为变化。Collapse 两 case 为单 `provider_accepts_vendor_slug` predicate at `hermes_cli/doctor.py:747-760`：
- `providers_accepting_vendor_slugs` set 删 `"custom"`（保留 `openrouter / auto / kilocode / opencode-zen / huggingface / lmstudio / nous`）
- 新 `provider_accepts_vendor_slug` OR 三 case：set 命中 / `== "custom"` / `.startswith("custom:")`
- slug-warning gate at `:761-766` 改 `and not provider_accepts_vendor_slug`

---

<a id="21-deps-and-misc"></a>
## 21. Deps / Packaging / Nix / Install / Docs

### 21.1 `f6416f50f fix(deps): bump urllib3 and PyJWT to clear CVEs (#40179)`（liuhao1024）

`pyproject.toml:85-91`：
- `PyJWT[crypto]==2.12.1 → 2.13.0`（PYSEC-2026-175/177/178/179）
- 加 `urllib3>=2.7.0,<3`（GHSA-mf9v-mfxr-j63j 解压炸弹 bypass + GHSA-qccp-gfcp-xxvc 敏感 header 跨 origin proxy redirect 转发）
- `uv.lock` 同步（pyjwt 2.12.1→2.13.0；urllib3 2.6.3→2.7.0，transitive→direct）

### 21.2 `b5421f4ba fix(deps): declare packaging as a core dependency so it ships everywhere (#40522)`（Brian D. Evans）

`packaging` 在三条生产路径直接 import 但从未声明，仅经 pip/uv pull-from-tools transitively 到达用户。slim 官方 Docker image 没装，静默降级：`plugins/memory/hindsight/__init__.py:_meets_minimum_version` 返 False（关 `update_mode='append'` 让每 session 漏 separate Hindsight docs — #40503）、`tools/lazy_deps.py:_is_satisfied` 降级 "installed counts as satisfied"（破每 lazy extras 版本约束）、`hermes_cli/main.py` 降 naive `name==version`。

`pyproject.toml:64-73` 加 `"packaging==26.0"` 入 `[project.dependencies]` 带 long-form 注释。pin 与 `uv.lock` 已存版本一致。pure-Python `py3-none-any` wheel。

测试 `tests/test_packaging_metadata.py:18-30` 加 `_distribution_name(requirement)` PEP 508-robust（剥 markers / direct-reference / extras / 任何 operator），`:81-100` `test_packaging_declared_as_core_dependency` 锁定。

### 21.3 `5750d058f fix(tests): use cross-platform pytest-timeout method (#39881)`（Rod Boev）

`pyproject.toml:330-335` `--timeout-method=signal → thread`（signal 是 POSIX-only `SIGALRM`，thread 跨 Windows）。

### 21.4 `a5d05cf30 fix(nix); don't run .#fix-lockfiles`（HEAD）

`nix/lib.nix:152-160` npm-lockfile-changed branch 不再调 `${fixLockfilesExe}` after `npm ci`。理由 *"its so slow"*。注释从 "Runs npm ci + fix-lockfiles" → "Runs npm ci"。

### 21.5 `02f878ec5 docs(windows): correct native data dir to %LOCALAPPDATA%\hermes (#42856)`（xxxigm）

Windows-native 文档原称 config/auth/skills/sessions 住 `%USERPROFILE%\.hermes`，但 `scripts/install.ps1` 设 `HERMES_HOME=%LOCALAPPDATA%\hermes`。

- `website/docs/user-guide/windows-native.md:48, 78, 205-216, 238, 265, 272-273` 全部纠正为 `%LOCALAPPDATA%\hermes`；PATH/HERMES_HOME 步骤 + 数据布局表 + 拆分原因 + env-var 节 + uninstall 全部一致
- `scripts/install.ps1:1725-1815` 每条 `Write-Success "Created ~/.hermes/..."` 现 interpolate `$HermesHome`

### 21.6 `68a997fed add website links to readme for seo`

`README.md:6-8` H1 下加居中行 `Hermes Agent | Hermes Desktop`（双链接 `hermes-agent.nousresearch.com`）。

---

<a id="22-tests-author-merges"></a>
## 22. Tests / Author maps / Merge nodes

### 22.1 测试 harden

- `50ad191a8 test(hermes_cli): harden concurrent-gate fixture against partial-import race (#42626)`：`tests/hermes_cli/conftest.py:44-56` autouse `_suppress_concurrent_hermes_gate` fixture `monkeypatch.setattr(main, "_detect_concurrent_hermes_instances", ...)` 加 `raising=False` 防 xdist spawn race
- `d6c11a457 test(run_agent): fix racy ordering in test_concurrent_handles_tool_error (#42356)`：`tests/run_agent/test_run_agent.py:2402-2433` tool call 现按 `args.get("q") == "boom"` 决定 raise 而非共享计数器，断言显式 check `tool_call_id`

### 22.2 AUTHOR_MAP entries

四条 `scripts/release.py:+1` 各加 contributor email → GitHub handle：
- `3dcfbbfc4` underthestars-zhy（PR #42444 Photon gRPC channel work salvage）
- `ba622d44e` m4dni5
- `300371c3f` ruangraung（PR #42308 salvage）
- `a1cb84aca` mnajafian-nv（unblock #41551）
- `c3420d91a` jooray（PR #27978 simplex salvage）

### 22.3 Merge 节点（合入的 PR）

- `49dd776d8` PR #43041 `fix/fable-anthropic`
- `6a8dda171` PR #42515 `fix/desktop-debug-report-links`
- `1e3b3dfab` PR #40560 `kamonspecial/fix/langfuse-usage-sanitized-response`
- `d3992d1a2` PR #42331 `mnajafian-nv/fix/nemo-relay-adaptive-config-shape`
- `1db79bfe1` Merge `main` into `fix/nemo-relay-adaptive-config-shape`（intermediate）
- `3f1758d2e` PR #41551 `mnajafian-nv/fix/hermes-plugin-openinference-finalization`
- `cf4963037` Merge `main` into `fix/hermes-plugin-openinference-finalization`（intermediate）
- `9fd3d5cf8` PR #42380 `kshitijk4poor/chore/author-map-mnajafian`

---

<a id="23-verification"></a>
## 23. 验证基线

工作目录：`/tmp/hermes-agent`（blobless clone）
HEAD 验证：`a5d05cf30e8f5d79689ff8cee3ed806a6ca1c00e`（`a5d05cf30 fix(nix); don't run .#fix-lockfiles`，2026-06-09T16:47:42 -0400）
排除基线：`754154a9c2faaff9e00932fa3c9e32b3ed936fb4`（2026-06-08T13:32 -0700）
范围：`git log --oneline 754154a9c..a5d05cf30` = **105 commit**（97 non-merge + 8 merge）

每条结论都至少一条 `grep -n` / `Read` / `git show <sha> --stat` 命中 `/tmp/hermes-agent` checkout @ `a5d05cf30`；每个 `file:line` 引用现行 source 实际行号；功能描述对照对应 PR 标题 + commit body + 实际 hunk 内容。

**已实证的关键 file:line（按章节）**：

- **Photon**：`plugins/platforms/photon/{adapter.py(1162行),auth.py(1065行),cli.py(387行),sidecar/index.mjs(542行),sidecar/package.json(24行),plugin.yaml(76行)}` 全部按章节给出 grep 验证锚点（`PhotonAdapter / validate_photon_token / store_user_numbers / load_user_numbers / refresh_user_numbers / user_assigned_line / list_users / create_user / find_user_by_phone / _spectrum_host / _basic / _device_response_token_candidates / _validated_dashboard_token / DEFAULT_CLIENT_ID="photon-cli" / DEFAULT_SCOPE / PhotonDashboardAuthError / _is_duplicate / format_message / _send_with_retry / _sidecar_send_attachment / _cache_inbound_attachment / _IMAGE_EXT_BY_MIME / _AUDIO_EXT_BY_MIME / _env_enablement`）
- **SimpleX**：`plugins/platforms/simplex/{adapter.py(1308行),plugin.yaml(57行)}`：`_pending_file_transfers / _pending_text_batches / auto_accept / group_allow_from / rcvFileDescrReady / rcvFileComplete / MEDIA: / send_image / send_voice / send_video / send_document / _redact_id / _send_command / _pending_responses / _corr_counter / HEALTH_CHECK_STALE_THRESHOLD / MAX_MESSAGE_LENGTH`
- **Plugins**：`tui_gateway/server.py:174-180, 9252-9328`、`ui-tui/src/components/pluginsHub.tsx(238行)`、`ui-tui/src/app/overlayStore.ts(55行)`、`cli.py:7303-7366`、`hermes_cli/plugins_cmd.py:138, 192, 430, 465, 537, 1542`、`MANIFEST.in:3`、`pyproject.toml:288-301`
- **Anthropic / Models**：`agent/anthropic_adapter.py:76-117, 126-128, 242-289`、`agent/model_metadata.py:144-145, 950-1030, 1815-1835`、`plugins/model-providers/openrouter/__init__.py:13-44, 118-140`、`hermes_cli/models.py:36, 77, 80, 158, 329, 772-889`、`hermes_cli/model_catalog.py:359-387`、`hermes_cli/main.py:5836-5838, 8402-8416`、`website/static/api/model-catalog.json:16, 152`（`updated_at: 2026-06-09T17:20:16Z`）
- **Observability**：`plugins/observability/nemo_relay/__init__.py:12, 48, 63-121, 142-166, 168-241, 288-323, 325-403, 608-, 696-719, 844-865`、`plugins/observability/langfuse/__init__.py:840-849`
- **Desktop**：`apps/desktop/src/app/session/hooks/use-route-resume.ts(142行)`、`apps/desktop/src/app/session/hooks/use-prompt-actions.ts:107-118, 230-336, 410-422, 487, 493-520`、`apps/desktop/electron/main.cjs:132-134, 2701, 4301-4327, 4514-4576, 4784, 5193`、`apps/desktop/tsconfig.json:3,5`、`apps/desktop/src/components/assistant-ui/thread.tsx:923, 934`、`apps/desktop/src/lib/external-link.tsx:19, 270-277`、`apps/desktop/src/lib/session-source.ts:40, 46-66, 71-75, 89-104`、`apps/desktop/src/store/session.ts:85-100, 143-148`、`apps/desktop/src/app/desktop-controller.tsx:136-141, 303-321, 323-345`、`apps/desktop/src/app/chat/sidebar/index.tsx:143-167, 170-185, 545-582, 932-970`、`apps/desktop/src/types/hermes.ts:300-308`、`apps/desktop/src/app/types.ts:30-42`、`apps/desktop/src/store/updates.ts:88-92`、`hermes_cli/profiles.py:1010-1071`、`hermes_cli/main.py:5229-5253`、`scripts/install.sh:2293, 2352, 2369, 2437-2459`、`hermes_cli/dashboard_register.py:93-127, 184-198, 230-330, 354-372, 394-416`
- **TUI / CLI**：`hermes_cli/config.py:5156, 5187-5190, 5193-5198, 5201-5239, 6131-6133`、`hermes_cli/main.py:1825-1832`、`hermes_cli/web_server.py:8573-8580`、`tui_gateway/server.py:1997, 2592-2603, 2606-2612, 2649, 2908, 3844-3856, 5573, 5635-5665, 5745-5783, 5786-5823, 7295-7321, 9045-9052, 1041-1057, 6845-6911`、`cli.py:6221-6240, 6254-6262, 6265, 6306, 8268, 8295`、`tools/terminal_tool.py:2436-2444`、`scripts/check_subprocess_stdin.py`、`agent/anthropic_adapter.py:1203-1230`
- **hermes-ink**：`ui-tui/packages/hermes-ink/src/ink/components/App.tsx:33, 312-327, 343-351, 498-503`
- **Gateway-UI**：`gateway/run.py:691-695, 702, 766-787, 13017-13073, 13186-13187`、`gateway/platforms/base.py:1804, 2265`、`gateway/platforms/telegram.py:176, 184-187, 349, 361, 2213-2222`、`gateway/platforms/dingtalk.py:173`、`gateway/platforms/slack.py:320`、`gateway/platforms/whatsapp.py:245`、`gateway/platforms/matrix.py:423`、`gateway/platforms/weixin.py:1141`、`gateway/platforms/feishu.py:1412`、`plugins/platforms/discord/adapter.py:577`
- **Sessions / Dashboard**：`cli-config.yaml.example:531-536`、`hermes_cli/config.py:808-810`、`gateway/config.py:59-92, 534, 640, 686-696, 814-820`、`hermes_cli/active_sessions.py(321行)` 整个新文件、`gateway/run.py:1937, 3394, 3403, 3419, 5805-5815, 7295-7310, 12048-`、`cli.py:3462-3530, 10537-10545, 13325-13340`、`tui_gateway/server.py:345-385, 3317-3340, 3534-3605, 4239-4290`、`hermes_cli/dashboard_register.py:93-127, 230-330`
- **Agent / Codex / API**：`agent/model_metadata.py:950-1030`、`agent/codex_runtime.py:28-173, 270-322`、`agent/transports/codex_app_server_session.py:68-85, 504-543, 811-832`、`gateway/platforms/api_server.py:3486-3554`、`gateway/session_context.py:95-172`
- **Terminal**：`tools/environments/local.py:295-388`、`hermes_cli/config.py:5156, 5193, 5198, 6132`
- **Yuanbao**：`gateway/platforms/yuanbao.py:157-164, 940-952, 2195-2755, 2867+`
- **Skills-hub**：`scripts/build_skills_index.py:290-420`、`tools/skills_hub.py:540-860, 2448-2540`、`.github/workflows/deploy-site.yml:62-77`
- **Doctor**：`hermes_cli/doctor.py:735-775`、`hermes_cli/providers.py:492-499`
- **Deps / Packaging / Nix / Install / Docs**：`pyproject.toml:64-91, 327-335`、`uv.lock`、`nix/lib.nix:152-165`、`scripts/install.ps1:1725-1815`、`website/docs/user-guide/windows-native.md:45-275`、`website/docs/user-guide/configuration.md:1417-1437`、`README.md:6-8`
