---
title: 2026-06-08 跨日同步 — hermes-agent c9863772 → 754154a9c（178 commits，三大主题：Photon Spectrum iMessage **新平台插件** + **God-file 解耦运动 Phase 1-4**（agent/cli/gateway 三大主文件批量拆分入 mixin/subcommand 包）+ Desktop **客户端/后端版本与更新双按钮分离**；伴随 simplify-code skill 新增 + Windows ConPTY 启用 dashboard `/chat` tab + 主题 per-profile + 多 cluster 内存泄漏修补 + 压缩续会话防泄漏四连）
created: 2026-06-08
type: changelog
tags: [changelog, update, v0.16.0-maintenance, photon-imessage-platform, god-file-decomposition, cli-commands-mixin, gateway-slash-commands-mixin, gateway-kanban-watchers-mixin, gateway-authorization-mixin, cli-agent-setup-mixin, model-setup-flows, turn-context, turn-finalizer, turn-retry-state, subcommands-package, simplify-code-skill, desktop-update-split, desktop-version-bar, desktop-remote-update-overlay, desktop-themes-per-profile, desktop-hover-reveal-sidebar, desktop-yolo-global, desktop-viewport-no-follow, windows-conpty-pty-bridge, codex-pool-distinct-entries, openrouter-pool-autodetect, vision-supports-tool-messages, vision-custom-providers-honor, compression-cross-session-guard, compression-background-review-disabled, compression-overrides-recovery, compression-infinite-loop-fix, memory-overflow-instruct, memory-end-of-turn-off-thread, gateway-memory-leak, agent-close-clear, slash-worker-watchdog, cascading-interrupt-hang, codex-responses-converter, acp-provenance-metadata, anthropic-kwargs-sanitize, plugins-nested-discovery, plugins-toctou-fix, update-fetch-scope, install-recover-commitless, systemd-replace-drop, gateway-replace-survive, hindsight-delta-append, terminal-cwd-collapse, file-tools-sentinel-reject, dist-asar-unpack, telegram-polling-conflict-loop, weixin-typing-refresh, slack-channel-scope-fix, busy-input-fifo, modal-prompts-direct-paint, curator-protect-plan, terminal-markdown-bash, dashboard-recent-commits, hermes-time-reset-cache, hooks-thread-id, todo-store-bound, osc8-strip, nix-config-writable, photon-codeql, voice-during-busy]
sources:
  - hermes-agent/main c9863772..754154a9c
total_commits: "178 个独立 commit（含 Merge 节点；linear history）"
---

# 2026-06-08 跨日同步：hermes-agent main `c9863772 → 754154a9c`

> **基线**：wiki `05f3fc2` 对齐 hermes `c98637723…`（2026-06-07T19:54Z，"Merge PR #41482 SEARXNG_URL config-aware"，v0.16.0）
> **目标**：hermes-agent/main HEAD `754154a9c2faaff9e00932fa3c9e32b3ed936fb4`（2026-06-08T13:32 -0700，msg "fix(tests): retry per-file pytest subprocess once on exit-4 when the file exists"）
> **跨度**：约 24 小时单日窗口（实际 commit author date 跨度 04-24 → 06-09，含多个 historic salvage / merge）
> **版本**：仍 **v0.16.0**（`pyproject.toml:10 version = "0.16.0"` + `hermes_cli/__init__.py:17-18 __version__ = "0.16.0"` + `__release_date__ = "2026.6.5"` 不变）；本日全为 feat/fix/test/refactor/chore，未发新版
> **commit 类型分布**：约 **120 fix / 19 feat / 15 refactor / 12 test / 8 chore / 3 docs / 1 hardening**（Merge 节点不计；177 个非 Merge SHA + 1 Merge）
> **验证基线**：`/tmp/hermes-agent` blobless clone + `git checkout 754154a9c2faaff9e00932fa3c9e32b3ed936fb4 -- .` 全 checkout @ HEAD，所有 commit hash / file:line / 路径 / 函数名经 `grep -n` / `git show <sha> --stat` / `Read` 实证

---

## 章节索引

1. [Photon Spectrum iMessage — 全新平台插件（12 commits，inbound webhook + Node sidecar + 完整 channel parity）](#1-photon-platform)
2. [God-file 解耦运动 — agent/conversation_loop Phase 1（TurnContext / TurnRetryState / finalize_turn）](#2-god-file-agent)
3. [God-file 解耦运动 — gateway/run Phase 3（GatewayAuthorizationMixin / GatewayKanbanWatchersMixin / GatewaySlashCommandsMixin 共 5,883 LOC 析出）](#3-god-file-gateway)
4. [God-file 解耦运动 — hermes_cli/cli + main Phase 2/4（CLICommandsMixin / CLIAgentSetupMixin / model_setup_flows + subcommands/ 39 子文件）](#4-god-file-cli)
5. [Desktop — 客户端 / 后端版本与更新按钮分离 + 远端更新 overlay（9 commits）](#5-desktop-update-split)
6. [Desktop — 主题 per-profile + hover-reveal collapsed sidebar + Shift+click YOLO global + viewport 不再追流式输出 + 已结束 session 切走不消失 + scoped 事件 guard 收紧](#6-desktop-misc-cluster)
7. [Skills — 新 bundled skill `simplify-code`（并行 3-agent 评审 + 应用清理）](#7-simplify-code-skill)
8. [Windows — ConPTY (pywinpty) 接通 dashboard `/chat` tab（`hermes_cli/win_pty_bridge.py 新建 179 LOC`）](#8-windows-conpty)
9. [Auth — Codex OAuth 帐户作为独立 pool entry + OpenRouter pool 自动侦测 + approval 路径解析时机修](#9-auth-pool-cluster)
10. [Vision — providers 拒收 list-type tool content 时主动降级 + 尊重 custom_providers 的 per-model `supports_vision`](#10-vision-cluster)
11. [Compression — 四连防泄漏：cross-session `_previous_summary` 守 / background-review fork 关 compression / topic+split 后 `/model`+`/reasoning` override 保 / `transcript 全装 tail budget` 时无限 loop 修](#11-compression-cluster)
12. [Memory — overflow 错语指令 in-turn 整合 + end-of-turn sync 离 turn thread + AIAgent.close 清 _session_messages + gateway 缓存释放 client](#12-memory-leak-cluster)
13. [Gateway — duplicate user msg、cascading interrupt 假 retry、busy_input FIFO、Telegram polling-conflict、DM session 凭 user_id 隔离、`--replace` survive 中止、systemd `--replace` 去除](#13-gateway-cluster)
14. [Auxiliary — Codex Responses 共用转换器（tool message 不再 leak）+ 临时 transport 错先单次重试再走 fallback](#14-auxiliary-cluster)
15. [Plugins — nested category 发现 + enable/disable alias 归一 + lazy-singleton 线程安全 helpers（fix honcho TOCTOU）+ 杂修](#15-plugins-cluster)
16. [Install / Update — 单分支 fetch + commit-less checkout 重 clone（不删旧） + nix config.yaml group-writable + uv 修补](#16-install-update-cluster)
17. [Hindsight / ACP / Curator — 仅新回合 delta 上送 + ACP session provenance 元数据（compression rotation 透明化）+ 内置 `plan` skill 保护](#17-hindsight-acp-curator)
18. [Terminal / File-tools — CWD-only override 合并到 default 容器 + raw task_id 读 cwd / TERMINAL_CWD 哨兵拒绝 + worktree edits anchor 提前](#18-terminal-file-tools)
19. [Slack channel-only scope + Volcengine 工具名 XML 片段剥 + WeChat 输入指示器 ticket 刷新 + Slack thread_ts 一致性 + ollama default_max_tokens 上调 + custom endpoint 256K fallback 前查 DEFAULT_CONTEXT_LENGTHS](#19-platform-models-cluster)
20. [其余修补 — voice 在 agent 跑时也转录 / 终端工具在 markdown 平台渲染 ```bash 代码块 / TUI process.on('exit') 终端态恢复 + Windows PTY stdin / Desktop 包 dist/ 解开 ASAR + 渲染 bundle 缺失硬失败 / hermes_time.reset_cache / hooks 暴 thread_id+chat_type / TodoStore 内容上限 / OSC 8 剥 / 杂](#20-misc)
21. [验证基线](#21-verification)

---

<a id="1-photon-platform"></a>
## 1. Photon Spectrum iMessage — 全新平台插件

**核心 commits**（按时序）：
- `5b4e431e8` `feat(gateway): add Photon Spectrum (iMessage) platform plugin`（2026-05-25 18:55 -0700，初始落地）
- `3a0f6ac3d` `fix(photon): satisfy Windows footgun + CodeQL checks`
- `91db0ab42` `fix(photon): clear remaining CodeQL clear-text-{logging,storage} alerts`
- `55fb422f6` `fix(photon): isolate ALL secret-touching prints behind auth.py helpers`
- `2ee7abf27` `fix(photon): emit credential summary via callback so no tainted value escapes auth.py`
- `6a0cc9bf9` `fix(photon): suppress CodeQL clear-text-logging false-positives in auth.py`
- `083d8b2d6` `fix(photon): collapse credential summary to single-emit literal-blob`
- `8f89c4615` `chore(photon): clean up ty type-checker warnings from lint-diff bot`
- `630318e95` `refactor(photon): fold device login into setup, drop standalone login verb`（2026-06-08 11:33 -0700）
- `d7f42e368` `feat(photon): full channel parity — gateway setup, pairing, PII redaction, doc fixes`（2026-06-08 12:03 -0700）
- `186651857` `feat(photon): group-chat mention gating for full channel parity`（2026-06-08 12:14 -0700）

**架构**（@ HEAD 实证）：
- **新建** `plugins/platforms/photon/` 目录（合计 **1981 行** Python + Node sidecar）：
  - `plugin.yaml 91 行`：`name: photon-platform / label: Photon iMessage / kind: platform / version: 0.1.0`，必填 env `PHOTON_PROJECT_ID + PHOTON_PROJECT_SECRET`，可选 env 14 个含 `PHOTON_WEBHOOK_SECRET / PHOTON_WEBHOOK_PORT (默 8788) / PHOTON_WEBHOOK_PATH (默 /photon/webhook) / PHOTON_SIDECAR_PORT (默 8789) / PHOTON_SIDECAR_AUTOSTART (默 true) / PHOTON_NODE_BIN / PHOTON_API_HOST (默 https://spectrum.photon.codes) / PHOTON_DASHBOARD_HOST (默 https://app.photon.codes) / PHOTON_ALLOWED_USERS / PHOTON_ALLOW_ALL_USERS / PHOTON_REQUIRE_MENTION / PHOTON_MENTION_PATTERNS / PHOTON_HOME_CHANNEL / PHOTON_HOME_CHANNEL_NAME`
  - `adapter.py 844 行` `:200 class PhotonAdapter(BasePlatformAdapter)` + `:795 def register(ctx)` 注册 `ctx.register_platform(name="photon", adapter_factory=lambda cfg: PhotonAdapter(cfg), required_env=["PHOTON_PROJECT_ID", "PHOTON_PROJECT_SECRET"], setup_fn=_cli.gateway_setup, max_message_length=_MAX_MESSAGE_LENGTH, emoji="📱", pii_safe=True, allow_update_command=True, platform_hint=...)`；inbound 经 aiohttp 监听 `_DEFAULT_WEBHOOK_PORT = 8788` `/photon/webhook` 验 `X-Spectrum-Signature` HMAC-SHA256(`v0:{timestamp}:{body}`) + ±5 分钟时钟漂移拒绝 + `message.id` 去重；outbound 因 Photon 暂未公开 HTTP send 端点改 spawn **Node sidecar** loopback 8789 跑 `spectrum-ts` SDK，shared bearer token 鉴权
  - `auth.py 581 行`：RFC 8628 device-code 流 + token 持久化 `~/.hermes/auth.json` 下 `credential_pool.photon`（token）+ `credential_pool.photon_project`（project id+secret）；所有 `print()`/`log()` 经 helper 闸（`_emit_credential_summary` 单次 literal-blob emit），把 CodeQL clear-text-logging 全清零
  - `cli.py 340 行` `hermes photon <subcommand>` 入口：`setup`（含 device login + project + user + sidecar npm deps 一站，**`630318e95` 后 `login` 子命令删除合并入 setup**） + `status` + `--no-browser` flag（从 `login` 迁来）
  - `sidecar/index.mjs 226 行` + `package.json`：`"name": "@hermes-agent/photon-sidecar", "type": "module", "engines": {"node": ">=18.17"}, dependencies: {"spectrum-ts": "^0.1.0"}`；sidecar `npm start` 入口、`Space.send()` 唯一 outbound 通道
  - `README.md 121 行` + `sidecar/README.md` 配套说明

**全 channel parity**（`d7f42e368`）：
- **gateway setup 集成**：`adapter.py:817 setup_fn=_cli.gateway_setup` 让 Photon 出现在 `hermes gateway setup` 统一向导（之前必须 `hermes photon setup` 单走）；新 `cli.py:gateway_setup()` zero-arg entrypoint 复用同 device-login+project+user+sidecar 序列
- **PII 安全**：`pii_safe=True`（之前 `False`，注释已写 "iMessage E.164 numbers should be redacted" 但值不一致），与 BlueBubbles channel 对齐 — session description 在送 LLM 前脱敏
- **支持 `/update`**：`allow_update_command=True` 允许从 Photon 触发 `hermes update`

**group-chat mention 门禁**（`186651857`）：
- `PHOTON_REQUIRE_MENTION` env 或 `config.extra.require_mention`：群聊默认 opt-in 必须命中 wake-word 才唤醒
- `PHOTON_MENTION_PATTERNS` 支持 list/JSON/comma/newline 4 种 shape，默认用 Hermes 通用 wake words
- `_dispatch_inbound` 路径：未命中群消息直接 drop；命中后 wake-word 前缀剥离再送 agent；**DM 永不门禁**
- `tests/plugins/platforms/photon/test_mention_gating.py` 新建 8 测试（默认 off、群拒、群通过、DM bypass、wake word strip、自定义 patterns、env comma-list、无效 regex 跳过）

**测试覆盖**：`tests/plugins/platforms/photon/`（含 `__init__.py + test_auth.py + test_inbound.py + test_mention_gating.py + test_signature.py`）

**意义**：iMessage 接入终于不再需要用户自己跑 BlueBubbles Mac relay；Photon 托管 Spectrum 平台分配 iMessage 线路、处理 delivery 与 abuse-prevention，免费层用 Photon shared line pool。当 Photon 公开 HTTP send 后可移除 Node sidecar 把 `_sidecar_send` 折叠为 `_send_via_http` — 其他层无需改动。

---

<a id="2-god-file-agent"></a>
## 2. God-file 解耦运动 — agent/conversation_loop Phase 1

整个会话期最大主题：`agent/conversation_loop.py`、`gateway/run.py`、`hermes_cli/cli.py`、`hermes_cli/main.py` 四个 god-file 累计 ~58k LOC 占整库相当比例，今日完成 **Phase 1（agent 内 run_conversation 拆解）** + **Phase 2/4（CLI 拆解）** + **Phase 3（Gateway 拆解）** 大规模析出。所有 refactor 均自陈 "Behavior-neutral / byte-identical / MRO-resolved"，配合无新增 / 无变更测试断言（mixin 路径与之前 self.<handler> 走同 MRO）。

### 2.1 `agent/turn_context.py` —— run_conversation 前奏（388 LOC）

`54870847c refactor(agent): extract run_conversation prologue into agent/turn_context.py`

`run_conversation` 开始时 ~470 行的"一次性 per-turn 准备"代码被搬出来：
- stdio guarding、retry-counter resets、user-message sanitization（surrogate strip）、todo/nudge-counter hydration、system-prompt restore-or-build、crash-resilience persistence、preflight context compression、`pre_llm_call` plugin hook、external-memory prefetch
- **新建** `agent/turn_context.py 388 LOC`：
  - `:39 @dataclass class TurnContext` 携带 prologue 产物 `user_message / original_user_message / messages / conversation_history / active_system_prompt / ...`
  - `build_turn_context(agent, ...)` 执行 setup 并返回 ctx（仍重度 mutate `agent` 状态 — 这正是其语义）
- `run_conversation` 留下 unpack + loop 部分；纯移动重命名，行为字节一致

### 2.2 `agent/turn_retry_state.py` —— 内 retry-loop 一次性 guards（68 LOC）

`524453dab refactor(agent): consolidate inner-retry-loop recovery flags into TurnRetryState (god-file Phase 1b)`

之前 inner retry loop 用 **~15 个散布的裸 boolean** 跟踪 per-attempt 一次性 guard（per-provider OAuth refresh、format-recovery、restart 信号）；析为单 dataclass：
- **新建** `agent/turn_retry_state.py 68 LOC`：
  - `@dataclass class TurnRetryState`：
    - per-provider OAuth/credential refresh guards：`codex_auth_retry_attempted / anthropic_auth_retry_attempted / nous_auth_retry_attempted / nous_paid_entitlement_refresh_attempted / copilot_auth_retry_attempted`
    - format/payload recovery guards：`thinking_sig_retry_attempted / ...`（共 ~15 字段）
  - 每外层迭代 new 一份；mutate in place（`_retry.codex_auth_retry_attempted = True`）
- loop-control 变量（`retry_count / max_retries / max_compression_attempts`）**留作普通 local**：它们是 `while` 机制而非 recovery 簿记

### 2.3 `agent/turn_finalizer.py` —— run_conversation 后奏（428 LOC）

`55b83c3d9 refactor(agent): extract run_conversation post-loop tail into finalize_turn (god-file Phase 1)`

`run_conversation` 主 tool-calling `while` loop 之后的所有后处理：budget-exhaustion summary / trajectory save / session persist / turn diagnostics / response transforms / result-dict assembly / steer drain / memory+skill review trigger，整段 verbatim 上移：
- **新建** `agent/turn_finalizer.py 428 LOC` `def finalize_turn(agent, *, final_response, api_call_count, interrupted, failed, messages, conversation_history, ..., _should_review_memory, _turn_exit_reason)` → 返回 `result` dict
- 同步 single return、无 await、无 early return（精镜原代码）
- module logger 经 lazy `from agent.conversation_loop import logger` 让 log record 仍归属 `"agent.conversation_loop"` 不出现新 logger 名

**统计**：`run_conversation` 4204 LOC → 3846 LOC，`conversation_loop.py` 4578 LOC → 4220 LOC。

---

<a id="3-god-file-gateway"></a>
## 3. God-file 解耦运动 — gateway/run.py Phase 3

`gateway/run.py` 是最大 god-file（HEAD 15815 LOC 但析出前 ~20k LOC，`GatewayRunner` 含 220 方法）。整合大块功能进 mixin，`GatewayRunner` 改多继承：

`gateway/run.py:1866 class GatewayRunner(GatewayAuthorizationMixin, GatewayKanbanWatchersMixin, GatewaySlashCommandsMixin)`（HEAD 实证 `:1163-1165` 三 mixin 导入 + `:1866` 继承声明）

### 3.1 GatewayAuthorizationMixin

`a706a349b refactor(gateway): extract authorization cluster into GatewayAuthorizationMixin`

- **新建** `gateway/authz_mixin.py 426 LOC`：4 个 inbound-message 鉴权方法（`_authorize_event / _check_allowlist / _check_rate_limit / _check_mention_gate` 类）
- `gateway/run.py` 16200 → 15812 LOC

### 3.2 GatewayKanbanWatchersMixin

`1c68f6f81 refactor(gateway): extract kanban watcher loops into GatewayKanbanWatchersMixin`

- **新建** `gateway/kanban_watchers.py 1064 LOC`：6 个 kanban 相关方法 `_kanban_notifier_watcher / _kanban_dispatcher_watcher / _kanban_advance / unsub / rewind / _deliver_kanban_artifacts`（~1,035 LOC）
- 析出后 mixin 方法仍走 `self.<method>` dispatch + 测试引用经 MRO 不变

### 3.3 GatewaySlashCommandsMixin（最大单批析出）

`619bd7827 refactor(gateway): extract 42 slash-command handlers into GatewaySlashCommandsMixin (god-file Phase 3b)`

- **新建** `gateway/slash_commands.py 3393 LOC`：把 42 个 `_handle_*_command` 处理器（`/model / /reset / /usage / /compress / /voice / ...`）整体下移
- module 顶层 docstring 明示 import 策略："`from gateway.run import ...` 写在 handler body 内"，让 run.py module 加载完后才 resolve，破 import cycle
- 测试 mock 经 `7a5827c8b test: repoint percentage-clamp source guard to gateway/slash_commands.py` + `de5fe2fa7 test(gateway): repoint slash-command mocks after mixin extraction` 重锚到新文件路径

**累计**：3 mixin 合计 **4883 LOC** 自 `gateway/run.py` 析出（426 + 1064 + 3393）。

---

<a id="4-god-file-cli"></a>
## 4. God-file 解耦运动 — hermes_cli/cli + main Phase 2/4

`hermes_cli/cli.py` HEAD 13506 LOC 含 `HermesCLI` 类；`hermes_cli/main.py` HEAD 11573 LOC（之前 14050）含一个 3,297-line `main()` 函数有 179 inline `add_parser`。本日双轨拆解：

### 4.1 `hermes_cli/subcommands/` 包 — 39 子文件

`b2e605324 refactor(cli): extract hermes cron parser into hermes_cli/subcommands/ (god-file Phase 2)` 立模式

后续连环析出：
- `4da45e872 refactor(cli): extract profile + gateway/proxy parsers into hermes_cli/subcommands/`
- `568e12761 refactor(cli): extract 25 more subcommand parsers into hermes_cli/subcommands/`（覆盖 model/setup/postinstall/whatsapp/slack/login/logout/auth/status/webhook/hooks/doctor/security/dump/debug/backup/import/config/version/update/uninstall/dashboard/gui/logs ...）
- `1a626470c refactor(cli): promote 9 closure handlers to top-level + extract their parsers (god-file Phase 2 follow-up)`（memory/acp/tools/insights/skills/pairing/plugins/mcp/claw — handler 从 `main()` 内闭包升顶级，parser 析入 `subcommands/`）
- `a77efada5 refactor(cli): extract 18 model-flow wizard functions into model_setup_flows (god-file Phase 2)`：**新建** `hermes_cli/model_setup_flows.py 2648 LOC` 装 18 `_model_flow_*` provider-setup 向导；`select_provider_and_model` 调度器留 `main.py` 再 re-import 析出的 helpers，main.py 14050 → 11479 LOC

**HEAD 实证**：`hermes_cli/subcommands/` 含 **39 个子文件**含 `__init__.py + _shared.py + acp.py + auth.py + backup.py + claw.py + config.py + cron.py + dashboard.py + debug.py + doctor.py + dump.py + gateway.py + gui.py + hooks.py + import_cmd.py + insights.py + login.py + logout.py + logs.py + mcp.py + memory.py + model.py + pairing.py + plugins.py + postinstall.py + profile.py + prompt_size.py + security.py + setup.py + skills.py + slack.py + status.py + tools.py + uninstall.py + update.py + version.py + webhook.py + whatsapp.py`，合计 **2857 LOC**

### 4.2 `CLICommandsMixin` — 32 slash-command 处理器（cli.py 内）

`0904bc7ea refactor(cli): extract 32 slash-command handlers into CLICommandsMixin (god-file Phase 4)`

- **新建** `hermes_cli/cli_commands_mixin.py 2175 LOC` 装 `_handle_*_command` 集群（2,077 LOC）
- `cli.py:3083 class HermesCLI(CLIAgentSetupMixin, CLICommandsMixin)` 多继承（HEAD 实证）
- 模块顶层注释"Import discipline (mirrors gateway/slash_commands.py, PR #41886)"：cli.py 内部 helper（`_cprint / _ACCENT / save_config_value` 等）经 handler 内 lazy `from cli import ...` 解 cycle

### 4.3 `CLIAgentSetupMixin` — agent 构建集群（681 LOC）

`094aa85c3 refactor(cli): extract agent-construction cluster into CLIAgentSetupMixin (god-file Phase 4)`

- **新建** `hermes_cli/cli_agent_setup_mixin.py 681 LOC`：5 个 agent-construction / session-resume 方法
- `cli.py` 14139 → 13492 LOC

**累计**：CLI 双轨累计析出 **6 480 + 2 175 + 681 = 9 336 LOC** + `model_setup_flows.py 2 648 LOC` = ~12k LOC 自 cli.py / main.py 拆走。

---

<a id="5-desktop-update-split"></a>
## 5. Desktop 客户端 / 后端版本与更新按钮分离

9 commit 连续推进 remote 模式下 client/backend 版本可见性 + 更新流程可控性。

- `9e360681f feat(dashboard): return recent commits from /api/hermes/update/check` — `hermes_cli/web_server.py +53`，新增 best-effort `commits` list（sha/summary/author/at）到 `/api/hermes/update/check` 响应，附加非破坏：旧 consumer 忽略；commits 经独立 git 调用，与 shared `check_for_updates() int` 契约保持隔离
- `228414704 docs: document commits field on /api/hermes/update/check`
- `ed1e2533b feat(desktop): show client and backend versions in status bar when remote` — 8 文件 +203/-3 含**新建** `apps/desktop/src/lib/version-bar.ts 74 LOC` + `version-bar.test.ts 93 LOC`（**注意：当时新建后于 `56be1a63a` 删除**）；在 `connection.mode === 'remote'` 下显 `'client vX · backend vY'`，drift 加 flag；local mode byte-identical
- `64da518db feat(desktop): remote update overlay sourced from backend` — `checkUpdates()/applyUpdates()` 分支 `connection.mode`，remote 走 `GET /api/hermes/update/check` 映射 `DesktopUpdateStatus`（behind/commits/supported=can_apply/message），overlay 渲 commit list；`applyUpdates → POST /api/hermes/update` 经 command-center 路径 + poll action 完成 + handle 期间 connection drop 当作 restart 阶段
- `9c264555b fix(desktop): name the update target in the overlay; honest no-changelog copy` —**新建** `apps/desktop/src/lib/update-copy.ts`（HEAD 实证 `resolveUpdateCopy({target, shownItems, copy})` 纯函数 + `update-copy.test.ts`），remote 模式显 "Backend update available" 含具体后端名，无 changelog 时 honest "no notes available" 替通用 filler
- `87ac7cac1 fix(dashboard): log update changelog against origin/main, not @{upstream}` — behind-count 看 `HEAD..origin/main`，但 `_recent_upstream_commits` 之前看 `HEAD..@{upstream}`，feature branch 上 `@{upstream}` 是 branch 自己的 tip = 0 commits，造成 behind>0 而 commits 空
- `cfaa46fca fix(desktop): pre-check backend updates in poller; client button first`
- `56be1a63a fix(desktop): split client and backend into two distinct update buttons` — `apps/desktop/src/app/shell/hooks/use-statusbar-items.tsx +93/-?`：把 status bar 合并的 pill 拆 **client vX**（始终在）+ **backend vY (+N)**（仅 remote），各自 behind-count + 各自 overlay target；**删除 `version-bar.ts` merge helper**（再也不合并两版本）；store 改两套独立 atom：`checkUpdates/applyUpdates`（client）+ `checkBackendUpdates/applyBackendUpdate`（backend）；`openUpdateOverlayFor(target)` 驱动 overlay
- `81647458c fix(desktop): recover the backend update overlay after the remote restarts` — backend Install 路径设 `stage:'restart'` 后 remote 模式无 boot-progress 事件搬 overlay 至 done；改 poll backend 直答再清 overlay + 刷 backend status；apply poll sleep 从 `window.setTimeout` 改 `globalThis.setTimeout` 离 renderer 可测
- `cd030f5f4 fix(desktop): close the backend update overlay on success; error on no-return`
- `b000e05b1 fix(desktop): don't claim the backend update succeeded when it never returns`
- `74239b494 i18n(desktop): translate backend update apply status messages` —上述 cluster 的 i18n 同步（en/ja/zh/zh-hant）

伴：`9b2a64fa6 fix(desktop): reflect env-override remote in gateway connection state` — `HERMES_DESKTOP_REMOTE_URL` 强制 remote 但不写 `connection.json`，让 gateway panel 显 "local with no url"；fix 改让 env-override 写入 in-memory connection state

---

<a id="6-desktop-misc-cluster"></a>
## 6. Desktop 主题 per-profile + 多个 UI 改良

### 6.1 主题 per-profile（`9b1e0d6f7 feat(desktop): assignable themes per profile (#42286)`）

- 之前 desktop skin 是单一全局 pref —— 每 profile 共一种外观
- HEAD `apps/desktop/src/themes/context.tsx`：
  - `:25 const SKIN_KEY = 'hermes-desktop-theme-v2'` 保留作 legacy 全局 fallback
  - `:29 const PROFILE_SKINS_KEY = 'hermes-desktop-profile-themes-v1'` + `:30 PROFILE_MODES_KEY` per-profile 记录
  - `:33 const LAST_PROFILE_KEY = 'hermes-desktop-active-profile-v1'` boot-time paint 用上次 active profile 的 theme 避非默认 relaunch 闪
  - `:53 profilePref<T>()` factory：`"default"` 不是真 profile —— 它**是**legacy global slot，读写直走 global；命名 profile 自己一条 record entry，未赋值前 fallback 全局
- `setTheme()` 写到当前 live profile（default profile 同时种 legacy global fallback 让 single-profile 安装零变化）
- `themes/profile-theme.test.ts` 新建 + `apps/desktop/src/themes/presets.test.ts` 同步

### 6.2 Hover-reveal 折叠 sidebar（`d65b513f2 feat(desktop): hover-reveal collapsed sidebars as fixed overlays (#41670)`）

- session sidebar 折叠时，hover 左边界 float 回为 fixed overlay 覆盖 main content（之前只是隐藏）
- collapsed grid track stay 0px 永不预留空间，从下层滑出，pointer-leave 退回
- `PaneShell` 新 `hoverReveal` prop：collapsed + hoverReveal 时渲 edge hot-zone + side-anchored floating panel
- `ChatSidebar` 经 `data-[pane-hover-reveal=open]` variant 强 opacity-100 + interactive
- `desktop-controller` 把 chat-sidebar opt-in

### 6.3 Shift+click 状态栏 zap 切全局 YOLO（`fa42ac094 feat(desktop): Shift+click the status-bar zap to toggle YOLO globally (#41666)`）

- 之前 status-bar zap 切 per-session approval bypass（同 TUI Shift+Tab scope）
- 新增 global escape hatch：**Shift+点击** zap 翻 `approvals.mode` config 在 `"off"`（bypass 开）↔ `"manual"`（bypass 关）—— 影响**所有** session + CLI + TUI + cron，且 **survive 重启**
- `statusbar-controls`：threadPress 的 `shiftKey` 经新 `StatusbarSelectModifiers` arg
- `yolo-session.ts` 新 `setGlobalYolo()` 调 `config.set` with `scope="global"`

### 6.4 Viewport 不再追流式输出（`e029b7597 feat(desktop): stop the chat viewport from following streaming output (#41414)`）

- 之前 desktop chat GUI 在 turn 流式时把 viewport pin 到底部
- 删除 streaming `ResizeObserver` re-pin loop + post-run bottom lock（~1.2s after completion）
- 保留**一次性** jump-to-bottom on user submit / new turn / session change 让新发消息能看到

### 6.5 Just-finished session 切走不消失（`395ed9189 fix(desktop): keep a just-finished session visible after switching away (#42285)`）

- 新 session 第一回合持久化到 SessionDB 比 gateway emit `message.complete` 略晚一拍 —— 此窗口内 `listSessions(min_messages=1)` page 不含新行
- `sessionsToKeep()` 之前只护 active chat；导航离开的 session 在下次 refresh 既不 working/pinned/active，被 `mergeSessionPage()` 驱逐
- 新增"recently settled (working→idle transition)"跟踪让它撑过这一拍

### 6.6 Scoped session 事件 guard 收紧（`de80d28f3 fix(desktop): require session ids for scoped gateway events (#42178)` + `6e7033bb4 fix(desktop): don't drop the focused chat's own stream when unscoped (#42359)`）

- 双阶段：先要求 `message/reasoning/tool/subagent` 类带 `session_id`（async 活动不混入 focused chat），但**意外吞了 focused turn 自家无 session_id 的 live answer**
- `6e7033bb4` 把 guard 收窄到 `subagent.*`（唯一真正背景/async 家族）；其它 fall back to active session

### 6.7 Zoom level 持久化（`b5a457c03 fix(desktop): persist zoom level via renderer localStorage (#41747)`）

### 6.8 新配置 model provider 在 Settings → Model 出现（`039fbb41f fix(desktop): show newly configured model providers (#41545)`）

- **新建** `apps/desktop/src/store/model-visibility.ts +27` + `model-visibility.test.ts +37`：刚配置好的 provider 出现在 picker（之前只显已通过 `/api/model/options` exposed 的 set，新建后需重启）

### 6.9 sleep/wake 后 stale remote backend 修（`cadb74ada fix(desktop): recover chat after sleep/wake by revalidating a stale remote backend`）

### 6.10 Renderer bundle 缺失硬失败（`bddc5fd08 fix(desktop): fail loudly instead of blank-paging when the renderer bundle is missing (#41729)`）

- **新建** `apps/desktop/scripts/assert-dist-built.cjs + .test.cjs` 在 build tail 跑，aborts before electron-builder if `dist/index.html` 或 vite JS bundle 缺/空
- 防 build 失败但 electron-builder 仍打包出空 `dist/`（packaged app launches to blank page + bare ERR_FILE_NOT_FOUND #39484）

### 6.11 Dashboard 静态 unpack 自 ASAR（`53a2ac8f2 fix(desktop): unpack dist/ from asar so dashboard static files are servable`）

- packaged builds 服 HTTP 404 因为 `resolveWebDist()` 指 `app.asar.unpacked/dist/` 但 `dist/**` 未在 `asarUnpack` glob

### 6.12 Windows electron-builder pack 锁（`96fd9d497 fix(desktop): stop running Hermes.exe locking win-unpacked before Windows pack (#42100)`）

- 跑中的 Hermes.exe 对 `release/win-unpacked/Hermes.exe` 持 exclusive lock，pack 时 "Access is denied"
- 修：每 pack 前强结束遗留 Hermes.exe

### 6.13 Desktop 边栏 messaging source folders / drag-sort 6 commit（D'Angelo Rodriguez 主笔）

`9d6992ee8 Show platform sources in desktop sessions` + `ede4f5a4a Show messaging source folders in desktop sessions` + `3fc67b733 Persist desktop sidebar drag order` + `0f500fc41 Render grouped sessions when local list is empty` + `f0fcaa1e5 Preserve dragged order inside source folders` + `694adec63 Smooth desktop sidebar drag sorting` + `d759c13c0 chore(salvage): lint fix + AUTHOR_MAP for desktop source-folders PR #40272`

- **新建** `apps/desktop/src/lib/session-source.ts 62 LOC`：messaging 来源（Telegram/Discord/Slack/Matrix 等）会按平台 source 自动分文件夹聚合
- `session-row.tsx +22/-?`：每行加 platform icon
- `chat/sidebar/index.tsx +131/-?`：grouped view
- `dnd-kit` 路径支持 within-source 拖动顺序持久化

---

<a id="7-simplify-code-skill"></a>
## 7. simplify-code skill（新 bundled skill）

`ace4b722d feat(skills): add simplify-code skill — parallel 3-agent code review and cleanup (#41691)` —— 灵感来自 Claude Code 的 `/simplify`。

- **新建** `skills/software-development/simplify-code/SKILL.md 175 行` + `website/docs/user-guide/skills/bundled/software-development/software-development-simplify-code.md` 文档
- frontmatter `name: simplify-code / version: 1.0.0 / metadata.hermes.tags: [code-review, cleanup, refactor, delegation, subagent, parallel, simplify]` 
- 核心思想："Three narrow reviewers beat one broad reviewer." —— 三个 focus 不同的 reviewer 并行：
  - **Reviewer 1 — Code Reuse**：搜 codebase 找重复
  - **Reviewer 2 — Code Quality**（隐含）
  - **Reviewer 3 — Efficiency**（隐含）
- 触发条件：用户说 "simplify" / "simplify my changes" / "review my code" / "/simplify"（Claude Code 习惯）
- 可选 modifier：`focus on efficiency`（只跑一个 reviewer）/ `but don't change anything`（dry run）/ `the last commit`（scope）/ `staged`（git diff --staged）
- 实现：用 `delegate_task` **batch mode** —— `tasks` 数组传三任务并发跑（fan-out 3 在默认 `delegation.max_concurrent_children` 内）；每 reviewer 拿**完整 diff**（不裂片）+ repo path + `terminal/file/search` toolset
- Phase 1 优先级：`git diff` → `git diff HEAD` → 用户指定 scope；>2000 改行警告 token 重并 offer scope down
- 零核心修改 —— 全靠现成 `delegate_task / terminal / search_files` orchestration

---

<a id="8-windows-conpty"></a>
## 8. Windows ConPTY 接通 dashboard `/chat` tab

`abcf996b1 feat(windows): enable dashboard /chat tab via ConPTY (win_pty_bridge) + tests (#42251)`

- **新建** `hermes_cli/win_pty_bridge.py 179 LOC`：POSIX `hermes_cli.pty_bridge.PtyBridge` 的 drop-in 对应物
  - 公开 surface 完全一致：`spawn / read / write / resize / close / is_available + PtyUnavailableError`
  - 后端用 `pywinpty.PtyProcess`（已是 win32 declared 依赖，无新增）替 POSIX 的 ptyprocess/fcntl/termios
  - clamp ceiling 与 POSIX 对齐（`_MAX_COLS = 2000 / _MAX_ROWS = 1000 / _MIN_DIMENSION = 1`）防 broken winsize probe 抵达 resize call
  - `_PTY_AVAILABLE = sys.platform.startswith("win")` 仅 win32 启用
- 拼接处：`hermes_cli/web_server.py:8285-8316` 平台分支
  ```python
  if sys.platform.startswith("win"):
      from hermes_cli.win_pty_bridge import WinPtyBridge as PtyBridge, PtyUnavailableError
  else:
      from hermes_cli.pty_bridge import PtyBridge, PtyUnavailableError
  ```
  使 `/api/pty` WebSocket handler 完全 platform-agnostic
- 配套 `tests/hermes_cli/test_win_pty_bridge.py + test_web_server_pty_import.py + test_windows_native_docs.py`
- ConPTY read 路径走 `run_in_executor` 不阻塞 event loop

---

<a id="9-auth-pool-cluster"></a>
## 9. Auth / Credential Pool 集群

### 9.1 Codex OAuth 账户作独立 pool entry（`c78b3e1d3 fix(auth): add Codex OAuth accounts as distinct pool entries`）

- `hermes auth add openai-codex` 之前路由经 singleton `_save_codex_tokens` save 路径，每次都覆盖 — 第二次 add 把第一个账户的 singleton-mirrored device_code entry 写掉
- 修：改创建独立 `manual:device_code` pool entry per account；entry 从自家 token pair refresh，互不干扰
- 与 `761b744ab fix(auth): preserve independent Codex pool entries on re-auth (#39236)`（re-auth 路径，5 月已落）共同闭环 #39236 add-path + re-auth-path

### 9.2 OpenRouter pool 自动侦测（`9c9d9113a fix(auth): auto-detect OpenRouter credential from the pool, not just env (#42263)`）

- `resolve_provider()` 自动侦测只查 env (`OPENROUTER_API_KEY / OPENAI_API_KEY`)，从不查 credential pool
- 经 `hermes auth add openrouter`（manual pool entry，无 env var）添的 key **隐形**：provider 解析失败或返空 api_key，请求出去无 Authorization → "HTTP 401: Missing Authentication header"，而 `hermes auth list` 显示 credential 存在
- 修（`hermes_cli/auth.py:1589-1595` HEAD 实证）：
  ```python
  try:
      from agent.credential_pool import load_pool as _load_pool
      if _load_pool("openrouter").has_credentials():
          return "openrouter"
  except Exception as e:
      logger.debug("Could not check OpenRouter credential pool: %s", e)
  ```
  插到 env-var loop 之前；闭 #42130

### 9.3 Approval 路径在检测时解析 home 而非 import 时（`89d380261 fix(approval): resolve Hermes home at detection time, not import time` + `b0efe1d64 fix(approval): gate resolved Hermes config paths`）

- helix4u 的 fix 把 resolved `HERMES_HOME` snapshot 到 `tools.approval` 的静态 config/env patterns 在 module-import time
- 但若 `HERMES_HOME` 在 `tools.approval` 之后才设（hermetic test conftest、deferred-profile-resolution）就破：fix 自己的 4 新测都因此红
- 改：解析移到 `_normalize_command_for_detection()`：live 解析 + symlink-resolved 形式重写到 canonical `~/.hermes/` 再做 pattern 匹配；跟踪 live env，无需 regex recompile

---

<a id="10-vision-cluster"></a>
## 10. Vision 集群

### 10.1 Providers 拒收 list-type tool content 主动降级（`9513793ad fix(vision): proactive downgrade for providers rejecting list-type tool content (#41072)`）

- Xiaomi MiMo（潜在其他 provider）支持多模态 user 消息但**拒收** list-type tool 消息 content，返 400 "text is not set"
- 之前 reactive：先发出去，被拒后 strip image 重试 — 丢视觉信息
- 修：`ProviderProfile` 加 `supports_vision_tool_messages: bool = True`；Xiaomi 设 False；`_tool_result_content_for_active_model` 现 proactively 查此字段，False 时返 text summary 替 list content，免 round-trip 失败
- 测试 `tests/run_agent/test_vision_tool_messages.py 新建`

### 10.2 尊重 custom_providers 的 per-model `supports_vision`（`41f071428 fix(vision): honor custom_providers per-model supports_vision (#41036)`）

- `_supports_vision_override()` 检 `model.supports_vision` 与 `providers.<name>.models`，**未检** legacy list-style `custom_providers` config
- 自定义 provider 形如：
  ```yaml
  custom_providers:
    - name: my-provider
      models:
        my-model:
          supports_vision: true
  ```
  之前 `supports_vision: true` 被吞
- 修：扩 lookup 覆盖 legacy list 路径
- 测试 `tests/agent/test_custom_providers_vision.py 新建`

---

<a id="11-compression-cluster"></a>
## 11. Compression 四连防泄漏

### 11.1 cross-session `_previous_summary` 守（`8513a6aec fix(compression): guard against cross-session stale _previous_summary contamination` + `cca3b77a4 fix(compression): clear _previous_summary on session end (defense-in-depth)`）

- cron 或 background session 压缩时设 `_previous_summary` 用于 iterative updates；若该 session 不经 `/new` 或 `/reset`（不调 `on_session_reset()`）结束，stale summary 留在 `ContextCompressor` 实例
- 后续 live 消息 session 压缩时会把它当 `PREVIOUS SUMMARY:` 注入 summarizer prompt —— 跨 session 污染
- 修：`compress()` 加 else guard，当前 messages 找不到 handoff summary 而 `_previous_summary` 非空时**丢弃**
- `cca3b77a4` 在 session end 主动清，双层防御

### 11.2 background-review fork 关 compression（`a77bc2c08 fix(compression): disable compression on background-review fork to prevent cross-turn stale-parent fork (#41708)`）

- per-session 压缩锁防同窗口 concurrent fork 但**不防** cross-turn —— background-review fork 共享 parent `session_id`
- 若 fork 赢 compression race，新 child session 永不被 gateway adopt（fork 是单生命周期）；下次前台 turn 从 stale parent 启动再压一次，造成同 parent 两 sibling children
- 修：`review_agent.compression_enabled = False` 让 fork 永不触发 compression
- 测试 `tests/gateway/test_compression_concurrent_sessions.py 新建`

### 11.3 topic + split 后 `/model` + `/reasoning` override 保（`2a10da3a1 fix(gateway): keep /model + /reasoning overrides on topic recovery & compression splits`）

- Telegram DM/forum topic 与压缩 split 后会让 session-scoped `/model` 与 `/reasoning` override 静默消失（#30479）
- 根因：`_handle_message_with_agent` 经 `_recover_telegram_topic_thread_id` 重写 `source.thread_id` 后才推 session key；但 `/model` `/reasoning` handler 从 raw inbound `event.source` 推 override key —— 写入用一种 key，下一回合读另一种
- 修：handler 也走同一 thread_id recovery 推 override key
- 测试 `tests/gateway/test_session_override_thread_recovery.py 新建`

### 11.4 `transcript 全装 tail budget` 时无限 loop 修（`b18490b89 fix(compaction): prevent infinite loop when transcript fits in tail budget`）

- `summary_target_ratio` 大（如 0.45）+ `context_length` 中（如 96000）时 `soft_ceiling = token_budget * 1.5` 可超 transcript 总大小
- `_find_tail_cut_by_tokens` 不 break early 走完，compress window 要么空（`compress_start >= compress_end`）要么单消息（summary-of-one overhead ~0 tokens save）
- 两种 outcome 都是 no-op compression 但**不增** `_ineffective_compression_count`，`should_compress()` 每回合返 True → 真无限 loop
- 修：detect tail 容纳条件 + 增 counter 让 ineffective compression 被识别
- 测试 `tests/run_agent/test_infinite_compaction_loop.py 新建`

伴：`cb5c24e37 fix(agent): sync logging session context on compaction id rotation` + `648706936 test(gateway): add compression session_id rotation integration tests (#34089)` + `3714caa1b fix(session): follow compression continuations for transcript reads` + 新测 `tests/agent/test_compression_logging_session_context.py` / `test_context_compressor_cross_session_guard.py`

---

<a id="12-memory-leak-cluster"></a>
## 12. Memory / RSS 泄漏集群

### 12.1 Overflow 错语指令 in-turn 整合 + retry（`86c537d20 fix(memory): instruct in-turn consolidation + retry on overflow (#41755)`）

- bounded memory 满时 add/replace overflow 错语现明示 model "consolidate (merge/remove/shorten) and retry the write in the same turn"
- replace-overflow 路径现也 echo `current_entries + usage` 与 add-overflow 对齐 — 让模型有等同上下文
- 闭 #23378（"working-as-documented"；本修补让 runtime 符 docs）

### 12.2 End-of-turn sync 离 turn thread（`aa6f2775f fix(memory): run end-of-turn sync off the turn thread (#41945)`）

- 误配 / 慢 external memory provider 能把 agent 卡在 `'running'` 几分钟（actual: 一次观察 broken Hindsight daemon 阻塞 298s 才失败）
- `MemoryManager.sync_all / queue_prefetch_all` 在 turn-completion 路径 inline 跑 `provider.sync_turn / queue_prefetch`；阻塞调用让 `run_conversation` 不返回
- 所有 interface（CLI/TUI/gateway）将 agent 标 `'running'` 至 `run_conversation` return，于是 agent busy 整个阻塞期；跟进消息触发 aggressive interrupt 丢失新消息
- 修：sync 移到 thread pool 让 turn-thread 不被阻
- 测试 `tests/agent/test_memory_async_sync.py 新建`

### 12.3 `AIAgent.close()` 清 `_session_messages`（`5e06c9ffe fix(agent): clear _session_messages in AIAgent.close() (#42123)`）

- `close()` 是真 session boundary（`/new / /reset / session expiry`）的硬 teardown；之前关 OpenAI client + child agents 但**留** conversation-history list
- soft-eviction 路径（`_release_evicted_agent_soft`）已清 `_session_messages`；hard close 镜像之
- 闭 close-call site 持引用（draining background task）不再 pin 几十 MB tool outputs

### 12.4 Gateway 释 evicted agent clients（`e9c1e757f fix(gateway): release evicted agent clients to stop RSS leak (#29298) (#41974)`）

- `_evict_cached_agent`（`/new / /model / /undo / session reset / 17 call site` 共用 chokepoint）只 pop cache entry，drop AIAgent 引用
- AIAgent 含 ref cycle（callbacks + tool state）让 CPython refcounting 不立即 free `httpx` client pool；steady traffic 下 held socket+buffer 堆 RSS 单调增（#29298）
- 修：chokepoint pop **+** schedule soft `release_clients()` 在 daemon thread（镜像 cap-enforcer / idle-sweeper）

### 12.5 Gateway 余下泄漏点（`3d029a53e fix(gateway): close residual memory-leak sites under heavy scheduled workload`）

- long-lived gateway 在重 cron/build workload 下 ~18 MB/hr 增（phantom-dispatch-fix 后），最终 OOM
- 4 个 retention site（全部 confirmed live on main）：
  1. `_evict_cached_agent()` （`/model / /reasoning / codex-runtime / /undo` 等）pop cache entry 不释 OpenAI client + httpx transport + SSL context + conversation history。**仅 `/new` 之前清**。改 daemon-thread release 与 `_enforce_agent_cache_cap` 对齐
  2. （其余 3 个 site 见 commit body）
- 配套：`400e6e43c test(gateway): de-flake concurrent-compression lock test with a barrier`

### 12.6 Slash-worker 自终止 / 句柄回收（`8b6a8f667 feat(slash-worker): self-terminate on parent death via create_time watchdog` + `b31c6c33b fix(pty-bridge): terminate PTY process groups on teardown` + `cb13723f5 fix(pty-bridge): mark os.killpg/getpgid windows-footgun-ok (POSIX-only module)` + `ae94ed172 fix(tui-gateway): reap leaked slash_worker sessions on disconnect + active_list liveness` + `a3fca26c5 fix(tui): close slash_worker inside _finalize_session (defense-in-depth, #38095) (#42149)`）

- daemon thread 轮询 `_is_orphaned`（原 ppid 检 + psutil `create_time` PID-reuse guard，不用 `PR_SET_PDEATHSIG`）
- orphan 则把 in-flight 命令 drain 至 grace window 后 `os._exit(0)`
- HermesCLI 构建前启动覆 spawn 窗口
- `ws.py` `finally` 之前只把 dead transport 重指 stdio；现 `_close_sessions_for_transport` reap `close_on_disconnect` sessions + 调度 grace-reap 余下，经 `asyncio.to_thread` 卸 blocking `worker.close()` + DB write
- 测试 `tests/test_slash_worker_watchdog.py + test_tui_gateway_ws.py + test_honcho_client_concurrency.py 新建`

---

<a id="13-gateway-cluster"></a>
## 13. Gateway 集群

### 13.1 Duplicate user message in state.db（`8e4c447e5 fix(gateway): prevent duplicate user messages in state.db`）

- agent 有 SessionDB 引用时 `_flush_messages_to_session_db()` 已 persist user message 到 SQLite
- 两条 gateway fallback 路径（`agent_failed_early` 在 transient 429/timeout / `not-new-messages` 在 `history_offset >= len(messages)` edge case）再写同条 user message **不带 `skip_db=True`**，造成重复
- 修：`agent_persisted` flag 定义提前到 if/elif/else 之前；两 fallback 都加 `skip_db=` flag

### 13.2 Cascading interrupt 假 retry（`dd0d1222a fix(agent): don't retry interrupt-induced transport errors (cascading-interrupt hang)`）

- `agent.interrupt()` 在 active LLM call 时 force-close worker-local httpx client 停 token 生成 → 抛 transport 错（RemoteProtocolError）于 worker thread —— 这是**我们自家 close 的预期后果**，不是网络 bug
- streaming retry loop 误分类为 transient connection error 而 retry；每次 doomed retry 停 stream-stale timeout（≤300s）
- gateway 缓存 AIAgent per session → stale worker 撑过 interrupted turn 与下一回合 request 抢 → cascading-interrupt hang
- 修：interrupt 标志置时 transport 错不 retry
- 测试 `tests/agent/test_cascading_interrupt_6600.py 新建`

### 13.3 Voice 在 agent 跑时也转录（`d55304c39 fix(gateway): transcribe voice messages during active agent runs`）

- 来自 #6600 (@kristianvast) salvage —— voice 半重新 scope 到当前 main（cascading-interrupt 半已落 `dd0d1222a`）
- voice/audio 在同 session agent 忙时进 interrupt 路径但 text 空 —— STT 在 running-agent guard 之**后**跑，voice effectively 丢
- 修：先转录再走 guard
- 测试 `tests/gateway/test_auto_voice_reply_format.py 新建`

### 13.4 busy_input FIFO（`4d0f2bd24 fix(gateway): use FIFO queue for busy_input_mode pending messages` + `fec5ca71d fix: preserve telegram queue fifo during grace window` + `4eb897239 Merge PR #33817 from sweetcornna/fix/28503-busy-input-fifo`）

- 闭 #28503；新测 `tests/gateway/test_queue_consumption.py + test_busy_session_ack.py`

### 13.5 Telegram polling-conflict（`2e6286278 fix(telegram): use get_running_loop in polling-conflict retry reschedule (#41716)`）

- conflict-retry 路径调 `asyncio.get_event_loop()` reschedule 自己；Python 3.11+（floor）上若 PTB dispatch error callback 时 thread 没 loop 即抛 `RuntimeError: There is no current event loop`
- retry 永不 schedule，adapter 静默活着，gateway `--replace` 继续 spawn 新实例 hit 同墙 — `#19471` crash loop（multi-profile 时两 bot 持同 conflict 更糟）
- 改 `get_running_loop` 让 schedule 在 callback 当前 loop 上

### 13.6 DM session 凭 user_id 隔离（`540801336 fix(gateway): isolate DM sessions on user_id when chat_id is absent (#41764)`）

- `build_session_key` 把所有无 `chat_id` 的 DM collapse 到共享 `'agent:main:<platform>:dm'` key
- 单一 cached AIAgent 服多用户 conversation，history 在 sender 间渗血
- 修：DM 现 fallback 到 sender 的 `user_id_alt/user_id`（镜像 group-path participant precedence + telegram auth-path fallback）

### 13.7 `--replace` 老 PID 抗 SIGKILL 时中止（`e02f4c03c fix(gateway): abort --replace when old PID survives SIGKILL`）

- `--replace` force-kill 不响应的老 gateway 时 SIGKILL 可能 reap 失败（uninterruptible sleep / zombie-reaping parent）
- 之前无条件清 PID file + scoped locks 启新实例 → 两 gateway 抢同 bot token（duplicate-gateway 失败模式 of #19471）
- 修：force-kill 后重 verify（Windows-safe `_pid_exists`）；仍活则清 takeover marker 中止替换而非重复

### 13.8 systemd `--replace` 移除（`4d18717b6 fix(gateway): drop --replace from systemd unit templates (#41892)`）

- 在 systemd `Restart=always` 下 `--replace` 把每次重启变 self-kill loop：新实例读 `gateway.pid` 杀前任写自己 PID，下次再循环
- 进程监督者拥有生命周期 —— `--replace` 是手动一次性 takeover 用的，与监督者打架
- 修：system + user 双 systemd `ExecStart` 移除 `--replace`；保留供 manual `hermes gateway run --replace` + macOS launchd fallback 路径

---

<a id="14-auxiliary-cluster"></a>
## 14. Auxiliary 集群

### 14.1 Codex Responses 共用转换器（`2789bf4e2 fix(auxiliary): route Codex Responses path through shared converter (#5709)`）

- auxiliary Codex adapter 自己维护 chat→Responses 转换 loop，把每条非 system 消息的 role 原样 forward 入 Responses `input[]`
- `flush_memories() / compression` replay session history 含 assistant `tool_calls` + `role=tool` results 时，那些 tool 消息泄入 request → Responses API 返 `HTTP 400: Invalid value: 'tool'`
- 修：把 `_CodexCompletionsAdapter.create()` 接入与主 agent transport 同的 `_chat_messages_to_responses_input` 共用转换器

### 14.2 Transport 错先单次重试（`02a4d6695 fix(auxiliary): retry transient transport error once before fallback (#16587)`）

- auxiliary LLM 调用的一次性 transient（streaming-close / incomplete chunked read / 5xx / 408）直接 escalate 到 provider/model fallback（compression 时是 drop summary + cooldown），就算同 provider 立即 retry 能成
- 修：`call_llm() / async_call_llm()` 顶部加单次同 target retry，gated on `_is_transient_transport_error()` 复用 canonical 判定

### 14.3 Anthropic kwargs sanitize（`399b8ee5f fix(anthropic): strip Responses-only kwargs before Messages SDK call (#31673) (#42155)`）

- Responses-API shape payload 带 `instructions= / input= / store= / parallel_tool_calls=` 在 api_mode-flip 罕见 race（concurrent aux vision 调用 mutate shared agent 在 kwargs build 与 stream dispatch 之间）能到 native Anthropic `messages.stream() / messages.create()`
- Anthropic SDK 抛 non-retryable TypeError 杀整 turn 串走 fallback chain
- 修：双 Anthropic dispatch site 加 `sanitize_anthropic_kwargs()` drop Responses-only key in place + WARN log（含 #31673 breadcrumb）
- 测试 `tests/agent/test_anthropic_kwargs_sanitize.py 新建`

---

<a id="15-plugins-cluster"></a>
## 15. Plugins 集群

### 15.1 Nested category 发现 + alias 归一（`ccacfdbd6 fix(plugins): discover nested category plugins in 'plugins list' (issue #41066)` + `2b89afec7 fix(plugins): alias-normalize enable/disable for nested category plugins (follow-up to #41076)` + `b99c6c427 Merge #42076`）

- `_discover_all_plugins()` 之前 flat `iterdir()`，漏所有 category-namespaced plugin（`web/* / image_gen/* / browser/* / video_gen/*`）
- 现 recurse up to 2 levels，与 `PluginManager._scan_directory_level()` 对齐
- `_plugin_status()` 双查 manifest name AND path-derived key 让 category plugin 如 `web/tavily` 在 config 启用时显示正确状态
- `2b89afec7` 补 enable/disable mutation path：之前 `hermes plugins enable nemo_relay` 报 "Plugin 'nemo_relay' is not installed or bundled."，因 `cmd_enable/cmd_disable` 经 `_plugin_exists()` 只查 top-level
- 测试 `tests/hermes_cli/test_plugins_cmd_category_discovery.py + test_plugins_cmd_enable_disable_nested.py 新建`

### 15.2 Lazy-singleton 线程安全 helpers（`47d5177a7 fix(plugins): thread-safe lazy-singleton helpers; fix honcho TOCTOU (#24759) (#42150)`）

- `get_honcho_client()` 与 fal 的 `_load_fal_client()` 用 unlocked check-then-init：两 thread 都跑昂贵 build，loser 的 client（open connection）泄
- 替单点 lock：**新建** `plugins/plugin_utils.py` 两 thread-safe lazy-singleton helper
- 测试 `tests/test_plugin_utils.py + test_honcho_client_concurrency.py + test_dashboard_sidecar_close_on_disconnect.py 新建`

---

<a id="16-install-update-cluster"></a>
## 16. Install / Update 集群

### 16.1 Fetch scope 限到目标 branch（`e88116256 fix(update): scope git fetch to target branch`）

- bare `git fetch origin`（和 `git fetch upstream`）拉所有 ref；repo 有数千自动生成 branch，非 single-branch checkout 下 installer 更新路径 + `hermes update` 花数分钟下载完整 branch list — 足够 stall desktop installer 或 trip 后续 `git pull --ff-only`
- 修 `scripts/install.sh` + `hermes_cli/main.py:7471 + :8009-8011`（HEAD 实证）把 update-path 每个 fetch scope 到要比较/合并的 branch

### 16.2 commit-less checkout 重 clone 不删（`fc0900d12 fix(install): re-clone interrupted (commit-less) checkout instead of failing` + `5d7abf911 test(install): cover commit-less checkout handling (#40998)` + `a5c12f5f5 fix(install): move broken checkout aside instead of deleting it`）

- 上次 clone 中断留下 `.git` 但无 initial commit；`rev-parse --is-inside-work-tree` 和 `git status` 仍成功，installer 进 update 路径跑 `git stash` 抛 "You do not have the initial commit yet"，desktop install 在 "Cloning Hermes repository" 阶段失败
- `install.ps1`：加 `git rev-parse --verify HEAD` probe 让 commit-less checkout 视为 broken 重新 clone
- `install.sh`：在 `clone_repo()` 顶部 mirror
- `a5c12f5f5` review feedback：`rm -rf` / `Remove-Item -Recurse -Force` 是 destructive — 用户可能仍想保留；改 rename 到 `<dir>.broken-<ts>` backup 再重 clone，永不删
- 测试 `tests/test_install_no_initial_commit.py 新建`

### 16.3 nix config.yaml group-writable（`4219a91df fix(nix): make config.yaml group-writable under addToSystemPackages (#41940)` + `d02a59b67 fix(nix): cold npm builds + fix-lockfiles real-build verification + auto-fix workflow (#41867)` + `7230fcb7f revert(nix): drop the cp patchPhase workaround from #41867 (#42151)`）

- `addToSystemPackages` export `HERMES_HOME` 系统级 + 把 hermes CLI 上 interactive user 的 PATH，那些 user（在 `hermes` group）共享 gateway state — 这是该 option 的全部目的
- 但 activation script 把 `config.yaml` 写为 0640（group read-only），interactive user 经 CLI/TUI save 设置 hit `[Errno 13] Permission denied: '/var/lib/hermes/.hermes/config.yaml'`
- 修：mode 改条件 — `0660` 当 `addToSystemPackages` 设（group `hermes`）；否则保留 0640
- `d02a59b67` cold npm 构建 + fix-lockfiles 真实构建验证 + auto-fix workflow
- `7230fcb7f` revert `cp` patchPhase workaround，原因待跟进

### 16.4 NixOS node-gyp PYTHON env（`15c99b437 fix(cli): set PYTHON env for node-gyp native builds on NixOS (#40690)`）

---

<a id="17-hindsight-acp-curator"></a>
## 17. Hindsight / ACP / Curator

### 17.1 Hindsight 仅发新回合 delta（`09d66037f fix(hindsight): send only new-turn delta on append retains instead of whole session (#40605)`）

- 闭 #40503；salvage 自 #40519
- 之前每次都重发整 session — append retains 应该只发 delta

### 17.2 ACP session provenance 元数据（`777dc9da6 feat(acp): emit session provenance metadata for compression rotation (#41724)`）

- 闭 #33617
- 加 additive `_meta.hermes.sessionProvenance` 到 ACP session surfaces 让 client 能检测 compression-driven internal session rotation —— 不再需要解析 status text、猜 token drop、读 state.db
- 经 existing compression chain（`parent_session_id / end_reason`）on-demand 派生 — 无新持久 state，无 schema 变化，无 ACP 协议变化
- ACP `session_id` 保持稳定 client handle
- **新建** `acp_adapter/provenance.py` derive provenance from SessionDB
- 测试 `tests/acp/test_session_provenance.py 新建`

### 17.3 ACP interrupt sentinel 简化（`9b631e4ae fix(acp): suppress cancel interrupt sentinel` + `f5bd09af4 refactor(acp): share interrupt-sentinel prefix, simplify guard`）

- `acp_adapter/server.py` 之前 ACP-local prefix/suffix matcher + helper，替为单 `startswith()` check vs `INTERRUPT_WAITING_FOR_MODEL_PREFIX`，prefix 现在 `conversation_loop.py` 唯一定义 — 一处 source-of-truth 防漂移；server.py 净 -17 LOC

### 17.4 Curator 保护内置 `plan` skill（`8e223b36e fix(curator): protect load-bearing built-in skills from archival/consolidation (#41817)`）

- curator idle-archival 路径（`apply_automatic_transitions` 在 `prune_builtins`）可 archive bundled `plan` skill，静默杀 `/plan` slash command — 输 `/plan` 返 "Unknown command" 无信号 skill 不见
- archived skill 的 hash 留 `.bundled_manifest`，`hermes update` 不会 re-seed
- 加 `PROTECTED_BUILTIN_SKILLS ({plan})` 守在 master gate `is_curation_eligible()`（覆 `archive_skill` + transition walk）+ 候选枚举（LLM consolidation pass 永不看到）

---

<a id="18-terminal-file-tools"></a>
## 18. Terminal / File-tools

### 18.1 CWD-only override collapse 到 default 容器（`6459b3d99 fix(terminal): collapse CWD-only overrides to shared container` + `329c33dac fix(terminal): read cwd overrides under raw task_id after container collapse`）

- `register_task_env_overrides` 只带 `cwd` key（ACP adapter workspace tracking）调用时 task_id 应 collapse 到 `'default'` 让所有交互 surface（TUI/gateway/dashboard）共一 long-lived container
- 之前任意 override（即 CWD-only）让 `_resolve_container_task_id` 返 session key 不变 — 每 session 单容器；不能"一次认证外部服务后跨 session 复用"
- `329c33dac` 补：PR #41822 collapse 到 'default' 容器后**三 call site** 仍 route env/override lookup 经 collapsed id；写在 raw task_id 下，读在 collapsed id 下 — CWD-only override 的 cwd 被静默丢，env 在错根启动（exit 126）
- 修：lookup 改 raw task_id

### 18.2 file-tools 拒收 sentinel `TERMINAL_CWD`（`e45b74583 fix(file-tools): reject sentinel TERMINAL_CWD; anchor worktree edits before live cwd exists (#41861)`）

- worktree-misroute 修复（#35399 让 misroute 可见 via `resolved_path`）不防 misroute：divergence warning 只在 terminal command populate live cwd registry 后才 fire
- 新 worktree session（registry 还空）带 stale `TERMINAL_CWD='.'` 既无 worktree anchor 也无 warning — 相对 `write_file/patch` 静默落到主 checkout
- 修：拒收 `TERMINAL_CWD` 哨兵；worktree edits 在 live cwd 存在前先 anchor

---

<a id="19-platform-models-cluster"></a>
## 19. Platform / Models 杂修

### 19.1 Slack channel-only scope（`133e0271e fix(slack): scope top-level channel messages by channel-only when reply_in_thread=false (#15421)` + `ab0a6270c fix(slack): align thread_ts check with is_thread_reply invariant (Copilot #15464)`）

- 两 Copilot review finding
- `event.get("thread_ts")` truthy vs `event_thread_ts != ts`：new channel branch 把 ANY truthy thread_ts 误为 thread reply
- 新测 `tests/gateway/test_slack_channel_session_scope.py 新建`

### 19.2 Volcengine 工具名 XML 片段剥（`132d6fe6d fix(volcengine): strip XML attribute fragments from tool_use.name (#33007)`）

- VolcEngine api/plan 偶发把 raw XML attribute 片段 leak 入 `tool_use.name`，名字像 `terminal" parameter="command" string="true`
- 修：剥前缀至首个空白/引号让 tool name 干净

### 19.3 WeChat 输入指示器 ticket 刷新（`09a554862 fix(weixin): refresh typing ticket on expiry to prevent stuck indicator (#38085)` + `b8469a81e fix(weixin): add rate-limit circuit breaker`）

- iLink typing ticket 600s TTL；长会话超窗后 ticket evict from `TypingTicketCache`；`send_typing` 与 `stop_typing` 在 ticket None 时 silent early-return — `TYPING_STOP=2` 信号永不送 → WeChat UI 卡 "..." 显示
- 修：refresh ticket on expiry
- `b8469a81e` 加 rate-limit circuit breaker
- 测试 `tests/gateway/test_weixin_typing.py 新建`

### 19.4 ollama `default_max_tokens` 上调（`09ec26c66 fix(ollama): set default_max_tokens for custom/Ollama provider`）

- 之前无 `default_max_tokens` → 请求不带 `max_tokens` → Ollama 回退到内部 `num_predict=128` 截断响应到几 token，`finish_reason='length'`（#39281 e.g. gemma4）
- 设 65536 与 qwen-oauth tier 对齐；用户可 per-model override
- max_tokens 解析序：`ephemeral > user model.max_tokens > profile default`

### 19.5 Custom endpoint 256K fallback 前查 `DEFAULT_CONTEXT_LENGTHS`（`2e61de063 fix(model_metadata): consult DEFAULT_CONTEXT_LENGTHS before 256K fallback on custom endpoints`）

- `get_model_context_length()` custom-endpoint probe branch（step 3）末有 early return 返 `DEFAULT_FALLBACK_CONTEXT`（256K）**不查** step 8 的 hardcoded `DEFAULT_CONTEXT_LENGTHS` catalog
- 经 custom/proxied gateway 服的 model（企业 vLLM/Ollama proxy）即使是知名 model（GPT-5/Claude-Opus-4.8 等）也被钳到 256K

### 19.6 Profiles 跳 default 防重复（`f1d3afb15 fix(profiles): skip 'default' in named profiles scan to prevent duplicates`）

- `~/.hermes/profiles/default/` 目录存在时 `list_profiles()` 返 'default' 两次（一次内置 default profile = `~/.hermes`；一次目录扫）
- 修：命名 profile 扫时跳 'default'

---

<a id="20-misc"></a>
## 20. 其他修补

- **`549b1aa5/dde9c0d19 feat(gateway): render terminal tool calls as native bash code blocks on markdown platforms (#41215)`** — tool-progress 现示 terminal command in ``` ```bash 围栏块（全命令、无周围引号、无 label、无 40-char 截断）替 noisy `terminal: "cmd…"` 行，覆盖每个渲 markdown code 块的平台（Telegram/Slack/Matrix/WhatsApp/Feishu/Weixin/Discord）；plain-text 平台保 compact preview 行；gated on 新 `BasePlatformAdapter.supports_code_blocks` capability（默 False）而非 hardcoded 列表，让 plugin adapter（Discord 住 `plugins/platforms/`）opt-in
- **`fd1e7c2bc fix(tui): install the process.on('exit') terminal-mode backstop (#42165)`** — #19194 修的 die()/dieWithCode() process.exit(0) comment 依赖 entry.tsx 的 `process.on('exit')` handler 重置 terminal modes — 但 handler **从未安装**；`/quit + Ctrl+C + Ctrl+D` 和所有 `process.exit()` 路径都让 DEC mouse tracking（?1000/1002/1003/1006）卡开，终端鼠标输入被吞
- **`cef00ae60 fix(tui): handle Windows PTY stdin and detached WS frames (#41953)`** — pywinpty (Windows) 期 str；ptyprocess (POSIX) 期 bytes — PTY stdin write 现 platform-aware
- **`30c791361 fix(api_server): report hermes version on /health and /health/detailed (#40620)`** — salvage 自 #40479
- **`ad8e57793 fix(hermes_time): implement reset_cache() referenced in docstrings (#41728)`** — module docstring 与 `get_timezone()` cache 注释提及 `reset_cache()` 用于 config 改后强 tz 重解，但函数从未定义 — doc-follower 调用 hit `AttributeError`；加 helper 清缓存 tz state
- **`e3b8b6d32 feat(hooks): expose thread_id and chat_type in agent:start/end context (#41672)`** — 加 `thread_id + chat_type` 入 `agent:start/end` plugin hook context（经 `getattr` safe defaults；都已是 `source` real attrs gateway 在用）；`agent:end` 经 `**hook_ctx` 继承；纯 additive 无 prompt/history mutation；hooks.py 文档化全 ctx dict
- **`69a293b41 hardening(todo): bound TodoStore item content length and count`** — todo list 在每次 compression event 重注入模型 context（`TodoStore.format_for_injection`）— 超大 todo item 或无上限数 defeat 它本要 ride 过的 compression；write/_validate 之前无 size/count 强制
- **`4129092fd fix(cli): strip OSC 8 hyperlink sequences in ChatConsole output`** — prompt_toolkit 的 ANSI parser 不处 OSC（`\x1b]...\x07 / \x1b]...\x1b\`），让 Rich 的 `[link=...]` markup raw OSC 8 payload 泄入 `/clear` 后的 banner title
- **`550b72dd8 fix(cli): gate tool-rendering paths with tool_progress_mode, not quiet_mode`** — `quiet_mode` 用来抑制 tool-result 显示当 `tool_progress_mode == 'off'`；但 `quiet_mode` 也 gate operational status，让 `/verbose + tool-progress off` 用户丢所有 status；加 dedicated `tool_progress_mode` 到 AIAgent；`tool_executor` 结果渲染 gate 在 `tool_progress_mode != 'off'`
- **`8e71b5136 fix(cli): paint approval/clarify/sudo/secret modal prompts directly, not via the throttle (#41098)`** — classic CLI 的 dangerous-command approval prompt（加 clarify/sudo/secret-capture）可能 fail to render：用户 60s 后看 `⏱ Timeout — denying command` 而从未见 panel，让 `approvals.mode: manual` 不可用；改直绘
- **`c9094f5e5 fix(stream): don't report dropped mid-tool-call streams as output truncation (#42314)`** — streaming tool call 的 SSE 以 no `finish_reason` 结束（upstream deliver tool name + opening '{' 后 cleanly close，无 terminator，无 `[DONE]`）被 mock builder 戳 `finish_reason='length'`；route 到 output-cap 截断路径：3 次无用 max_tokens-boosted retry，然后 misleading "Response truncated due to output length limit" 错；改不把这种 dropped mid-tool-call stream 当作 output 截断
- **`8ae0d054f fix(tui): guard automatic heap dumps against disk fill`** — TUI memory monitor auto heap dump 可在每次跨阈值时写 multi-GiB `.heapsnapshot`，让 `~/.hermes/heapdumps` 长到几十 GiB；加 4 层 safeguard：gate auto-high/auto-critical on `HERMES_AUTO_HEAPDUMP=1`；总是写 lightweight diagnostics JSON sidecar；cap total bytes (`HERMES_HEAPDUMP_MAX_BYTES`)
- **`4ce9caed0 fix(tui): type execFileNoThrow stdio/ChildProcess and make memoryMonitor critical test heap-independent (#40612)`** — salvage 自 #40415
- **`c6d27addf fix(deps): align aiohttp extras pins with lazy Slack pin (3.13.4)`** + **`2f510ca8e fix(deps): align anthropic extra pin with lazy pin + guard whole pin surface (#42335)`** — pyproject `[project.optional-dependencies]` 的 extras pin 与 `LAZY_DEPS` pin 漂；`hermes update` extra pin 赢覆 lazy 更新过的版本，重开 CVE；改：bump extras pin 与 lazy 对齐 + 加 generalized regression guard `test_pyproject_pins_match_lazy_deps_pins` 把任意 lazy/extra 双 pinned 包漂移作 fail
- **`5916248dc chore: add AUTHOR_MAP entry for rbrtbn (salvage #25939)` + `4d926f248 / 39c4ac3af / b5f7a1f29 / 0c67d4015 / 240c5d454 / 9c1bb8d2c / d759c13c0 / 38d1a414a / 1c7ae46f0 / 8cb1908e1 / 9c1bb8d2c / 6bcb1d96/f96eb857a / 9c1bb8d2c chore: map ... to AUTHOR_MAP`** — 多个作者映射条目用于 salvage PR 归属
- **`6bcb1d96 / 6bcb1d96 / 6bcb1d96 docs(codex): document --sandbox danger-full-access for gateway bubblewrap failures (#40619)` `b97cd81c7 refactor(insights): drop dead pricing/duration wrappers, call usage_pricing directly (#40618)` `ad399b922 docs(update): document updates.* config keys (pre_update_backup, backup_keep, non_interactive_local_changes) (#40617)` `2aa316ec9 docs(windows): fix Get-Command PATH guidance to venv\\Scripts\\hermes.exe (#40613)` `6bd6c4023 test: skip curses tests on Windows where _curses is unavailable (#40611)` `628780b4f fix(desktop): pin empty PostCSS config so Vite stops walking up the home tree (#40609)` `cb83149dc fix(yuanbao): bound ws.close() so an idle server can't stall shutdown ~5s (#40607)` `2b119baac docs: add Urdu translation of README (#40578)`** — 一连 #406xx 系列的 #41XXX 系列 follow-up（salvage + 文档 + 杂修）
- **`48ae8029a fix(delegate): resolve custom-endpoint subagent pools by endpoint identity (#41730)`** — 委派到 custom endpoint 的子 agent 在 parent 跑另一 custom endpoint 时被 misroute（两 runtime 都 collapse 到 `provider="custom"`，`_resolve_child_credential_pool()` 视作可互换，把 parent 的 pool 给 child；leasing 经 `_swap_credential()` 覆 child delegated `base_url` 为 parent endpoint — child 把 delegated model 名发到错 endpoint）；修：custom runtime 凭 endpoint identity 解析（`custom:<name>` pool）
- **`74744795a docs(tui): correct HERMES_TUI_GATEWAY_URL — dashboard-internal, not remote-attach (#42162)`** — 之前 docs 误说 `HERMES_TUI_GATEWAY_URL` 能用于 remote-attach；实际仅 dashboard-internal
- **`d3b670e63 docs(codex): document --sandbox danger-full-access for gateway bubblewrap failures (#40619)`**
- **`78e2101cd fix: reap zombie subprocesses in web_server action status and meet_bot cleanup`** — `proc.poll()` 返 non-None 后调 `proc.wait()` reap child 移 entry 从 `_ACTION_PROCS` 到 `_ACTION_RESULTS`；`.poll()` 单独留 `<defunct>` zombie
- **`e53b74c39 fix(dist): stop USER_OWNED_EXCLUDE from filtering nested directories`** — `_copy_dist_payload` 的 copytree ignore lambda recurse 每层应用 `USER_OWNED_EXCLUDE`；嵌套目录名匹 exclude entry（`bin/logs/cache` 等）被静默 drop 在 dist install/update
- **`7c4aa3e4d fix(image_gen): expose backend-visible artifact paths`** — `tools/image_generation_tool.py +131`
- **`b31c6c33b fix(pty-bridge): terminate PTY process groups on teardown`** — POSIX-only；teardown 时通过 `os.killpg` 杀整 PTY 进程组
- **`62739fc4 fix(plugins): preserve telegram queue fifo during grace window`** — 见 13.4
- **`b18490b89 fix(compaction): prevent infinite loop when transcript fits in tail budget`** — 见 11.4
- **`b99c6c427 Merge #42076: nested category plugin discovery + alias-normalized enable/disable (#41066)`** — 见 15
- **`754154a9c fix(tests): retry per-file pytest subprocess once on exit-4 when the file exists`** — HEAD；exit-4 在 file 实存在时偶发，retry 一次

---

<a id="21-verification"></a>
## 21. 验证基线

- 全部 178 commit hash 经 `cd /tmp/hermes-agent && git log c9863772..HEAD --pretty=format:"%h %ai %s"` 完整枚举（含 Merge）
- 关键 cluster 文件路径 / 行号 / 函数名 经 `Read` 在 `/tmp/hermes-agent` HEAD `754154a9c` checkout 上实证：
  - `plugins/platforms/photon/{adapter.py:200,795 / auth.py / cli.py / plugin.yaml / sidecar/index.mjs}` — 直读
  - `agent/turn_context.py:39 + agent/turn_finalizer.py:30 + agent/turn_retry_state.py` — 直读
  - `gateway/run.py:1163-1165,1866` 三 mixin 继承 + `gateway/slash_commands.py + kanban_watchers.py + authz_mixin.py` 头部 docstring — 直读
  - `cli.py:55-56,3083 class HermesCLI(CLIAgentSetupMixin, CLICommandsMixin)` + `hermes_cli/cli_commands_mixin.py + cli_agent_setup_mixin.py + model_setup_flows.py` — 直读
  - `hermes_cli/subcommands/` 39 子文件 — `wc -l` 加和 2 857 LOC
  - `hermes_cli/web_server.py:8285-8316` ConPTY 平台分支 + `hermes_cli/win_pty_bridge.py:1-50` — 直读
  - `hermes_cli/auth.py:1589-1605` OpenRouter pool 自动侦测 — 直读
  - `apps/desktop/src/themes/context.tsx:25-60` per-profile 主题 — 直读
  - `apps/desktop/src/lib/update-copy.ts` 与 `version-bar.ts`（已删）git 历史 — 直读
  - `skills/software-development/simplify-code/SKILL.md` — 直读
- 每条 fix/feat 至少经 `git show <sha> --stat` 确认 file counts 一致
- 178 commit 主体覆盖：12 Photon + 15 god-file refactor + 9 desktop update split + 6 desktop misc + 1 simplify-code + 1 ConPTY + 3 auth pool + 2 vision + 4 compression + 6 memory leak + 8 gateway + 3 auxiliary + 3 plugins + 4 install/update + 3 hindsight/acp/curator + 2 terminal/file-tools + 7 platform/models + ~80 其他独立修补
- 编辑器渲染 `commit author dates`：跨度 04-24 → 06-09，许多老 PR 经 salvage 重新 land
