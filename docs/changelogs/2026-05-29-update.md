---
title: 2026-05-29 跨日同步 — hermes-agent 963d22c → 689ef5e2（275 commits，v0.15.0 + v0.15.1）
created: 2026-05-29
updated: '2026-06-08'
type: changelog
tags:
- timeline
- agent-repo
- tool-search
- memory-provider
- kanban
- sessions-optimize
- skills-catalog
- security
sources:
- title: Hermes-Wiki Repository
  type: web
  url: https://github.com/cclank/Hermes-Wiki
confidence: medium
contested: false
total_commits: 275
---
# 2026-05-29 跨日同步：hermes-agent main `963d22c → 689ef5e2`

> **基线**：wiki `66e5d25` 对齐 hermes `963d22c`（2026-05-27 13:55 -0700）
> **目标**：hermes-agent/main HEAD `689ef5e2`（2026-05-29 13:30 -0700）
> **跨度**：约 48 小时窗口集中合并 **275 个 commit**
> **版本**：含两次发布 —— **v0.15.0**（commit `0c859a1c0`，#34008，2026-05-28）+ **v0.15.1**（commit `e71a2bd11`，#34222，2026-05-29）；`pyproject.toml:7 version = "0.15.1"`
> **验证基线**：`/tmp/hermes-src` blobless clone @ `689ef5e2`，所有 commit hash / file:line / 路径均经 `git grep` 或 `git show` 实证

---

## 章节索引

1. [v0.15.0 + v0.15.1 双版本发布](#1-v0150--v0151-双版本发布)
2. [模型目录更新（claude-opus-4-8 / gemini-3.5-flash / step-3.7-flash / mimo-v2.5-pro）](#2-模型目录更新)
3. [工具渐进式披露扩展到 MCP / 插件工具](#3-工具渐进式披露扩展到-mcp--插件工具)
4. [MCP mTLS 客户端证书 + 解析与 OAuth 修复](#4-mcp-mtls-客户端证书--解析与-oauth-修复)
5. [web_crawl 工具与 provider crawl 管线全面移除](#5-web_crawl-工具与-provider-crawl-管线全面移除)
6. [SessionDB FTS5 段合并 + `hermes sessions optimize`](#6-sessiondb-fts5-段合并--hermes-sessions-optimize)
7. [Kanban 可靠性 wave（terminate / 并发 / SQLite 抗撕裂）](#7-kanban-可靠性-wave)
8. [记忆系统：completed-turn 消息上下文 + hindsight observation 收窄](#8-记忆系统)
9. [Context Engine pluggable ABC](#9-context-engine-pluggable-abc)
10. [Skills 目录扩张 + 两个新可选 skill](#10-skills-目录扩张--两个新可选-skill)
11. [媒体生成：Krea 2 进入 FAL 目录 + FAL 视频走 Nous 网关](#11-媒体生成)
12. [Docker s6 wave：跨进程持久化 + 孤儿回收](#12-docker-s6-wave)
13. [安全 / 认证 wave](#13-安全--认证-wave)
14. [网关、Codex、provider 与其它修复](#14-网关codexprovider-与其它修复)
15. [验证基线](#15-验证基线)

---

## 1. v0.15.0 + v0.15.1 双版本发布

- **v0.15.0**：commit `0c859a1c0`（#34008，2026-05-28）— 触及 `pyproject.toml`、`hermes_cli/__init__.py`、`acp_registry/agent.json`、`scripts/release.py`、`uv.lock`，并新增 `RELEASE_v0.15.0.md` 655 行。
- **v0.15.1**：commit `e71a2bd11`（#34222，2026-05-29）— 同套发布文件 + `RELEASE_v0.15.1.md` 110 行。
- 当前 `pyproject.toml:7` → `version = "0.15.1"`。

---

## 2. 模型目录更新

| 变更 | commit / PR | 验证 |
| --- | --- | --- |
| 新增 `claude-opus-4-8` + `claude-opus-4-8-fast` | `1a7479573`（#34003） | `agent/anthropic_adapter.py:98`（`"claude-opus-4-8": 128_000`）；`agent/model_metadata.py:144-145`（含 `claude-opus-4-8` 与 `claude-opus-4.8` 两种写法）；`agent/usage_pricing.py:89-111`；`hermes_cli/models.py:35-36`、`:144`；`website/static/api/model-catalog.json` |
| `gemini-3.5-flash` 加入 OpenRouter + Nous 列表 | `5e7c2ffa9`（#34581） | `hermes_cli/models.py:52`、`:159`；**注意**：`gemini-3-flash-preview` 仍作为默认 aux 模型存在于 `agent/auxiliary_client.py:262,274,417,418` 等处，本次替换仅限 OpenRouter+Nous 列表，并非全局替换 |
| `step-3.7-flash` 替换 `step-3.5-flash`（OpenRouter + Nous）；Vercel 403 时回退 raw.github | `f2d88c820` | model-catalog fallback 测试 `bc736ff54` |
| OpenCode-Go `mimo-v2.5-pro` max_tokens 上限 131072 | `8cf6b3da9` | — |
| `/model` 与 `hermes model` 列表统一 + 磁盘缓存 | `3a9bc9d88`（#33867） | — |

---

## 3. 工具渐进式披露扩展到 MCP / 插件工具

`feat(tools): progressive tool disclosure for MCP and plugin tools`（`369075dc9`）改动以下 6 个文件：

- `agent/tool_executor.py`
- `hermes_cli/config.py`
- `model_tools.py`
- **`tools/tool_search.py`**（新工具检索入口；working tree 含 `scripts/tool_search_livetest.py` 与 `tests/tools/test_tool_search.py`）
- `website/docs/user-guide/features/tool-search.md`

`fix(tool-search): scope bridge catalog + dispatch to the session's toolsets`（`7427b9d58`）将检索目录与调度按会话 toolsets 作用域收敛，避免污染。

> 含义：MCP / 插件工具不再一次性全量注入 system prompt，由 `tools/tool_search.py` 按需检索披露；与 v0.13 起已存在的 skills 渐进式披露（`tools/skills_tool.py`，"progressive disclosure tier 1/2-3"）相对应。

---

## 4. MCP mTLS 客户端证书 + 解析与 OAuth 修复

`feat(mcp): support TLS client certificates (mTLS) for HTTP and SSE servers`（`87e5b2fae`，#33721）实际改动：

- **`tools/mcp_tool.py`** 新增 `_resolve_client_cert(server_name: str, config: dict)`（`tools/mcp_tool.py:573-625`）；接受三种形态：
  - 单字符串 = 合并 PEM 文件路径；
  - 2 元素 list/tuple `[cert, key]`；
  - `(cert, key, password)` 三元组；
  - 或 `client_cert` + `client_key` 分离形式（二者不可与列表形式混用，会抛配置错）
- 配套：`tests/tools/test_mcp_client_cert.py`、`website/docs/reference/mcp-config-reference.md`

其它 MCP 相关修复：

- 解析裸 `npx` / `npm` / `node` 至 `/usr/local/bin`（`e7c99651f`）
- 未取得 token 时不再误报 OAuth 成功（`27a2c4f36`，#34807）
- `CancelledError` 的 isinstance 检查放宽到 `BaseException`（`9f5afc763`）

---

## 5. web_crawl 工具与 provider crawl 管线全面移除

`chore(web): remove web_crawl tool + provider crawl plumbing`（`5e1f793430`，#33824）扫动 23 个文件，包括：

- `tools/web_tools.py`（-255 行 net）
- `agent/web_search_provider.py`、`agent/web_search_registry.py`、`hermes_cli/config.py`
- `plugins/web/firecrawl/provider.py`（-185 行）、`plugins/web/tavily/{__init__.py,provider.py}`、`plugins/web/xai/provider.py`
- 文档：`website/docs/user-guide/features/web-search.md`、`website/docs/developer-guide/web-search-provider-plugin.md` 等
- 测试：`tests/integration/test_web_tools.py`、`tests/tools/test_web_*.py` 全部清理 crawl 相关用例

收尾：`fix(web): ensure plugin discovery before web_*_tool registry lookups`（`6e179c44b`）。

> **影响 wiki**：`web-tools-architecture.md` 中 `web_crawl` 段落与"三大工具"叙述需要更新为"两大工具（web_search + web_extract）"。

---

## 6. SessionDB FTS5 段合并 + `hermes sessions optimize`

`perf(state): merge FTS5 segments on VACUUM + add 'hermes sessions optimize'`（`38695254f`）实际改动 3 个文件：

- **`hermes_state.py`**：
  - 新增 `def optimize_fts(self) -> int:`（`hermes_state.py:3267`）—— FTS5 段合并（`INSERT INTO ...(fts) VALUES('merge', N)` 增量合并）
  - 新增 `def vacuum(self) -> int:`（`hermes_state.py:3306`）—— 先 `optimize_fts()` 再 `VACUUM`，把空间还给 OS
- **`hermes_cli/main.py`**：
  - 新增 `hermes sessions optimize` 子命令 help（`hermes_cli/main.py:13414`：`"Reclaim disk space: merge FTS5 segments + VACUUM (no data change)"`）
  - 执行入口 `print("Optimizing session store (FTS merge + VACUUM)…")`（`hermes_cli/main.py:13596-13598`）
- **`tests/test_hermes_state.py`**

跟进：

- `refactor(state): return FTS index count from vacuum()`（`904c0b479`）
- `fix(state): never silently downgrade WAL to DELETE on transient EIO`（`5c49cd0ed`）

> 注意：本仓 wiki 现有 `concepts/session-search-and-sessiondb.md` 引用 `hermes_state.py` 路径，与上一段一致；不存在 `state/session_db.py` 路径。

---

## 7. Kanban 可靠性 wave

| 变更 | commit / PR | 源码定位 |
| --- | --- | --- |
| `POST /runs/{run_id}/terminate` 端点 | `9d4fda995` | `plugins/kanban/dashboard/plugin_api.py:1317-1318`（`@router.post("/runs/{run_id}/terminate")` → `def terminate_run_endpoint(...)`），完整路径 `/api/plugins/kanban/runs/{run_id}/terminate` |
| per-profile 并发上限 + `default_assignee` 回退 | `3b6347af1`（#34244, #27145, #21582） | — |
| CLI dispatch 遵循 config 的 `max_in_progress`/`max_spawn` | `69b74c15a`（#34337） | — |
| SQLite 抗撕裂写（`secure_delete` + `cell_size_check` + `synchronous=FULL`） | `6416dd518` | — |
| zombie reaper 从 `dispatch_once` 上提 + 崩溃 worker 宽限期检测 | `ffdc937c1` / `c002668ff` | — |
| blocked / iteration-exhausted 三处缺口修复 | `592a4ffb6`（#29747） | — |
| worker SIGTERM 必须终止进程 | `f30db14ce`（#28181） | — |
| worker 运行时活动桥接到看板心跳 | `bc31ee5cf`（#31752） | — |
| Windows init lock guard + sqlite 并发加固 | `3ba896273` / `90b6b3d18` | — |
| close kanban.db FD after every connect() | `ebe04c66c` | — |
| corrupt-DB backup filename 用内容寻址 | `6f9182cb3` | — |

测试：`tests/plugins/test_kanban_worker_runs.py`（含 `POST /runs/{run_id}/terminate` 用例 `:306-425`）。

---

## 8. 记忆系统

### 8.1 sync_turn 增加 `messages` 上下文参数

`feat: expose completed-turn message context to memory providers`（`5a95fb2e1`）改动 8 个文件：`agent/conversation_loop.py`、`agent/memory_manager.py`、**`agent/memory_provider.py`**、`run_agent.py`、`tests/agent/test_memory_provider.py`、`tests/run_agent/test_memory_sync_interrupted.py`、`website/docs/developer-guide/memory-provider-plugin.md`、`website/docs/user-guide/features/memory-providers.md`。

实际是给现有的 `sync_turn` 方法**追加一个可选 `messages` 参数**（不是新增独立 `on_completed_turn` 方法）：

```python
# agent/memory_provider.py:115-133（HEAD）
def sync_turn(
    self,
    user_content: str,
    assistant_content: str,
    *,
    session_id: str = "",
    messages: Optional[List[Dict[str, Any]]] = None,
) -> None:
    """Persist a completed turn to the backend.

    ``messages`` is the OpenAI-style conversation message list as of the
    completed turn, including any assistant tool calls and tool results.
    Providers that do not need raw turn context can ignore it.
    """
```

Honcho 实现：`plugins/memory/honcho/__init__.py:1119` `def sync_turn(self, user_content, assistant_content, *, session_id="")` —— 当前尚未消费 `messages`，按基类契约可忽略。

### 8.2 hindsight 默认 recall_types 收窄

- `feat(hindsight): default recall_types to observation only`（`490b3e76b`）
- `docs(hindsight): correct recall_types scope — tool path is also narrowed`（`4df62d239`）

> 影响 wiki：`memory-system-architecture.md` 的 MemoryProvider ABC 与 hindsight provider 章节描述。

---

## 9. Context Engine pluggable ABC

`feat(context-engine): host contract for external context engines`（`9b5dae17a`）落地 `agent/context_engine.py`，定义 `class ContextEngine(ABC)`（`agent/context_engine.py:32`）—— **可拔插的上下文引擎抽象基类**。

类文档（摘自源码）：

> A context engine controls how conversation context is managed when approaching the model's token limit. The built-in `ContextCompressor` is the default implementation. Third-party engines (e.g. LCM) can replace it via the plugin system or by being placed in the `plugins/context_engine/<name>/` directory.
> Selection is config-driven: `context.engine` in config.yaml. Default is `"compressor"` (the built-in). Only one engine is active.

引擎职责：决定何时压缩、执行压缩（摘要、DAG 构造等）、可选暴露工具（如 `lcm_grep`）、跟踪 token 用量。Lifecycle hooks：`on_session_start` / `update_from_response` / `should_compress` / `compress` / `on_session_end`。

改动 8 个文件：`agent/agent_init.py`、`agent/context_engine.py`、`agent/conversation_compression.py`、`agent/conversation_loop.py`、`hermes_cli/plugins_cmd.py`、`plugins/context_engine/__init__.py`、`run_agent.py`、`tests/agent/test_context_engine_host_contract.py`。

跟进 `fix: expose context engine tools with saved toolsets`（`8595281f3`）。

---

## 10. Skills 目录扩张 + 两个新可选 skill

| 变更 | commit / PR |
| --- | --- |
| skills.sh 经 sitemap 拉取全量目录（858 → 19,932） | `7050c052e`（#34025） |
| ClawHub 目录纳入索引（200 → 20k+） | `fb9f3a4ef`（#33748） |
| NVIDIA/skills 作为可信 skills hub tap（add → revert → re-add） | `9992e32db` → `bfecfabd0`(revert) → `4de8009ce`(re-add) |
| tap skills 按 `skills.sh.json` 分组归类 | `b6ed3913d` |
| **新增可选 skill：`antigravity-cli`** —— 路径 `optional-skills/autonomous-ai-agents/antigravity-cli/`（含 `SKILL.md` + `references/cli-docs.md`） | `1bba5f27a` → `632a7088a` → `0dba60f73`（catalog + sidebar 重新生成）；2026-05-29 又移到 `autonomous-ai-agents/`（`78d7fa1b5` + `75cd420b3`） |
| **新增可选 skill：`grok`**（xAI Grok Build CLI） | `10cd4138c` → `99ddba94e` → `d6f2bdabd` |
| skills 页面惰性拉取目录（不再把 34MB 打进 JS） | `a1eaad2fc`（#33809） |
| Skills hub category 侧栏、source pills 显示 ALL 折叠修复 | `78a54d2c0`（#34194） |

---

## 11. 媒体生成

### 11.1 Krea 2 进入 FAL 目录（注意：与既有 `plugins/image_gen/krea/` 直连后端并存）

`feat(image_gen/fal): add Krea 2 Medium + Large to FAL catalog`（`6d947e4d7`，#33506）仅改动 1 个文件：

- `tools/image_generation_tool.py` 中的 `FAL_MODELS` 字典新增 `fal-ai/krea/v2/medium/text-to-image` 与 `large` 两条目（实测 `:317+`）

源码注释明确说明：*"Same model family as our direct `plugins/image_gen/krea` backend, exposed here for users who prefer to bill through their existing FAL key / Nous Portal subscription rather than register directly with Krea."*

> 修正先前 wiki 的"新增 Krea image plugin"叙述：Krea 直连插件 `plugins/image_gen/krea/{__init__.py,plugin.yaml}` 早在上一次 sync（`963d22c`）就已存在；本次新增的是 **FAL 通道下的同家族模型条目**。

### 11.2 FAL 视频走 Nous 网关

`feat(video_gen): route FAL video gen through managed Nous gateway`（`d04b3c193`）改动 3 个文件：`plugins/video_gen/fal/__init__.py`、`tests/plugins/video_gen/test_fal_plugin.py`、`tests/tools/test_managed_media_gateways.py`。

源码核心：`plugins/video_gen/fal/__init__.py:326-331` —— `from tools.managed_tool_gateway import resolve_managed_tool_gateway` + `_get_managed_fal_video_client(managed_gateway)`。

跟进修复：

- `feat(video_gen): wire Nous subscription override into hermes tools UX`（`a4c18f65d`）
- `fix(video_gen): veo3.1 duration format and 4k resolution`（`3183b2e28`）
- `fix(video_gen): parse duration suffix in success_response`（`e46e4bcf4`）
- `test(video_gen): cover gateway decision matrix gaps and 4xx error path`（`b6294ea9f`）
- `fix(test): add fal_client.submit stub to surface matrix test`（`0563ab065`）

---

## 12. Docker s6 wave

主线 issue **#20561**「persist-across-processes contract + orphan reaper」：

- 容器跨进程复用 + 清理泄漏修复（`ac8e238bc`）
- 启动期孤儿容器回收 reaper（`d77d87766`）
- persist 模式 cleanup 为 no-op + `force_remove` kwarg（`5c2170a7c`）
- `cleanup_vm()` 默认遵循 persist 模式（不在 session 关闭时杀容器，`2f0f03c40`）
- 以 `hermes-agent` 标签标识容器（`8d129d013`）
- 文档：`docs(docker): document new persist-across-processes contract and orphan reaper`（`3c6e70aef`）
- s6-overlay 现实对齐：`docs(docker): refresh user-guide page for s6-overlay reality`（`42612aa35`）

config 关键字：`terminal.docker_persist_across_processes` / env `TERMINAL_DOCKER_PERSIST_ACROSS_PROCESSES`（`cli.py:588`、`gateway/run.py:834`、`hermes_cli/config.py:5621`）。

其它 Docker：

- `PUID`/`PGID` 作为 `HERMES_UID`/`HERMES_GID` 别名（`48083211e`，#25872）
- 自动加入 docker socket group（`ec7736f8a`）
- 安装 `python-is-python3` 使裸 `python` 可解析（`71b4a6b18`）
- 文档：网络出口隔离指南（`90f0f32ea`，#26385）
- 镜像内构建期固化 git SHA（`66489f38c`）
- `hermes update` 在 Docker 内打印 `docker pull` 指引而非误报 git error（`b924b22a9`）
- dashboard `--insecure` 仅在 env 开启时走（`fb5125362`）
- `mcp`/`hermes-agent labels` 修复（`8d129d013`）
- arm64 PR 构建不用 gha cache（`130396c65`）

---

## 13. 安全 / 认证 wave

- `refactor(auth): Disable Nous legacy session key fallback`（`41ff6e593`）+ 仅 JWT 期望（`4e4984a11`/`95cf8f984`/`7e958dafc`）
- 移除残留 `min_key_ttl`/`inference_auth_mode` 参数（`a22c25000`）
- `fix(security): block AWS SDK creds from subprocess env`（`95b5b7240`）
- Bedrock 子进程仅剥离 inference bearer token（`6bebab476`）
- **code-exec 审批旁路回归簇**：
  - `fix(code-exec): restore approval context in execute_code RPC threads + guard entry`（`108397726`）
  - `propagate agent-turn context into tool worker threads`（`21aeefe5f`）
  - `feat(gateway): warn at startup on manual approvals with no risk assessor`（`655090b3d`）
  - 回归测试套件（`4bdae3477`）
  - 沙箱 scrub 中可诊断丢弃的 `HERMES_*` env（`317184547`）
- `fix(security): require API_SERVER_KEY before dispatching API server work`（`1a9ef8314`）
- msgraph 公网 webhook 绑定要求源 CIDR allowlist（`43abc51f6`）
- `approval: add docker restart/stop/kill to DANGEROUS_PATTERNS`（`54bf79876`，#33438）
- `fix(security): add bws_cache.json to file_safety read guard`（`4126da65a`）
- Android `psutil` 兼容安装拒绝不安全 tar 成员（`aa3466063`）
- `tirith`：自动安装期拒绝非常规 tar 成员（`a91b1c8b3`）
- xAI OAuth 403 bad-credentials 归类为 auth error（`f6a2ba626`）

---

## 14. 网关、Codex、provider 与其它修复

### 14.1 Gateway / 流式

- MEDIA 提取扩展名集合统一 + 关闭未知扩展黑洞（`781604ce4`，#34517）；MEDIA 拒绝可诊断化（`e28a668b4`）
- planned-stop watcher 仅对自指标记触发 + Windows consume 修复（`7379f1755`，#34749）
- adapter-owned access policy 优先于 env default-deny（`fd09b2c55`，#34515）
- 多 profile 网关运维指南（`0c0a90501` + sidebar `0aa9f6acf`，新增 `website/docs/user-guide/multi-profile-gateways.md`）
- Telegram DM topic 批处理键修复（`5407d2559`）；topic 恢复经 adapter hook 泛化（`100536134`）
- ntfy 出站消息打 tag 防回声循环（`9405cdc8d`）
- 锚定 systemd 服务 `WorkingDirectory` 在 `HERMES_HOME` 而非源码 checkout（`a1cb5fa2c`）
- discord 自动建 thread 不回填 + 测试 fake 更新（`b243afb68`/`68ddd6b33`/`eafe11d45`）
- compression session-id fork 防护（`a30480bd2`，#34351）；锁子系统缺失时 fail-open（`db2ce9e7d`，#34475）

### 14.2 Codex / Anthropic / Provider

- Codex no-byte TTFB watchdog 默认 12s → 120s（`73d73f1f0`）
- OpenRouter `extra_body` 传 `session_id` 实现粘性路由（`e8b9369a9`）
- provider content-policy 拦截时立即 fallback（`0554ef1aa`，#33883）；retry/fallback 状态缓冲、终态失败才浮现（`67011cc0d`，#33816）
- `fix(stt,tts): restore mistralai — 2.4.8 is clean, ban lifted`（`3a2c03061`，#34841）
- `feat: embedder environment-hint hook for the system prompt`（`e4b9532c1`，#34574）
- universal task-completion guidance + 本地 Python 工具链探测（`a4d8f0f62`，#34340）
- model picker 凭据隔离（`aa283d1e4`）

### 14.3 CLI / 其它

- `chore: remove dead code — 28 unused functions/classes across 16 files`（`dc235e93c`）+ 测试侧 re-export 与 side-effect import 恢复（`66827f894`/`e371bf5d6`/`00b8204cf`/`f61fd59b6`）
- `hermes update` 警告 pip 安装不支持的情况 + 修复 stale update-check cache（`689ef5e2`，#34491/#34846）
- `/resume` 后跟裸数字时恢复对应会话（`edfdc7766`）
- TUI Ghostty 保留 Ctrl+J 换行（`cf8862cfa`）
- TUI 短终端 panel 截断提示（`2fc2280e6`，#34808）
- packaging：在 wheel 中带上 `hermes_cli` 子包（`45b00bb49`，#34811）
- Windows `.bat` wrapper（`6312dd8c3` + `8836b3a11`）
- `gemini-3.5-flash` 已加入 OpenRouter+Nous，**注意旧 `gemini-3-flash-preview` 仍是默认 aux 模型**（见 §2 注解）
- Cron `jobs.json` 不被磁盘清理误删（`d473e7c93` + `3845d86b9`）
- feishu `_chat_locks` 用 LRU 限界（`91b174038`，#34836）
- `fix(mcp): stop reporting false OAuth success when no token was obtained`（`27a2c4f36`，#34807）
- skills hub categorize NVIDIA tap（`b6ed3913d` + `4de8009ce`）

---

## 15. 验证基线

每条结论可经 `git -C /tmp/hermes-src grep -n "<term>" HEAD -- <path>` 或 `git show <commit>` 验证。

| 主题 | 主源码 | 关键定位 |
| --- | --- | --- |
| 版本 | `pyproject.toml:7` | `version = "0.15.1"` |
| Opus-4.8 模型 | `agent/anthropic_adapter.py:98`、`agent/model_metadata.py:144-145`、`agent/usage_pricing.py:92,104` | `claude-opus-4-8` / `claude-opus-4-8-fast` |
| 工具渐进式披露 | `tools/tool_search.py`、`agent/tool_executor.py`、`model_tools.py` | commit `369075dc9` 改 6 个文件 |
| MCP mTLS | `tools/mcp_tool.py:573-625` `_resolve_client_cert` | commit `87e5b2fae`（#33721） |
| web_crawl 移除 | `tools/web_tools.py`、`plugins/web/{firecrawl,tavily,xai}/provider.py` 等 23 文件 | commit `5e1f793430`（#33824） |
| Sessions optimize | `hermes_state.py:3267 optimize_fts` / `:3306 vacuum`；`hermes_cli/main.py:13414,13596` | commit `38695254f` |
| Kanban terminate | `plugins/kanban/dashboard/plugin_api.py:1317-1318` | commit `9d4fda995` |
| Memory sync_turn 扩展 | `agent/memory_provider.py:115-133`（新加 `messages: Optional[List[Dict[str, Any]]]`） | commit `5a95fb2e1` |
| Context Engine ABC | `agent/context_engine.py:32 class ContextEngine(ABC)` | commit `9b5dae17a` |
| antigravity-cli skill | `optional-skills/autonomous-ai-agents/antigravity-cli/SKILL.md` | `1bba5f27a` |
| grok skill | `optional-skills/.../grok/SKILL.md` | `10cd4138c` |
| Krea2 in FAL | `tools/image_generation_tool.py` `FAL_MODELS["fal-ai/krea/v2/..."]` | commit `6d947e4d7`（#33506）；与既有 `plugins/image_gen/krea/` 直连后端并存 |
| FAL 视频 Nous 网关 | `plugins/video_gen/fal/__init__.py:326-331` | commit `d04b3c193` |
| Docker persist | `terminal.docker_persist_across_processes`（`cli.py:588`、`gateway/run.py:834`、`hermes_cli/config.py:5621`） | issue #20561 系列 |

---

## 跨页引用

- [Mcp And Plugins](../concepts/mcp-and-plugins.md) —— 新增 mTLS 客户端证书段；工具渐进式披露扩展到 MCP/插件工具
- [Web Tools Architecture](../concepts/web-tools-architecture.md) —— web_crawl 工具移除，工具收敛为 web_search + web_extract
- [Session Search And Sessiondb](../concepts/session-search-and-sessiondb.md) —— `hermes sessions optimize` + FTS5 段合并 + `vacuum()`
- [Smart Model Routing](../concepts/smart-model-routing.md) —— claude-opus-4-8 / gemini-3.5-flash / step-3.7-flash 模型目录更新
- [Memory System Architecture](../concepts/memory-system-architecture.md) —— `sync_turn` 新增 `messages` 上下文参数
- [Kanban Multi Agent Board](../concepts/kanban-multi-agent-board.md) —— `POST /runs/{run_id}/terminate` 端点 + 一系列可靠性硬化
- [Context Compressor Architecture](../concepts/context-compressor-architecture.md) —— pluggable `ContextEngine` ABC（兼容现有 compressor）

*验证 HEAD：`689ef5e2`（2026-05-29 13:30 -0700）。*
